# 引き継ぎ書 v201 (2026-06-11 autopilot、 決算ハイライト Phase2 = 粗利率 ship(opt-in) + セグメント DEFER / cron 稼働確認)

user 出社中の autopilot セッション。 handover v200 の 🔴 を推奨順で処理。 最新本番 = `e2abcae`。

## 🎯 完了 (autopilot)

| 領域 | 内容 | commit |
|---|---|---|
| 🔴1 nightly cron 稼働確認 | `nightly_guidance.yml` を dry_run dispatch → HTTP200 / universe_size=9 / degrade=false で配管確認。 09:10 JST スケジュール実発火は **未到来** (workflow 追加が昨晩 UTC、 初回発火が今朝 09:10 JST)。 09:40 JST に実発火を確認する 1-shot scheduled-task 設定済 (`verify-nightly-guidance-cron`) | — |
| 🔴3 Phase2 SPEC | planner で `SPEC_2026-06-11_earnings-flash-phase2-segment-grossmargin.md` 詳細化 → **6体合議 6/6 条件付賛成** (§10 に verdict) | — |
| 🔴3 Phase2 粗利率行 | 四半期グロスマージン行を `?flash_gm=1` opt-in / **default OFF** で実装。 sector gate + 妥当域 + fallback | `e2abcae` |

## 🔴 次セッション最優先 (推奨順)

### 1. ★セグメント別売上行の設計判断 (Phase2 の DEFER 分、 user 判断必須)
**6体合議マーケ (Opus) がコード裏取りで重大発見**: セグメント別売上は既に **DiagramCard (SegmentBar `:2519`) + ProfileCard (SegmentSection `:389`、 citation「直近四半期の構成比」) の 2 箇所に live** (同じ `build_segment_summary`、 同一四半期粒度)。 章冒頭に 3 箇所目を純増させると同一データ 3 重描画 = **Trust Cliff** (cache TTL 差で値がズレると「どっちが正?」)。 ∴ SPEC §3-2「ProfileCard=年次」は事実誤り (四半期が正)。
- **user が以下から選択** (SPEC §10、 推奨 = (c) or (a)):
  - **(a)** 章冒頭はアンカー導線 (既存 SegmentBar へ内部 scroll、 二重描画なし、 LLM なし)
  - **(b)** ProfileCard か DiagramCard の SegmentSection を 1 箇所に集約してから章冒頭移設 (純増させない、 blast radius 中)
  - **(c)** セグメントは現状 2 箇所のまま、 章冒頭には追加しない (Phase2 = グロスマージンのみで完了)
- マーケ verdict:「セグメント 3 箇所目の限界効用ほぼゼロ〜マイナス」「計測 (GA4/Clarity)・pricing 未了なら後ろ倒し」。

### 2. 粗利率行の dogfood → default ON 昇格 (user 判断)
- `?flash_gm=1` で AAPL / NVDA / MSFT 等を目視 → ファンダ章冒頭に「粗利率 NN.N%」が EPS/売上の後・来期の前に出る。 OK なら `isGrossMarginEnabled()` (EarningsFlashSummary.jsx) を default true 化。
- 昇格時の未消化条件 (SPEC §10): CLS envelope (minHeight) の snap 再計測 / ×100 unit test / 粗利率ラベルの四半期明示 (現状は section 上部「直近四半期」caption で代替)。
- 銀行/REIT/保険/証券/公益 は sector gate で粗利率非表示が正 (バグでない)。 公益 (utilities) も `_roe_sector_guard` で保留される (粗利率は本来意味があるが保守側、 必要なら gate を financials/REIT に絞る refine 余地)。

### 3. (継承) ガイダンス `?guidance_pit=1` dogfood → default ON (handover v200 🔴2)
- 未着手 (user 目視必須)。 SNOW/CRM/MU 等で前回比/発表時比バッジを確認。

## ☀️ 自動検証済 (再確認不要)
- 粗利率 backend (本番 `e2abcae` を curl): **AAPL=49.3% (Q2 2026、 じっちゃま模範 note と完全一致) / NVDA=74.9% / MSFT=67.6% / JPM=None (銀行 sector gate 作動)**。 妥当域 + sector gate で「粗利率 100%」桁違い Trust Cliff を根治確認。
- frontend bundle: `flash_gm` ×2 + `粗利率` ×1 が `JudgmentDetail-*.js` に deploy 確認。 build 通過 / 判断語 grep guard clean / raw hex・shadow・!important なし / pre-commit Check 6/7 通過。
- ⚠️ frontend visual (authed chart = 個別株 detail) は **headless 検証不可** → §2 の dogfood で目視必須 (`?flash_gm=1` で粗利率行が EPS/売上の後に出るか)。

## 🟡 残バックログ (継承)
- 上記 🔴1 (セグメント設計判断) + 🔴2 (粗利率昇格)。 Stripe Premium 配線 (launch blocker、 pricing=autopilot対象外)。 scan async化 (3000 universe 502)。 ガイダンス基盤 Phase2 (`project_guidance_history_foundation`)。 ▲▼ 監査 (spawn chip)。

## ⚠️ 危険箇所 / 運用知見 (新規 + 継承)
- **粗利率は sector gate 必須**: `_roe_sector_guard` で金融/REIT/保険を保留しないと「粗利率 100%」桁違い Trust Cliff (FMP grossProfitRatio が銀行で 1.0)。 妥当域 `0<ratio<1.0` と二重防御。
- **セグメント別売上は既に 2 箇所** (DiagramCard/ProfileCard、 同一四半期粒度)。 章冒頭 3 箇所目は純増させない (Trust Cliff)。
- `build_segment_summary` / `get_segment_data` (main.py:538/570) は `/api/visualize` と共有の純粋関数 → **改変禁止** (呼ぶだけ)。
- **§38 境界** (継承): 「上方/下方修正」は前回**会社**ガイダンス比のみ事実 OK。 `GUIDANCE_REVISION_JP` / `GUIDANCE_PIT_CONSENSUS_JP` は別 dict。 方向記号 ↑↓ / 中立 — (▲▼ 全廃)。
- guidance_snapshots は per-filing 履歴 (unique に source_accession)。 戻すと raised/lowered 破綻。
- 発光系 .panel-card / sticky 検索バー / VITE_ ARG/ENV / aggregator LLM import 禁止 (Check 3)。
- deploy = `git push origin main` (Railway auto-deploy ~30-120s)。 `railway up` 不使用。
