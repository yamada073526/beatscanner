# 引き継ぎ書 v201 (2026-06-11 autopilot 全日 = 決算ハイライト Phase2 完遂 + 全昇格 + デザイン4周 + 色§38再定義 + consensus drift)

user 対話型 autopilot (出社→出先→帰宅、多数往復、長セッション)。 最新本番 = `4d71495`。

## 🎯 完了 (本番 live、全て default ON)
| 領域 | commit |
|---|---|
| 🔴1 cron logic 検証 (実 run processed9/upserted3/error0)。⚠️ GitHub schedule 初回未発火、検証 scheduled-task が fallback dispatch で補完 | — |
| 粗利率 (sector gate 必須) / 部門別 (gate 不要) / guidance_pit バッジ | `e2abcae`〜`5d49f89` |
| consensus drift 前回比 (既存 `/api/analyst/consensus-drift` 配線、MU で「↑アナリスト予想 直近引き上げ」live) | `25bbf94` |
| デザイン v2 (EPS hero 1点 + 従属 + hairline) / v3 (単位従属化 + 行 hover reading-lamp) | `fdc9783`/`45b5cdd`/`79a8d94` |
| 記号 ↑↓+絶対値 統一 (+/− 全廃、SSOT 準拠) | `337eef4` |

## 🔴 次セッション最優先 (user 判断待ち)
### 1. v4 色 (過去確定実績の緑/赤) を default ON にするか — preview スクショ送付済
- `?flash_v4=1` で確認。**過去確定実績の予実差 (EPS/売上 beat/miss) + 売上前年比に muted 緑/赤** (deltaColor、color-mix で token から導出)。部門別/粗利率(水準)/来期(未来) は中立。
- **§38 verdict (3体合議 2026-06-11)**: 「過去確定=色OK / 未来予想=中立」 は法的に妥当 (過去実績の着色は「陽線=緑」 同型の事実符号化、§38 射程外)。**従来の「色なし」 は §38 要件でなくデザイン選択だった** = 「文字壁」 の根本原因は色禁止。詳細 memory [[project_chapter_summary_jitchama_style]]。
- **user OK なら** `isFlashV4Enabled()` を default true + `?flash_v4=0` kill。さらに洗練希望なら: muted の調整 / 部門別前年比も着色するか / 売上前年比を中立に戻し予実差だけにするか (ui-designer は「予実差だけ=2点」 が最restraintと提案、 現状は前年比も着色=3点)。

### 2. consensus drift の universe 点検
- AAPL/NVDA/MSFT=flat(非表示)、MU=up(表示)、**SNOW/CRM=consensus_snapshots 0 件** (universe 外?)。 `_build_consensus_universe` (main.py:16578) に主要銘柄が入るか要確認。

### 3. nightly schedule 自動発火 monitor (1-2 日)
- `gh run list --workflow=nightly_guidance.yml` で schedule trigger run を確認。

## ☀️ 自動検証済
- 全機能 (粗利率/部門別/guidance_pit/consensus drift/v2 hero/v3 単位従属+hover/記号統一/v4 色) を headless authed dogfood。v4: EPS 予実差 = oklab muted緑 (ON) / 中立gray (OFF) を対照確認。BAN語/pageerror なし。
- harness `frontend/scripts/snap-flash-gm.mjs` (`node --env-file=.env`、GM_V2/V3/V4 で各段階、hover/色/drift/seg/badge 抽出、eps行 load 待ち付き)。

## ⚠️ 危険箇所 / 運用知見 (重要)
- **★色 §38 ルール (今回再定義)**: **過去確定実績 (EPS予実差/前年比/部門別前年比) = 緑/赤 OK** (事実の符号化、§38射程外)。**来期(未来予想コンセンサス/会社ガイダンス) = 絶対中立 (§38)**。**粗利率 = 「水準」 で方向でない → 色NG** (高い=良いの断定回避、 前期比 Δ なら可)。色は数値本体でなく差分だけ + muted (color-mix で 5条件カードの面の緑=verdict と線の緑=事実 を格分離) + 評価語と併用しない + 全 field 一貫。
- **記号 ↑↓+絶対値 統一** (+/− 不可、初心者の絶対値混乱)。 [[feedback_chart_hover_direction_symbol]] 更新済。
- **デザイン教訓 (4周)**: 淡白=色不足でなくメリハリ不足 → 全数値拡大は逆効果(焦点分散) → EPS hero 1点+従属 → 単位従属化+hover → **色(過去確定の緑/赤)が文字壁の本命**。
- **§38 consensus drift**: 会社ガイダンス比 (`GUIDANCE_REVISION_JP`、Check7 ホワイトリスト) と別語彙。consensus は `CONSENSUS_DRIFT_JP`「引き上げ/引き下げ/まちまち」。「上方修正」 は Check7 がコメントでも BLOCK。
- 粗利率 sector gate 必須 / セグメント gate 不要。build_segment_summary/get_segment_data (main.py:538/570)=visualize 共有純粋関数、改変禁止。
- **flag**: `?flash=0`/`?flash_gm=0`/`?flash_seg=0`/`?guidance_pit=0`/`?flash_v2=0`/`?flash_v3=0` (各 kill、default ON) / `?flash_v4=1` (色 opt-in、default OFF)。
- 発光系/sticky/VITE_/aggregator LLM import 禁止。deploy=git push。
