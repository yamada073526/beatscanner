# 引き継ぎ書 v201 (2026-06-11 autopilot、 決算ハイライト Phase2 完遂 = 粗利率 default ON + 部門別 opt-in / cron logic 検証)

user 出社→出先の autopilot セッション。 handover v200 の 🔴 を推奨順で完遂 + Phase2 を user 対話で着地。 最新本番 = `1de0289`。

## 🎯 完了 (本番 live + 検証済)

| 領域 | 内容 | commit |
|---|---|---|
| 🔴1 cron logic | `nightly_guidance.yml` を dry_run + 実 run で検証 (processed:9/upserted:3/error:0/skipped:6=正常)。 ⚠️ **GitHub schedule(09:10 JST) は初回自動発火せず** (新規 cron 既知挙動)、 検証 scheduled-task が fallback で実 dispatch し補完 | — |
| 🔴3 Phase2 SPEC | planner で `SPEC_2026-06-11_earnings-flash-phase2-segment-grossmargin.md` → **6体合議 6/6 条件付賛成** (§10 verdict) | — |
| 🔴3 粗利率行 | 四半期グロスマージン行を **default ON** (user 承認)。sector gate(`_roe_sector_guard`) + 妥当域 + fallback | `e2abcae`→`58fabd5` |
| 🔴3 部門別行 | セグメント別売上行を **`?flash_seg=1` opt-in** (user 承認、 昇格待ち)。上位2件+他N部門、 実額億ドル+前年比↑↓ | `d31d14b` |

## 🔴 次セッション最優先 (推奨順)

### 1. 部門別行の default ON 昇格 (user preview 承認後すぐ)
- user に preview スクショ送付済 (AAPL「iPhone 570億ドル↑21.7%・Service 310億ドル↑16.3%・他3部門」/ MSFT「サーバ・クラウド・M365法人」)。 **user OK が出たら** `isSegmentEnabled()` (EarningsFlashSummary.jsx) を default true + `?flash_seg=0` kill に変更 (粗利率と同じ昇格パターン、 CLS minHeight は 5 行で再計測 — 現状 132 は 4 行基準)。
- 昇格時: 粗利率の minHeight 132 を部門別込み 5 行 (NVDA 162px 級) に合わせ再評価。

### 2. nightly schedule 自動発火の monitor (1-2 日)
- 今日 09:10 JST の schedule は GitHub が初回 skip (新規 cron で頻発)。 明日以降 `gh run list --workflow=nightly_guidance.yml` で `schedule` trigger の run が出るか確認。 数日 skip が続くなら要調査 (workflow は default branch 上=条件は満たす。 最悪 `nightly_consensus.yml` 等の稼働中 cron に snapshot step を相乗りさせる案)。
- 当面は手動 dispatch or 検証タスクで毎日収集可 (実害は schedule の自動性のみ)。

### 3. (継承) `?guidance_pit=1` dogfood → default ON (handover v200 🔴2)
- 未着手 (user 目視必須)。 SNOW/CRM/MU 等で前回比/発表時比バッジ確認。

## ☀️ 自動検証済 (再確認不要)
- **粗利率 default ON** (本番 curl + headless dogfood): AAPL=49.3%(模範一致)/NVDA=74.9%/MSFT=67.6%/JPM=null(銀行 sector gate)。 default URL(実ユーザー)で表示確認、 中立色(EPS と同色)、 `?flash_gm=0` で kill。
- **部門別 opt-in** (headless dogfood `?flash_seg=1`): AAPL/MSFT/NVDA で行順 eps→revenue→segment→gross-margin→nextq、 中立色↑↓、 displaySegmentName 和訳OK(サーバ・クラウド/M365法人)、 銀行 JPM もセグメント表示(gate不要=正)、 予想比なし(FMP consensus未接続)、 BAN語なし、 pageerror なし。
- cron 実 run: processed9/upserted3/error0/skipped6(8-K非開示=正)。

## 🟡 残バックログ (継承)
- 上記 🔴。 Stripe Premium 配線 (launch blocker、 pricing=対象外)。 scan async化(502)。 ガイダンス基盤 Phase2 (`project_guidance_history_foundation`)。

## ⚠️ 危険箇所 / 運用知見 (新規 + 継承)
- **粗利率は sector gate 必須** (`_roe_sector_guard` で金融/REIT/保険保留 + 妥当域 0<ratio<1.0)。 銀行 grossProfitRatio=1.0 で「粗利率100%」桁違い Trust Cliff を防ぐ。 ※公益(utilities)も _roe_sector_guard で保留される(粗利率は本来有意、 必要なら financials/REIT に絞る refine 余地)。
- **セグメントは gate 不要** (銀行も Commercial Bank/Consumer Bank 等で有意)。 粗利率と挙動が違う点に注意。
- **実 DOM probe の教訓**: 既存セグメント表示 (DiagramCard SegmentBar / ProfileCard SegmentSection) は **折りたたみ/on-demand でデフォルト非表示** → 章冒頭インラインは「3箇所同時表示」 にならず Trust Cliff でない (6体合議マーケ verdict を probe で再評価して採用)。 アンカー導線(案a)は scroll 先が無く不成立。
- **flag**: `?flash=0`(ハイライト全体 kill) / `?flash_gm=0`(粗利率 kill、 default ON) / `?flash_seg=1`(部門別 opt-in、 default OFF) / `?guidance_pit=1`(履歴バッジ opt-in)。
- **build_segment_summary / get_segment_data (main.py:538/570)** = `/api/visualize` 共有の純粋関数 → 改変禁止 (呼ぶだけ)。
- §38 境界 (継承): 「上方/下方修正」 は前回会社ガイダンス比のみ。 ↑↓ / 中立 — (▲▼ 全廃)。 guidance_snapshots は per-filing 履歴。
- 発光系 .panel-card / sticky 検索バー / VITE_ ARG/ENV / aggregator LLM import 禁止。 deploy = git push (Railway auto-deploy)。
- **dogfood 代行**: `frontend/scripts/snap-flash-gm.mjs` (`node --env-file=.env`、 DOGFOOD creds は frontend/.env) で authed headless 検証可。 SPA でスマホ目視不可な user の代替。
