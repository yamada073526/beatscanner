/**
 * @deprecated round 7 で `components/ui/Chip.jsx` に統合済。
 * 後方互換 (旧 import path) のため再 export のみ残す。新規コードは
 *   import Chip from '@/components/ui/Chip.jsx'
 * を使用する。
 */
export { default } from '../../../components/ui/Chip.jsx';
