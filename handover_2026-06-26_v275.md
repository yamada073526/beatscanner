# handover 2026-06-26 v275 — screener 隠れフィルタ/機械検査 着地 + 次の UI 改善バックログ確定

> 前 session (v274) から継続。PR #30 を merge & 本番反映 & 全 CI green & user 目視確認まで完了。
> 本 handover は **次にやる screener UI 改善 (user が優先度順に依頼)** を actionable に引き継ぐ。
> 現セッションは context が長くなったため CLAUDE.md「機能着地直後は新セッション」規律で交代。

## ✅ このセッションで完了 (PR #30・merge sha 0ccd359)

| 項目 | 内容 | 検証 |
|---|---|---|
| hot_sector 隠れ grade フィルタ可視化 | `PRESET_DISPLAY_CONDS.hot_sector` に eps_yoy/eps_cagr/roe/rs を追加 (真の Trust Cliff=①crow/②適用バー両方に非表示で list を削っていた)。funda_pass の backend 5条件 (main.py:4200-4267) は grade 閾値を内包しないと裏取り | build / invariant test 9/9 / 本番 vision CI |
| 隠れフィルタ invariant 機械検査 | `frontend/src/components/CustomScreenerPanel.invariants.test.js` (vitest) + `.github/workflows/screener_invariants.yml`。各 preset で「適用条件 ⊆ PRESET_DISPLAY_CONDS」を CI gate (案A・保守的)。**今後 screener を触ったら必ず `cd frontend && npm run test:unit` で緑維持** | CI success |
| earnings_pass funda_pass 可視化一貫化 | ※当初「第3の隠れフィルタ」は**過大評価**と是正済 (funda_pass は②適用バー screener-applied-bar L1935 に既出=隠れていない)。①crow にも出す保守的一貫化のみ | 3 体 review |
| カスタムタグ拡張 (免責) | `isCustom` (L881 付近) を「override あり OR binary トグルが preset 既定=PRESET_PREDICATES.extra から変化」に拡張。funda_pass OFF で「決算合格」を緩めたら「カスタム」表示 | user 目視 OK |
| hover affordance | `.screener-crow__sw` / `.screener-precision-seg__btn` / `.screener-crow__mseg-btn` に :hover (color-mix token)。index.css | user 目視 OK |

- 重要文書: 乖離カタログ SSOT = `docs/specs/AUDIT_2026-06-25_screener-mockup-implementation-divergence.md` (P0-P3 + 3体合議 + 今回の是正を全記録)。
- mockup 正本: `docs/specs/mockups/screener-strategy-presets-v8.html`。

## 🔴 最優先 — user が優先度順で依頼した次タスク

### 別件 A: UI 改善 (user 2026-06-26 明示・mockup 寄せ + 新規)。**mockup-fidelity skill + designing-workspace-ui skill を経由**。screener は danger zone。

すべて `frontend/src/components/CustomScreenerPanel.jsx` (2531行・大きいので sub-agent 委譲 or offset/limit read)。変更後 `npm run build` + `npm run test:unit` (invariant 緑) + `design-system-check`。

1. **「合致度TOP3」表示を削除** (件数不変)
   - 場所: L1485-1518「決断支援ヒーロー (合致度TOP3 + 件数 + staleness + 免責)」(Sprint 5 Pass A)。左の合致度TOP3 (L1493-1518) を削除。
   - 理由 (user): ①下の銘柄リストにも出る ②「なぜTOP3か/選定条件」が読み手に伝わらない (ⓘ tooltip L1501 はあるが不十分)。
   - 注意: 件数は refine header「該当N銘柄」(L1519 コメント) に一本化済。staleness「本日更新（毎朝更新）」(L1523) を残すか含め next session が判断 (hero ごと消すか TOP3 だけ消すか — user は TOP3 削除が主旨)。

2. **折りたたみを「詳細」ボタン → 「絞り込み条件」行全体に** (mockup 準拠)
   - 現状: `detailOpen` state (L714) / 詳細 toggle ボタン L1658-1672 (`screener-detail-toggle`, setDetailOpen, chevron) / detail panel L1676-1681 (`screener-detail-panel--open/closed`)。
   - refine header 見出し「絞り込み条件」h3 = L1437 (`screener-refine-fh__lbl`)、header ブロック L1432〜。
   - タスク: refine header 行全体 (L1432-1437 周辺) を clickable な開閉トリガにし (onClick=setDetailOpen / aria-expanded / role=button)、独立した「詳細」ボタンは廃止。mockup は「絞り込み条件 … 該当26銘柄 ▲」の**行全体**がトグル。
   - ⚠️ sticky 検索バー (`.sticky-search-band`) と近接する領域。CLAUDE.md「触ると危険」。header 構造を壊さない。

3. **市場局面 (フォロー・スルー・デー) バナーを絞り込み条件から分離** (user: 全く違う内容)
   - 場所: `FtdRegimeBanner` (L1426-1430・`features/workspace/FtdRegimeBanner.jsx` 共有)。現状 refine header の直前にある。
   - タスク: バナーを絞り込み条件ブロックと**視覚的に明確分離** (折りたたみtrigger=#2 に巻き込まれないよう DOM/余白を確認)。#2 で header 全行をトグル化する際、バナーがその外にあること。

4. **「アドバンスド（個別に緩急）」トグルを精度行へ移動** (mockup 準拠)
   - 現状: `advOpen` state (L716) / アドバンスド toggle L1693-1703 (`screener-adv-toggle`)、位置=detail panel 内・各条件の真上。
   - 精度 seg (緩い/標準/厳しい) = `screener-control-bar` L1538 / `screener-precision-seg` L1545。
   - タスク: アドバンスド toggle を精度行 (screener-control-bar) へ移動。
   - ⚠️ **要 user 確認**: user は「緩い・標準・厳しい行の**左端**」と記載したが、mockup v8 (screener-strategy-presets-v8.html) では精度 seg が左・アドバンスドが**右端**。どちらに置くか next session が AskUserQuestion で1問確認してから実装 (Trust Cliff でなく好み確認)。

5. **アニメーション追加** (user: 設定変更時に動きが欲しい)
   - 対象: ①折りたたみ開閉 (#2 で全行化する「詳細」相当) ②緩/標/厳 切替ボタン。
   - 現状: precision seg は thumb が translateX で滑走済 (`screener-precision-seg__thumb` transition L1552-1556 + color L1504)。detail panel は display:none+opacity (L1676-1681) で height アニメは無い可能性。
   - タスク: detail panel 開閉に height/opacity の滑らかな transition を付与 (framer-motion 不使用方針・CSS で)。精度ボタンの press/active にも subtle な動き。motion-reduce 配慮必須。design_system.md の motion token を使う。
   - **他の改善案があれば提案可** (user 歓迎・sub-agent review 起動可)。mockup と現状の差分を mockup-fidelity skill で洗うと良い (AUDIT P3 cosmetic 群も候補)。

### 別件 B (🟡 issue #27 復旧後・件数実測 → user 承認): user が別セッションで本番 scan 実行中。**完了後**着手。
- **P1-b セクター別リーダー整理**: ① mcap≥中型 cap 追加 (mcapBands) ② 機関保有QoQ↑ gate 必須化 (45日遅延で件数激減リスク・qa 慎重) ③ 余分 grade (eps_cagr/eps_yoy) 除去。AUDIT L106/L147/L178。**件数を変える**ので scan 復旧 → 実測 → 承認後。
- 精度スライダー段階公開 + default 緩/標 最終判断 (qa公開ゲート: 新高値ブレイク 15-50件で安定 → 現状24件=健全)。

### 別件 C (🔵 Phase C・大きめ・設計判断要): planner で SPEC 起票推奨。
- **P2-e 旬のセクター topN/inrs 可変化 = master-detail 置換** (現状 top5 固定・セクター内 inrs 未実装)。AUDIT L50/L168。これで「上位5セクター自動絞り」理由が①に未説明な点 (AUDIT L237) も解消。**screener 最大の未完**。原則4「人力の代替」に効く。

### 別件 D (⚪ 軽微・記録のみ): 着手可だが優先低。
- 適用バー override チップの「ファンダ:」prefix (L1891) が new_high_break の RS/出来高に意味的に不正確 → `CROW_LAYOUT` group 引きで「モメンタム:」等に。
- `beatOnly` が②適用バー hasActive (L1868) に非登録 (①gate 表示あり=Trust Cliff でない)。
- `ocfMarginOnly` state 完全除去 (no-op 残置・10箇所 extra cleanup)。
- P3-9 (新高値ブレイク tile desc 文言) / P3-11 (cfps「連続増(4期)」vs 3期 tooltip 整合)。

## ⚠️ 触ると危険 / 必須規律 (継続)
- **新設 invariant test を緑に保つ**: screener 変更後 `cd frontend && npm run test:unit` (9/9)。`PRESET_DISPLAY_CONDS` を減らすと fail しうる。
- **件数 SSOT 不変**: `PRESET_PREDICATES`/`itemPasses`/`buildActiveGrades`/`countPreset` を変えると count==list が動く。表示変更は DISPLAY_CONDS / JSX のみ。
- screener `extra` 5箇所重複・sticky 検索バー (Apple 方式・8回試行で安定)・発光系 (.panel-card/.bs-panel/.surface-card) は触らない。
- CSS は token のみ (raw hex/!important 禁止)・color-mix で tint。`design-system-check` 必須。
- vitest 導入済 (`npm run test:unit` = `vitest run`、`__tests__` は exclude 済 standalone node test)。

## ⚙️ 環境メモ
- deploy: `git push origin main` → Railway auto (本番 ACTIVE は /health の commit)。session から本番 URL は egress 403 (api.supabase.com のみ可)。本番 visual 検証は CI (screener_v2_dogfood / vision_eval / playwright_smoke) が代行。
- issue #27 (本番 nightly_scan が gateway 502 で stale) は user が別セッションで scan 実行中。
