# 引き継ぎ書 v177 (2026-06-07)

CAN-SLIM スクリーナー拡張を Phase 1〜3 Sprint 1 まで一気に着地させたセッション。次セッションは **Phase 3 Sprint 2 (N条件 populate)** から。

## 🎯 完了済み (全て merge+deploy+本番検証済)

### CAN-SLIM Phase 1 (UX地ならし) — 完了 (fa44b44)
S1 ラベル明確化 / S2 Pane3 ファンダ・テクニカル2大分類 + ライター憲法サマリー / S3 M地合いバナー surfacing (FtdRegimeBanner) / S4 ローディング skeleton。+ **固有名詞平易化 hotfix (7c96ad4)**: zone-label の出典/説明文から O'Neil/IBD/書名/CAN-SLIM を「一般的なルール / 米国成長株投資の標準的な売買ルール」へ (UI 固有名詞ゼロ、§38 grounding 維持)。

### CAN-SLIM Phase 2 (条件拡張本体 C=四半期EPS YoY%) — 完了 (5702f21)
- **S1** `screener_fundamentals` table (C=eps_yoy_pct + A/N/S カラム NULL 先行、UNIQUE(ticker,calc_date)、RLS、index) + GRANT (4DML+sequence) + retention cleanup cron。
- **S2** 独立 cron `/api/cron/canslim-scan` + `_calc_eps_yoy_pct_from_surprises` (current/prev とも earnings_surprises = quarterly-history 表示と同一 source)。
- **S3** read endpoint `/api/scanner/canslim?condition=eps_yoy&min_pct=18` (DB SELECT only、NULL 除外、partial guard、graceful empty、free)。
- **6体合議 backend gate (2447aa6)**: 全員条件付賛成。critical/major hotfix = ①canslim-scan 並列化 (worker_count+Semaphore、逐次→並列、GHA timeout 回避) ②read に failed_count (3状態 達成/未達/データなし) ③response key tickers→items ④単体+4。
- **S4** frontend C filter (oneill 交差に C を 4th source、未populate 時 graceful「集計前」) + 0件内訳 3状態。
- **S5** chip整理 2本柱 (「全て」=ファンダ柱 + 複合 both/oneill + テクニカル柱 折りたたみ cup/breakout/rs)。funda chip 削除。

### CAN-SLIM Phase 3 Sprint 1 (A条件 + turnaround) — 完了 (5ff3375)
- `_calc_eps_cagr_3y` (3年 EPS CAGR、GAAP annual、赤字base/0除算/<3年→NULL) + ROE (key-metrics-ttm `returnOnEquityTTM`×100、gate1 ROE≥17% 単一) + `_roe_sector_guard` (銀行/REIT/保険/証券/公益/与信を industry ベース除外) + `_calc_turnaround` (前年同期赤字→当期黒字 boolean、率なし§38)。
- migration `turnaround` カラム = **user 適用済 (2026-06-07)**。pytest 214。
- **ROE hotfix (Rule5)**: generator は ROE を `/stable/ratios-ttm` (ROE field 無し) から取り常時 NULL → `/stable/key-metrics-ttm` に修正。dead `financial_ratios` method 削除。

## 🔴 次セッション最優先: Phase 3 Sprint 2 (N条件 populate)
- SPEC = [`docs/specs/SPEC_2026-06-07_canslim-phase3-conditions.md`](docs/specs/SPEC_2026-06-07_canslim-phase3-conditions.md)。6 sprint: **S1✅ → S2 N条件 → 【6体gate】→ S3 S条件+minor → S4 read拡張+excluded分割 → S5 frontend → S6 本番populate+3体/L4**。
- **gate1 確定 (Generator に渡す)**: A=ROE17%単一+広め除外✅ / **N=単独可+extended警告 (near_high 95%)** / turnaround採用✅ / S=出来高+40%独立カラム / A・S=free・N将来Premium / batch endpoint=Phase4送り。
- 起動: `/generator docs/specs/SPEC_2026-06-07_canslim-phase3-conditions.md 2` 相当 (Agent subagent_type=generator)。N は `near_high_pct` カラム populate + extended 警告 (§38: 裸ATH を「買い時」と読ませない)。

## 🟡 重要知見 (Rule 5 = main 側で subagent 出力を必ず実データ検証)
本セッションで 3 件の **FMP field/source mapping + 並列性バグ** を捕捉。いずれも単体テスト (crafted値) はすり抜け、**本番 chunk grep / 実データ helper 呼び** で発覚:
1. **EPS source 混在**: prev を income_statement(GAAP) から取り表示(surprises)と乖離 (DIS 符号反転)。→ surprises 統一。
2. **canslim-scan 完全逐次**: worker_count 無視で GHA timeout。→ cup-scan 並列パターン移植。
3. **ROE field**: ratios-ttm に ROE 無し。→ key-metrics-ttm returnOnEquityTTM。
→ **教訓**: PGE generator/evaluator は real-data 検証が甘い。FMP field 名・source 一貫性・並列実働は main 側で実データ確認必須。memory [[feedback-pge-loop-pitfalls]] (Rule 5) / [[feedback_revenue_basis_mismatch]] / [[feedback_fmp_ttm_field_map]] と整合。

## 📊 運用
- **canslim-scan は nightly (GHA `nightly_scan.yml`、08:07 JST、worker_count:3)** で C+A を毎晩 populate。C filter は populate 後「集計前」→実データに切替。即試すなら GitHub Actions「Nightly … scan」を Run workflow。
- turnaround migration 適用済 → 黒字転換バッジが次 populate で有効化。

## 🔧 Supabase MCP 導入 (保留中、user 相談 → 設定 → 再起動で有効化)
- 目的: migration の手動 SQL Editor 適用を不要化 (私が直接適用)。project_ref=`qwaepmdyybfsnsyitqrm`。
- 手順: PAT 生成 (supabase.com/dashboard/account/tokens) → `claude mcp add -e SUPABASE_ACCESS_TOKEN=<PAT> supabase -- npx -y @supabase/mcp-server-supabase@latest --project-ref=qwaepmdyybfsnsyitqrm [--read-only]` → 再起動。
- A案 read-only (安全、私が検証・user が書込) / B案 write (私が migration 直接適用、human-in-the-loop)。**user が A/B 選択中**。

## 📋 繰り越しバックログ (v176 から、CAN-SLIM 外・未着手)
本セッションは CAN-SLIM 集中のため未着手。前 案B セッションからの継続項目:
- **Stripe Premium 配線** (launch blocker、pre-release 最重要)。
- **count-up「入場時リセット」解消** (soft、未着手): ForwardOutlookSection / GuidanceCard / AnalystPanel の view 前 FINAL→0。
- **案C (RPO/ARR)** — SaaS 将来売上先行指標。2.5人日。
- **通期 EPS YoY の正確化** — basis mismatch で抑止中 ([[feedback_revenue_basis_mismatch]])。
- **nightly pre-warm cron (保有銘柄)** (#2-B、任意)。
- **RS scan 健全性確認**: v176 時点で rs_ratings 最新 calc_date が 3 行のみの pre-existing 状態。nightly RS scan 後に rs_top が ~300 へ回復したか確認 (まだ ~1 なら RS scan 健全性を調査、Sprint 独立)。

## ⚠️ 触ると危険 (CLAUDE.md)
sticky 検索バー / 発光系 (.panel-card/.bs-panel/.surface-card) / VITE_ ARG/ENV 同期 / aggregator への LLM import (pre-commit Check 3) / 既存 migration・既存 endpoint (canslim-scan/scanner/canslim/cup-scan/quarterly-history)。

## メモリ更新済
`project_canslim_screener_expansion.md` に Phase1/2/3-S1 完了記録。`feedback_pge_loop_pitfalls.md` Rule 5。MEMORY.md index 更新済。
