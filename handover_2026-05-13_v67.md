# BeatScanner Handover v67 (2026-05-13)

v66 §1 の Pane 4 click 沈み問題の完全解決 + 観測可能性 (observability) 投資。Sentry + Playwright Tier 1、visual harness exception、ad-blocker 回避 tunnel まで scaffold 完了。Sentry が即座に 9 件の本番未知バグを発見。

## 0. 本セッションで完了した主要タスク

### A. Pane 4 click feedback root cause fix (3 セッション空回りの真相判明)
- **真の root cause**: `.ws-pane4-news-item` の `animation: ws-pane4-slide-in 320ms ... both` の `both` fill-mode で `to { transform: translateX(0) }` が **永続適用** → `:hover` / `:active` の transform を CSS spec 上完全に上書き (animation origin > author origin)
- 過去 3 試行 (`translateY(-1px)` / `(-3px) scale(0.99)` / `translateY(0) scale(0.97)`) は全て無効だった (動いていないものを直していた)
- **fix**: `frontend/src/index.css` L5274 `both` → `backwards` (1 字変更) で stagger delay 維持 + 終了後 natural state に戻る (`commit dda494f`)
- user dogfood で「Pane 3 と同動作」確認済

### B. 視覚検証ループ短縮の構造投資 (Anthropic engineer 強力推奨)
- **Visual harness exception** を CLAUDE.md §デプロイ運用 配下に追加 (4 条件で限定解禁)
- **`frontend/scripts/snap-active.mjs`** (headless Playwright 60s teardown):
  - SNAP_MODE=workspace (既定) → Pane 4 NewsItem の matrix 検証
  - SNAP_MODE=pane3 → Pane 3 NewsCard の matrix 検証 (`commit cb782bc`)
- 検証結果: 両 Pane で hover `matrix(1.008, 0, 0, 1.008, 0, -3)` → press `matrix(0.97, 0, 0, 0.97, 0, 0)`、Δy=3px / Δscale=0.038、Perceivable: YES ✓

### C. Sentry + Playwright Tier 1 scaffold (100 点ロードマップ Tier 1)
- **Sentry frontend** (`@sentry/react`): `frontend/src/lib/sentry.js` で dynamic import + DSN env-var injection
- **Sentry backend** (`sentry-sdk[fastapi]`): `backend/app/main.py` で FastAPI / Starlette integration
- **Dockerfile**: `ARG VITE_SENTRY_DSN` 追加 (既存 VITE_* と同パターン)
- **Playwright smoke test** (3 本): LP / workspace mount / Pane 4 click feedback regression guard
- **GitHub Actions**: `.github/workflows/playwright_smoke.yml` (PR + push to main、continue-on-error)
- `commit 9db471a`

### D. Sentry ad-blocker 回避 tunnel
- **問題**: sentry.io への直送が user の Chrome で「ブロック: その他」(ad-blocker / privacy extension)
- **解決**: `/api/sentry-tunnel` 経由で envelope を backend forward (Sentry 公式パターン)
- **罠**: forward URL に `?sentry_version=7&sentry_key=<public_key>` が必須 (無いと "ProjectId" rejection)
- Playwright で tunnel POST 200 + sentry.io 直接 request 0 件を確認
- `commits 45593c8 + 0486808`

### E. Sentry が即座に発見した本番未知バグ 9 件 ⚡
| project | message | path | severity |
|---|---|---|---|
| BACKEND-6 | `ValueError: Out of range float values are not JSON compliant` | `app.main.get_chart_summary` (`/api/chart/AAPL/summary`) | **Unhandled (要修正)** |
| BACKEND-9 | `OperationalError('database is locked')` | `app.main.get_movers` (UPST) | 同時接続 |
| BACKEND-7 | SPY: No earnings dates found, symbol may be delisted | `app.main.price_history` | delisted |
| BACKEND-1/2/3/4/8 | yfinance 404 / NKLA, $NKLA possibly delisted / 1-2 Failed downloads | `app.main.get_movers` / `app.main.stream_movers` | delisted |
| BACKEND-5 | ZeroDivisionError (test 検証用) | `app.main.sentry_debug` | 検証用 |

これは Sentry なしでは engineer に届いていなかった **real bug**。「testing 15→50」の本質。

## 1. Sentry Phase 1 完全完了 ✅ (2026-05-14 セッション末で確定)

frontend project `javascript-react` の dashboard に user の `Error: CORRECT DSN TEST 1778700990547` (Unhandled) を含む 2 件 event が出現確認 — tunnel 経由で正常届くようになった。

### 真の root cause (4 時間溶けた原因)

**Sentry onboarding flow の Configure SDK 画面で表示された DSN は placeholder で、実際の project に紐付いていなかった**。正しい DSN は project 確定後に **Settings → Projects → javascript-react → Client Keys (DSN)** ページで取得する必要があった。

- 誤 DSN (placeholder): `https://e1aa724b89b3406b35aa4ebf5f1721b4@.../4511382392078336`
- 正 DSN: `https://95cc8f71ca80b436fac2b0444b23cc6c@.../4511382394437632`

Sentry は無効な DSN への event を **200 + event_id 返却で silent drop** する挙動だったため、dashboard 空のままで気付くまで時間を要した。memory `sentry_setup.md` に anchor 済 — 次セッション以降は同じハマりを避けられる。

修正: `railway variables --set VITE_SENTRY_DSN=<正 DSN>` + `railway up` で完了。

## 2. このセッションで先送り (memory に anchor 済)

### handover v66 §2 / §3 のうち未着手
- **§3 Railway 課金 Hobby tier**: 期限 7 日 (画面右上 "8 days or $0.96 left")。次セッション最優先 user action
- **Sentry Phase 1 完全完了**: 上記 §1 残課題に対応必要
- **Pro tier + Stripe**, **Cmd+K universal search**, **Next.js 16 移植**, etc. (handover v66 §2 で anchor 済)

### 構想 memory (将来タスク)
v66 と同じ (project_article_generator / project_hot_topic_discovery / project_kb_integration)。

## 3. 重要 — Railway 課金 (期限 7 日切り、v66 から継続)

画面右上「**8 days or $0.96 left**」(本セッション末の値)。trial credit 残高、次セッション最優先 user action 候補:
- Railway dashboard → Account Settings → Billing → Add payment method → Upgrade to Hobby ($5/月)

## 4. 触ると危険な箇所 (CLAUDE.md + v66/v67 追加)

### v67 追加
- **`animation-fill-mode: both` + transform の罠** (詳細 `feedback_press_feedback_delta.md`):
  - running animation の forwards fill 由来 transform は **`:hover` / `:active` の transform を CSS spec 上完全に上書き** (animation origin > author origin、specificity / !important 無効)
  - 新規 card 系 (`:hover` / `:active` で transform 使う) では `getAnimations()` で animation 確認 + `animation-fill-mode` を `backwards` か `none` に
  - pixel が動かない疑いがあれば DevTools Computed の `transform` 行で matrix を読む (Style pane の overridden 表示は misleading)
- **Visual Harness Exception** (CLAUDE.md 配下):
  - `frontend/scripts/snap-*.mjs` の headless Playwright (60s 内 + 自動 teardown) は preview ban の限定例外
  - long-running dev server / preview / HMR / `localhost:5173` bind / user セッション並走起動は引き続き禁止
- **Sentry tunnel ハマりポイント** (`sentry_setup.md` 参照):
  - tunnel から Sentry へ forward する URL には `?sentry_version=7&sentry_key=<public_key>` が必須
  - public_key は DSN の userinfo 部 (`<key>@host` の前半)、`urlparse(dsn).username` で取れる
  - direct send は SDK が自動付与するが tunnel は backend 側で明示が必要

### v66 から継続
- §-1-B Phase 1 失敗の教訓、`translateEvent` object bug の教訓、発光バグ系 4 セット compound 規律 (memory `glow_elevation_postmortem.md` / `css_specificity_gotchas.md`)
- `.news-list-card` / `.ws-pane4-news-item` は `.is-arriving` 非対象なので compound 4-set 不要
- `pane3_v1` feature flag (PR-2 5 条件統合) は撤回コスト最小化の仕組み

## 5. memory 更新サマリ

### 新規 (v67)
- `feedback_press_feedback_delta.md` (`:active` Δy/Δscale 必須数値 + animation forwards fill 罠)
- `visual_harness_exception.md` (CLAUDE.md 例外の運用 anchor)
- `sentry_setup.md` (2 project 構成 + tunnel + DSN injection 経路 + frontend dashboard 残課題)

### 更新 (v67)
- `MEMORY.md` index 3 行追加

## 6. 本番状況 (v67 末時点)

- 本番 URL: https://beatscanner-production.up.railway.app/
- main HEAD: `commit 0486808` (Sentry tunnel auth fix)
- 全 commit が main 直送デプロイ済、git 履歴と本番一致 (CLAUDE.md「本番直送」ルール準拠)
- 未 commit ファイルなし
- Sentry: backend 完全動作 (9 件本番バグ visible)、frontend は§1 残課題

## 7. 新セッション開始テンプレ

```
handover v67 を読んで、§3 Railway Hobby 課金 (user 操作 5 分) → BACKEND-6 ValueError
NaN JSON 修正 (Sentry が 18 events 検出した最重要 unhandled bug、Pydantic encoder で
NaN → null) → get_movers の delisted ticker フィルタ (NKLA/UPST/SPY/HTTP 401 を
一括解消) を順に進めてください。
```

## 8. 推奨タスク順序 (次セッション以降)

| 順 | タスク | 工数 | 備考 |
|---|---|---|---|
| 1 | **Railway Hobby 課金** (§3) | 5 分 (user 操作) | 7 日後の停止防止、最優先 |
| 2 | **BACKEND-6 ValueError NaN JSON 修正** | 1 時間 | Pydantic encoder で NaN → null。Sentry 検出の最重要 unhandled error (18 events) |
| 3 | **get_movers delisted ticker フィルタ** | 1 時間 | BACKEND-1〜4, 7-A (NKLA/UPST/SPY/HTTP 401) を一括解消 |
| 4 | **Pro tier + Stripe** | 2 週 | 要 user 設定、CVR 直撃 |
| 5 | **Cmd+K universal search 拡張** | 1 週 | news / 図解 / 履歴横断 |
| 6 | **保有 × マクロ AI コメント** | 1 週 | Anthropic 推奨 Phase 1.5 |
| 7 | **Pane 4 NewsRow primitive 化** | 大 | 記事タブ launch と同時 |
| 8 | **Next.js 16 移植** | 2-3 週 | 記事タブ前提、SEO 復活 |

## 9. 本セッションのサブエージェント / 検証実績

- **2 体並列レビュー × 2 回** (Pane 4 click feedback、4 回目): Web app expert + Anthropic engineer
- **CSS animation precedence specialist** 含む 4 回目 review で `animation-fill-mode: both` root cause 判明 (3 セッション空回りの真因)
- **Playwright harness** で pixel matrix を engineer 側で機械検証可能に
- **Sentry が即座に発見した 9 件の本番バグ** = Sentry なしでは届かなかった real bug

合議で converge した実装方針はすべて memory + docs に anchor 済、新セッションで参照可能。
