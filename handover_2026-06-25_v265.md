# 引き継ぎ書 v265 (2026-06-25) — beat/cfps Phase 2 全 Sprint 着地(本番 verified)/ 次=mockup↔実装 乖離監査

> 本セッション成果: **SPEC_2026-06-25 (beat/cfps Phase 2) の Sprint 1/2/3 を main 直接実装 → 本番 deploy → snap で ground-truth 検証、全 PASS**。Sprint 3 で CROW_LAYOUT 漏れ bug を snap が検出し修正。
> 🔴 **次セッション = user 依頼の「mockup ↔ 実装 全体乖離監査」**(本件で 1 件発覚済 = beat の CROW_LAYOUT 漏れ)。

## 🧭 30 秒サマリー

| 項目 | 状態 |
|---|---|
| **main / 本番** | `8bce40a`(Sprint 3 fix)。Railway deploy 反映済 |
| **beat/cfps Phase 2** | ✅ **Sprint 1/2/3 全着地・本番 verified**(SPEC 完了) |
| **commit** | `4f8538e`(S1 backend)→`ff8380b`(S2 trio)→`6d76d6c`(S3 beat gate)→`8bce40a`(S3 CROW_LAYOUT fix)。全 push 済 |
| **実装方式** | main 直接実装(CLAUDE.md 準拠・generator 委託せず)+ snap 検証厳格化(user 承認) |

---

## ✅ 本セッションの着地(全て本番 ground-truth 検証済)

- **Sprint 1(backend `main.py`)**: `_build_universe_payload` freshness map に 4 key(`latest_beat`/`cfps_3y_rising`/`eps_3y_rising`/`rev_3y_rising` = 全て `sf_cd`)追加。本番 curl で 4 key 非null + items 配線確認。cfps true率≈22%(default OFF 判断の裏付け)。
- **Sprint 2(frontend `CustomScreenerPanel.jsx`)**: earnings_pass に連続性 trio(eps3/rev3/cfps3)を **binary 任意トグル・default OFF**。CROW_LAYOUT 新 sub「決算の継続性（連続増）」。**段階UI(mseg)は kind:'flag' で構造的に不可**。C-2 死守: 全 extra(8)+ deps(7)+ applied-bar chip / applied-summary part / empty-suggest / hasBinary / hasActive を adVolumeOnly と同等の first-class 化。snap verdict=PASS(件数 7→6→7 で filter 結線確認)。
- **Sprint 3(frontend 同 file)**: new_high_break に latest_beat を **gate「必須」**(南京錠+必須pill・トグル不可)。PRESET_GATE_CONDS + PRESET_PREDICATES.beatOnly + applyStrategyImpl setBeatOnly。binBindings 非登録(gate)。snap verdict=PASS(data-gate=1・is-gate・pill=1・switch=0・mseg=0)。
- **Sprint 3 fix(`8bce40a`)**: latest_beat の **CROW_LAYOUT 追加漏れ**(描画されない bug)を snap が検出 → 「品質/決算の裏付け」sub 追加で解消。**build では捕捉不能・snap が捕捉**(visual harness の価値実証)。

---

## 🟡 重要知見(永続化候補)

- **new_high_break hero=0 は beat 起因でない(pre-existing)**: premium・locked なしでも 0。実測で `buy_zone(141) ∩ new_high_52w(100) = 5 件`、その 5 件は **全て beat=true → beat は 0 件除外**。0 の真因は「技術的交差が今日 5 件 + standard grades(EPS/ROE/RS)で 0 に」。**SPEC 保険条件「beat で過剰に消える」は不成立 → toggle 降格 不要**。
  - ⚠️ ただし **new_high_break preset が 0 件を返す**こと自体は pre-existing な UX 課題(buy_zone と new_high_52w が定義上ほぼ排他 + grades)。本 SPEC scope 外。別途検討候補(本日データ依存の可能性もあり)。
- **CROW 描画は CROW_LAYOUT の keys 反復が起点**: crow を出すには CROW_BINARY_META + PRESET_DISPLAY_CONDS だけでなく **CROW_LAYOUT への key 追加が必須**(Sprint 3 で漏れた)。新 crow 追加時のチェックリスト。
- **gate crow の条件**: PRESET_GATE_CONDS に key + PRESET_PREDICATES.extra で常時ON + applyStrategyImpl で setXxx(true) + binBindings 非登録。renderCrow の isGate 経路で南京錠描画。

---

## 🔴 次セッション最優先 = mockup ↔ 実装 全体乖離監査(user 依頼)

v264 から継続の user 依頼。本件で既に 1 件(beat の CROW_LAYOUT 漏れ)が乖離例。`screener-strategy-presets-v8.html` 他 mockup と実装を **全 preset・全条件**で突き合わせ、乖離をリスト化 → 修正推奨 or サブエージェントレビュー。SPEC §将来拡張 にも記録。

---

## ⚠️ 触ると危険 / 規律(継続)

- **CustomScreenerPanel C-2**: count(countPreset/PRESET_PREDICATES/applyStrategyImpl)と list は同一述語・同時変更。`extra` は 8 occurrence + deps 7 + 表示系。**`replace_all` は同一 full-form 文字列に限定**(差異ある occurrence は個別・本セッションで実証)。
- **段階UI を boolean に出さない**(trio は kind:'flag' で構造的に不可)。**freshness 無しフィールドを gate にしない**(Sprint 1 で付与済みが前提)。
- 発光系(`.panel-card/.bs-panel/.surface-card`)・sticky 検索バー・VITE_ ARG/ENV 同期・投資色ルール。
- **screener_v2 scope に閉じる**(L1377 gating・legacy 不変・default OFF・`?screener_v2=1` opt-in)。
- 実装は main が直接(委託は調査・多視点のみ)。deploy=`git push origin main`。

## 🧹 掃除 / commit 推奨

- 本 v265 が最新。**v264 は削除**(本 handover で置換)。
- 全コード commit/push 済(未 commit 残なし)。
- 検証 snap(`frontend/scripts/snap-screener-beat-cfps-s2.mjs` / `-s3.mjs`)は untracked(visual harness 例外・regression 用に残置可、不要なら削除可)。
