# BeatScanner Handover v145 — Pane 3 読込速度 + AI要約高速化 + content-quality 監査 (2026-06-01)

> v143b-2 起点。 Pane 3 読込速度改善 → user dogfood feedback (AI要約/記事リンク) → content-quality 監査
> (売上集計基準ミスマッチ修正 + content-audit skill/自動化) まで一気通貫。 全て本番 LIVE・実測検証済。
> v144 handover は本 v145 で置換 (削除)。 詳細 SSOT は各 memory anchor 参照。

## ✅ git 状態 (クリーン・同期済)
- working tree **clean**、 最終 commit **`5adacd1`**、 origin/main へ push 済 = **git=本番一致**。
- 本番 backend commit: **`curl -s https://beatscanner-production.up.railway.app/health | jq -r .commit`** = `5adacd14a41d`
  (v144 で追加した恒久 deploy 検証 marker。 backend-only deploy も一意確認可能)。
- deploy 機構: GitHub 連携で main push → Railway 自動 build (~3-4分、 queue 済なら <30s)。 push 前 `git pull --rebase` 推奨。

## ✅ 本セッション完了 (全て本番デプロイ + 実測検証済)

### A. Pane 3 読込速度改善 (Tier 1/2/3) — [[project_pane3_load_perf]]
- **Tier1 frontend GET coalescing** (`12625a8`、 api.js `dedupGet`): 重複 fetch 撲滅 (analyst **4→1** / insights/guidance/price-history 各2→1)。 prefetch 結果を panel mount が 0ms 取得。 + guidance/basic 6h cache。
- **Tier2 insights** (`1d4b338`/`9575ee9`): KB prompt cache + **KB path 結果を Supabase 永続化** → cold 24s が ticker/24h **1回** (全 user 共有) に。 SQM 0.86s source=supabase で証明。 ⚠️latency は output 生成律速 (13-18s) で input 最適化だけでは下がらない (非自明発見)。
- **Tier3 charts chunk 分離** (`c1286c6`): `charts`(774KB) を recharts/lightweight-charts に分離 → Pane 3 から未使用の lightweight-charts **-197KB**。 chart 描画完全同一。

### B. AI 生成物の高速化
- **冒頭 AI 要約 (SummaryBrief)** (`1f9ea02`): 全 buffer→一括yield の「5s blank」 を **true line streaming** で解消 (TTFB 0.89s)、 + 結果 cache (再訪 0.8s) + system_cache。 ※per-line `apply_deterministic_rules` は whole-text と byte 一致。
- **AI 詳細レポート (detail)** (`eda3ab5`): 結果 cache で再オープン 17.5s→1.1s。

### C. 記事リンク強化 (`05b2d81`)
- Pane 3 末尾の「解説記事」 を preview card 化: 新タブ + タイトル + 相対日付「4日前」 + ↗。 撤去でなく強化 (user の鮮度/中身不明 懸念を日付可視化で解消、 撤去は 1 行 revert 可)。

### D. content-quality 監査 — 売上集計基準ミスマッチ修正 (最重要) — [[feedback_revenue_basis_mismatch]]
**Trust Cliff バグ**: FMP の `revenue_actual`=総収益 vs analyst estimate=純収益 の集計基準ミスマッチで、
**銀行・与信業**に偽の売上サプライズ (JPM/WFC/C +45〜87%、 COF +25.9%) が出ていた。 finance リテラシー高い user に即誤りと判る。
- **`6708af3`/`af38804`**: 40% 閾値 guard を guidance_basic / full guidance / 過去8Q history / **AI図解 trends** に適用。
- **`5ffc582`/`7be90d7`**: **セクター連動に精緻化** (user 選択)。 `_rev_surprise_threshold(sector,industry)` で
  industry 別閾値 = **銀行 0(無条件) / 与信 18 / 他 40**。 ⚠️**FMP は決済 V/MA を与信 COF/AXP と同じ
  'Credit Services' に混在** → 閾値 18 で COF 捕捉しつつ V/MA(±11%) 保護。
- **全レイヤー**: backend guidance (sector fetch + `revenue.compare_unreliable` flag) / frontend GuidanceCard (40%mirror OR flag、 ScorecardCell はフロント再計算なので mirror 必須) / AI図解 trends (`_rev_unreliable`) / 要約 (_format_context 自動)。
- **検証**: JPM/USB/COF=抑止「比較基準が相違」 / V/MA/保険/証券/非金融=据置。 EPS は対象外 (CVX +41% は原油変動で実 beat)。 AXP 12.2% は閾値下で保守的に据置。

### E. content-audit system (誤出力ゼロの仕組み化) — skill `content-audit`
user 要望「機械的に検出して誤出力 100% ゼロに」 への 3 層回答:
- **① runtime guard** (本番コード、 既存) = 既知パターンを毎リクエスト 100% 抑止 (唯一の 100% 機構)
- **② `scripts/content-audit-check.sh`** (`6bd640a`/`5adacd1`) = known-pitfall を本番に assert する機械回帰ネット。
  release-check Step 3.3 に deploy 前ゲート組込。 銀行抑止(1a)+V/MA非抑止(1b) bidirectional。 現状 PASS。
- **③ `content-audit` skill** (`e14af4a`) = 未知パターンの定期発見。 ペイウォールは frontend gate=backend curl で監査可。
- 正直な限界: 既知は 100%、 **未知の新パターンは機械保証不可** (発見のたび①へ落とし網を狭める)。

## ✅ content 監査の clean 確認 (新 must-fix なし)
個人名ガード (insights 6セクター) / AI図解 BAD pattern (断定/最上級 §38景表法) / ETF graceful / 多セクター売上 = 全てクリーン。

## 🔴 残タスク (pre-release 優先: コンテンツ→release準備→集客)
- **content-quality は概ね着地** (売上バグ全層修正 + 自動ネット)。 残 edge: AXP 等 与信 12-18% は据置 (実beat可能性、 user 判断あれば閾値再調整可)。
- **release 準備** (次フェーズ、 大半は business 判断要): Stripe Premium/Signature 配線 (pricing 決定要) / 法務 (特商法・privacy、 本名/住所要、 本格集客直前) / QA (e2e)。 [[project_tier_pro_premium_restructure]] [[project_signature_tier_10k_strategy]]
- **保留** (品質リスクで user defer): Tier2c insights Haiku化 / Tier3p2 recharts lazy-load。

## 🟡 主要知見 (memory 化済)
- [[project_pane3_load_perf]] — perf 全施策。 insights/要約 latency は **output 生成律速** (input cache だけでは下がらない)。 dedup pattern 横展開可。
- [[feedback_revenue_basis_mismatch]] — 売上基準ミスマッチ SSOT。 sector連動閾値 + **GuidanceCard はフロント再計算 (backend guard だけでは効かない)** + visualize は LLM出力上書き (choke point で post-guard) + FMP が V/MA を与信に混在。
- [[feedback_deploy_verify_discipline]] 更新 — `/health.commit` で backend deploy 一意検証。 in-memory warm-read は `/api/insights/cache/clear` で確認。
- content-audit skill — DETECTIVE 監査 + known-pitfall registry + 3層自動化モデル。

## ⚠️ 触ると危険 (継続)
- 発光系 `.panel-card/.bs-panel/.surface-card` / sticky 検索バー / VITE_ ARG/ENV / aggregator への LLM import 禁止 / 「じっちゃま」 UI 禁止 / DailyDigest summary は flex 維持。
- **api.js `dedupGet`**: GET 専用・成功時のみ cache・返却時 clone (参照共有 mutation 防止)。
- **売上 guard の二重構造**: backend (sector閾値) + frontend GuidanceCard (40%mirror+flag) + visualize trends (choke point) を**同期**。 pitfall 追加時は runtime guard + content-audit-check.sh + skill registry の 3 点同期。

## release status
pre-release。 Premium/Signature は LP「近日公開」 (Stripe 未配線=購入不可)。 Pro ¥980 のみ実購入可。
