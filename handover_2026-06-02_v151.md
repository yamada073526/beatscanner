# BeatScanner Handover v151 — ⑩ Phase 1 + Option A + MSFT canary 本番投入 (2026-06-02〜06-03)

> ⑩ Phase 1 (transcript ガイダンス) を Option A まで実装 + 8銘柄 dogfood + 3体合議 (全員 GO-with-changes)。
> **2026-06-03 朝: user gate-3 で「MSFT のみ whitelist 段階 rollout」 承認 → MSFT canary 本番 LIVE**
> (main `316bdf7` push 済、 env `TRANSCRIPT_GUIDANCE_ENABLED=1` + `TRANSCRIPT_GUIDANCE_TICKERS=MSFT`)。
> **canary で §38 bug 1件検出・即修正** (過去実績 margin 混入 → citation 紐付けで解決)。 content-audit PASS。
> 残り全銘柄 rollout + LP unlock は gate-3 継続。 v150 は本 v151 で置換。

## 🌅 起床時サマリー (A/B/C)

### A. 目視 dogfood してほしい (MSFT canary LIVE)
- **MSFT 個別株 detail → AI図解 (DiagramCard) → 次Qガイダンス** を本番で確認:
  「売上高・マージンの公式ガイダンスは公開されていません。 代わりに...引用します（当社の予測ではありません）」
  注記 + opex/capex/margin-direction の narrative + 発言原文 blockquote が出るか。 ※AI図解は Premium gate。
- (v150 carryover) **⑦ 高値圏突破 nightly**: 2026-06-03 09:00 JST verify-v148-extended-gate。 朝に結果通知。

### B. 判断待ち (gate-3、 deploy 前に user 決定が要る)
1. **⑩ deploy GO/範囲**: `TRANSCRIPT_GUIDANCE_ENABLED=1` にするか。 + `TRANSCRIPT_GUIDANCE_TICKERS`
   で mega-cap 限定 (例 "MSFT,META,AMZN,NVDA,JPM,NOW") で段階 rollout するか全銘柄か。 (Railway env 設定)
2. **⑩ LP「ガイダンス」 訴求 unlock**: MSFT/JPM 型 narrative-only も「ガイダンス」 として LP 訴求に
   含めるか (Premium 限定 + funnel-cro 文言精査)。 事実訴求案「CEO が決算 call で語った数字を発言原文リンク付きで確認」。
3. **⑩ UI 文脈中立性**: narrative guidance が 5条件 PASS と同一カード内に並ぶ Trust Cliff を
   content-audit で 1回 dogfood (金融§38 Opus 推奨)。
4. (低優先) スクリーナー UI backlog A-1/A-2/A-3 (sub-agent review 必須)。 release 準備 (Stripe/法務/QA)。

### C. 自動検証済み (確認不要)
- **⑩ Phase 1 + Option A 実装完了** (branch commit `97c69a2`、 push未・deploy未)。 §38 4重防御を
  全 dogfood で実証。 test 31 PASS (transcript_source) + backend suite 85 PASS + frontend build OK。
- **dogfood 全8銘柄 §38-safe** (FMP Ultimate + Sonnet 実測、 content-audit DoD set):
  META/AMZN/NVDA/NOW=構造化 / MSFT/JPM=narrative-only / GOOGL/COST=破棄→記載なし。
- **3体合議 (§38重) = 全員 GO-with-changes** (BLOCK なし)、 必須 change 全反映済。
- **stale test 修正** (commit `1821998`): test_prompt_examples を 3 block 期待に更新 (v120 追従、 ⑩ と無関係)。

## ✅ 本セッション成果

### ⑩ Phase 1 LLM path (commit 97c69a2、 branch、 deploy保留)
8-K が空の企業 (MSFT/JPM 等は決算 call で開示) のガイダンス穴を transcript LLM 抽出で埋める。

- **① sec_guidance.py**: `source_type=transcript` 分岐 (model=Sonnet)、 transcript専用 few-shot 3件、
  BAD-7 (modality/Q&A/過去実績/±%計算/margin=income÷sales計算/**hedge保持**)、 `source_quote` schema、
  confidence 機械的1段降格。
- **② transcript_source.py** (pure-Python、 @no-llm): management-only 抽出、 **Q&A境界を強マーカー文字位置に修正**
  (真因: CFO の prepared remarks 末尾 "let's go to Q&A" 誤マッチで CFO が analyst 誤分類 → セグメント
  guidance 消失する §38 over-correction bug)、 **数値密度優先選択** (前半散発hitで guidance cluster が
  truncate される問題)、 per-field 逐語verify、 source_quote 文単位救済、 narrative数値 §38 backstop、
  blob(全unknown話者)スキップ、 parse_fiscal_quarter (DoD#6)。
- **③ main.py**: A案 fallback配線 (visualize endpoint のみ・guidance/basic 不可触)、 transcript cache
  (key=ticker::year::quarter + per-key Lock + None短TTL 1h)、 ticker whitelist、 Option A presentability判定。
- **④ frontend DiagramCard**: source_type分岐 (transcript は外部link出さず source_label+発言原文blockquote)、
  narrative-only 中立注記 (引用主体・当社予測でない明示)、 精度chip非表示、 sanitize3層目。

### Option A (user gate-3 で承認)
8-K空で発火する MSFT型 (opex/capex/margin-direction = 総売上/margin schema 非適合) を捨てず、
**逐語 source_quote + narrative全数値逐語verify** を満たせば low-confidence narrative で提示。

### 重要 dogfood 戦略的発見
- transcript は「8-K空の時のみ発火」設計 → META/AMZN/NVDA は 8-K にガイダンスあり (`dogfood_8k_presence.py`
  で確認) で **本番非発火**。 本番で発火するのは MSFT/GOOGL/JPM/COST (8-K空)。 → Option A が無いと
  mega-cap で構造化純増ゼロだったため、 Option A が ⑩ の本命価値 (MSFT/JPM の opex/capex/NII 穴埋め)。

## 🌿 branch / deploy 状態
- **main** (tip `316bdf7`、 origin push 済): ⑩ Phase 1 + Option A + §38 canary fix。 **MSFT canary 本番 LIVE**。
- **feat/transcript-guidance-phase1**: main に ff 済 (origin にも push)。
- **本番 env (Railway production)**: `TRANSCRIPT_GUIDANCE_ENABLED=1` / `TRANSCRIPT_GUIDANCE_TICKERS=MSFT`。
  → MSFT のみ transcript 発火。 他銘柄は従来通り (8-K/記載なし)。 全銘柄 rollout は env から TICKERS 追加 or 空に。
- 本番検証済: MSFT visualize?years=4 → narrative_only=true / q_margin=null (46% 除去) / 逐語 citation。
  GOOGL → source_type=8k (whitelist gate OK)。 frontend 柔らか文言 LIVE (DiagramCard-B6rlV7Hn.js)。
- ⚠️ 次 deploy は `railway up` (main から、 worktree 不可)。

## 🔴 残タスク (gate-3 後、 優先順)
1. **⑩ gate-3 user 決定** → enable範囲 + deploy + LP unlock 判断 (上記 B)。
2. **⑩ 追加 dogfood** (QA verdict): 直近2Q連続 / 8-K と call の数値食い違い頻度 / 中型株 blob format。
   `backend/scripts/dogfood_transcript_guidance.py <TICKER>` で再現可 (ANTHROPIC/FMP key inline)。
3. **⑩ Phase 2** (conference カードを transcript 実引用裏付け化、 §38 別gate) → **Phase 3 LP unlock**。
4. **⑩ Motley Fool fallback 削除** (main.py:~5616 検索404 dead path、 FMP transcript 統合済で不要)。
5. **FMP Ultimate フル活用 roadmap** (13F/議員取引/インサイダー/bulk scan、 個別SPEC化)。
6. スクリーナー UI backlog A-1/A-2/A-3 (低優先・sub-agent review)。 release 準備 (Stripe/法務/QA)。
7. (hygiene) 古い handover (v30-v150) 未削除。

## ⚠️ 触ると危険 (継続)
- 発光系 (.panel-card/.bs-panel/.surface-card) / sticky 検索バー / VITE_ ARG/ENV / aggregator LLM import 禁止
  / 「じっちゃま」UI 禁止 / DailyDigest summary flex。
- ⑩ LLM path: **guidance/basic に足さない** (Pane3 latency、 visualize endpoint のみ) / transcript由来は
  confidence降格+逐語grep / analyst発言除外 / model=Sonnet / deploy は gate-3 まで保留。
- Cup-Handle 閾値変更時は LLY/GE/META/NVDA dogfood ([[feedback_cup_handle_thresholds]])。 isNonEquity gate。

## release status
pre-release。 Premium/Signature は LP「近日公開」。 Pro ¥980 のみ実購入可。 ⑩ は Premium 想定 (gate-3)。
