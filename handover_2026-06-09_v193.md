# 引き継ぎ書 v193 (2026-06-09、ファンダ章 hairline+見出し階層 完成 + 会社概要 polish + C-3 設計確定)

v5 ペイン3 ファンダ章を「枠なし hairline + 見出し階層 + 4原則」に再設計しきった。grill→3体合議×5セット→dogfood 3巡の PDCA で v189→192 + C-2 を本番投入。C-3 (競合ナビ) は設計確定・実装は本セッションに持ち越し。設計 SSOT = `docs/references/design_recipes.md §C-11` (4原則+見出し階層)。

## 🎯 完了 (本番 live、全 deploy + snap pageerror 検証済、全て v5 限定・v4/v2/legacy 完全不変)

| commit | 内容 |
|---|---|
| `3e059e9` v189 | hairline 化 (面の引き算: コンテナ背景なし、5条件カードのみ発光で主役、従属3セクションを border-top hairline+space-8) + TtmValuation 枠撤去 + 会社概要 defaultOpen=false |
| `657e853` v190 | 「決算」L2 冠新設 + 会社概要 title L2 統一(titleStyle) + バリュエーション sub 差別化 + 章サマリー非表示 |
| `b7333af` v191 | 会社概要 chevron 右端(chevronPosition prop) + バリュエーション親子(gold left accent+インデント) + 今期/来期 L3化(headingVariant prop) + §C-11(4原則) |
| `2766c6f` v192 | 会社概要 prefetch(prefetchAll 追加、クリック5秒待ち解消) + AI要約チップ削除 + 今期/来期中間見出しキャプション格下げ + 来期 gold bar + 通期窮屈解消 + 次期見通し hover + 章扉 L1 強調(emphasized prop) |
| `bb805d7` C-2 | 会社概要末尾の競合を枠グルーピング + 企業ロゴ併記(CompanyLogo)+ size up |

- **見出し4階層**: L1章扉(gold hairline 60%+primary/700、emphasized) > L2冠(13/700/uppercase/primary、決算/バリュ/会社概要 統一) > L3サブ(12/500/muted、今期/来期/TTM label) > L4値。
- **共有 component の v5 変更は全て prop 出し分け** (renderSection/titleStyle/chevronPosition/headingVariant/sectionHeadingStyle/emphasized)、default で v4 不変。発光系 class 新規追加ゼロ。

## 🔴 次セッション最優先: C-3 競合ナビ (パンくず) 実装
- **設計確定済 (3体合議+user 確認)**。詳細は memory **`project_competitor_nav_breadcrumb.md`** (実装手順・注意・2経路問題)。
- 要点: 横並びパンくず「⌂ › AAPL › NVDA」(Hero 上独立28px) / workspaceStore detailHistory / replaceState 維持 / resultCache hit で 0秒戻り / **スクロール+accordion 復元込み(Phase1、sessionStorage)**。
- ⚠️ blast radius 中: detail 遷移が **2経路** (onAnalyze / setActiveTicker)、scroll container 実行時特定。planner→generator (PGE) or 集中実装推奨。

## 🟡 残タスク
- **テクニカル章への横展開** (推奨順、C-3 の次): 4原則 + hairline + 見出し階層(L1-L4)をテクニカル章に。2章目確定で SectionLabel/HairlineSection を primitive 化(frontend verdict)。
- **dead code クリーンアップ** (軽、次のついで): ProfileCard.jsx の A-3 で `CITATION_TEXT_SHORT` / `CITATION_TOOLTIP` / `Sparkles` import が unused(build は通る)。A-3 コメントの「末尾で継続使用」は不正確、要修正。
- **章サマリー速報スタイル再設計** (memory `project_chapter_summary_jitchama_style`、§38 6体gate、未着手)。
- **v5 → v4 昇格** (dogfood 十分後、価格 ladder と合わせ全ユーザーへ)。

## ⚠️ 危険箇所 / 運用知見
- **GuidanceCard は panel-card + tier-m-glow + ？モーダル + §38免責 + SignalQualityChip が密集**。見出し(h3/h4 = headingVariant)以外は不触。panel-card 枠は外さない(useArrivalSpotlight observe 消失 + skeleton 崩壊、glow_postmortem §v62)。
- **build green ≠ ランタイム安全**。今セッションで JSX コメントを `return (` 直後に置き build エラー1回(Stop hook が検出)。検出器 = `snap-v5-dogfood.mjs` console/pageerror。
- 発光系 `.panel-card/.surface-card` / sticky検索バー(§C-6) / VITE_ ARG/ENV / aggregator LLM import。
- gold hairline/accent は `color-mix(var(--color-gold) N%)`、 elevation_scale whitelist(#d4af37)内。raw hex 禁止。
- **Railway 連続 push で queue 詰まり** → feedback はまとめて1 deploy(今セッションは v189/190/191/192/C-2 を間隔あけて個別 push、問題なし)。

## ⏳ pending (継承)
- 明朝 09:00 JST `canslim-null-reasons-verify` / S5b バッジ列 pixel 未確認。
- スクリーナー メンタルモデル再編 grill-me (発表会FB §C、未着手)。

## 📋 deferred backlog (継承)
- upgrade モーダル design polish / screener tier 方針 / Pane2 検索結果レイアウト。
- Stripe Premium 配線(launch blocker) / @dataclass ScanResult 化・scan async/chunk化(3000 universe 502)。
