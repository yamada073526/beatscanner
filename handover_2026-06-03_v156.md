# BeatScanner Handover v156 — 案A本番化 + FMP②③着地 + ①FMP-key blocker (2026-06-03)

> autopilot 長時間セッション (user 出勤、18時帰宅予定)。 図解を 案A (serif+ゆとり余白) に本番化、
> FMP② アナリスト予想 + FMP③ 決算後反応 を本番投入・検証済。 FMP① 13F は **Ultimate 契約済なのに
> 本番 restricted** という blocker を発見 (要 user 対応)。 v155 を本 v156 で置換。
> **次セッション: ①FMP-key blocker 解消後に 13F 実装 / それまでは ②③ の content-audit が残**。

## 🌅 現状サマリー (全て本番 LIVE)

### 図解デザイン = 案A「編集的」 本番化 (e5ff844)
- user が `/diagram-preview/` で 3案比較 → **案A** (Noto Serif JP 見出し + ゆとり余白、 ブランド色 cyan 維持) を採択。
- `DEFAULT_VIBE = {headingFont:'serif', spacing:'loose'}` を DiagramCard 既定に → production 全 caller が案A。 preview は 3案比較継続。
- gold (案B) は未採用 ([[feedback_gold_accent_continuity]] 全 panel 一貫が前提のため見送り)。

### FMP② アナリスト予想 (f9be7f8) + FMP③ 決算後反応 (52db0a8) — 本番検証済
両方 build_analyst_view / compute_reaction (§38-safe 計算済) を visualize payload に並列 attach、 frontend section 追加。
- **②** アナリスト予想: 目標株価レンジバー (安値—中央値—現在値—高値) + 評価分布 (強気/中立/弱気) + 直近修正件数 + 免責。 将来グループ (ガイダンス直後)。 本番 curl 検証: AAPL median $326/high $400/low $253, 強気68/中立25/弱気7。 ⚠️§38: 上昇余地% 出さない。
- **③** 決算後反応: 「決算後±5営業日 Beat時 平均+X% / Miss時 平均−Y%」 (過去実績) + 免責。 成長ストーリー直下。 本番 curl 検証: AAPL Beat時+3.06% n=5 (Miss 0=graceful)。 じっちゃま「決算は反応を見ろ」 直結。
- backend は ②③ を 1 つの並列 gather (quotes/earnings/prices) に統合して latency 抑制。 best-effort (失敗で figure 不変)。

### スマホ閲覧: 公開プレビュー URL (継続)
- **https://beatscanner-production.up.railway.app/diagram-preview/preview.html** (案A=採用 / 旧デザイン / 案B-gold の 3案比較、 ②③ section 込み)。 review 用一時ページ、 方向確定済なので削除可。

## 🔴🔴 次セッション最優先: FMP① 13F の blocker 解消 (user 対応必須)

### ⚠️ 発見: Ultimate 契約済なのに 13F 機関保有が本番 restricted
- `/api/insider/AAPL` → `sources.holders = "restricted"`、 holders 0件。 `fmp_client.institutional_holder` (`/institutional-ownership/symbol-positions-summary`、 path は FMP 公式で正しい) が空を返す。
- 原因推定: **本番 Railway の `FMP_API_KEY` が Ultimate キーに未更新** (user が plan 上げたが env var 旧キーのまま) か、 当該 endpoint が契約 tier 対象外。
- **user action**: ① Railway `FMP_API_KEY` が Ultimate アカウントのキーか確認・更新 ② FMP dashboard で `institutional-ownership` が plan に含まれるか確認。
- 解消後: ① 13F = 「機関保有比率の方向 + 増やした/減らした機関数」 (個社名は出さない、 45日遅延注記) が **差別化の目玉** として実装可 (analyst verdict、 SPEC `docs/specs/round3-fmp-ultimate-content.md`)。 institutional_holder の holders[].change で増減社数を集計する ①-lite から着手可能。

### FMP ④⑤ は低優先 (本セッションで feasibility 確認済)
- **④ インサイダー買い**: form4='ok' だが AAPL の Purchase(P) **0件** (S7/G2/M17/F4)。 経営陣の自発的買いは稀で大型株は大半空 → 散発的価値、 見送り。
- **⑤ 議員取引**: fmp_client に method 未実装 (net-new) + α 薄い engagement 枠 + ①同様 restricted の可能性。 後回し。

## ⚠️ 触ると危険 (継続)
- 発光系 / sticky 検索 / VITE_ ARG/ENV / aggregator LLM import / 「じっちゃま」UI / DailyDigest summary flex。
- DiagramCard: outer wrapper overflow:hidden / `<style>` / 新規 glow host 禁止。 ②③・締めカード・SectionConclusion 文言は**静的維持** (LLM 化で §38 risk 復活)。
- **DEFAULT_VIBE (案A) = production 既定**。 preview だけ各案を明示上書き。 gold は新色 taxonomy 判断。
- visualize の ②③ attach は best-effort。 ⑩ MSFT canary LIVE。 viz 診断 curl は throwaway years。

## ✅ FMP②③ content-audit 完了 (8 ticker、 2 fix 着地)
- 8 ticker (MSFT/JPM/NVDA/KO/PLTR/LLY/BRK-B/SMCI) で /api/analyst + /api/earnings-reaction を監査 → レンジ妥当・rating 合計一致・極端値なし・sources 全 ok。
- **fix1 (5873bfa)**: target_range.count は擬似 field 数 (全 ticker=4)、 「n=4社」 は Trust Cliff → FMP に numberOfAnalysts 無し → 人数表記 omit (「目標株価レンジ」)。 SSOT = memory [[feedback_fmp_consensus_field_count]]。
- **fix2 (958bfe2)**: NVDA Beat時 −5.14% (good news 効かず下落) を Beat=緑固定で表示 = 色ルール違反 → 値の符号で色付け (signColor)。

## 🟡 残バックログ
- ①FMP-key 解消 → 13F 実装。 FMP⑤ (要 key 確認後)。
- ⑩ 全銘柄 rollout / LP ガイダンス unlock。 スクリーナー UI。 release 準備 (Stripe/法務/QA)。 古い handover 未削除。
- 図解 vision スコア 59.3 (案A で typography/aman 微増見込、 次回 score-diagram で再計測)。 70+ は構造/ブランド判断領域。

## release status
pre-release。 Premium/Signature は LP「近日公開」。 Pro¥980 のみ実購入可。 ⑩ + AI図解 (案A + FMP②③) は Premium 訴求の核。
