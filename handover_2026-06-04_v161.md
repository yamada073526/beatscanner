# BeatScanner Handover v161 — スクリーナー master-detail 化 (D2) + 朝 dogfood 全件 LIVE (2026-06-04)

> v160 (Phase 2 + B/D1/D3/D4) に続き、 D2 (スクリーナー pane2 移設 master-detail + ペイン3 ウォッチ追加ボタン)
> を planner SPEC → user gate (案B) → 実装 (Sprint1+2) → 3体合議 (全員 SHIP-WITH-MINOR) → deploy → 検証 まで完遂。
> **現本番 bundle: `index-DJng2WBb.js`**。 chunks: Workspace=index-CLWfc6lp / ScreenerPane=iPTljVSz / JudgmentDetail=CZYpQND0 / CustomScreenerPanel=(v160 系)。

## 🟢 本番 LIVE (検証済) — D2 スクリーナー master-detail (commit 129c17b / 211e75c)

SPEC: `docs/specs/SPEC_2026-06-04_screener-pane2-relocation.md` (案B、 backend/LLM/DB 全不変の frontend 局所)。

### Sprint 1: master-detail 化
- **Pane 2 (狭) = Explorer** (`CustomScreenerPanel`: 絞り込み chip + RS/Cup/Funda 結果) を常駐 = master 面。 結果クリックは `setActiveTicker` のみ (tab 離脱せず) → 絞り込み/sort/scroll が**保持**される (= user dogfood「結果に戻れない」 を構造解決)。
- **Pane 3 (広) = 銘柄未選択時 Hero (今注目 3 セクション) / 選択時 JudgmentDetail**。
- `ScreenerPane` は Hero only 化 (Explorer 撤去 + WIP banner 撤去 + `setActiveTab('home')` 除去 + 未使用 import 削除)。
- 3体合議 fixes: **[必須]** screener tab 切替時に `activeTicker` reset (home の選択銘柄が screener 入場時に残る問題解消、 `prevTabRef` で transition のみ・mount/deep-link は維持)。 **[推奨]**「← 今注目に戻る」 deselect button を Pane 2 上部に (JudgmentDetail layout 不触、 低リスク)。

### Sprint 2: ペイン3 ウォッチ追加ボタン
- `JudgmentDetail` Hero 右上 (EarningsRing + verdict chip の並び) に「ウォッチ追加」(accent/add) /「追加済」(muted+Check)。
- `App.jsx` の detailContext 2 箇所に `watchlist` + `onAddToWatchlist:addToWatchlist` 配線 (sticky 検索 div 不触)、 Hero/JudgmentDetail へ prop drill。
- `addToWatchlist` 経由で 重複ガード + 無料 3 件制限 toast + 未ログイン同期 toast を継承 (qa-dogfooder Trust Cliff 7 項目 PASS、 制限バイパスなし)。 緑赤の投資色不使用。

## ☀️ 朝 dogfood してほしい (headless 検証不可、 Premium gate / login 必須)
1. **スクリーナー master-detail**: スクリーナー tab を開く → Pane2 に絞り込み結果、 Pane3 に「今注目」Hero。 結果クリック → Pane3 が詳細に、 **Pane2 の絞り込み結果が残る** か。 詳細表示中に Pane2 上部の「← 今注目に戻る」 で Hero に戻れるか。 home で AAPL 分析中 → スクリーナー tab に切替で Hero から始まるか (AAPL 詳細が残らないか)。
2. **ウォッチ追加ボタン**: 銘柄詳細 Hero 右上の「ウォッチ追加」 → 楽観追加 + 「追加済」 化。 無料 user で 4 件目 → toast + upgrade modal。 未ログインで押下 → 同期 toast (登録強制モーダルなし)。
3. (v160 持ち越し) B カップ絞り込み / D4 NKE ガイダンス文言 / D1 Hero chip 整列 / D3 図解 skeleton。

## 🟡 残 minor (3体合議 deferred、 次 sprint 候補)
- **deselect 導線**: 現状 Pane2 上部の「← 今注目に戻る」 button。 ui+architect は Pane3 breadcrumb 化を推奨 (JudgmentDetail layout に手を入れる要あり)。
- **Hero refetch flicker**: activeTicker null→ticker→null の往復で ScreenerPane remount → Hero 3 fetch 再走。 display:none mount 維持 cache pattern で対応可 ([[feedback_diagram_card_remount_cache]] 前例)。 deselect UI 追加で頻度上がるため優先度中。
- **CustomScreenerPanel user/isPro prop 未宣言**: Workspace/ScreenerPane から渡しているが component が destructure せず (既存、 demo blur は backend _masked で機能)。 frontend 早期 gating で Trust Cliff 低減。

## ⚠️ 触ると危険 (継続)
- 発光系 / sticky 検索 div (App.jsx) / VITE_ ARG/ENV / aggregator LLM import 禁止 / 「じっちゃま」UI / DailyDigest summary flex。
- DiagramCard: outer wrapper overflow:hidden / 新規 `<style>` / glow host 禁止。 GuidanceSection の 3 分岐文言は §38/§5 査読済。
- **Workspace pane2/pane3 の isScreener 分岐**: master-detail の心臓部。 触る前に SPEC §8 リスク (filter reset / TickerBridge / PaneErrorBoundary key / LazyMotion scope) を再読。
- **MetaFilterPanel / useMetaFilter** は RS/Cup 共有。

## 🟢 russell3000 nightly = LIVE (v160 から継続)
RS universe 989→1699 (手動 dispatch 完了)。 差 ~750 は 6ヶ月 history なし ticker skip (期待)。 GitHub Actions cron は scheduled 遅延あり、 即時反映は手動 `gh workflow run nightly_scan.yml`。

## 🟡 残バックログ
- D2 残 minor (上記)。 RS full universe coverage (1699→2449)。
- ⑩ 全銘柄 rollout (§38) / LP ガイダンス unlock / release 準備 (**Stripe Premium 配線=Trust Cliff blocker** / 法務 / QA e2e)。
- 古い handover (v30〜v159 大量) 掃除 (user 承認後)。 global coverage / ETF保有 / vision 70+ 構造案。

## release status
pre-release。 Premium/Signature は LP「近日公開」、 Pro¥980 のみ実購入可。 AI図解 (案A+FMP①②③④⑤) + russell3000 screener + RS/Cup sort/filter + master-detail スクリーナー が Premium 訴求の核。
