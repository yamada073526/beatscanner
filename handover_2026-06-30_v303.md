# Handover v303 — §③ テクニカル・買い場 価格機能 mockup 反復 (v1→v5) + 次は「原版 vs v1-v5」客観比較

> 作成 2026-06-30。branch `claude/technical-buy-zone-l4-vdxl5d`（PR #140 draft）。前任 v302 の続き。

## このセッションでやったこと

### 1. §③「テクニカル・買い場」価格機能の改修 mockup を新規作成 (v1→v5)
自宅PCで以前作成した原版 mockup が **未 push で復元不可**（git 全 ref・Drive・当環境セッション履歴を探索したが無し）と判明したため、v5 正本（`pane3-full-v5.html`）を素に新規作成。`docs/specs/mockups/pane3-technical-buyzone-v{1..5}.html`：

| 版 | 内容 | 主な転機 |
|---|---|---|
| v1 | 初版（買い目安レンジ・R/R比 7.5:1・横の距離ルーラー） | — |
| v2 | **プロトコル是正** | 6体（KB抽出×2 + 金融[Opus]/UI/frontend-arch/QA）で v1 の誤り発覚 |
| v3 | 4体デザインレビュー反映 | Q1 サマリー位置 / Q2 グラデ判定 |
| v4 | 距離ルーラーとラダーの**重複解消** | "余地"をラダー現在地マーカーへ統合 |
| **v5** | **チャート下を de-noise**（現行提案） | 3状態凡例削除・chips 4→2・文簡潔化 |

### 2. 小修正2件（同 PR #140）
- §③ 完全性台帳を detail 最上部 → テクニカル章カード末尾へ移設（mockup 準拠）
- §④ 図解 章見出しに Pro バッジ追加（mockup `pro-tag` 欠落）
- 検証: `vite build` exit0 / `vitest` 130 passed / design grep 0件 / 単一経路で二重 mount なし

## 確立した設計原則（実装で厳守・レビュー合議で確定）
- **buy zone = Pivot〜Pivot+5%**（O'Neil 正統 `canslim_oneill_rules.md`）。じっちゃま KB「ブレイクで買う／『下がったら買う』は無意味」。**現在地(Pivot未到達)=「ブレイク待ち(監視)」**、買いを断定しない。
- **§38/景表法**: 緑赤を価格帯に塗らない（行全体色塗り=売れ示唆で BAN 済・rail 全塗りはその拡大版）。「買い場/押し目/好機/急騰」は BAN ワード → 状態記述へ。confirmed breakout(出来高≥1.5x)のみ緑可（過去確定事実）。
- **色規律**: cyan を「上昇」に使わない／gold は Premium 専用。
- **Trust Cliff**: 無料 plan で Pivot 距離(+12%)を出さない（逆算で価格漏洩）。null pivot(カップ未検出)時は「未検出(対象外)」表示・ティーザー非表示。
- 数値=Python 物理層 / narration=別 layer（Hallucination Guard）。

## 🔴 次セッションの本命タスク（user 依頼・最優先）
**自宅PCの原版 mockup と、本セッションの v1-v5 を客観比較するサブエージェントレビュー。**
1. **前提**: user の原版 mockup（§③価格改修＋目次フロート）は未 push。**user が新セッション開始時に push する**（`docs/specs/mockups/` 等へ）。push 後に branch 名/ファイル名を確認。
2. **手順**: 原版 と v1-v5（本 branch）を並べ、多視点サブエージェントで**客観比較** → 最良案 or ハイブリッドを推奨。
3. **比較軸**: 2秒理解 / ブランド世界観(最高級ホテル) / §38・色規律 / じっちゃまプロトコル整合 / 実装可能性。

## 残バックログ
- **§③ 実装**（比較で確定した正本を実装）: PriceLadder.jsx 中心。frontend-arch プラン = ① summary 昇格 ② "余地" ③ ブレイク出来高ゲージ ④ リターン折りたたみ。**未着手**。最重要 gate = 無料 plan で Pivot 距離非表示。
- **#4 §④ 図解(L5 Pro gate)**: Pro badge 済、free placeholder の Trust Cliff(funnel-cro)、DiagramCard 2 instance(API cost 2x)。
- **#2 未ログイン完全 workspace 化(案B)**: mockup 作成が先決 gate・master key `App.jsx:964`・memory 原文は当環境に無し。
- **PR #139**(hr削除) merge は別途 user gate。

## 厳守事項
- 件数/方針 SSOT は user 承認 gate（勝手に確定しない）。
- 検証=ground-truth: `cd frontend && npx vite build` + `npx vitest run` + design-system-check grep（raw hex/box-shadow/contain:paint/:has/!important=0件）。eslint exhaustive-deps not-found は pre-existing quirk（無視可）。
- deploy=PR squash-merge→Railway auto-deploy、**merge は user gate**。本番視覚も user gate（authed headless 不可・preview 禁止）。
- danger zone: 発光系(.panel-card/.bs-panel/.surface-card)・sticky 検索バー・`index.css`（screener 並行編集中）は触らない。
- mockup の視覚確認は headless Playwright (`frontend/scripts/snap-buyzone-mockup.mjs <ver>`, 例外4条件遵守) で `.visual/` に PNG 化できる（executablePath=/opt/pw-browsers/chromium-1194/chrome-linux/chrome）。
- 和文応答（tool description も和文）。
