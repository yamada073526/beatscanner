# handover v223 (2026-06-17: breakout SPEC v3 → 6体合議 6/6 GO → Sprint 0(nightly 502是正)+Sprint 1(_detect_breakout) 着地。次=Sprint 2 nightly配線=BLOCKING#1)

> 前提: v222 supersede。本セッション = user 在席で sprint-by-sprint 進行(各 sprint deploy 前に user 確認)。コンテキスト50%で新セッション移行。
> SSOT: memory `project_breakout_signal.md`(breakout全体) / `nightly_502_gateway_persist.md`(今回の重要発見) / SPEC `docs/specs/SPEC_2026-06-16_breakout-signal_draft.md`(**v3**, 1340行台、§1が実装SSOT) / `SPEC_2026-06-16_breakout-decisions-worksheet.md`(決裁台帳)。

## ✅ 本セッション完了(全て push 済)
- **Task#4 retest A先行公開 LIVE**(`36d9ac8`): /api/scanner/retest + ScreenerPane「リテスト接近」+ StateCompass label + Premium gate一本化。本番22件・検証PASS。
- **tool-call プレーンテキスト化 再発防止 三重anchor**(`c6ec05a`): memory + index + CLAUDE.md。コンテキスト過重で tool-call が `<invoke>`化する再発障害の防止SOP。
- **breakout SPEC v3 + 再6体合議**: 決裁9件 final lock(user「全部推奨で」) + §12.3 MAJOR 7件反映 + 敵対critic で BLOCKER(bo_low_vol leak)/§1.7.5 §38 landmine 修正。**再6体合議(decision⑨)= 6/6 GO_WITH_CONDITIONS / implementation_ready=true / blocking ゼロ**。commit `84c5447`〜`d8d9fdc`(全push済)。
- **Sprint 0 (nightly 502 是正)** `f8fa840`: nightly_scan.yml に Freshness gate。**🔑検証で重大発見: Railway gateway は ~5min で 502 を返すが backend scan は完走し DB に persist する(502≠stale)**。当初の HTTP-502-で-赤化 gate は fresh データでも毎晩 false alarm → **read endpoint の DB date を `>=前日` で判定する freshness gate に修正**、緑確認済。memory `nightly_502_gateway_persist.md` に永続化(handover v221「502=データ喪失」を訂正)。
- **Sprint 1 (_detect_breakout)** `8614b28`: backend 純粋検出関数。**配線ゼロ=本番挙動不変(dead code)**。main.py +196/-0 additive、15 golden test 全pass。

## 🔴 次セッション = Sprint 2 (nightly配線 = BLOCKING#1・最注意)
**配線して初めて /api/technical に bo_* が現れ観測可能になる。** 以下を additive 実装:
1. **`_scan_one`(main.py:L16352)の 5要素化【BLOCKING#1】**: 現状4要素 `(ticker, result, retest, err)`。全 return(**L16359 / L16364 / L16367**)+ unpack(**L16373**)を `(ticker, result, retest, breakout, err)` の5要素に**漏れなく**揃える。1箇所漏れると `asyncio.gather` unpack で**全502銘柄 ValueError → nightly全停止**(`feedback_pge_loop_pitfalls` ルール1)。sequential path はループ内ローカル変数 + upsert ブロック追加で書き分け(tuple拡張でない)。
2. **`_scan_breakout` ラッパー新設**: `_scan_resistance_retest`(L12987)を mirror。同一OHLCV使い回し(追加fetchゼロ)。`_detect_breakout`(L13105、実装済)を呼ぶ。
3. **pattern_type='breakout' upsert**: retest の `_upsert_pattern_signal(ticker, "resistance_retest", ...)` を mirror。`on_conflict="ticker,pattern_type,signal_date"` で別namespace。**bo_pending は nightly 非保存**(§4.1、confirmed/soft/extended のみ)。response に breakout_detected_count/breakout_upserted_count。
4. **`get_technical`(L13794、assembly L13854-13908)に breakout additive**: resistance_retest 追加点(L13899-13908)を mirror。`patterns_result["breakout"]` に。spy_up(L13861)を渡す。**M3: bo_* を `_STATE_PRIORITY`(L18817)/`_detect_signal_transitions`(L16548)に additive 登録(cup の breakout_*/`_CUP_TRANSITION_MAP`/`_CONSENSUS_CUP_STATES` 不変)**。
5. **nightly_scan.yml Freshness gate に breakout 追加**: `/api/scanner/breakout`(Sprint 5)に as_of を返す設計にし `chk "breakout as_of" ... ".as_of"` 1行追加(Sprint 5 で。Sprint 2 では未)。
6. **検証**: py_compile + _scan_one の return/unpack 整合を grep で厳密確認(全 return が5要素) + golden test 維持 + deploy 後 `/api/technical?ticker=X` に breakout キーが出るか curl。**TECHNICAL_CANONICAL_PATTERNS(api.js:5) に 'breakout' 追加(M7: 全ticker cache 一斉miss、deploy は UTC23:30以降=nightly後推奨)**。

## 🟡 残 Sprint (順序)
- Sprint 3: frontend 無料viz(出来高バー、StockPriceChart + lib/volume.js 新設) / Sprint 4: Premium分類(classifyBreakoutZone + BUY_ZONE_LABEL_JP/COMPASS/BREAKOUT_STATE_LABEL_JP bo_*辞書) / Sprint 5: screener(/api/scanner/breakout、Phase A から backend 物理除去=_resolve_premium、retest の frontend blur をコピーしない) / Sprint 6: §38層(blocklist BAD-10/prompt_negatives BAD-11) + 訴求確定。

## 📌 実装時 条件(6体合議・SPEC §12.3-A checklist)
- F②extended閾値+10% / F③stage filter は **breakout 実装後に③(S&P500)再計測**して最終確認(confirmed/extended比 + confirmed内base_rise分布)。激減なら訴求降格(funnel-cro gate)。
- **breakout email配信は後続Phase DEFER**(retest B と同じ。`_detect_signal_transitions("breakout")` は現状None。着手時は専用/api/cron/breakout-notify + bo_*専用mailer + funda filter、cron_cup_notify 拡張禁止)。§4.2-A(4)。
- masking backend物理除去は Phase B(現状 plan==='premium' 表示gate)。

## 🔵 残バックログ(retest 系・breakout とは別系統・DEFER)
- **Phase B masking 強化**: retest/breakout の backend 物理除去(/api/technical の payload を非Premium 物理除去 + fetchTechnical 認証付与)。現状は `plan==='premium'` 表示gate(P0 leak は止まっている)。優先度中。
- **Task#5 retest B本番公開**: retest の配信(6体合議 賛成1/6=DEFER)。配信flow gate + mailer retest 専用テンプレ(DISCLAIMER_*_RETEST) + LP/UpgradeModal + 公開前6体合議。breakout email配信(§4.2-A(4))も同じ後続 Phase で順序整合。
- v219繰越(D2構造改修 / B7図解3層 / B7 §38 false negative)未着手 DEFER。

## ⚠️ 触ると危険 + 運用
- **Sprint 2 BLOCKING#1**: _scan_one 全 return を5要素で揃える(漏れ=nightly全停止)。
- nightly 502 は gateway timeout で backend は persist(502≠stale)。健全性は DB freshness で判定([[nightly_502_gateway_persist]])。
- bo_* state 改名は実装時 `_STATE_PRIORITY`/`_detect_signal_transitions` に additive(cup の breakout_* 不変)。
- deploy=git push origin main→/health commit→検証。git add は明示path。pre-commit hook(Hallucination Guard)有効。
- 発光系/sticky検索バー/aggregator LLM import禁止/blocklist⇄prompt_negatives mirror。

## 次セッション 推奨着手
1. /fetch-handover(本ファイル) + memory `project_breakout_signal.md` / `nightly_502_gateway_persist.md` 確認。
2. **Sprint 2 を sub-agent(Opus)で実装** → _scan_one 整合を厳密 grep 検証 → deploy 前に user 確認 → push → /api/technical curl で bo_* 確認。
3. 以降 Sprint 3-6 を順次(各 deploy 前 user 確認)。
