# handover v219 (2026-06-16 夜間 autopilot — リテスト検出 MVP + B10公開 + 図解修正)

> 前提: v218 superseded。本番 = `edc52d4`。2026-06-15 夜〜16 朝、user 就寝中の autopilot。
> 「安全に出せるものは flag default OFF / 法務修正として ship、本番公開(Premium課金/メール配信/LP掲載)と6体合議は朝の承認に残す」方針で実装。

## ✅ 完了・本番反映済み

### A. 夕方〜夜の dogfood 反映
- `9238776` **B10 chart tabs v2 を default ON 公開**(縮尺タブ 1M/3M/6M/1Y/5Y、user dogfood GO)。`?chart_tabs_v2=0` で個別 OFF 可。
- `e5f7435` **AI図解2点修正**: ① summary/headline に `word-break: auto-phrase`(文節改行、「増/加」分断解消、Chrome119+/Safari17.4+) ②「詳しく見る」L3 toggle を中央揃え(`.diagram-l3-toggle__text` align-items center)。bundle 反映確認済。

### B. リテスト検出 MVP (SPEC_2026-06-15_resistance-retest)
じっちゃま 6/13 ライブ「ブレイクアウトした旧抵抗が支持転換し、そこまで押し戻した銘柄」の自動検出。**本番実OHLCで NVDA=deep(72%)/AAPL=shallow(37.6%)/GOOG=非該当/AVGO=非該当 を再現確認済**。
- `4ab8e92`/`0fbcfb8` backend `_detect_resistance_retest`(`/api/technical` に additive、try/except ガード)。**pivot 基準** `retracement_pct=(cup pivot - 現在値)/(pivot - band_high)`、deep≥50%/shallow30-50%/非該当。box_support(role=resistance_turned_support, **touch≥5**)主語、band_low割れ除外、高値圏 soft backstop 0.70、**market_context=="weak"(SPY下降)除外**、pivot不在(GOOG型)非該当。RS非gate。
- `ca6f63b` golden test 11+2ケース(`backend/tests/test_resistance_retest.py`、CI専用=local py3.8はzoneinfo不可)。
- `bf80ed9` §38層: **BuyZoneCard 緑(gain)/シアン(accent)→中立(muted/amber)是正(全ユーザー・法務)** + resistance_retest 表示(**flag `?retest=1` default OFF**) + buyZoneLabels 静的辞書(免責込・LLM不使用) + **blocklist BAD-11**(リテスト/サポート将来条件付き買い断定)を frontend⇄backend 1:1 mirror。
- `edc52d4` Phase-gate review の安全 must-fix 3件: touch 4→5 統一 / market_context weak 除外 / BAD-11 に なら/たら 追加。
- `eb6297c` SPEC を tracking 化 + pivot修正 + 6確定事項 + 実装ステータス追記。

### ⚠️ SPEC §1.2 の重要修正(実装で確定)
SPEC の「band内(band_high→band_low)retracement」は role の定義上 today>band_high で**常時負=3段閾値が発火しない数学的破綻**。検証エージェントの **pivot 基準**に修正。pivot不在GOOGは自然に非該当=確定事項①と一致。C1b は 0.85→**0.70**校正(NVDA0.824/AAPL0.829 誤除外回避、崩れ除外は role が担う=PLTR/TSLA/MSFT/META overhead_resistance, AMZN pivot不在を実データ確認)。

## 🟠 朝に承認が必要(B: 本番公開 = Phase-gate)
**3体 Phase-gate verdict は全て CONCERNS**: 現状(flag OFF + detail + 色是正)は **§38安全で本番据え置き GO**。ただし**本番公開(配信ON/Premium/LP)に進む前の must-fix**:
1. **mailer の `ファンダ 5 条件: PASS (5/5)` ハードコード是正**(funda非必須なのにPASS虚偽表示=景表法§5)→ 配信実装PRのDoD。現状 mailer に retest 実装なし=安全。
2. **detail/screener に tier gate + Free payload masking**(現状 ?retest=1 で誰でも見える、LP訴求した瞬間 Trust Cliff)→ funnel-cro。
3. **`_CONSENSUS_CUP_STATES` allowlist に resistance_retest 追加**(無いと nightly保存されない=安全装置)。追加PRで §7一式(transition map/件名別定数/funda崩れ除外/healthy_retest)同時に。
4. **閾値50/30%の S&P500上位50-100銘柄 分布検証**(現状実測2点のみ、日次3-10件に収まるか)。
5. **決算直前(3日以内)警告**(ギャップダウンでサポート割れリスク)。
- → **本番公開(default ON)は上記後 + 6体合議 + funnel-cro Trust Cliff 7項目**。screener Hero section / chip / planGating / UpgradeModal / LP bullet / StateCompass label も未実装(本番公開面なので朝の合議後に着手)。

## 🟡 朝 dogfood してほしい(A: flag付き目視)
- **リテスト**: `localStorage.setItem('retest','1')` → リロード → 個別株 detail の BuyZoneCard。
  - NVDA=「旧レジスタンス・リテスト水準」カード(retr≈72%)、AAPL=shallow+「押し戻し浅い」注記、**GOOG/AVGOはカード出ない**(非該当)。
  - **緑・シアンが1pxも無い**(delta/chip/label全て muted/amber)。免責 footer + 「ご自身で確認」両方可視。
  - `?retest=0` で消える(可逆)。
  - SPY等の指数/PLTR・TSLAで出ない(非該当)。
- **§38 実地リスク(唯一)**: AI図解(DiagramCard)で NVDA 生成 → 図解が「リテスト水準なら反発」「サポートで買い」を喋らないか(BAD-11 でガード済だが実出力確認)。
- **B10/図解(夕方分)**: `?chart_tabs_v2=1` 不要(公開済)。図解の改行が自然か(auto-phrase未対応ブラウザなら BudouX に切替可)。

## ⚠️ 触ると危険(継承)
- 発光系 / sticky検索バー / aggregator LLM import禁止 / plan gating は getPlan(subscription)経由。
- deploy = git push origin main → /health commit一致 → bundle grep。**git add は明示 path**。
- **blocklist.js ⇄ prompt_negatives.py は必ず 1:1 mirror**。
- BYPASS_TOKEN無し=authed/flag は headless視覚検証不可、検証は build+test+本番curl+bundle grep、視覚は dogfood。
- **resistance_retest/`_detect_resistance_retest` は additive・LLM不使用・数値物理層**。narration は buyZoneLabels静的辞書のみ(LLM拡張BAN)。
- mailer の SUBJECT_TEMPLATE 変数名を変えると既存配信が KeyError(別定数 RETEST_SUBJECT_TEMPLATE を新設)。

## task list 状態
B10公開✅ / 図解修正✅ / リテスト検出backend+test✅(本番再現確認) / §38(blocklist BAD-11+色是正+辞書)✅ / detail表示flag✅ / Phase-gate 3体review✅(CONCERNS=据え置きGO) / handover✅ / **残=配信/Premium/LP/screener section/閾値分布検証(朝合議後)**。

## 🔵 v218 からの繰り越し / 状態更新
- **D2 構造改修**(株価 identity統合 / KpiStrip純化 / 完全性バッジ圧縮): 未着手 DEFER。`?pane3_header_v2=1` の dogfood は済(静止時/hover/会社力 OK)だが、構造改修本体は視覚検証不可 + 情報設計判断(skill 3d)のため次セッションで着手。**KpiStrip移動は bs-panel glow 巻き込みリスク（発光高リスク）に注意**(1つずつ flag 内で)。
- **B7 図解3層**(DiagramCard 累進開示 L1/L2/L3 再編): 未着手 DEFER。L1境界を先に確定してから。§38=**将来材料を緑にしない**(緑=過去/現在の確定 polarity 限定)+ bull/bear を中立カテゴリ名に。layer-2/3(blocklist/narration)は先行対応済。
- **B7 §38 false negative 追加候補**(底上げ/貢献/ドライバー/ブースト): dogfood で実出力を見てから判断(過剰削除リスクと天秤)。
- 解決済(drop): **B10 chart tabs**=default ON 公開済 / **D2 第3手**=dogfood OK / **D2 rest glow**=ゼロ維持で確定(静止時 dogfood OK、dark 優先で確認済) / **stash@{0}: B10 chart_tabs_v2 を default ON 化する未commit残骸**=pop して公開済(9238776)。

## 次セッションの最初の一手
1. `/fetch-handover`(本ファイル)→ 朝 dogfood verdict 収集(上記A)。
2. リテスト本番公開を判断するなら: ①閾値 S&P500分布検証 → ②6体合議 + funnel-cro → ③配信PR(mailer是正/allowlist/§7一式/tier gate)→ ④screener section → ⑤ default ON 昇格 + LP掲載。
3. SPEC = `docs/specs/SPEC_2026-06-15_resistance-retest_draft.md`(pivot修正・6確定事項・実装ステータス反映済)。
