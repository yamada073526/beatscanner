# BeatScanner Handover v140 — 夜間 PDCA + D1 課金戦略 + D3 直近8Q + dogfood 多数 (2026-05-31)

> v139 セッションの続き。 夜間自律 PDCA の後始末 → D1 (¥10k 課金戦略 6体合議) → D3 (直近8Q 条件3=売上YoY) →
> 直近8Q の user dogfood 7 round を経て着地。 v139 handover は本 v140 で置換 (削除可)。

## 🎯 本セッション完了済 (commit、 全て origin 同期済)

| commit | 内容 |
|---|---|
| (夜間) `a233de0`〜`73d59a0` | 夜間自律 PDCA Cycle 1-16 (全て deploy なし audit/health/SPEC seed)。 user push 依頼で全 push 済 |
| `31bd46d`/`5a07d54`系 | OVERNIGHT_PDCA_LOG / handover の **捏造 hash 訂正** (実 fix=`6d96fd2` / 本番=index-BYOpiuZP系) |
| `14995d6` | **D3 Phase A**: 直近8Q に 条件3「売上高成長率 YoY」 追加 (backend date照合計算 + frontend muted badge + summary stat + 加速/減速注記) |
| `3ecb881`/`cabc1d9`/`5a07d54`/`3457da9`/`fb9dbba` | 直近8Q dogfood: 健全性 tooltip / streak 色(amber→中立グレー) / skeleton 真因(ghost-bar 0px→height) / native title→**InfoTip(createPortal)** / viewport clamp / **じっちゃま露出除去** |
| `6c992bf` | 直近8Q: EPS サプライズ列ヘッダー統一 (サプライズ→EPS サプライズ) |

- **最新 HEAD = `6c992bf`** (origin/main 同期済、 working tree clean)。 直近 deploy の本番 bundle は次セッション開始時に `curl`+grep で再確認すること ([[feedback_deploy_verify_discipline]]: 検証してない hash を書かない)。

## 💰 D1 課金戦略「月1万円」 6体合議 verdict (memory [[project_signature_tier_10k_strategy]] が SSOT)
- ¥10k = 機能の足し算でなく **「Co-Pilot 自動執行」 positioning**。 看板 = 「保有/WL を毎晩スキャン→決算ブリーフ+Cup-Handle/breakout シグナルを朝 push」 (= user 既存最優先要望と一致)。
- 順序制約: Stripe Premium 配線 (Phase 3) が先 → PMF 計測 → ¥10k は招待制/waitlist → DAU100 後本配線。 pre-release で即出さない。
- **tier 構造**: user 補足「認知ゼロ=今プラン変更しても問題なし」 で Trust Cliff 消滅 → **funnel-CRO anchoring 案 (Free/¥980/中間decoy/¥10,000) 寄り**。 中間価格 + 各 tier 機能配分は **後タスクでサブエージェントレビュー** (user 指定、 優先度低)。

## 🔴 残タスク (user 承認順、 次セッションはここから)
1. **A2 (承認済み・実装待ち)**: サンプルカード残存絵文字 📄(LandingPage:1091)/⚡(:1248)/📌(:1281)/🟢🔴(:1221-23) を lucide 化。 user は **②ミニマル + 市場の声🟢🔴のみ③方向ドット の hybrid** を推奨採択済。 実装 → build → deploy → 本番 grep。
2. **2b (新規・要レビュー可)**: 株価チャートの ◯ アイコン (現在株価 / Pivot の ReferenceDot) に hover で「アイコンの意味と内容」 を表示。 現状は他点と同じ tooltip (日付/終値/Pivotまで%/損切り%) のみ。 StockPriceChart.jsx の ReferenceDot。 tooltip 実装は [[feedback_tooltip_portal_pattern]] 遵守 (portal+fixed+clamp)。
3. **D2 (Trust Cliff 修正)**: UpgradeModal:38 / LandingPage:1724 が Premium「抵抗線」 を訴求しているが、 チャートに**抵抗線ラベルが未表示** (box_support.role==='overhead_resistance' は StockPriceChart:1124 で除外)。 訴求 vs 実装の乖離 = Trust Cliff。 D4 とチャート系合同レビュー推奨。
4. **D4 (要レビュー)**: breakout チップ強調 (Cup-Handle 同様 amber+点滅級に) + スクリーナーに breakout 条件追加 (慢心銘柄混入の懸念は user 認識済) + 「breakout」→「Breakout/ブレイクアウト/新波動入り」 呼称変更。 サブエージェントレビュー希望。
5. **B3 (要レビュー)**: LP 未ログイン時の空 detail pane の cyan 枠。 [[feedback_no_baseline_cyan]] と要確認 (選択先アフォーダンスの可能性も)。
6. **(優先度低)** tier 中間価格 + 機能配分レビュー / Pane3 読込速度改善 (user 言及、 低優先)。

## 🟡 本セッションの重要知見 (memory 永続化済)
- [[feedback_tooltip_portal_pattern]] — tooltip は createPortal+fixed+clamp。 native title 不可。 ghost-bar は height 必須。 transform 祖先(tier-m-glow/SectionFade)で fixed 壊れる
- [[project_signature_tier_10k_strategy]] / [[project_quarterly_3conditions]] / [[feedback_deploy_verify_discipline]] (v139 で content-hash 一致法は Railway 不可、 本番 chunk 直接 grep + git cat-file -t)

## ⚠️ 触ると危険 (継続)
- 発光系 `.panel-card/.bs-panel/.surface-card` (design_recipes §C) / sticky 検索バー / VITE_ ARG/ENV / aggregator への LLM import 禁止 / **「じっちゃま」 は UI 禁止** (内部コメントのみ可、 v140 で tooltip 露出を 1 度やらかした → 「ファンダメンタル5条件」「独自プロトコル」 を使う)

## release status
pre-release。 Premium は LP「近日公開」 表示のみ (Stripe 未配線=購入不可)。 Pro ¥980 のみ実購入可。
