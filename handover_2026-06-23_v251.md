# 引き継ぎ書 v251 (2026-06-23) — screener 戦略プリセット層: SPEC + モックアップ v8 + Sprint A frontend先行スライス着地

> v250 を置換。**screener「戦略プリセット層」を SPEC 化(原典再導出) → モックアップ v1-v8 で詰める → Sprint A の frontend 先行スライスを実装**。全て feature branch `claude/jolly-wozniak-bgdvzi` / **PR #1 (draft)** に push 済。**本番未デプロイ**(screener_v2 flag default OFF)。
> v250 は削除可。

---

## 🧭 30 秒サマリー

| 項目 | 状態 |
|---|---|
| **branch / PR** | `claude/jolly-wozniak-bgdvzi` (HEAD `54a612b`) / **PR #1 draft** https://github.com/yamada073526/beatscanner/pull/1 |
| **本番 HEAD** | 変更なし(main 未マージ)。screener_v2 flag = **default OFF**、dogfood は `?screener_v2=1` |
| **完了** | SPEC v2(原典再導出4プリセット) + モックアップ v8 + Sprint A frontend先行スライス実装(commit `1b17cf4`) |
| **最優先の次** | 🔴 残 Sprint A: **backend 0-call fields** + 新高値ブレイク完全化 + UI①合否理由(静的) + モック準拠リッチ表示昇格 |
| **push 権限** | ✅ 今セッションで解決(GitHub App に beatscanner write 付与済)。`git push` 通常動作 |

---

## ✅ 今セッション完了（commit 付き）

### 1. SPEC（DEFER-SPEC: 提案のみ）
`docs/specs/SPEC_2026-06-22_screener-strategy-presets_draft.md`（§7.5-7.9 に dogfood 反映の経緯）
- **核心**: 他AI提示の叩き台5案(パーフェクトゲーム/新波動入り/IPOサバイバー/Rule of 40/全天候型配当)を、独立2体Opusレビューで**じっちゃま原典(投資哲学10箇条+絶対6条件)に逐語照合 → 却下/純化 → 原典忠実な買い側4プリセットへ再導出**:
  - **決算合格**(funda+CF・第1+6条) / **新高値ブレイク**(第4条) / **旬のセクター**(第7条テーマ物色・既存5案の最大空白) / **セクター別リーダー**(第3条業界No.1)
  - 全案 **0-call**(既存22 field の集計のみ)。崩れ検知(売り側)は user 判断で「今は入れない」
- **KB抽出 条件閾値ラダー**(原典頁つき): EPS +20/25/50/100%、buy zone ≤+5%、RS≥70床、CFマージン≥15% 等
- **アドバンスド=Pro課金ゲート**(funnel-cro Opus合議): 精度3段はFree100%動作(Trust Cliff回避)、個別緩急のみPro、件数ちら見せ。競合差別化「勝ち筋が最初から入ったスクリーナー」。価格方向性 Pro¥1,480-1,980/Premium¥2,980/Signature¥10k

### 2. モックアップ v1-v8（`docs/specs/mockups/screener-strategy-presets-v{1..8}.html`、**v8最新**）
実装前に詰めて手戻り削減(user講義指針)。v8 までの主な確定:
- Lucide公式アイコン(badge-check/trending-up/layout-grid/crown)、**選択中のみゴールド + hover発光**、タイルはシアン下地
- アニメ: 精度segmentedスライドthumb / トグルknob(in-place更新) / 件数pop(spring) / パネル開閉grid-template-rows(開350/閉200非対称) / prefers-reduced-motion
- **全条件に個別緩急**(緩/標/厳/最厳・数値併記)、二値は「必須ゲート」明示、グループ化、カスタムタグ
- **v8: 精度の件数単調性バグ修正** = `eff()`(override優先/無ければtargetに追従) で 緩い49→標準26→厳しい11

### 3. Sprint A frontend先行スライス実装（commit `1b17cf4`・screener_v2 flag裏・本番影響なし）
| ファイル | 内容 |
|---|---|
| `frontend/src/components/StrategyPresetBar.jsx` | **新規**。Chip(segmented)で「決算合格/新高値ブレイク」。`STRATEGY_PRESETS` がSSOT |
| `frontend/src/features/workspace/ScreenerMaster.jsx` | custom モード上部に bar 差込、`customPanelRef.applyStrategy()` 呼出 |
| `frontend/src/components/CustomScreenerPanel.jsx` | **forwardRef化 + useImperativeHandle `applyStrategy`** (既存挙動不変、export default は L1575) |
| `frontend/src/index.css` | `.screener-strategy-bar`(semantic token・shadow/glow なし) |
- マッピング: 決算合格→`fundaPassOnly+ocfMarginOnly+ocfGtNiOnly` / 新高値ブレイク→`buyZoneOnly`(pivot≤+5%)
- ビルド✓(13.17s)、Chip primitive再利用・raw hex/shadow/!important なし

### 4. hook修正（commit `54a612b`）
`.claude/hooks/post_edit_build_check.sh` の Mac 絶対パス(`/Users/yamadadaiki/...`)を `${CLAUDE_PROJECT_DIR:-<fallback>}/frontend` に変更 → リモートで ENOENT 誤作動していたのを portable化(ローカル/リモート両対応)。

---

## 🔴 最優先の次 = 残 Sprint A

1. **backend 0-call fields追加**(nightly canslim-scan `_compute_one` に既存fetchのデータ再利用で): `eps_3y_rising`/`rev_3y_rising`/`cfps_3y_rising`(monotonic)、**セクター別RS集計**(既存 rs_vs_spy_pct を sector で集計=0 call)、breakout/52週高値 facet。SPEC §3 / handover内のコスト表参照。
2. **新高値ブレイクの完全化**: 現状 `buyZoneOnly` のみ(薄い)。cup/breakout/52週高値 facet 追加後、CustomScreenerPanel の `applyStrategy('new_high_break')` ブランチを拡張。
3. **frontend**: UI①「合否理由」**静的dict**(LLM不使用・STATE_LABEL_JP方式・§38安全)、モック準拠のリッチ表示への昇格(発光は design_recipes §C-1〜4 経由)、4プリセットへ拡張、IA昇格(現状 custom モード内 → 画面トップ)。
4. **DEFER**: 旬のセクター/セクター別リーダー(セクター別RS前提=#1完了後)、崩れ検知(売り側・「今は入れない」決定済)。

---

## 🟡 user 確定事項（永続化候補）
- v2ハイブリッド方向で確定 / P5全天候型(配当NISA)は「今は決めない」 / 崩れ検知は「今は入れない」 / 着手は「frontend先行(flag裏)」
- 「実装前にモックを詰めて手戻りを減らす」(AI講義第8回の指針)。模範解答PDF=Lucide系線画アイコン×角丸タイル
- deep-research/ultrathich は**計画段階のみ**許可、実装段階は不可(トークン節約)

---

---

## ✅ 前版(v250) backlog の決着（carryforward 確認）

v250 の「🔴 残タスク = KB参照の指標追加・プリセット改善案」(**user 依頼 (2026-06-22)** / **指標追加・プリセット設計 = user 判断必須(DEFER-SPEC)** / **判断軸**) は本セッションで**完了**:
- 他AI叩き台5案 = **パーフェクト・ゲーム** / **新波動入り** / **次世代の主役** / **高効率SaaS** / **全天候型ブルーチップ** → 独立2体Opusレビューで原典照合し**却下/純化 → 買い側4プリセットへ再導出**(§1 参照)。脱落なし。

---

## ⚠️ 触ると危険 / 規律
- **発光系**(`.panel-card`/`.bs-panel`/`.surface-card`) → 戦略プリセットは Chip primitive で回避済。リッチ化時は `design_recipes.md §C-1〜4` 必読・compound 4セット。
- **CustomScreenerPanel は forwardRef化済**(applyStrategy expose) → 既存の蛇口(preset/overrides/binary facet)挙動を壊さないこと。
- **screener_v2 flag**(`isScreenerV2()` in ScreenerMaster.jsx): default OFF。dogfood `?screener_v2=1`。昇格は Sprint 6 C-16 ゲート後。
- deploy=`git push origin main`(Railway auto)。今は feature branch のみ、**main 未マージ**。
- sticky検索バー / VITE_ ARG/ENV同期 は不触。

## 🧹 掃除
- handover **v250 は削除可**(本v251最新)。
- モックアップ v1-v7 は履歴(v8最新)。残置可。実装が進めば古い版は削除可。
- PR #1 は draft のまま。レビュー/実装が進んだら ready 化。
