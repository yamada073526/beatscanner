# handover 2026-06-26 v270 — screener mockup-fidelity 完了 (C/D/pane幅/X-2) + Zenn tool-call 対策

> 前提 (不変): 正本 mockup `docs/specs/mockups/screener-strategy-presets-v8.html` を完璧再現。
> v269 の残タスク **C / D / pane幅 / X-2 を本セッションで全消化・本番検証済**。残は **Q4 (defer) のみ**。
> 現本番 = `76cbcf3` (health.commit で確認)。

## ✅ このセッションで ship + 本番検証済 (蒸し返し禁止)

すべて `git push origin main` → Railway auto-deploy → authed snap (computed-style + screenshot) で ground-truth 検証済。

| 項目 | commit | 検証 (snap) |
|---|---|---|
| **C** adv-toggle iOS switch化 (Q1 3つ目) | `2f8e9ea` | OFF/ON computed 完全一致 (gold35% / thumb gold-mid translateX14)。`snap-screener-adv-switch.mjs` |
| **D** 絞り込みヘッダー mockup 1行化 + 該当N銘柄 gold + hero-count廃止 (Q2/Q4) | `5ac9981` | lbl 13/600・live b 20/700 gold-mid・hero-count removed。`snap-screener-refine-header.mjs` |
| **pane幅広化 (B案)** Pane2 idle 62% + strategy tile 縦化(icon上)で全title 1行 | `53ab736` / `8f55b08` | Pane2 61-62%・tile 172px・全 title 1行 natural。`snap-screener-pane-width.mjs` |
| **X-2** 精度 seg 左に「精度」ラベル常時表示 | `76cbcf3` | label 左・12px muted・control-bar 36px 1行・overflow なし。`snap-screener-precision-label.mjs` |
| dead code 除去 (Workspace.jsx useState / SectionHeader / WatchlistRow) | `b1c09a8` | -117行・build + pre-commit 通過 |
| Zenn tool-call 崩壊対策 → CLAUDE.md §節 + memory | `3ab3b96` | 振る舞い指針化 (予防3 + escalation)、逐語例は memory 遅延ロード |

## 🔴 残タスク — Q4 (defer・user 判断保留)

着手時は大型 UX 再構成のため**別途 SPEC 化が必要**:
- **D-8 sort select** (時価総額 / 出来高順、master-detail 再設計を伴う)
- **cup 状態トグル** (.th.state)
- **seasonchip**

> pane が idle 62% に広がったので、追加 UI の余地は v269 時点より広い。

## 🟢 意図的変更 = 維持 (mockup に戻さない)

- **I-1〜I-6** (v269 継続): new_high_break desc / 営業CF>純利益 / CFPS 4期 / accordion closed起動 / preset null起動 / 件数=実 universe predicate
- **X-8** mseg Free locked は `cursor:pointer` 維持 (Pro 誘導の意図的設計)
- **X-11** strategy-bar bg-subtle (IA 境界)
- **★ 本セッション新規 preserve**:
  - **strategy tile = 縦レイアウト (icon 上)** — mockup は icon 横並びだが、埋め込みペイン (62%) で title 1行化するための適応 (user 承認 2026-06-26)。`.screener-strategy-tile__top` の `flex-direction: column` を横並びに戻さない
  - **Pane2 idle = 62% / Pane3 = 20%** (WorkspaceShell `pane2.maxSize 62` / `pane3.minSize 20`、Workspace.jsx idle `p2.resize(62); resizeP3(20)`)

## ⚙️ ツール

- skill: `.claude/skills/mockup-fidelity/` (検出 / triage / 検証の機械化)
- 検証 snap (本セッション新規・untracked・回帰用): `frontend/scripts/snap-screener-{adv-switch,refine-header,pane-width,precision-label}.mjs`
- 実行: `cd frontend && set -a && . ./.env && set +a && node scripts/snap-*.mjs` (Premium 注入 = .env `DOGFOOD_TEST_*` / `lib/auth-helper.mjs`)
- 正本 mockup: `docs/specs/mockups/screener-strategy-presets-v8.html` / 監査台帳: `docs/specs/AUDIT_2026-06-25_mockup-cosmetic-drift.md`

## ⚠️ 触ると危険 (継続)

- 発光系 (`.panel-card / .bs-panel / .surface-card`) — design_recipes §C-1〜C-4。tile halo は別系統
- sticky 検索バー / VITE_ ARG/ENV 同期 / aggregator の LLM import 禁止
- `.screener-control-bar` は nowrap 1行固定 (X-2 追加後も applied-summary の truncate が吸収・余裕あり)
- pre-commit no-unused-vars BLOCK (dead code 削除漏れに注意)

## 環境メモ

- deploy: `git push origin main` → Railway auto (~90-120s)。`/health` の commit で確認。現本番 = `76cbcf3`
- **並行セッション**: dead code 除去の chip タスクは本セッションで `b1c09a8` 着地済 → 別 worktree 出力は冗長 (クローズ可)。別セッションの `074da3f` (Sentry 修正) が log に混在するが独立
- branch: main 直 push。**明示 path add で entanglement 回避** (本セッションで実証: `074da3f` 混在時も私の commit は意図した 2 ファイル限定だった)
- **Opus 4.8 tool-call 崩壊対策** が CLAUDE.md §「コンテキスト過重による tool-call 崩壊の防止」に反映済 (予防 = 散文短縮 / 並列発行 / ターン冒頭、escalation = 復唱しない → `/rewind` → `/model sonnet`)。本セッションは並列発行 + 散文短縮で崩壊ゼロ
