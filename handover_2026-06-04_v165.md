# BeatScanner Handover v165 — autopilot 3rd round (hairline 真因fix + breadcrumb + release prep) 2026-06-04

> 2h autopilot: hairline ズレ真因修正 (border-box)、 承認済 cleanup、 D-1 breadcrumb、 価格/集客 review (Opus)、 スクリーナー豪華化 SPEC、 法務 scan、 release runbook。 D-2 は Trust Cliff 理由で defer。
> **現本番 bundle: `index-C88A_QiC.js`** (価格反映 + スクリーナー豪華化 案1/2/3/6 まで含む)。

## 🟢 本 round 完了 (本番反映 + 検証済)

1. **hairline ズレ 真因修正** (commit 系列、 deploy A `index-BrZB7UWn.js`): 前回 minHeight:2.5em が **box-sizing:border-box** で padding-bottom(8)+border(1) を含み、 2行 heading(content 45px)が 54px に成長・1行(45px)と **9px ズレ**ていた。 → `minHeight: calc(2.5em + 8px + 1px)=54px`。 **実測検証: hairline Y [208,208,208] ALIGNED (1920/1440 両幅)**。
2. **承認済 cleanup**: 古い handover 48件削除 (v165 のみ)、 `/dogfood/` ギャラリー削除 (本番 404 確認)。
3. **D-1 deselect breadcrumb** (deploy B `index-DO1wEfw3.js`): screener mode Pane3 上部に「スクリーナー > TICKER」。 JudgmentDetail 内部不触 (flex column wrap)。 **screenshot 検証: breadcrumb 表示 + JudgmentDetail 完全描画 (崩れなし) + back で Hero 復帰**。 ⚠️Pane2「← 今注目に戻る」 と併存 (案C)、 breadcrumb 一本化希望なら Pane2 button 撤去可。

## 🔵 提案・defer (user 判断待ち)

### 価格/集客 review (funnel-CRO Opus、 task #17 完了) — ✅価格 user 確定+反映済
- **価格確定 (2026-06-04 user 承認): Free / ¥980 / ¥3,980 / ¥9,800** (Opus 推奨=過去合議案を採用)。 理由: ¥3,980 の anchoring(decoy) + ¥9,800「1万円の壁」回避。
- **反映済 (deploy `index-B3NTf_O0.js`)**: LP Premium 表示 ¥1,800→¥3,980 (grep 検証: 3,980×1/1,800×0、 「近日公開予定」維持=購入路なし Trust Cliff 安全)、 planGating コメント、 runbook §4。
- **残 (Stripe sprint)**: Stripe ¥3,980 price 配線 (yearly ¥39,800? 要確認)、 Signature ¥9,800 列新設+tier 実装 (招待制 waitlist 推奨)。
- **価格論より CVR 本丸**: ①Free 内 AI図解 1回体験 (Aha 前倒し、 現状 trial 弱点) ②件数+銘柄名blur 飢餓感 gate (但し買える状態=Stripe 先)。
- **集客 Top**: X 決算速報 (AI図解 画像)、 SEO 記事タブ (post-release)、 招待制 waitlist。
- ⚠️**実装乖離 Trust Cliff**: planGating は free/pro/premium のみ (Signature 未実装)、 LP は Premium ¥1,800 表示。 価格確定時に LP/planGating/Stripe を完全一致させる。

### スクリーナー豪華化 v2 (`docs/specs/SPEC_2026-06-04_screener-richness-v2.md`) — ✅案1/2/3/6 実装済
user「stagger イマイチ、 home Pane3 級に豪華に」→ 推奨順で実装 (user「推奨の順番で」 承認):
- **✅案2 L字 gold frame**: HeroSection の eyebrow+h4 を 左 3px gold bar + 下 hairline で囲み home ds-section-header の「格」 移植。
- **✅案1 halo sweep**: HeroSection に tier-m-glow + useHaloSweepOnce (home Pane3 と同 proven pattern、 IO 進入で 1 回 sweep、 fired ambient glow)。 inline radius 8px 維持 (halo inherit)、 boxShadow active?ring:undefined。
- **✅案3 空 section「設計された沈黙」**: Hourglass icon + breathing room (事実文言不変/§38 safe)。 空 card の bare 感解消。
- **✅案6 stagger**: section base delay 0/80/160 → 0/120/240 で cascade を deliberate 化 (SPEC 0/160/320 は遅すぎ risk で中間値、 体感調整可)。
- **⏸案4 (live dot) / 案5 (gold foil rank1) は hold**: 過剰装飾 regression risk ([[feedback_minimalism_over_additive]]) + 実装複雑性 (gold foil の theme-contrast、 dot の絶対 positioning)。 案1/2/3 で home idiom は達成、 案4/5 は user が live を見て要否判断。 案7 (見出し行) は文言 gate 必要。
- **検証**: 発光安全 (新 glow host なし、 既存 pattern 流用)、 build/grep PASS、 vision-eval 3 run mean **73.7 (vs 74.5、 Δ-0.8 = noise 内 = regression なし)**。 ⚠️richness の価値は halo motion + gold frame 格 + empty 整え (静止 vision-eval に乗らない) → **user 体感確認推奨** (特に halo 入場 + stagger feel)。 screenshot 検証済 (gold frame / Hourglass 描画 OK)。

### D-2 非Pro gating 強化 (task #16、 defer)
- user 承認済だが、 価格 review で **「Stripe 未配線で gate を強めると dead-end Trust Cliff」** と判明。 価格も流動中。 → **Stripe Premium 配線 → その後 gate 強化** の順序が正 (funnel-CRO verdict)。 Stripe 後に再開。

## 📋 release runbook (`docs/runbooks/release_prep.md`、 task #18 完了)
**朗報: Stripe Premium も GA4/Clarity も infra 実装済**、 大半は env 設定 + redeploy で完了:
- **Stripe Premium**: backend `_STRIPE_PRICE_ENV` が premium mapping 済 / frontend tier 対応済 → **price ID を Railway env (`STRIPE_PREMIUM_MONTHLY/YEARLY_PRICE_ID`) に入れる + LP 価格更新 + railway up** のみ。
- **GA4/Clarity**: analytics.js + Dockerfile ARG/ENV 橋渡し済 → **`VITE_GA4_ID`/`VITE_CLARITY_ID` を Railway env に入れて railway up** のみ (VITE_ は build-time、 再ビルド必須)。
- **Signature ¥10k**: これは code 変更必要 (PLAN.SIGNATURE enum + rank + Stripe mapping + LP 列、 ~1-1.5人日)。 pre-release は招待制 waitlist 推奨。

## 法務 scan (task #19 完了、 専門家 sign-off 推奨)
§38 ガード (blocklist 45 regex + backend BAD-5/6) 稼働、 明白な景表法/§38 違反は source になし、 screener 出力は事実+disclaimer。 **専門家確認推奨**: LP コピー / Signature「自動執行」 positioning (§38) / 利用規約・プライバシー。 (autopilot は弁護士でないため最終 sign-off 不可)。

## 📊 RS universe (D-3 解決)
nightly cron で **universe_size 2414 まで成長済** (目標 ~2449 にほぼ到達)。 手動 dispatch 不要。

## ⚠️ 触ると危険 (継続)
- 発光系 / sticky 検索 div / VITE_ ARG/ENV / aggregator LLM import / 「じっちゃま」UI / DailyDigest summary flex。
- Workspace isScreener 分岐 = master-detail 心臓部 (D-1 breadcrumb は flex wrap で内部不触)。
- ScreenerPane: `.screener-reveal`(stagger)/`.screener-hero-row`(hover)/`_heroCache`(flicker TTL5分)/h4 `minHeight:calc(2.5em+9px)`(hairline 整列、 border-box 前提)。 触る時は前提維持。
- `.hero-watch-add` gold override (Hero.jsx)。

## release status
pre-release。 launch blocker = 価格決定 → Stripe Premium 配線 (runbook 化済)。 核 = AI図解 + russell3000 master-detail スクリーナー (刷新+flicker解消+hairline整列+breadcrumb) + RS/Cup。
