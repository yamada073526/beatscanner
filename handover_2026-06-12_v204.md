# 引き継ぎ書 v204 (2026-06-12 夜 = ④Phase1b 完遂 + 起床feedback 2件 + autopilot 就寝セッション)

最新本番 = `9b1f94a` (autopilot: ETF exposure GICS 11セクター完成)。/health commit + バンドル grep で全件検証済。

---

## 🟢 本セッション完了 (全て本番 live・検証済)

| commit | 内容 | 検証 |
|---|---|---|
| `497614d`+`7e159c8` | **④来期コンセンサス Phase1b**: guidance_extras (OpEx/capex) を 8-K/transcript 抽出。schema field enum(opex/capex)+period_type+basis+source_quote、label_jp=静的 FIELD_LABEL_JP、few-shot+BAD-8 は独立 bp3 (bp1/bp2 cache lineage 不変)、max_tokens 2048、per-item §38 verify (`null_unverified_extras`) | NVDA opex 8.5/8.3、8-K 逐語照合 |
| `104b594` | 会社見通しサブ行に金の左バー継承 (期帰属の視認性、user dogfood) | user OK |
| `f283d0d` | **③TriageBanner**: Cup-Handle→トレンド 統一 (triage=Pro / cup_handle=Premium の tier 不整合 Trust Cliff) | バンドル grep (トレンド:1, Cup-Handle:0) |
| `3204d8a` | **ETF チャート個別株 overlay 抑止**: isEtf で v147 非株式ガード拡張 (RS/売り目安/損切り/Cup-Handle 非表示) | user OK |
| `4393d93` | 会社見通し行の lazy fetch 中スケルトン (突然感解消、user dogfood) | user OK |
| `89625f8` | **guidance_extras allowlist 化 (autopilot 発見の systemic Trust Cliff)**: source_quote に "operating expense"/"capital expenditure" を逐語で含む item のみ表示 | 下記 dogfood |
| `9b1f94a` | ETF exposure allowlist に GICS 残り6セクター SPDR (XLC/XLI/XLP/XLU/XLB/XLRE) 補完 | GOOGL→XLC等 検証 |

**②planV2 / ⑤ETF組入銘柄 = 検証のみで解決済** (②は `8d6bfe8` で getPlan SSOT 統一済、⑤は backend etf_holdings:ok+10銘柄+frontend配線一気通貫。両方 handover note が stale だった)。

### ✅ v203 backlog で closed → v204 から意図的に除外 (ドロップでない)
- **[recurring 3回目] count-up がまだ速すぎる**: `3cdcbb5` で duration 2000ms 化、本セッションで user「OKです」確定 → closed。
- **[Trust Cliff] AAPL で「Pro で解放」表示 = owner が Free 扱い?**: Premium サブスク復元 + `8d6bfe8` getPlan SSOT で closed。user 確認済。
- **[funnel-cro] ProTeaser のテキスト不一致 + 🔒 ダサいアイコン**: `3cdcbb5` で「トレンド」+ lucide 化 closed。
- **[§38 重・先に実施] 来期コンセンサス内容拡充**: ④Phase1a+1b で完遂 (`9b1f94a` までの一連) → closed。
- **実施順 ④→⑤**: 両方完了。

### 🔴 autopilot 最重要発見: guidance_extras opex 誤ラベル (systemic §38 Trust Cliff)
field enum=[opex,capex] のみのため、LLM がガイダンス節の各種「$X-Y」指標を **全部 opex に押し込んでいた**:
- **META** FY: `total expenses $162-169B` → 「営業費用」 (総費用≠営業費用)
- **PINS** Q: `Adjusted EBITDA $256-276M` → 「営業費用」
- **DASH** FY: `SBC expense $1.3-1.4B` / `D&A expense $1.1-1.2B` → 「営業費用」×2

→ **allowlist guard (`89625f8`、数値物理層)** で解消。dogfood 検証済:
- NVDA=営業費用8.5/8.3 ✓ MSFT=営業費用19.3-19.4+設備投資40 ✓ META=設備投資125-145のみ ✓ PINS/DASH=空 ✓

---

## 🟡 朝の判断待ち (DEFER-SPEC、user 承認後に着手)

1. **guidance_extras enum 拡張 (LLM schema 変更)**: allowlist で META の total expenses ガイダンス ($162-169B、本来は valuable) も drop されている。`total_expenses`/総費用 enum (+ 場合により ebitda/sbc/d&a) を schema+FIELD_LABEL_JP に追加し prompt を厳格化すれば、誤ラベルでなく**正しいラベルで表示**できる。LLM schema 変更=hallucination-guard 経由 + few-shot 追加 (bp3)。→ 着手するか方針確認。
2. **GOOGL/AMZN capex coverage**: 両社は capex ガイダンスを call で出すが 8-K に明示数値が無く extras=空 (coverage 欠落、バグでない)。transcript whitelist 拡張は §38-sensitive → DEFER。
3. **モーダル文章推敲** (GuidanceInfoModal/ForwardOutlookInfoModal): user「今はまだ着手しなくていい」明示 → 保留中。
4. **上級者 review backlog / motion 提案**: 要相談。

---

## 🔵 朝に実機 dogfood してほしい (headless 認証不可だった分)

- **個別株 (NVDA/MSFT) の来期コンセンサス**: 営業費用/設備投資が金バー付き+スケルトン経由で出るか。**MSFT は capex 40億ドルも出るはず** (autopilot で追加確認した実データ)。
- **ETF (SPY/QQQ/VTI) チャート**: RS/売り目安/損切り が消え 価格+SMA のみか (済→OK済だが念のため)。
- **PINS/DASH/META の来期**: 「営業費用」 の誤った行が**出ていない**こと (META は設備投資のみ出る、PINS/DASH は extras なし)。

---

## ⚠️ 危険箇所 (不変)
- 発光系 `.panel-card/.bs-panel/.surface-card` / sticky 検索バー / VITE_ ARG/ENV / aggregator LLM import 禁止。
- guidance_extras の allowlist guard (`main.py` `_compute_forward_outlook`): source_quote 逐語照合が前提。enum 拡張時はこの guard も併せて見直す。
- deploy = `git push origin main` (Railway auto-deploy ~30-60s)。`railway up` 使わない。
- 実装 SSOT: [docs/specs/SPEC_2026-06-12_forward-outlook-expansion.md](docs/specs/SPEC_2026-06-12_forward-outlook-expansion.md) §7。

## 🧰 検証 harness
- `frontend/scripts/snap-forward-extras.mjs` (未追跡): DOGFOOD creds 設定で来期 extras を headless 検証可。
- guidance dogfood: `curl "本番/api/guidance/{t}/basic?with_guidance=1"` で company_guidance_extras 確認。
