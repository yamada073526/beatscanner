# BeatScanner Handover v168 — スクリーナーアニメ刷新 + P0 連続修正 + segment 構造的和文化 (2026-06-05)

> 長セッション (アニメ「ぐりぐり」要望起点 → multi-review → user dogfood 反復 → P0 3件 + segment i18n + richness)。
> **現本番 bundle: `index-IaBUoXTo.js`** (CSS は index-*.css 別ハッシュ)。 全 commit 済 (`railway up` deploy)。
> **deploy は `railway up` のみ** (CLAUDE.md override、 本セッションで実証。 autopilot skill の「push 自動build」 はこの repo では不適)。

## 🟢 本セッション完了 (全 deploy + 検証済)

### P0 修正 3 件
1. **scroll lock** (screener 詳細): breadcrumb wrapper (v165) の内側 div `overflow:hidden` が内部 scroll を
   持たない JudgmentDetail (display:grid) を clip → 下スクロール不能。 → `overflowY:auto`。
   `frontend/scripts/snap-screener-scroll.mjs` で BEFORE(hidden)/AFTER(auto) 実証。 user 確認済 OK。
2. **ASO 等「年次3期未満」分析失敗 (422)**: `judgment.py build_periods` が income/cash-flow を date 完全一致
   join → 52/53週決算の小売 (ASO 等) は期末日が数日ズレ join が 1 期に激減。 → **fiscalYear join** (date fallback)。
   `pytest test_judgment.py` 11 passed (回帰 test 追加)。 本番 ASO 422→200/periods 5、 回帰 5 銘柄 clean。 user 確認済 OK。
3. **segment 名 英文 fall-through** (会社概要 + 図解): frontend 手書き辞書のみ → 未登録銘柄が英文。
   → **backend `visualizer/segment_translate.py`**: 汎用辞書 → 永続 cache → Haiku 翻訳 → sanitize (数字混入/
   異常長/§38兆候 reject、 graceful 英語維持)。 profile-summary + visualize 両 endpoint で `name_jp` 付与。
   frontend `displaySegmentName()` = curated 辞書優先 → name_jp → 英語。 本番実証: ASO=アウトドア/アパレル/
   フットウェア…、 AAPL=curated 維持。 hallucination-guard 準拠 (label 翻訳のみ + sanitize + 原文 hover)。

### スクリーナーアニメ刷新 (user「ぐりぐり動かして」→ multi-review 3体 → dogfood 反復)
- 案2 rank pop (0.4→1.12 弾み) / 案4 入場 spring (slide 22px + scale-in) / choreography (見出し→row cascade)
- 案3 hover = pointer tilt 撤回 (user「傾き不要」) → `.news-list-card` idiom (cyan border+bg+`--ws-shadow-row-hover`
  発光+lift+press) + ticker cyan shift (hover accent)
- 案A 背景 ambient depth (`.screener-pane-ambient::before` radial cyan 0.11 + 超低速 breathe、 glow host 不触)
- 案6 chart draw-on (v3 確定): Recharts 内部 draw は詳細ページの多段非同期ロードの再レンダリングで中断され
  「描画が見えない」 真因だった → **CSS clip-path wipe (`chartWipeIn` / `.chart-draw-reveal`、 ease-in-out 1.6s)**
  に置換。 IntersectionObserver で viewport 入場時に `.h-72` wrapper へ class 付与 (再レンダリング非依存で堅牢)。
  Recharts price line は isAnimationActive=false。 harness `snap-chart-wipe.mjs` (削除済) で clip-path 途中→完了
  実測。 案7 cross-fade は revert (benefit ゼロ)。

## 🟡 残バックログ (次セッションで /fetch-handover から再開、 全て user approved or 要望済)

スクリーナー「全体感で寂しい」 richness の続き + dogfood 残:
- **③-a ambient 明滅が分からない**: 現状 breathe ~16s が微弱。 体感できる強度/周期に調整 (opacity 振れ幅↑ or 周期短縮、 但し Aman 節度維持)。
- **③-b カードに LP 風 hover 発光** (user 提案): section card (tier-m-glow=発光系) に hover で発光。 ⚠️発光系直接操作=
  監視下 + snap-screener-hero.mjs 系で検証必須。 既存 useHaloSweepOnce / tier-m-glow の hover 状態を careful に。
- **④ hover accent もっと**: ticker cyan 化済だが user「まだ欲しい」。 最新ニュース (`.news-list-card::before`) のような
  **左アクセントバー (scaleY slide-in)** を screener row に追加。 但し row 左端は rank circle があるので配置工夫。
- **案B データ脈動**: 「最終更新 X前」+live dot+tick flash。 ⚠️ scanner に更新時刻 field が無く backend が scan 時刻を
  返す改修が前提 (要設計)。
- **案C Hero 常時微光**: featured(01) に常時 ambient glow。 ⚠️発光系 (③-b と同領域)。
- **案D Pane2 Explorer 統一**: CustomScreenerPanel (1197行・複数 row 型) は既に hover(lift+border)。 `--ws-shadow-row-hover`
  発光 + stagger 統一。 複数 row 型に box-shadow 追加 = 監視下で。

### 低優先 follow-up
- frontend graceful 422: ASO は fix したが genuine に <3 期の近年 IPO は今も 422 → 無限「分析中」。 422 時に
  「年次データ不足で評価留保」 表示 (構造化 response or frontend catch)。
- segment i18n の backend cache は process 内 (restart でリセット)。 頻出 segment は再 Haiku だが cost 微小。
  必要なら Supabase 永続化 / 汎用辞書拡充で Haiku 呼出削減。

## ⚠️ 触ると危険 (継続)
- 発光系 (.panel-card/.bs-panel/.surface-card/tier-m-glow)・新 glow host・新 box-shadow (token は --ws-shadow-row-* 流用)。
- **Workspace isScreener 分岐 = master-detail 心臓部**。 breadcrumb wrapper `overflowY:auto` 維持 (scroll lock 再発防止)。
- `judgment.py build_periods` の fiscalYear join 維持 (date 完全一致に戻すと小売が再 422)。
- `segment_translate.py` は visualizer/ (LLM 許可層)、 aggregator/ に LLM import 禁止 (pre-commit Check 3)。
- sticky 検索 div / VITE_ ARG/ENV / DailyDigest summary flex。

## release status
pre-release。 launch blocker = 価格決定 (済 Free/¥980/¥3,980/¥9,800) → Stripe Premium 配線 (runbook 済)。
