# 引き継ぎ書 v255 (2026-06-23) — screener mockup v8 踏襲 Phase A 着地 + backend #4 + dogfood 完了

> v254 を置換。本セッションで **① deploy 後 dogfood 完了 / ② backend 0-call fields (#4) 完了 / screener mockup v8 踏襲 Phase A 完了** を全て本番 deploy 済。
> 次セッションは **screener mockup v8 踏襲 Phase B（preset 駆動の条件モデル再アーキテクチャ）** を planner で SPEC 化してから着手。
> branch `main` (HEAD `1a96de3`)。PR #1 は **merged**。本番反映済。v254 は削除可。

---

## 🧭 30 秒サマリー

| 項目 | 状態 |
|---|---|
| **branch / 本番** | `main` HEAD `1a96de3` = 本番反映済（/health commit で確認済）。PR #1 **merged** |
| **screener_v2 flag** | default **OFF**（一般ユーザー無変更）。dogfood は `?screener_v2=1`（ダーク）|
| **本セッション完了** | dogfood① PASS / backend #4 (`10011d5`) / Phase A タイル mockup化 (`1a96de3`) — 全 deploy 済 |
| **次の最優先** | screener mockup v8 踏襲 **Phase B**（フィルタ行 = preset 駆動条件モデル再設計）→ Phase C（旬のセクター master-detail view）|
| **要対応(別track)** | 🔴 **funda_pass backend bug**（5週間 stale + True=0、task_0d0659a8 spawn 済）/ タイル件数3問題（後で一括）|
| **mockup SSOT** | `docs/specs/mockups/screener-strategy-presets-v8.html`（承認済 visual SSOT）。実装はこれに忠実化中 |

---

## ✅ 本セッション完了（全て本番 deploy 済）

### ① deploy 後 dogfood — PASS（v254 最優先①、完了）
PR #1（screener #2 アドバンスド個別緩急＋Pro課金ゲート）を main に FF マージ→本番反映（`2f3e7b9`）。headless 認証注入で Free/Pro 両 state 検証（v254 最優先① の検証項目を全て充足・完了）:
- **Free時**: Pro badge / locked seg 24 / 操作で lockbar（Trust Cliff 適合コピー「9銘柄に絞り込み中→Pro へ」）/ 件数不変（override 不可が正）
- **Pro時**: 解錠 / override で件数 9→6 連動 / カスタム tag
- 原典較正で **Free 標準プリセット件数が動く** = 9件（universe 2,481 から・妥当）を確認済
- flag default OFF 確認 = 一般ユーザー無変更。

### ② backend 0-call fields (#4) — 完了（v254 最優先②、完了）
commit `10011d5`。migration `docs/migrations/2026-06-23_screener_fundamentals_monotonic_rising.sql` を Supabase に適用済（eps_3y_rising / rev_3y_rising boolean カラム + GRANT、検証済）。
- `eps_3y_rising` / `rev_3y_rising`: nightly `_compute_one` が既存 `annual_recs`（年次4期）再利用 = **0 FMP call**。実データ検証で NVDA/MSFT=True・AAPL/INTC/T=False（意味的に正しい）。**値は次回 nightly canslim-scan で populate**（現状は universe で null）。
- セクター別RS: `_build_universe_payload` read-time 集計 = 0 call。`sector_rs_median`（全2481件算出確認）+ `is_sector_rs_leader`（33件）。
- **deploy 順序非依存**: 新カラムを別 fetch に分離（共有 paged SELECT に混ぜると migration 前に全ファンダ silent 消失する Trust Cliff を回避）。memory `feedback_paged_select_missing_column_trap.md` に記録。
- **cfps_3y_rising は defer**（年次 cash-flow 未 fetch = +1 call で「0-call」前提に抵触）。

### screener mockup v8 踏襲 Phase A — 完了（戦略タイル）
commit `1a96de3`。承認済 mockup v8 への忠実化 Phase A。dogfood snap で視覚確認済（mockup 通り）。
- StrategyPresetBar: 2→**4タイル**（決算合格/新高値ブレイク/旬のセクター/セクター別リーダー）。縦カード（top: icon+label / desc / foot: 件数大字+tier badge）。LayoutGrid/Crown 追加。
- index.css: `.screener-strategy-bar` を 4列 grid。tile 縦カード化。`.screener-tier-badge`（free緑/pro gold/prem amber）。**`--shadow-glow-gold` token 追加**（cyan の対・light/dark 両テーマ・mockup §.preset.active 準拠）。design-system-check（hex/shadow/!important/発光兆候）クリア。
- CustomScreenerPanel: `itemPasses`/`buildActiveGrades`/`PRESET_PREDICATES`/`countPreset`/`topSectorsByRs` を export。`sectorLeaderOnly` predicate + state 追加（**全 extra 構築箇所6つ + 依存配列に配線** = count/list 整合）。applyStrategyImpl を hot_sector/sector_leader 対応。
- ScreenerMaster: universe（module-scope cache 共有・追加 fetch なし）で 4タイル件数算出 → StrategyPresetBar に配線。
- 件数整合(Trust Cliff): タイル件数と list は同一 `itemPasses`。hot_sector も共有 `topSectorsByRs` で count==list 担保。

---

## 🔴 次セッション最優先

### screener mockup v8 踏襲 Phase B（フィルタ行 = preset 駆動条件モデル）
**想定より大きい再アーキテクチャ**と判明したため、**planner で SPEC 化してから実装**推奨。
- **現行**: 汎用 facet パネル（品質/タイミング/需給の固定3カテゴリ・全 preset 共通条件）。
- **mockup v8**: **preset 毎に conds が変わる**（決算合格=成長性/収益の質/モメンタム、新高値ブレイク=型/需給、旬のセクター=セクター条件…。mockup HTML L241-263 の `PRESETS[].conds` 参照）。
- フィルタ行デザイン: トグル switch + 値チップ[標+25%] + adv ON で mseg（緩/標/厳/最厳）。精度はスライド thumb セグメント。2列グリッド。カテゴリ見出しは trailing line。
- 連続性条件（EPS/売上/CFPS連続性）= monotonic フィールド。eps_3y_rising/rev_3y_rising は #4 で deploy 済（nightly populate 待ち）。**cfps_3y_rising 未実装**（要 backend 追加 or 条件から除外）。
- 波及: itemPasses / 件数 / #2 アドバンスドゲート。

### screener mockup v8 踏襲 Phase C（Phase B 後）
旬のセクター（p3）の **master-detail view**（セクター一覧 master + Top3 detail。mockup `renderSectorMaster`/`renderSectorDetail` 参照）。現状 hot_sector は stock list 暫定。

---

## ⚠️ 要対応（別 track・後で一括）

### 🔴 funda_pass backend bug（task_0d0659a8 spawn 済）
本番 `/api/scanner/universe` の freshness で **`funda_pass` だけ "2026-05-15"**（他facetは当日）= 5週間更新停止。値 **True=0 / False=178 / None=2303**（AAPL/MSFT も False）。earnings 評価パイプラインが stuck かつ判定ロジックがバグの可能性。**「決算合格」中核概念が常時0件**でスクリーナー全体に影響。screener UI とは別の backend データ健全性問題。

### タイル件数3問題（user 判断: 後で一括）
1. funda_pass 起因（上記）→ 決算合格・旬のセクター件数=0
2. **Premium マスク**: unauth/Free view の `locked_facets`（pivot_distance/near_high 等）→ 新高値ブレイク=0。Free で「0銘柄」は誤解 → lock/Pro 表示に改善要（mockup は Free/Pro で件数変化）
3. **core grades 過剰適用**: preset に標準精度の eps/roe/rs 上乗せ → sector_leader 実6→タイル1。preset 件数を「定義条件のみ」にするか要判断

---

## 🟡 user 確定事項 / 重要知見
- **mockup v8 = 承認済 visual SSOT**。実装(#1-#3)がここから後退していたため user が「素直に踏襲」を指示（2026-06-23）。共有部品の既存"ダサい"仕様より mockup 優先（ただし発光/card 安全 recipe + token + Trust Cliff は維持）。
- gate 1 = 原典に素直（じっちゃま優先→オニール）。gate 2 価格 = ¥1,480/月（年¥12,800）第1推奨。
- ⚠️ 「Pro」価格/badge を出す時点で Stripe 課金フロー必須（Trust Cliff #4）。現状 flag OFF で露出なし。
- deep-research/ultrathink は計画段階のみ許可（実装段階は不可）。

## ⚠️ 触ると危険 / 規律
- **発光系**（`.panel-card`/`.bs-panel`/`.surface-card`）→ screener は plain bordered + token shadow。Phase A は `--shadow-glow-gold` token（1px ring + soft glow、生 shadow なし）。
- **CustomScreenerPanel 整合**: count と list は同一 `itemPasses`（Trust Cliff C-2）。新 facet/flag 追加時は **全 extra 構築箇所（filteredItems/presetCounts/facetLevelCounts/emptySuggest/sectorOptions/mcapOptions）+ 依存配列**に漏れなく配線。
- **paged SELECT 罠**: screener_fundamentals に新カラム追加時は別 fetch 分離 or migration 先行（`feedback_paged_select_missing_column_trap.md`）。
- screener_v2 flag default OFF。deploy = `git push origin main`（Railway auto）。`git add` は明示 path。sticky 検索バー / VITE_ ARG/ENV 同期 不触。

## 🧹 掃除
- handover **v254 は削除可**（本 v255 最新）。
- snap untracked（snap-screener-v2-dogfood / snap-screener-tiles / snap-mockup-render 等）は再利用可なので残置可。
- 旧 PR branch `claude/jolly-wozniak-bgdvzi` は merged 済（local/remote 削除可）。
- /tmp/uni_full.json 等の診断ファイルは ephemeral。
