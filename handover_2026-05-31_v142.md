# BeatScanner Handover v142 — D4 Sprint2 + B3 着地 + formation 再評価 (2026-05-31)

> v141 残タスク (D4 Sprint2 / B3) を着地、 formation フィルタ優先度を再評価し「保留」 維持で確定。
> v141 handover は本 v142 で置換 (削除済)。

## ✅ git 状態 (クリーン・同期済)
- working tree **clean**、 最終 commit `06e708a` (`feat(v142): screener ブレイクアウトフィルタ + B3 空detail pane neutral化`)、 **origin/main へ push 済 = git=本番一致**。
- 本番 bundle: root JS `index-DjUd_0ko.js` / chunk `CustomScreenerPanel-SRTDBviL.js` (breakout) / `JudgmentDetail-af7Pc7Ro.js` (B3)。 全て本番 chunk 直接 grep + snap で検証済 ([[feedback_deploy_verify_discipline]])。

## ✅ 本セッション完了済 (本番デプロイ + 検証済)
| タスク | 内容 | 検証 |
|---|---|---|
| **D4 Sprint2** | screener「ブレイクアウト」chip 追加 (cup 直後・premium 限定)。`runCupFilter('breakout')` で `fetchCupHandleScanner('cup')` → `state==='breakout_confirmed'` のみ抽出。`breakout_extended` (ATH追いかけ買い) 物理除外。free user は誤件数を出さない clean teaser (CupScannerResults に filterKey==='breakout' && !isPremium 分岐) | CSP chunk grep 4 文字列 ✅ |
| **D4 Sprint2 条件②調整** | QA「pivot 乖離率 +8% 超除外」 は **scanner item payload に現在価格が無い** (cron が `_detect_cup_handle` 戻り値=`pivot.price`/`cup.right_rim_price` 等のみ保存、 today_close 無し。 backend + 本番 endpoint 両方で verify) ため frontend 計算不可 → backend の `breakout_extended` state 除外 (today_close≥252週高値95%) に委譲。 **user 承認済** | code + endpoint verify ✅ |
| **B3** | 未ログイン空 detail pane「左のリストから銘柄を選択してください」の cyan 常時発光を neutral 化。真因=`useArrivalSpotlight` が全 `.bs-panel` 監視 → 短い空 pane が center band 固定で `is-arriving` 点灯 (no-baseline-cyan の常時強発光症状)。`JudgmentDetail.jsx` placeholder に `data-spotlight-skip="1"` 付与で opt-out (**glow CSS 無改変**) | JD chunk grep + snap cyanBigEls=0 ✅ |

## 🔴 残タスク (次セッション)
### formation 専用フィルタ — **保留維持 (v142 再評価 verdict)**
- breakout 着地後に再評価した結果、 **当初 3体合議 verdict の「保留」 を維持**。理由:
  1. formation は既に「カップ」 フィルタ内に表示され、 **v141 の faint cyan chip (監視候補) が監視 use case をカバー済** → 専用フィルタの marginal value 低
  2. 品質ゲート無し = overhead_resistance 型ノイズ再来 / 品質5ゲート適用 = 月3-8件で cup_completing と重複
  3. 「形成中」= pivot 未突破・非方向 → 専用フィルタは「買い候補」誤読の §38 リスク
- **実装するなら**: 品質5ゲート (weeks≥7/depth15-35%/RS≥70/ファンダ4/5/market) + 「監視候補(未突破)」 framing + premium gate 必須。 **dogfood で formation-only 需要が確認されてから着手**。

### (低優先・open-ended)
- **tier 中間価格 + 機能配分レビュー** (pricing は release 前決定必須、 3案 ¥980/¥4,980/¥10,000 の中間設計は方針議論が要る → [[project_signature_tier_10k_strategy]] / [[project_logout_plan_management_ui]])
- **Pane3 読込速度改善** (計測 → ボトルネック特定 → 施策、 スコープ広め)

## 🟡 本セッションの知見 (memory 永続化済)
- **[[feedback_oneill_screener_frontend_intersection]] 追記**: cup scanner item payload に現在価格無し → pivot 乖離率系の数値ガードは frontend 不可、 `breakout_extended` state 除外で同義カバー。 masking 整合のため state 抽出フィルタは premium:true + count 主張しない teaser。
- **[[feedback_no_baseline_cyan]] 追記**: inline 焼き込み無くても `useArrivalSpotlight` が全 `.bs-panel` 監視 → 短い空 pane で is-arriving 固定点灯。 fix は `data-spotlight-skip="1"` (glow CSS 無改変)。 検証 script = `snap-b3-empty-detail-cyan.mjs` (cyan-baseline auditor、 再利用可)。
- **index.css 技術的負債 (未修正・v141 から継続)**: `.ds-chip[data-cup-state="breakout_pending"]` が **6655 (ds-chip-cup-pulse 2s、 dead) と 11558 (cup-chip-pulse 2.4s + border!important、 cascade 勝者) で重複**。 今回の breakout フィルタは `breakout_confirmed` (6678 緑 glow、 重複なし) のみ表示で無関係 → 未統合のまま残置。 pending chip を触る際に grep 統合推奨 (cup/both/oneill フィルタの pending 表示に視覚影響、 発光系リスクあり別途検証)。

## ⚠️ 触ると危険 (継続)
- 発光系 `.panel-card/.bs-panel/.surface-card` (design_recipes §C) / sticky 検索バー / VITE_ ARG/ENV / aggregator への LLM import 禁止 / **「じっちゃま」 UI 禁止**。

## release status
pre-release。 Premium は LP「近日公開」 (Stripe 未配線=購入不可)。 Pro ¥980 のみ実購入可。
