# BeatScanner Handover v142 — D4 Sprint2 + B3 + tier/pre-release レビュー + feedback 着地 (2026-05-31)

> v141 残 (D4 Sprint2 / B3) 着地 + formation 再評価=保留 + tier 中間価格レビュー + pre-release 機能レビュー + フィードバック収集機能を本番着地。
> v141 handover は本 v142 で置換 (削除済)。

## ✅ git 状態 (クリーン・同期済)
- working tree **clean**、 最終 commit `51426cb`、 **origin/main へ push 済 = git=本番一致**。
- 本番 bundle: root JS `index-Bcel-_MT.js`。 全て本番 chunk 直接 grep + snap + smoke test + runtime 検証済 ([[feedback_deploy_verify_discipline]])。
- ⚠️ **deploy 機構 (user 確認)**: Railway は **GitHub 連携で push 済み main を読んでビルド**。 env 変更時は Railway UI「Deploy」 で env 反映 + main(push済) build が 1 発で済む。 CLAUDE.md「railway up のみ」 は実態と乖離 (railway up CLI 直送も動くが、 GitHub 連携が主)。 → 次セッションで CLAUDE.md 更新候補。

## ✅ 本セッション完了済 (本番デプロイ + 検証済)
| タスク | 内容 | 検証 |
|---|---|---|
| **D4 Sprint2** | screener「ブレイクアウト」chip 追加 (cup 直後・premium 限定)。`breakout_confirmed` のみ抽出、`breakout_extended` (ATH追いかけ買い) 物理除外。free user は誤件数を出さない clean teaser | CSP chunk grep ✅ |
| **D4 Sprint2 条件②調整** | QA「pivot 乖離率 +8%」 は scanner payload に現在価格が無い (verify 済) ため frontend 計算不可 → backend `breakout_extended` state 除外に委譲 (user 承認) | code + endpoint verify ✅ |
| **B3** | 未ログイン空 detail pane の cyan 常時発光を neutral 化。真因=`useArrivalSpotlight` が全 `.bs-panel` 監視 → 短い空 pane で `is-arriving` 固定点灯。`JudgmentDetail.jsx` placeholder に `data-spotlight-skip="1"` (glow CSS 無改変) | JD chunk grep + snap cyanBigEls=0 ✅ |
| **tier 中間価格レビュー** | 3体合議で方針確定 (**記録のみ・実装は DAU100+Stripe 後**): Free/¥980 Pro/**¥3,980 Premium**/**¥9,800 Signature**。 Cup-Handle 検出/チャート=Premium、 nightly push 自動監視=Signature 専有 (delivery 形態で分離)。 機関データ→Signature (FMP Ultimate $99 原価)。 [[project_signature_tier_10k_strategy]] に詳細 | memory 記録 ✅ |
| **pre-release 機能レビュー** | YouTube「後悔した5施策」3体合議。 #1 review=app store待ち/#2 feedback=実装/#3 計測=GA4+Clarity活性化方針/#4 料金=済/#5 UI=継続。 [[project_pre_release_feature_review]] | memory 記録 ✅ |
| **フィードバック収集機能** | Supabase `feedback` テーブル (migration 適用済) + `POST /api/feedback` (service_role insert + Resend 通知) + `FeedbackWidget.jsx` (faint 右下 button + modal、 Chip category、 匿名時 email)。 commit `fa81310` | chunk grep + **smoke test 200 (insert 成功=grants 正常)** + 配置 snap ✅ |
| **GA4+Clarity 活性化** | trackEvent 5 点配線 (paywall_view/checkout_start/analyze_run/lp_ticker_click/login_start、 commit a6838fb) + Railway env (`VITE_GA4_ID=G-B166L08PWT`/`VITE_CLARITY_ID=wzk0bedm83`) 設定済 + GitHub Deploy で焼き込み。 [[project_pre_release_feature_review]] | entry chunk に ID+5event 焼き込み + **runtime: gtag/clarity ロード + window globals + dataLayer 稼働 PASS** ✅ |

## 🔴 残タスク (次セッション)
### プライバシーポリシー更新 (GA4+Clarity 活性化に伴う、 日本法・軽微)
- GA4 (G-B166L08PWT) + Clarity (wzk0bedm83) が **LIVE で稼働開始**したため、 プライバシーポリシーに解析ツール名・取得データ・目的を明記 (改正電気通信事業法 外部送信規律)。 cookie banner は日本法では原則不要。 文面ドラフトは未作成 (user に提案済)。
- 本格 funnel dashboard 分析は active 50-100人後 (CRO verdict)。 今は配線済でデータ蓄積中。

### フィードバック機能の運用 (任意・軽量)
- (任意) Railway に **`FEEDBACK_NOTIFY_EMAIL`** を設定すると新着がメール通知される (未設定でも Supabase 保存は動作)。
- 本番 `feedback` テーブルに **smoke test 行が 1 件**ある (page_path=`/smoke-test`)、 削除可。

### formation 専用フィルタ — **保留維持 (v142 再評価 verdict)**
- breakout 着地後に再評価した結果、 **当初 3体合議 verdict の「保留」 を維持**。理由:
  1. formation は既に「カップ」 フィルタ内に表示され、 **v141 の faint cyan chip (監視候補) が監視 use case をカバー済** → 専用フィルタの marginal value 低
  2. 品質ゲート無し = overhead_resistance 型ノイズ再来 / 品質5ゲート適用 = 月3-8件で cup_completing と重複
  3. 「形成中」= pivot 未突破・非方向 → 専用フィルタは「買い候補」誤読の §38 リスク
- **実装するなら**: 品質5ゲート (weeks≥7/depth15-35%/RS≥70/ファンダ4/5/market) + 「監視候補(未突破)」 framing + premium gate 必須。 **dogfood で formation-only 需要が確認されてから着手**。

### (低優先・open-ended)
- **tier pricing 実装** (方針確定済、 実装は順序制約で **DAU100+Stripe Premium 配線後**): planGating に `PLAN.SIGNATURE`+rank=3、 機能再配分、 Premium ¥1,800→¥3,980 値上げ、 Signature ¥9,800 列。 [[project_signature_tier_10k_strategy]]
- **Pane3 読込速度改善** (計測 → ボトルネック特定 → 施策、 スコープ広め)
- ログアウト + プラン確認/変更 UI ([[project_logout_plan_management_ui]])

## 🟡 本セッションの知見 (memory 永続化済)
- **[[feedback_oneill_screener_frontend_intersection]] 追記**: cup scanner item payload に現在価格無し → pivot 乖離率系の数値ガードは frontend 不可、 `breakout_extended` state 除外で同義カバー。 masking 整合のため state 抽出フィルタは premium:true + count 主張しない teaser。
- **[[feedback_no_baseline_cyan]] 追記**: inline 焼き込み無くても `useArrivalSpotlight` が全 `.bs-panel` 監視 → 短い空 pane で is-arriving 固定点灯。 fix は `data-spotlight-skip="1"` (glow CSS 無改変)。 検証 script = `snap-b3-empty-detail-cyan.mjs` (cyan-baseline auditor、 再利用可)。
- **index.css 技術的負債 (未修正・v141 から継続)**: `.ds-chip[data-cup-state="breakout_pending"]` が **6655 (ds-chip-cup-pulse 2s、 dead) と 11558 (cup-chip-pulse 2.4s + border!important、 cascade 勝者) で重複**。 今回の breakout フィルタは `breakout_confirmed` (6678 緑 glow、 重複なし) のみ表示で無関係 → 未統合のまま残置。 pending chip を触る際に grep 統合推奨 (cup/both/oneill フィルタの pending 表示に視覚影響、 発光系リスクあり別途検証)。

## ⚠️ 触ると危険 (継続)
- 発光系 `.panel-card/.bs-panel/.surface-card` (design_recipes §C) / sticky 検索バー / VITE_ ARG/ENV / aggregator への LLM import 禁止 / **「じっちゃま」 UI 禁止**。

## release status
pre-release。 Premium は LP「近日公開」 (Stripe 未配線=購入不可)。 Pro ¥980 のみ実購入可。
