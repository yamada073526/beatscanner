# handover 2026-06-25 v267 — 新高値ブレイク着地 → 次は mockup v8 design 復元

## このセッションで完了 (蒸し返し禁止・機械/目視で検証済み)

新高値ブレイク preset の **0件恒常化を根治** (AUDIT の P0)。閾値確定値:
- 緩い = `near≥90` & `RS≥80` (オニール L 最低ライン。旧 RS'auto'(≥70) の 215件膨張を是正)
- 標準 = `near≥95` (13件・不変・理想)
- 厳しい = `near≥97` & `vol≥40` & **買い場圏(buy_zone_g)撤去** (実ブレイク手前も拾い 0件回避)
- 実測 **緩136 / 標13 / 厳3** — Premium 目視 + CI Premium dogfood の双方で一致確認済

その他:
- 非 Premium の preset card「0 銘柄」→ **🔒「Premium 限定」バッジ** (Trust Cliff 回避)
- CI dogfood (`.github/workflows/screener_v2_dogfood.yml` / `frontend/scripts/snap-screener-v2-dogfood.mjs`) を **Premium 対応化**:
  - `getAuthInjection` (`frontend/scripts/lib/auth-helper.mjs`) 配線 → `DOGFOOD_TEST_*` + `VITE_SUPABASE_*` secrets で実 Premium session を localStorage 注入 (bypass flag 無し)
  - B-4 stale 修正 (旧 key `buy_zone`→現 `new_high_signal`) / B-6 auth-aware 化 (Free はマスク0が正常・件数判定は Premium run のみ) / B-3 mseg 見切れ修正 (`eps_yoy_pct` crow 固定 + DOM 4段確定判定)
  - **GitHub Secrets 4つ設定済 + Premium テスト垢 (subscriptions tier=premium/status=active) 作成済**。run #43 で `mode:premium` 緩136/標13/厳3 を自動検証 = 毎デプロイ後に 0件回帰を CI が自動検出する状態
- PR #17 / #18 / #19 すべて squash merged (main 反映済)

任意の残務 (急がない): 古い壊れたテスト垢 `dogfood-premium@example.com` + orphaned subscriptions 行の掃除 / マージ済ブランチ `claude/clever-thompson-cyjtnw` 削除 / dogfood の未使用 `CHECKS.b3_mseg` 掃除。

## 次セッションの主タスク (user 2026-06-25 強い要望・最優先)

**mockup v8 ↔ 実装の design / copy 乖離のうち「根拠のない drift」だけを mockup に戻す。**

> user 原文: 「洗練さを受けた mockup から大きく後退していると感じる。根拠のない変更箇所はモックアップに合わせてほしい」。**ブランド世界観 (最高級ホテルのロビー) と 5 原則に直結する品質課題**で、機能でなく "洗練さの復元" が主眼。

### SSOT (まずこの2つを読む)
- **canonical mockup**: `docs/specs/mockups/screener-strategy-presets-v8.html` (v1-v8 あり・**v8 が最新**)。HTML なので `file://` で開いて computed style を直接比較できる
- **既存乖離監査**: `docs/specs/AUDIT_2026-06-25_screener-mockup-implementation-divergence.md` (P0-P3 分類済)。
  - P0 (0件) = 本セッションで解決済
  - **残る P1 semantic / P2 paradigm / P3 cosmetic(文言・ラベル) が次の主戦場**
- 補助: `docs/specs/REVIEW_2026-06-25_beat-cfps-mockup-reconciliation.md`

### user が挙げた具体例 (氷山の一角・他にも多数あると明言)
1. **上部4タブ (決算合格/新高値ブレイク/旬のセクター/セクター別リーダー) の hover ゴールド発光が ~1秒遅延**。マウスオーバー即時発火にしたい。
   - ※ **AUDIT 未記載の新規アニメ regression**。`frontend/src/components/StrategyPresetBar.jsx` の tile + `index.css` の発光定義を疑う (transition-delay / `is-arriving` entrance animation が hover glow を 1s 遅延させている可能性)。
   - ⚠️ **発光系は v54-v59 で6セッション溶けた最高リスク領域**。`docs/references/design_recipes.md §C-1〜C-4` 必読、compound `.X.is-arriving:hover` 4セット必須・`contain:paint` 禁止。
2. **検索条件セクションの文言・列が mockup と相違**:
   - 文言: mockup「絞り込み条件…」 vs 実装「銘柄スクリーナー…」(P3 cosmetic)
   - 決算合格タブの結果: mockup は「直近EPS YoY」+「判定」列 vs 実装「RS」のみ (P1/P2 semantic)

### 鉄則 (独断全戻し禁止)
各差分ごとに **「後続 SPEC 由来の意図的変更か / 事故 drift か」を AUDIT + `git log` で判定してから直す**。SPEC_2026-06-25 系 (0件修正・段階gating) の意図的変更を壊さないこと。理由のある変更は維持、根拠なき drift だけ mockup に戻す。

## 進め方の推奨
- **context をクリーンに保つ** (本セッションは CI ログ多数取り込みで重くなった反省): 大ファイル (`CustomScreenerPanel.jsx` ~1300行 / `index.css` 巨大) は Read offset+limit か sub-agent 委譲。全文再読込しない
- skill: `designing-workspace-ui` (必須・design SSOT 経由) / `funnel-cro` (LP/Trust Cliff 文言整合) / `screener`
- 検証: 本番 curl 不可環境。視覚は **snap-*.mjs harness** (mockup の `file://` と本番を並べて computed style diff) + Haiku vision (`snap-pdca-loop.mjs`) + 最終は user gate
- 発光タイミングは harness で `getComputedStyle` の transition-delay/duration を local 検証してから直す (空回り防止)

## 環境メモ
- deploy: `git push origin main` → Railway auto-deploy (~30-60s)。`/health` の commit で確認
- 本セッション branch: `claude/clever-thompson-cyjtnw`
- Premium dogfood secrets は設定済 (再設定不要)。dogfood 手動起動は Actions → "Screener v2 Dogfood" → Run workflow、verdict は MCP get_job_logs で読む
