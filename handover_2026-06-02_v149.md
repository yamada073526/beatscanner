# BeatScanner Handover v149 — ⑦ extended screener ship + ⑩ SPEC draft (2026-06-02)

> v148 起点。 出社中の自律セッション。 ⑦ は本番 LIVE + 検証済。 ⑩ は SPEC draft を gate-1 待ちで用意。
> v148 handover は本 v149 で置換 (削除済)。

## ✅ 本セッション SAFE-SHIP (本番 LIVE + 検証済)

### ⑦ 高値圏ブレイク(breakout_extended)を screener で誤シグナル抑制ゲート付き露出 — **ship 済**
- commit `8f78c0c`、 bundle `index-CStsWjDt.js` / `ScreenerPane-CUcD02i6.js`。 SPEC `docs/specs/extended_screener_2026-06-02.md` + **3 体合議** (金融 Opus=SHIP可 / frontend / qa-dogfooder=条件付OK、 必須 fix 反映済)。
- **3 数値 AND ゲート** (純 Python・LLM 不使用、 閾値 SSOT = [[feedback_cup_handle_thresholds]]「v148 ⑦」):
  ① 50DMA 乖離 大型(時価総額>$50B)≤+30%/中小≤+50% ② ベース上昇≤+25% ③ SPY>SMA200 かつ上向き。
  時価総額 = 既存 `get_market_cap` (FMP profile 24h cache)。 通過のみ screener 露出、 過延伸末期は drop。
- 実装層: `_extended_numeric_fields` (検出層、 main.py ~11567) + `_compute_extended_gate` + `_spy_market_uptrend_ok` (cron) + `scanner_cup_handle` serve drop + `_mask_signal_for_free` に state+乖離露出。 frontend `ScreenerPane.jsx` badge「高値圏突破 · 50DMA +X%」+ section③ disclaimer。 blocklist (blocklist.js + prompt_negatives.py 1:1 mirror) に extended action語追加。
- **3 体合議 fix**: extended は **section ③ のみ**露出 (section① + oneill Explorer から除外、 §5 優良誤認回避)。
- **検証済**: bundle grep ✅「高値圏突破」+ disclaimer / curl `/api/technical` で AAPL=breakout_extended sma50_dev=10.88 base_rise=0.22 mkt_up=true (= gate 通過 = 正当な初動 extended)。 LLY/GE/META/NVDA classification 不変 (formation/cup_completing/no_pattern/breakout_pending)。
- ⚠️ **ゲート完全発火は次 nightly cup-scan 後** (23:07 UTC=08:07 JST、 payload に extended_gate/sma50_deviation_pct 書込)。 deploy 直後は旧 payload に gate 無く後方互換で show (regression なし)。 **2026-06-03 09:00 JST に検証 scheduled-task 設定済** (verify-v148-extended-gate)。

## 📋 判断待ち (SPEC/提案、 承認後に実装)

### ⑩ 決算 call transcript から会社自身のガイダンス数値抽出 → **SPEC draft 用意、 user gate-1 待ち**
- draft: `docs/specs/transcript_guidance_2026-06-02.md`。 [[feedback_sec_guidance_8k_coverage_limit]] の穴埋め (MSFT/GOOGL 等は 8-K でなく call で guidance 提供 → 現状 confidence=low)。
- **recon 重要 finding**: 既存資産多数 — `fmp_client.earnings_transcript()` / `visualizer/sec_guidance.py` (8-K or transcript 両対応の citation 必須 LLM 抽出器) / Motley Fool fallback / GuidanceCard confidence banner。 → greenfield でなく「transcript を 1 級 source に昇格」。 memory の「+10-15 人日」 は抽出器無し前提だったので Phase 0 で再見積もり。
- §38 戦略: [[project_forward_visibility]] (v146) と同 — 会社開示数値+citation・色なし・我々の予測でない。 **§38 重 → 6 体合議必須**、 無監視 ship 不可。
- gate-1 で user が決める: 方向性 (8-K low 時のみ transcript or 常時並列) / 対象範囲 / LP unlock タイミング / cost 上限。 Phase 0 (FMP transcript coverage/cost 実測) は FMP Premium key 必要 = 本番環境 or key 共有後。

### ⑦ deferred polish (3 体合議 で出た非 block の MINOR、 いずれも product 判断 or pre-existing)
- ProTeaser: section③ が 1 件のみ(=extended 1)時 blurredCount=0 で ProTeaser 出ず (CVR 機会損失、 demo design・全 section 共通の pre-existing)。
- 「新規 Cup-Handle 検出」 title vs 7d window: SPEC comment の signal_date>=today-1 filter 未実装。 title 変更 or filter 実装の product 判断。
- WIP banner「Phase 4-A Sprint 4-A-4」 内部用語: ScreenerPane は isPillar2Pane1 flag default OFF で本番非表示、 gate-3 default-on 時に user-facing 文言へ。

## 🔴 残タスク
- ⑩ gate-1 (上記) → Phase 0 → 6 体合議 → 実装。 release 準備 (Stripe pricing / 法務 / QA)。 ⑦ deferred polish (product 判断要)。
- (hygiene) 古い handover (v30-v148 多数) 未削除。 cleanup 候補。

## ⚠️ 触ると危険 (継続)
- 発光系 / sticky 検索バー / VITE_ ARG/ENV / aggregator LLM import 禁止 / 「じっちゃま」UI 禁止 / DailyDigest summary flex。
- Cup-Handle 閾値は LLY/GE/META/NVDA dogfood 必須 ([[feedback_cup_handle_thresholds]])。 isNonEquity gate ([[feedback_non_equity_chart_overlays]])。
- authed chart は headless 描画不可 (BYPASS_TOKEN ローカル未配置)。 cup/個別株視覚は build+logic+bundle + 朝 dogfood。

## release status
pre-release。 Premium/Signature は LP「近日公開」。 Pro ¥980 のみ実購入可。
