# 引き継ぎ書 v252 (2026-06-23) — screener 戦略プリセット: 新高値ブレイク完全化 + リッチタイルUI(v8 slice1) 着地

> v251 を置換。**残 Sprint A を 2 増分前進**（新高値ブレイク完全化 / リッチタイルUI v8 slice1）。
> 全て feature branch `claude/jolly-wozniak-bgdvzi` / **PR #1 (draft)** へ push 済。**本番未マージ**（screener_v2 flag default OFF）。
> v251 は削除可。

---

## 🧭 30 秒サマリー

| 項目 | 状態 |
|---|---|
| **branch / PR** | `claude/jolly-wozniak-bgdvzi` (HEAD `8daaf17`) / **PR #1 draft** https://github.com/yamada073526/beatscanner/pull/1 |
| **本番 HEAD** | 変更なし（main 未マージ）。screener_v2 flag = **default OFF**、dogfood は `?screener_v2=1`（ダークモード） |
| **完了（本セッション）** | ① 新高値ブレイク完全化 `c489935` ② 戦略プリセット リッチタイルUI v8 slice1 `8daaf17` |
| **最優先の次** | 🔴 **リッチ表示昇格(v8) 残 slice**: 合否理由 静的dict / advanced 個別緩急 / IA昇格 |
| **visual 確認** | タイルは flag裏・未デプロイで production snap 不可 → **deploy 後 dogfood で最終確認必須**（mockup v8 = 承認済 visual SSOT で代替済） |
| **PR watch** | ✅ 本セッションで `subscribe_pr_activity` 購読開始 |

---

## ✅ 本セッション完了（commit 付き）

### 1. 新高値ブレイク完全化 `c489935`
v251 残 Sprint A #2。従来 `buyZoneOnly` のみで "薄い" と自認していた状態を解消。
- `is_new_52w_high` の binary facet「**52週高値を更新**」を追加（既存 `buyZoneOnly` Premium パターンの**鏡写し**・新トークン無し）。
- `applyStrategy('new_high_break')` = **買い場圏(pivot≤+5%) + 52週高値更新**。
- chip gate = `universe.freshness?.breakout && !(universe.locked_facets||[]).includes('breakout')`（is_new_52w_high の Premium マスクと整合、free は breakout locked chip）。
- **Trust Cliff 厳守**: 全 `extra` オブジェクト・全 useMemo 依存配列・集約 boolean に `newHigh52wOnly` を threading（`buyZoneOnly` と **37/37 一致**）。`newHigh52wCount` は自己除外。
- **backend 不要**（データは universe に既存）。`CustomScreenerPanel.jsx` +102/-31。

### 2. 戦略プリセット リッチタイルUI（mockup v8 slice1）`8daaf17`
v251 残 Sprint A #3「モック準拠リッチ表示昇格」の slice1。
- `StrategyPresetBar.jsx`: segmented Chip 2個 → **角丸タイル**（Lucide: 決算合格=`BadgeCheck` / 新高値ブレイク=`TrendingUp`、シアン下地アイコンコンテナ）。
- 選択/hover 発光 = **アイコン `filter: drop-shadow`（SVG filter・低リスク）+ gold border(`color-mix(--color-gold)`) + `translateY` lift**。**危険な card-glow box-shadow（`.panel-card`/`.bs-panel`/`.surface-card`）は新規作成せず非接触**。
- 全 box-shadow は `var(--shadow-*)` token / gold は `--color-gold`・`--color-gold-mid` token 経由（生 hex / 生 shadow / `!important` なし）。
- **compound 4-set**（`.tile` / `:hover` / `.is-selected` / `.is-selected:hover`）揃い、`is-arriving` 不使用で発光バグ領域を回避。
- a11y: `role=radiogroup/radio` + `aria-checked` + focus-visible gold ring + `prefers-reduced-motion` 無効化（WCAG）。
- CSS は `index.css` L14129-14265 の `.screener-strategy-*` スコープ。API 不変（`STRATEGY_PRESETS` SSOT / `active`/`onSelect` / data-testid 維持）。
- **Trust Cliff 修正**: `new_high_break` の説明文が cup-with-handle ベースを要求するかに読めたため（実 filter は is_new_52w_high + 買い場圏で cup 未判定）、実装一致の説明へ修正。

> 両 commit とも design-system-check 機械検査 PASS / build ✓ / backend 不変 / screener_v2 flag 裏（本番影響なし）。

---

## 🔴 最優先の次 = リッチ表示昇格(v8) 残 slice + 残 Sprint A

1. **合否理由 静的dict**（§38安全・LLM不使用・STATE_LABEL_JP 方式）: 結果行（`ScreenerRow.jsx` の `matchBadges`、親 = CustomScreenerPanel/ScreenerTable）に「**なぜ合致したか**」を事実言い換えで表示。叩き台 UI① の維持価値項目（SPEC §3）。
2. **advanced 個別緩急 UI**（mockup v8 §7.8）: 精度3段(緩い/標準/厳しい) + 各条件 mini-segmented（緩/標/厳/最厳・数値併記）+ **二値は「必須ゲート」明示** + **Pro 課金ゲート**（精度3段は Free 100%動作・個別緩急のみ Pro、件数ちら見せ）。一部は `CustomScreenerPanel` の overrides/facet 機構で既存 → 差分実装。
3. **IA昇格**: `StrategyPresetBar` を custom モード内（`ScreenerMaster.jsx` L221）から**画面トップ**へ。
4. **backend 0-call fields**（frontend先行方針で後回し・DEFER）: `eps_3y_rising`/`rev_3y_rising`/`cfps_3y_rising`(monotonic) + **セクター別RS集計**（既存 `rs_vs_spy_pct` を `sector` で集計=0 call）+ breakout/52週高値 facet。nightly canslim-scan `_compute_one`（main.py L21746〜）に既存 fetch 再利用で追加。→ **旬のセクター/セクター別リーダー** の前提。
5. **DEFER 確定**: 旬のセクター / セクター別リーダー（sector別RS 前提=#4完了後）/ 崩れ検知（売り側・「今は入れない」決定済）。

---

## 🟡 user 確定事項（v251 から継続・永続化候補）
- v2 ハイブリッド方向で確定 / P5 全天候型(配当NISA)は「今は決めない」 / 崩れ検知は「今は入れない」 / 着手は **frontend 先行(flag裏)**。
- 「実装前にモックを詰めて手戻りを減らす」（mockup v8 = 承認済 visual SSOT）。模範解答 = Lucide 系線画アイコン×角丸タイル。
- deep-research/ultrathink は **計画段階のみ**許可（実装段階は不可・トークン節約）。

---

## ✅ 前版(v251) backlog の決着（carryforward 確認）
v251「🔴 最優先の次 = 残 Sprint A」の各項目を本セッションで進捗:
- **新高値ブレイクの完全化**（cup/breakout/52週高値 facet）→ ✅ **完了** `c489935`（52週高値 facet 実装。cup/breakout 状態分類トグルは将来）。
- **frontend リッチ表示昇格 / 合否理由 / 4プリセット拡張 / IA昇格** → リッチ表示 slice1(タイル)✅、**合否理由 / advanced 個別緩急 / IA昇格 は残**（上記 §最優先の次 1-3）。
- **backend 0-call fields追加**（`eps_3y_rising`/`rev_3y_rising`/`cfps_3y_rising` / **セクター別RS集計** / breakout/52週高値 facet）→ 未着手（frontend先行方針で DEFER、§最優先の次 #4）。脱落でなく意図的 DEFER。
- **旬のセクター / セクター別リーダー / 崩れ検知** → DEFER 継続（脱落なし）。

---

## ⚠️ 触ると危険 / 規律
- **発光系**（`.panel-card`/`.bs-panel`/`.surface-card`）→ タイルは icon `drop-shadow` + token `box-shadow` で回避済。リッチ化の残 slice で card box-shadow glow を足すなら `design_recipes.md §C-1〜4` 必読・compound 4-set 必須。
- **StrategyPresetBar はタイル化済**（`role=radiogroup/radio`）。`active`/`onSelect` API・data-testid・再クリック null 解除を壊さない。
- **CustomScreenerPanel**: `newHigh52wOnly` facet 追加済。`buyZoneOnly` と同じ threading 規律（**全 extra/依存配列に並行追加** = count と list 同一 predicate、Trust Cliff）。`forwardRef` + `applyStrategy` expose 維持。
- **screener_v2 flag**（`isScreenerV2()` in ScreenerMaster.jsx）: default OFF。dogfood `?screener_v2=1`。昇格は C-16 ゲート後。
- deploy = `git push origin main`（Railway auto）。今は **feature branch のみ・main 未マージ**。`git add` は**明示 path**（並行セッション巻き込み防止）。
- sticky 検索バー / VITE_ ARG/ENV 同期 は不触。

## 🧹 掃除
- handover **v251 は削除可**（本 v252 最新）。
- snap untracked / mockup v1-v7 は残置可（v8 最新）。実装が進めば古い版は削除可。
- PR #1 は draft のまま。レビュー/実装が進んだら ready 化。
