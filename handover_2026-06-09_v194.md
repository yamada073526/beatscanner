# 引き継ぎ書 v194 (2026-06-09、autopilot: C-3 競合ナビ + 軽量 polish 2 件 + テクニカル章扉対称化)

user 出社中の autopilot セッション。軽い 2 件 + C-3 競合ナビ (PGE+3体合議) + テクニカル章扉対称化を実装。**当初 C-3 は commit のみ予定だったが、user 出先指示「チェックをお願い」で deploy + headless 検証まで実施 → 全て本番 live**。最新本番 commit = `9111a43`。

## 🎯 完了 (全て本番 live)

| commit | 状態 | 内容 |
|---|---|---|
| `9b206f3` v194-light | ✅ **live・検証済** | 競合チップ hover 視認性 (`.peer-nav-chip` 限定で hover 文字色を text-primary、size sm→md) + ProfileCard dead code 整理 (Sparkles / CITATION_TEXT_SHORT / CITATION_TOOLTIP 削除 + 誤コメント修正) |
| `025a535` C-3 | ✅ **live・headless 検証 PASS** | C-3 競合ナビ パンくず Sprint 1a + 1b(scroll 復元)。下記詳細 |
| `9111a43` テクニカル | ✅ **live** | テクニカル章扉 (②) に `emphasized` 付与しファンダ章扉 (①) と対称化 (gold hairline 30%→60%)。§C-11 横展開 Sprint1。Sprint2-5 は DEFER (`docs/specs/SPEC_2026-06-09_technical_chapter_hairline.md`) |
| `2f23c81` dogfood-1 | ✅ **live** | user 在宅 dogfood 受け 3 件: ①scroll 復元を「高さが届くまで毎フレーム再補正(最大~1.8s)」に作り直し(遅延ロードで先頭に飛ぶ問題) ②競合chip hover を cyan に戻す(size md で視認可、`.peer-nav-chip` override 撤回) ③テクニカル章のチャート⇔リターン⇔売買目安に hairline+space-8 |
| `d4b8a71` tab再設計 | ✅ **live・2体レビュー PASS** | 会社概要「概要/競合比較」タブを underline→segmented(塗りピル) gold UI に再設計(`.ws-pane4-jp-segmented.is-gold`)。背景同化解消。must-fix 反映: light gold text 78→55%(WCAG 4.52:1) + tablist keyboard nav。Scale import 除去 |

**本番反映**: 当初 commit のみ予定だったが 2026-06-09 user 出先指示「チェックをお願い」で deploy 実施 (58e7b1f live、bundle に DetailBreadcrumb/detailHistory/detail-breadcrumb-home 確認)。`snap-c3-breadcrumb.mjs` (demo headless) で検証 **PASS**: 1銘柄時 非表示 / 2銘柄後「⌂ › AAPL › NVDA」出現 (home/ancestor AAPL/current NVDA) / 祖先クリックで `?ticker=AAPL` に戻り + truncate / **pageError 0**。429 は demo rate limit (3銘柄ナビ) で C-3 無関係 (むしろ analyze 失敗時も breadcrumb 動作の堅牢性を実証)。スクショ = `.visual/c3-crumb-clip.png`。

## 🟢 C-3 競合ナビ (commit 025a535、3体合議 PASS-with-nits 反映済)
会社概要の競合チップから他社 detail に飛ぶと戻れない問題を解決。Hero 上に横並びパンくず「⌂ › AAPL › NVDA」(28px 独立バー)、祖先クリックで resultCache hit 0 秒戻り + スクロール位置復元。SPEC = `docs/specs/SPEC_2026-06-09_C3_competitor_nav.md`。

- **Sprint 1a**: `workspaceStore.js` に `detailHistory` stack + `computeNextDetailHistory` (純関数 single-source: falsy/非株式ガード + dedup + 既出 truncate 巻き戻し + 最大10件) を **`setActiveTicker` 1 点に結線**。両経路 (競合チップ `onAnalyze→runAnalyze` / Pane2 直 `setActiveTicker`) が `useJudgmentResult.js:90/160` の setActiveTicker に収束する実コード事実に基づく。`DetailBreadcrumb.jsx` 新規 (TickerBadge 流用・発光系 class 不使用・全 token・persist 除外)、`JudgmentDetail.jsx` の `ds-judgment-detail` first child に挿入 (2 件以上で表示)。`lib/tickerUtils.js` 新規 (`isNonEquityTicker` を StockPriceChart から切出し共用)。
- **Sprint 1b (scroll のみ)**: `useDetailScrollRestore.js` 新規。overflow 親辿りで scroll container 特定、per-ticker sessionStorage、cache hit 描画後 rAF 2 回 gate + **scrollHeight ガード** (cache miss の skeleton 段階で先頭に飛ぶのを防止) で復元。
- **planner の重要発見**: memory は「PaneDetailView case 'ticker' に配置」と記載だが**誤り**。ticker detail は `JudgmentDetail` が直接 render、`PaneDetailView` は index/portfolio 専用。挿入先は JudgmentDetail に確定。

## 🔴 帰宅後の判断待ち (全て本番 live 済、追加判断のみ)
0. **(済) C-3 deploy + 在宅 dogfood 1 巡** — deploy + headless 検証 PASS。user 在宅 dogfood で指摘 4 件 → `2f23c81`/`d4b8a71` で対応済 (scroll 再作り / hover cyan / テクニカル hairline / tab gold)。残りは user の再目視待ち (特に scroll 復元 + tab gold light コントラスト + ダークモード)。
1. **【F5 でパンくず消滅】** — **意図通り** (detailHistory は persist 除外で F5 クリア = 幽霊パンくず防止、qa 承認済)。user が「F5 でも残してほしい」なら **sessionStorage 永続化** (タブ閉じで消える=幽霊回避) を実装する選択肢あり。ただし **F5 後どの ticker を現在地にするか URL との整合** が要設計 (useUrlSync は凍結領域)。user 指示待ち。
2. **【URL dual-param】** — user F5 時の URL が `detail=t:NVDA&ticker=AAPL` と 2 銘柄併記。useUrlSync (凍結) の `detail=` と `ticker=` が divergence する既存 or C-3 隣接の癖。C-3 とは別件として要調査 (慎重に、blast radius あり)。
3. **テクニカル Sprint 2-5 の方針** — 章扉対称化 (Sprint1) は live。Sprint2 (TechnicalChapterSummary を A案=現状維持 / B案=短期/中期/長期 L2冠×3 に再構成) の判断 + Sprint4 (PriceLadder L2冠化、発光系 glow 検査必須)。SPEC = `docs/specs/SPEC_2026-06-09_technical_chapter_hairline.md`。
2. **accordion 開閉復元 (DEFER-SPEC)** — C-3 Phase1b の当初設計に含まれたが **autopilot で見送り**。理由: `AccordionSection` の `controlledOpen`/`onOpenChange` を detail 全 section (ChapterSection / FundamentalsAccordion 等のサブコンポーネント経由で分散) に配線する blast-radius 中の変更で、既存 `expandedSections` (Sprint 5 condition-click auto-expand) / halo sweep と絡み、authed chart が headless 検証不可のため無監視 ship は危険と判断。
   - qa 合議 verdict: **「DEFER は妥当。scroll 復元だけで『戻った感』は 9 割担保、accordion 閉じは『再度開く手間』止まりで『戻っていない』混乱には直結しない」**。
   - 実装方針 (次セッション): `useDetailScrollRestore` を accordion 対応に拡張 (git 履歴の generator 版に openAccordionIds/onAccordionChange あり、ただし今回 trim 済) → 各 `AccordionSection` の `onOpenChange` に追跡 callback + `controlledOpen` に復元 Set をマージ。既存 expandedSections と統合するか別管理か要設計。

## ☀️ 朝 dogfood チェックリスト (3体合議統合、authed chart = headless 検証不可)
1. **AAPL → 競合チップで MSFT → GOOGL**: 2 回目クリック後に `⌂ › AAPL › MSFT › GOOGL` が Hero 上に表示。1 銘柄表示中は非表示。
2. **祖先クリック scroll 復元 (cache hit)**: AAPL を中程度スクロール → 競合で MSFT → 5 秒以内にパンくず AAPL クリック → loading なし即時戻り + **scroll 位置復元** (先頭に飛ばない)。
3. **⌂ クリック**: スクリーナーに戻る + パンくず消える。**⌂ hover** で色変化 (3体 must-fix で追加済)。
4. **5 段省略**: 5 銘柄辿り `⌂ › … › 直前 › 現在` 省略表示。
5. **F5 リロード**: パンくず消える (persist 除外、幽霊表示なし)。
6. **非株式**: ^GSPC 等を挟んでもパンくずに混入しない。
7. **ダークモード**: バーのコントラスト・下線区切り・⌂ アイコン色が見やすいか (背景 transparent + border のみのため要確認、ui N-2)。
8. **(余力) cache miss**: 11 分以上待って祖先クリック → 再 fetch・先頭表示は許容、ただし「分析データ取得中」バナーが永続化しないか・rate limit 踏まないか。
9. **【テクニカル章扉】** AAPL 等の v5 詳細で ② テクニカル章扉の gold hairline が ① ファンダ章扉と**同じ強さ・トーン**に見えるか (9111a43、demo rate limit で headless visual 未検証)。違和感あれば JudgmentDetail.jsx L1305 の `emphasized` 削除で 1 行 revert。

## ⚠️ 危険箇所 / 運用知見
- **C-3 の blast radius 中**: `setActiveTicker` は 18 callsite (IndicesView ^GSPC / setActiveTicker(null) クリア / useUrlSync URL復元 含む) から呼ばれる。push は `computeNextDetailHistory` のガード (falsy/非株式) で吸収。**今後 setActiveTicker を触る際は履歴 push 副作用に注意**。
- **rollback 容易**: C-3 を無効化するなら JudgmentDetail の `<DetailBreadcrumb />` 1 行削除 (or store の setActiveTicker から push 結線を外す)。state 追加は persist 外で無害。
- `navigateBack` は 3体合議で「未使用 + 危険コメント」指摘 → 削除済 (breadcrumb は setActiveTicker 直叩き + truncate で代替)。`pushDetailHistory` は外部 API として残置 (現状未呼出)。
- 発光系 `.panel-card/.surface-card` / sticky 検索バー §C-6 / Hero 発光 host / VITE_ ARG/ENV / aggregator LLM import は不触。
- **テクニカル章扉** (9111a43): emphasized 付与は v5 render path (L1305) のみ。v4 (L1388付近) / legacy (L1404付近) は別 path で不変。違和感あれば L1305 の `emphasized` 削除で revert。
- **Railway 連続 push queue 詰まり**: 本セッションは 9b206f3 / 58e7b1f / 9111a43 を間隔をあけて個別 push、queue 問題なし。

## 🟡 残バックログ
- **テクニカル章 Sprint 2-5** (SPEC `docs/specs/SPEC_2026-06-09_technical_chapter_hairline.md`): Sprint1(章扉)済。残=本文 L2冠化 + PriceLadder/売買目安の見出し層化 (発光系 glow 検査必須)。
- **C-3 accordion 復元** (上記 DEFER-SPEC)。
- 章サマリー速報スタイル再設計 (memory `project_chapter_summary_jitchama_style`、§38 6体 gate、未着手)。
- v5 → v4 昇格 (dogfood 十分後)。

## ⏳ pending (継承)
- 明朝 09:00 JST `canslim-null-reasons-verify` / S5b バッジ列 pixel 未確認。
- スクリーナー メンタルモデル再編 grill-me (発表会FB §C、未着手)。

## 📋 deferred backlog (継承)
- upgrade モーダル design polish / screener tier 方針 / Pane2 検索結果レイアウト。
- Stripe Premium 配線 (launch blocker) / @dataclass ScanResult 化・scan async/chunk化 (3000 universe 502)。
