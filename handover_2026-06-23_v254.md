# 引き継ぎ書 v254 (2026-06-23) — screener #2 アドバンスド個別緩急＋Pro課金ゲート 実装着地

> v253 を置換。**#2 実装（slice 2-a〜2-e）を完了**し PR #1 に push 済。残バックログは **backend 0-call fields（#4）** のみ。
> branch `claude/jolly-wozniak-bgdvzi` / **PR #1 (draft, HEAD `6db4844`)**。**本番未マージ**（screener_v2 flag default OFF）。
> 次セッションは **deploy 後 dogfood** か **backend #4** から。v253 は削除可。

---

## 🧭 30 秒サマリー

| 項目 | 状態 |
|---|---|
| **branch / PR** | `claude/jolly-wozniak-bgdvzi` (HEAD `6db4844`) / **PR #1 draft** https://github.com/yamada073526/beatscanner/pull/1 |
| **本番 HEAD** | 変更なし（main 未マージ）。screener_v2 flag = **default OFF**、dogfood は `?screener_v2=1`（ダーク）|
| **完了（本セッション）** | #2 slice 2-a `0d40938` / 2-b〜2-e `6db4844`（アドバンスド個別緩急＋Pro課金ゲート 全 slice）|
| **CI** | 「Smoke tests against production」**success**。未解決レビューコメントなし |
| **PR watch** | 本セッションで `subscribe_pr_activity` 購読中。**購読はセッション固有** → 新セッションで再購読必要 |
| **最優先の次** | 🔴 ① deploy 後 dogfood（flag裏で production snap 不可）② **backend 0-call fields**（#4）|

---

## ✅ 本セッション完了（#2 実装 slice 2-a〜2-e）

SPEC `docs/specs/SPEC_2026-06-23_screener-advanced-individual-gating.md` を全 slice 実装。`CustomScreenerPanel.jsx` + `index.css`。

- **2-a**（`0d40938`）: ラダー4段化＋閾値数値併記＋原典較正。`FUNDA_FACETS` を eps_yoy `severe:100` 追加 / eps_cagr loose段廃止（`{standard:25,strict:50}`）/ roe `{17,25,50}` / rs `{70,80,90}`。`GRADE_ORDER`/`facetLevels`/`clampLevel`/`gradeAnnot` ヘルパー。`buildActiveGrades` を per-facet clamp（RS<70 ハードフロア / eps_cagr loose→standard）。`itemPasses` に未定義段 no-op ガード。data-testid を `screener-grade-{facet}-{level}`。
- **2-b〜2-e**（`6db4844`）:
  - **2-b**: 必須ゲート badge（営業CFマージン≥15% / CFPS>EPS・lock・変更不可）。aria-label は「制限」でなく「戦略の絶対条件」。
  - **2-c**: アドバンスド(個別緩急) toggle（`role=switch`/`aria-checked`）。OFF=精度のみ / ON=per-facet mini-segment 露出。`screener-adv-rows` grid-template-rows 0fr↔1fr（開350/閉200ms 非対称）。`カスタム` tag（`isCustom`=override 有無）。
  - **2-d（核）**: Pro 課金ゲート。`advLocked = screenerV2 && !isProUser`。Free は segment 淡色ちら見せ（`disabled` 非使用＝Chip が onClick を殺すため `screener-grade-seg--locked` class + onClick→nudge）＋件数 peek（`facetLevelCounts` 実集計・§38）。操作時のみ lockbar（常駐させない）。lockbar コピーは Free 価値（N銘柄）を先に肯定→Pro 案内（list可視語撤廃）。CTA は `onProUpgrade`(fallback `onUpgrade`)で実 upgrade modal へ到達 → **Trust Cliff #4 解消**。
  - **2-e**: focus-visible gold ring / prefers-reduced-motion 二重防御 / WCAG2.5.5 タップ域クランプ（`min-w-[36px]`・860px 1カラム化）。

> 検証: `npm run build` ✓ / design-system-check（raw hex・shadow・glow 違反なし、`#fff`→`var(--text-inverse,#f8fafc)`）✓ / 全 prop 配線（`ScreenerMaster` が `screenerV2`/`isProUser`/`onProUpgrade` 渡す）✓ / legacy（screenerV2=false）grade rows 2箇所無傷で物理隔離 ✓。

---

## 🔴 最優先の次

### ① deploy 後 dogfood（flag裏のため必須）
> v253 の **⚠️ 2-d 完了まで flag-ON 禁止** は本セッションで解消: 2-d 着地（`onProUpgrade` 接続 = Trust Cliff #4 クリア）により **flag-ON dogfood が解禁**された。
main マージ→deploy 後に `?screener_v2=1`（ダーク）で目視確認:
- アドバンスド toggle 開閉（grid-rows アニメ）
- **Pro時**（isProUser=true）: 個別緩急 segment が機能（override 反映・件数連動）
- **Free時**: segment 淡色ちら見せ＋件数 peek、操作で lockbar 出現、「Pro を見る」で upgrade modal
- 原典較正で **Free 標準プリセット件数が動く**（roe床17/rs標準80・gate1 承認済）→ 件数の妥当性も確認

### ② backend 0-call fields（#4・frontend先行方針で DEFER 継続）
`eps_3y_rising`/`rev_3y_rising`/`cfps_3y_rising`（monotonic）+ **セクター別RS集計**（既存 `rs_vs_spy_pct` を `sector` 集計=0 call）。nightly canslim-scan `_compute_one`（main.py L21746〜）に既存 fetch 再利用で追加。→ **連続性state（3期連続増）/ 旬のセクター(p3)/ セクター別リーダー(p4)** の前提。完了後に別 SPEC で state ラダー（直近正/3期連続増/3期+加速）と p3/p4 プリセット。

---

## 🟡 user 確定事項（永続化候補）
- v2 ハイブリッド方向で確定 / P5 全天候型(配当NISA)・崩れ検知は「今は入れない」/ 着手は frontend 先行(flag裏)。
- 「実装前にモックを詰めて手戻りを減らす」（mockup v8 = 承認済 visual SSOT）。模範解答 = Lucide 系線画アイコン×角丸タイル。
- **gate 1 = 原典に素直に従う**（じっちゃま優先→オニール）。**gate 2 価格 = ¥1,480/月（年¥12,800）第1推奨**（SPEC §9・Claude 一任で確定）。
- ⚠️ **「Pro」価格/バッジを LP/UI に出す時点で Stripe 課金フロー実装が必須**（Trust Cliff #4）。#2 の Pro ゲートは個別緩急 UI + 既存 upgrade modal 到達まで。課金処理本体は別 SPEC。
- **deep-research/ultrathink は計画段階のみ許可**（実装段階は不可）。

---

## ⚠️ 触ると危険 / 規律
- **発光系**（`.panel-card`/`.bs-panel`/`.surface-card`）→ screener は plain bordered + token box-shadow のみ。#2 で追加した `.screener-adv-*`/`.screener-lockbar`/`.screener-gate-badge` は color-mix(gold) tint + border のみ（新規 card-glow なし）。
- **CustomScreenerPanel**: #1 `MATCH_REASON_JP`/`buildMatchReason`、#3 `initialStrategy`/`applyStrategyImpl`、#2 `advOpen`/`advLocked`/`isCustom`/`renderGradeRow`(activeGrades 駆動)/lockbar。count と list は同一 predicate `itemPasses`（Trust Cliff C-2）。`forwardRef`+`applyStrategy` expose 維持。
- **renderGradeRow は legacy/v2 共有**。severe段・閾値併記・Pro ゲートは `screenerV2` 条件で v2 限定（legacy 物理隔離 §6）。pressed は `activeGrades`（clamp済）駆動に変更済 — 壊さない。
- **screener_v2 flag**（`isScreenerV2()`）: default OFF。`ScreenerMaster` が `screenerV2={isScreenerV2()}` 渡す。
- deploy = `git push origin main`（Railway auto）。今は **feature branch のみ・main 未マージ**。`git add` は明示 path。
- sticky 検索バー / VITE_ ARG/ENV 同期 は不触。

## 🧹 掃除
- handover **v253 は削除可**（本 v254 最新）。
- snap untracked / mockup v1-v7 は残置可（v8 最新）。
- PR #1 は draft のまま。deploy 後 dogfood OK なら ready 化を検討。
- **PR watch 購読はセッション固有** → 新セッションで PR #1 を再 `subscribe_pr_activity` 推奨（`send_later` は本環境で未提供のため定期チェックインは未設定）。
