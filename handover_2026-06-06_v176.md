# BeatScanner Handover v176 — 案B コンセンサス修正トレンド Sprint 3 (nightly snapshot cron) 着地 (2026-06-06)

前セッション (v175) から継続。**案B「コンセンサス修正トレンド」Sprint 3 (nightly snapshot cron)** を autopilot で 6体合議 → 実装 → 実検証 → commit/push/deploy まで完遂。**commit `aa0270c` (push 済、Railway auto-deploy 確認済)**。次は **Sprint 4 (drift API)**。

## 🆕 2026-06-07 朝の追記 (user 確認 + RS hotfix git 記録)

- **別セッションの RS hotfix を git 記録** (commit `6a16a9b`): yfinance→FMP Ultimate 移行 + `_latest_valid_calc_date` (partial scan guard、`_MIN_VALID_RS_ROWS=200`)。本番は railway up 直送で反映済だった分を後追い commit し **git⇔本番 乖離を解消**。py_compile + pytest 57 + import OK。**案B `_build_consensus_universe` の RS source もこの guard 経由に改善済** (別セッションが私の Sprint 3 を整合改修)。
- **Railway deploy 方式 確定**: `git push` で auto-deploy (実証) + `railway up` 直送も可 (未commit は git 乖離)。CLAUDE.md「デプロイ運用」を実態に更新済。
- **effort high** に復帰済。

## 🔴 次セッションの順序 (user 指示)

1. **案B Sprint 4 (drift API)** ← 最優先 (下記 §案B 残 Sprint)。3体合議で可。snapshot 2日分で drift 確認可 (最短 06-08)。
2. **改善希望レビュー** (Sprint 4 完了後、下記 §🎨)。multi-review (UX / Trust Cliff / オニール原則)。

## 🎨 改善希望 (発表会フィードバック → Sprint 4 後に multi-review)

**背景**: AI スクール発表会で BeatScanner を共有 → 株経験者でも「パッと見で何が表示されているか分からない」反応。Pane3 と screener の認知コスト低減が課題。

**A. 銘柄分析 Pane3 の改修**
- 大分類を「ファンダメンタル」「テクニカル」の2つに整理し、各冒頭にサマリーをざっくり表示。
- 各指標の整理に**ライター憲法**を適用 (文章でないが「読み手にストレスなく伝える」は同方針): ①短文で結論先出し (メンタルモデル形成) ②既知→未知・抽象→具体で重み付け ③並列情報は表示要素を統一。

**B. スクリーナー機能アップデート**
- 外観改修 (パッと見で使い方不明):
  - 問題1: "探索"チップで絞り込む UX が検索のメンタルモデル (検索バー) と不一致。Pane2 で絞った結果が Pane2 下でなく **Pane3 に出る**のが自然。Pane2 を**ファンダ+テクニカルの2本柱検索**へ。
  - 問題2: 検索条件拡張。現ファンダ5条件(3年通期)はクリア2件で物足りない → **4半期決算(+来期ポジティブ)追加**。テクニカル条件候補: 要観察(ブレイクアウト挑戦中=cup右半分等) / ブレイクアウト確定(cup完成) / RS(リーダー銘柄)。
  - 確認事項: **ATH検索(ハンドルなしブレイクアウト)は実装済か?** (以前 multi-review で提案あり) → 要コード確認。
  - "Leader" 表記 = RS80以上の意味だが分かりづらい。素直に「RS80以上」or 表記は残しつつ補足併記 (オニール原則遵守を暗示)。
  - chip 上の "Pass 2銘柄・Fail 3銘柄" も分かりにくい。
- **C. その他**: ロード中表示に**絵**を追加 (今テキストのみで寂しい)。screener の "Loading screener" も同様に改善。

## ✅ 本セッション完了 (commit aa0270c)

- **6体合議** (engineer/finance=Opus + SRE/backend/data/qa=Sonnet、mixed model): 全員 **CONCERN (BLOCK ゼロ)**。全 CONCERN を実装に反映。
- **consensus_history.py `fetch_and_build_snapshot` を forward-only + keep_nearest に拡張**:
  - ⚠️ **最重大 fix**: FMP `/stable/analyst-estimates` は **date 降順 (遠未来先頭) で返る**（実 FMP で実証: META 先頭=2030、AAPL=2028）。旧 `limit=4/8` だと near-term が抜け drift が無価値になる。→ `limit=40` で全カバー後 **`fiscal_date >= snapshot_date` で forward 期のみ + 最近 N 期 keep**。near-term 捕捉 + 過去確定期を蓄積せず容量節約 + **calc.py 無改修** (テーブルに未来期しか入らないので `min(fiscal_date)` が自動的に near-term を指す)。
  - 実 FMP 検証済: META/AAPL/NVDA とも near-term 次4四半期 (例 NVDA 2026-07-26〜2027-04-26) を正しく捕捉。
- **main.py `/api/cron/consensus-snapshot` endpoint + helpers**:
  - `_build_consensus_universe(sb)`: **保有(transactions) ∪ WL(watchlist) ∪ RS≥90(rs_ratings 最新calc_date) ∪ Cup-Handle検出(pattern_signals 最新signal_date, state∈{breakout_pending,confirmed,extended})** の和集合。**各 source 独立 try/except** (1 source 落ちても継続) + **`_select_all_column` range pagination** (PostgREST 1000行 cap 回避 = transactions/watchlist の silent 歯抜け防止) + RS/Cup は最新日使用で当日 scan 未実行でも graceful degrade。
  - `_check_cron_secret` 再利用 / **Semaphore(10)** で FMP burst 抑制 / **upsert 500行 chunk** / supabase 同期 client を **`asyncio.to_thread`** / **retention 90日 cleanup を upsert の前に** 実行 / body `{tickers:[...]}` override + `{dry_run:true}`。
  - return 観測性: `by_source / source_errors / skipped_count / error_count / failed_tickers[:20] / retention_deleted / is_first_run / universe_degrade_warning / first_snapshot_date / latest_snapshot_date`。
- **`.github/workflows/nightly_consensus.yml`** (新規): **23:40 UTC** 独立 workflow (nightly_scan 23:07 の rs/cup 完了後)。`concurrency: cancel-in-progress:false` + CRON_SECRET guard + `--max-time 1200` + GITHUB_STEP_SUMMARY + freshness verify。
- **検証**: pytest **57 passed** (forward-only/keep_nearest/boundary の test 4件追加) / Python 3.12 で `app.main` import + route 登録 OK / yaml OK / **本番 endpoint LIVE (push 後 ~30s で 401 = auto-deploy 確認)**。

> 🔎 **重要な実証発見**: **Railway は main push で auto-deploy する** (push 後 30s で `/health.commit`=aa0270c + endpoint 401)。CLAUDE.md「デプロイは railway up のみ」と異なる実態。memory [[supabase_gotchas]] or 新規に記録推奨。

## 🔴 次セッション最優先

### A. Sprint 3 本番稼働の確認 (autopilot で dry_run + 本実行を実施済)
- **dry_run 検証済** (gh workflow run, HTTP 200): `universe_size=85, source_errors=[], by_source={watchlist:8, holdings:6, rs_top:1, cup_handle:77}`。universe builder は本番 Supabase で正常動作 (4 source 和集合)。
- ⚠️ **rs_top=1 は RS データ状態 (Sprint 3 のバグではない)**: 本番 screener `/api/scanner/rs?min_percentile=90` も同 calc_date 2026-06-06 で 1件 (universe_size=3)。**rs_ratings の最新 calc_date が 3 行しかない** pre-existing 状態 (私のクエリは screener と完全一致)。今夜 23:07 の RS scan が full populate すれば rs_top ~300 に回復見込み。**翌朝 rs_top がまだ ~1 なら RS scan の健全性を別途調査** (Sprint 3 と独立の課題)。
- **本実行 (dry_run=false) 検証済** (HTTP 200): `processed=85, upserted=510 (≈6行/ticker = quarter4+annual2), skipped=0, error=0, retention_deleted=0, is_first_run=true, latest=2026-06-06`。**upsert/retention/freshness パス全て正常 + 本番に 510 行 蓄積開始済** (drift は 2 晩目から算出可)。
- nightly cron は **23:40 UTC** 自動稼働。翌朝 GitHub Actions の GITHUB_STEP_SUMMARY で by_source/upserted/freshness 確認。**初回は is_first_run=true、drift は 2 晩目以降** (蓄積中が正常表示)。

### B. Sprint 4 — drift API (`/api/analyst/consensus-drift?ticker=`)
- `consensus_history.py` に Supabase fetch → `classify_consensus_drift` (Sprint 2 完成済) 呼出を組む。6h cache (key=`ticker::window_days`)、sources schema + signal_quality 降格、analyst prefetch 相乗り。
- **6体合議は Sprint 3 で endpoint 設計をレビュー済 → Sprint 4 は 3体合議で可** (CLAUDE.md 軸②③、frontend 局所)。
- ⚠️ **申し送り (6体合議 qa)**: drift return に **analyst_count** を反映 (snapshot に `analyst_count_eps/revenue` 保存済)。「1人アナリストの上方修正」を「市場の上方修正」と誤読させない signal_quality 降格材料に。

### C. Sprint 5 — frontend 表示 (AnalystPanel)
- SPEC §5 Sprint 5 通り。3体合議。
- ⚠️ **申し送り (6体合議 qa、必須)**: `direction:"flat"` (蓄積済かつ全修正±0.5%未満) の表示文言を決める。「据え置き N回」等の**事実表示**にする。**flat を「蓄積中」(insufficient) と同じ muted にすると「実は蓄積済なのに『まだ』と出す逆 Trust Cliff」**になる。SPEC §5 の静的 dict 仕様に追記してから着手。

## 📋 案B DEFER (6体合議で出た判断・将来改善)

- **calc.py の会計期 roll 自己修復**: forward-only 蓄積で過去確定期は入らないが、near-term 期が決算到来で roll した直後 ~30日は「直前に終わった期」の最終snapshotを drift 対象にしうる (実害小・self-heal)。Sprint 4 で calc に `fiscal_date >= today` filter を足せば即時化可 (任意)。
- **breakout_extended の universe 包含**: Sprint 3 で含めた (修正トレンドを見たい主役)。`cup_completing` は「未完成」のため除外維持。
- **quarter/annual の同一 fiscal_date 衝突**: unique 制約に period_type 含むので行衝突なし。calc は quarter 優先 fallback 済。

## 📋 残バックログ (v175 から carry-forward、silent drop 防止)

- **count-up「入場時リセット」解消** (soft、未着手): ForwardOutlookSection / GuidanceCard / AnalystPanel の view 前 FINAL→0。
- **案C (RPO/ARR)** — SaaS 将来売上先行指標。2.5人日。
- **通期 EPS YoY の正確化** — basis mismatch で抑止中。
- 旧 v171 8件 (継続): データ脈動/Hero常時微光/Pane2 hover統一/frontend graceful 422/chart wipe/segment i18n Supabase永続化/B4 stat帯/Pane3 JudgmentDetail content 拡充。
- **Stripe Premium 配線** (launch blocker)。
- **nightly pre-warm cron (保有銘柄)** (#2-B、任意)。

## ⚠️ 触ると危険 (継続)

- `_compute_forward_outlook` §38 ガード / 発光系 (.panel-card 等) / sticky 検索 / **aggregator LLM import 禁止** (pre-commit Check 3) / VITE_ ARG/ENV 同期。
- **SummaryBrief の fetch gate** (`guidanceSecLoading` 依存、dep 戻すと #3 二度生成再発)。
- **inline 関数コンポーネント禁止** (transition 用、module-level に hoist)。
- **consensus_snapshots テーブル**: RLS=service_role only。anon/authenticated は SELECT 不可 (Premium 保護)。migration 不変。

## release status

pre-release。launch blocker = Stripe Premium 配線。
