# BeatScanner Handover v164 — スクリーナー刷新 + dogfood 修正 + flicker fix 完了 (2026-06-04 autopilot)

> 12h autopilot で **スクリーナーデザイン刷新 (A-1〜A-6 + B) → 3体合議 → deploy → 認証ハーネス vision-eval → user スマホ dogfood → 修正3件 → Hero flicker fix** まで完走。 残りは DEFER-SPEC (下記、 user 帰宅後判断)。
> **現本番 bundle: `index-CYtUkaAu.js`**。
> headless Premium 認証ハーネス (`snap-screener-vision.mjs` + `auth-helper.mjs`) で Premium 視覚を自律検証する pattern が確立 ([[feedback_auth_harness_vision_eval]])。

## 🟢 本セッション完了 (実装+本番検証済)

### 1. スクリーナーデザイン刷新 A-1〜A-6 + B (commit `5ad728c`)
真因=3 hierarchy 欠落 (同格/数値脇役/入場演出ゼロ) → hierarchy山+数値主役化+stagger で解消。
- A-1 連番 eyebrow(01/02/03)+18px見出し+gold hairline(32%) / A-2 ランクcircle(上位3 gold)+ticker(fw700)+stat badge(fw700) / A-3 stagger(reduced-motion対応・初回fetchのみ) / A-4 交差のみ Crown gold+padding / A-5 PASS章扉(Crown+件数stat) / A-6 探索 chip / B ウォッチ★(hover gold fill+浮上、追加済 gold点灯、緑不使用)
- 3体合議 SHIP-WITH-MINOR + 修正5件。 **vision-eval Δ +5.9** (旧68.6→新74.5、 aman+8.7/typography+6.7/hierarchy+6.6、 全5軸プラス)。

### 2. user スマホ dogfood 修正3件 (commit `4c62167`)
- ① 01交差のみ gold hairline ズレ → featured padding を top のみ他列と一致 (`var(--space-4) var(--space-5) var(--space-5)`)。
- ② ウォッチ追加済の ✓ が2行目回り込み → Check 撤去、`★+追加済` のみ。
- ③ PASS/FAIL/cup/oneill カードの緑バッジ↔ticker 干渉 → grid を viewport基準→**`repeat(auto-fill,minmax(180px,1fr))`** (狭い Pane2 で1列化)。

### 3. Hero refetch flicker fix (commit 直近、 D2 残 minor 解消)
- 銘柄選択→deselect で ScreenerPane remount→Hero 3 fetch 再走→loading flicker。 **ScreenerPane 内に module-scope cache (TTL 5分)** を追加し remount 時 hydrate+refetch skip。 Workspace の isScreener 分岐 (心臓部) 不触。
- frontend-architect レビュー SHIP-WITH-MINOR (minor は可読性 comment のみ、 機能 block なし)。 **harness 検証: deselect→remount で loading=0/tickers=5 = FLICKER_FIXED**。
- 任意の clarity 改善余地 (reviewer 指摘): `isRetry` flag 命名 / cache 保存時 loading:false の console.assert。 cosmetic、 次 deploy 時に同梱可。

## 🔵 DEFER-SPEC (ship せず・user 帰宅後判断)

### D-1. deselect 導線 breadcrumb 化 (NEEDS-REVIEW)
現状: Pane2 上部「← 今注目に戻る」 button (`screener-back-to-hero`、 常時DOM+visibility、 v161 jank fix 済、 動作OK)。
- 案A (推奨): **現状維持**。 deselect 動作に問題は出ていない (flicker も解消済)。
- 案B: Pane3 JudgmentDetail 上部に breadcrumb「スクリーナー > AAPL」。 P: 視線移動↓ / D: JudgmentDetail layout 改修が全 ticker 詳細に波及 (リスク中)。
- 案C: A+B 併存。
→ user が deselect 導線に不満を感じたら B、 でなければ A 維持。

### D-2. CustomScreenerPanel user/isPro frontend gating (NEEDS-REVIEW=funnel-cro)
現状: Workspace が user/isPro を渡すが component は destructure せず (dead props)。 demo blur は **backend `_masked` で機能** (Trust Cliff 実害なし)。
- 案A (推奨): **現状維持**。 backend mask で十分、 緊急性低。
- 案B: dead props 削除 (Workspace 受け渡しも) = cleanup のみ、 安全だが価値小。
- 案C: frontend gating 実装 (非Pro に ProTeaser/blur)。 **funnel-cro skill レビュー必須** (Trust Cliff/CVR 設計判断)。

### D-3. RS full universe (1699→2449) = cron 委譲
nightly scan workflow は既に `universe_size:3000` (Russell3000 相当) で dispatch 設定済 ([[feedback_railway_native_cron]]: GH Actions cron が active scheduler)。 coverage は nightly で漸進拡大 → **手動 dispatch 不要** (FMP quota 消費 + 検証困難のため autopilot では未実行)。 即時拡大したい場合のみ `gh workflow run nightly_scan.yml`。

### D-4. 古い handover 掃除 (user 承認待ち)
現 **50 ファイル** (v30〜v164)。 v163 以前は賞味期限切れ ([[CLAUDE.md]]「古くなったら削除して構わない」)。 v164 のみ残して bulk 削除候補だが **user 1-click 承認待ち** (過去の作業記録を留守中に大量削除しない安全策、 git 復元可)。

### その他 (release-gated、 従来通り DEFER)
⑩ 全銘柄 rollout(§38) / LP ガイダンス unlock / **Stripe Premium 配線=Trust Cliff blocker** / 法務 / QA e2e。

## ⚠️ 触ると危険 (継続)
- 発光系 (`.panel-card/.bs-panel/.surface-card`) / sticky 検索 div / VITE_ ARG/ENV / aggregator LLM import / 「じっちゃま」UI / DailyDigest summary flex。
- **Workspace pane2/pane3 の isScreener 分岐** = master-detail 心臓部 (D-1 はここに波及)。
- スクリーナー Hero の `.screener-reveal`(stagger)/`.screener-hero-row`(hover)/`.hero-watch-add`(gold) は **wrapper と hover を分離して forwards fill 干渉回避**する設計。 触る際は li=reveal / button=hover の分離維持。
- ScreenerPane の `_heroCache` (module-scope、 TTL 5分) は flicker fix。 fetch ロジック変更時は cache 保存 (`_heroCache = {...}`) と hydrate (lazy useState) の整合維持。
- 新規 glow host / contain:paint / 入れ子 surface-card / 新 box-shadow 禁止。

## 🧰 一時成果物 (cleanup 候補)
- **`/dogfood/` ギャラリー** (`frontend/public/dogfood/`、 noindex、 binary PNG ~1.5MB): user スマホ dogfood 用。 dogfood 完了後に削除 (user「消していい」 確認待ち)。

## release status
pre-release。 Premium/Signature は LP「近日公開」、 Pro¥980 のみ実購入可。 核 = AI図解 + russell3000 master-detail スクリーナー (本セッションで視覚刷新+flicker解消) + RS/Cup sort/filter。
