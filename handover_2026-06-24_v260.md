# 引き継ぎ書 v260 (2026-06-24) — B-3/Phase C 本番検証完了 + CI dogfood 自動化 + B-4/B-3.5 SPEC 着地（Sprint 1 着手待ち）

> v259 を置換。本セッションの成果: **① B-3/Phase C を本番で視覚検証完了（CI dogfood・overall PASS）/ ② 本番視覚 dogfood の自動化基盤を構築・本番投入（PR #5 merged）/ ③ funda_pass>0 を本番 ground truth で確認 / ④ B-4(preset→conds)+B-3.5(gate 南京錠) の SPEC 起票＋gate-1 全決定取得（PR #6 draft）**。
> 🟢 **次セッション最優先 = B-4/B-3.5 Sprint 1 の実装**（重い本セッションを避け新セッションで着地）。全決定・SPEC・検証基盤は確定済。

---

## 🧭 30 秒サマリー

| 項目 | 状態 |
|---|---|
| **branch** | `claude/funny-dijkstra-iz1jn6`（origin/main `1be033e` の上）|
| **main / 本番** | `1be033e`（B-3/Phase C 反映済 + dogfood 自動化）。Railway auto-deploy 済 |
| **PR #5（dogfood 自動化）** | ✅ **merged**。毎 deploy + screener PR で本番を自動目視 |
| **PR #6（B-4/B-3.5 SPEC）** | 🟡 **draft**（doc-only: SPEC + 本 handover）。merge で main 発見性確保 |
| **🟢 次の最優先** | **B-4/B-3.5 Sprint 1 実装**（`CustomScreenerPanel.jsx`・pass 不変の表示レジストリ）|
| **実装ルール** | CLAUDE.md「実装は委託せず main が手を動かす」→ **Generator 丸投げ禁止**、main が直接編集 |
| **検証** | CI dogfood（下記）を MCP で起動→verdict 取得（egress 制約を回避済）|

---

## ✅ 本セッション完了（4 件）

### ① B-3 / Phase C を本番で視覚検証完了（handover v259 の🔴最優先・未検証を解消）
- 本番 `?screener_v2=1` を **GitHub Actions runner（egress 開放）+ Playwright + Claude Haiku vision** で自動 dogfood。**overall PASS**:
  - **Phase C**: master-detail（資本財=主戦場オレンジ縦バー/テクノロジー=上位緑、AGX「5条件達成」Top3、**シアン不使用**＝投資色ルール準拠）
  - **B-3 mseg**: 「緩 +20%／標 +20.5%／厳 +50%／最厳 +100%」4段
  - **B-3 lock crow**: 「買い場圏/出来高の質/機関保有率」が南京錠+「Premium で解錠」、重複なし
- DOM audit も機械裏取り（mseg=24・lock crow=3・sector-master-detail=1）。**anon/Free 状態で universe が本番ロードされることも確認**。

### ② 本番視覚 dogfood の自動化基盤を構築・本番投入（PR #5 merged）
- **なぜ作ったか**: この remote 環境は本番ドメインへ egress 403（組織ポリシー、迂回禁止）。README 通り**迂回せず、元から egress が開いた GitHub runner に実行を移す**正攻法。
- **成果物**:
  - `frontend/scripts/snap-screener-v2-dogfood.mjs`: 診断優先（DOM presence audit + region capture + Haiku verdict）。data-testid 固定・async IIFE+process.exit・110s hard timeout。
  - `.github/workflows/screener_v2_dogfood.yml`: `push(main)` + `pull_request`（screener パス）+ `workflow_dispatch`。`continue-on-error`（gate 化せず検知のみ）。verdict を **job log + artifact(30日)** に出力。`ANTHROPIC_API_KEY` secret は設定済。
- **使い方（新セッション必読）**: main session が **MCP `actions_run_trigger`(run_workflow) で起動 → `get_job_logs`(return_content=true) で verdict JSON を読む**。これで本環境の egress 制約に依存せず本番を視覚検証できる。PDCA-over-CI（run→log読取→修正→再push）で反復。
  - run #1 で mseg が capture-framing で誤 fail → 単一 crow に zoom する iteration #2 で解消（overall pass）。**製品を「壊れている」と誤報告しない**好例。

### ③ funda_pass True>0 を本番で確認（v259 🟢「要再確認」を解消）
- run #2 で `hot_sector`(=`fundaPassOnly`)が本番で 2 セクター+AGX「5条件達成」を返した＝**funda_pass>0 が ground truth で確定**。この guard は dogfood が毎回自動で回り続ける（再発の永久監視）。

### ④ B-4/B-3.5 SPEC 起票 + gate-1 全決定取得（PR #6 draft）
- **SPEC**: `docs/specs/SPEC_2026-06-24_screener-v2-b4-preset-conds-b35-gate.md`（planner subagent 起草）。
- **B-4** = mockup `PRESETS[].conds` を実装 cond key にマップした**表示専用レジストリ `PRESET_DISPLAY_CONDS`** を追加し、現状全 preset 一律の `CROW_LAYOUT`（L1293 付近）を `activePreset` 駆動に置換。**pass 述語不変**。
- **B-3.5** = mockup `o.gate:true` 条件を「常時 ON・トグル不可（南京錠）」化。**pass 算入＝件数変化**を伴うため `applyStrategyImpl` と `PRESET_PREDICATES.extra` の**両方**に反映（count==list 死守）。

---

## 🔴 gate-1 で確定した設計判断（実装時に厳守）

| 決定 | 内容 |
|---|---|
| **Q2 = (a) 件数を変えない** | ocf_margin_pct / ocf_gt_netincome は **既に true の preset でのみ南京錠表示**。デフォルト件数は不変（Trust Cliff 最小）|
| **Q3 = 減少許容・0件は代替提示** | gate 化で件数減は許容。**0 件 preset は emptySuggest（緩和候補の提案）で空白を埋める** |
| **進め方 = (A) 段階的** | 今すぐ cup + データ有り条件で実装。cfpsgt/beat はデータ整備後に gate 昇格 |
| **FMP call = +2本/銘柄/夜 許容** | cfpsgt/beat の nightly populate 増加を承認（Ultimate plan quota 内）|

### gate 対象データ状況（調査済・SSOT）
| cond | 実データ | 結論 |
|---|---|---|
| **`cup`（カップ&ハンドル）** | ✅ 既存（`_build_universe_payload` の `cup_state`、Premium マスク。`main.py:20219-20236,20346,20472`）| **即 gate 化可**（frontend filter のみ・small）|
| **`cfpsgt`（CFPS 3年連続増加）** | ❌ 未populate（`screener_fundamentals` に `cfps_3y_rising` カラム無し。判定ロジックは個別タブ `main.py:4222-4227`/`judgment.py:210-218` にあるが nightly 未接続）| データ整備要・**medium**・FMP+1。migration + `_compute_one`(`main.py:22236付近`) + `_upsert_screener_fundamental`(`main.py:21743`) + `_build_universe_payload` SELECT 追加 |
| **`beat`（決算 Beat）** | ❌ 未populate（universe 未 join。`last_verdict` は earnings_calendar 専用 `main.py:1626-1633`。判定ロジックは `aggregator/earnings_reaction.py:32,45`）| データ整備要・**medium**・FMP+1（`/stable/earnings`）。同様に migration + populate + SELECT |

→ **嘘の南京錠（no-op/空リスト）= Trust Cliff** を避けるため、cfpsgt/beat はデータ整備前に gate 化しないこと。

---

## 🟢 次セッション最優先 — B-4/B-3.5 Sprint 1 を実装

1. **SPEC を読む**: `docs/specs/SPEC_2026-06-24_screener-v2-b4-preset-conds-b35-gate.md`（Sprint 構成・count==list 配線 9箇所+橋3箇所の付録あり）。
2. **Sprint 1（最安全・pass 不変）から**: `PRESET_DISPLAY_CONDS` 表示レジストリを追加し、`CROW_LAYOUT`（L1293付近）を `activePreset` 駆動に。**件数に一切影響しない**ので Trust Cliff リスクなし。
3. **実装は main が直接**（CLAUDE.md「実装は委託せず手を動かす」）。Generator 丸投げ禁止。委託は調査・多視点レビューのみ。
4. **検証**: `cd frontend && npm run build`（構文 ground truth）→ push → **CI dogfood を MCP で起動して本番 verdict 取得**（DOM audit で gate 化後の件数変化も機械確認）。
5. **Sprint 3-4（gate 南京錠）着手時**: Q2(a)/Q3 を厳守。count==list は filteredItems/presetCounts/facetLevelCounts/emptySuggest/sectorOptions/mcapOptions の全 extra + 依存配列に配線（SPEC 付録参照）。

---

## ⏸️ 段階的ロードマップ（(A) 確定）
1. **Sprint 1-4**: cup + 既存データ条件で B-4 表示 + B-3.5 gate（mockup 忠実な範囲）。
2. **別タスク（承認済）**: cfpsgt / beat の backend データ整備（各 medium・FMP+1）。
3. データ揃い次第、cfpsgt/beat を本物の gate に昇格（mockup 完全達成）。

---

## ⚠️ 触ると危険 / 規律
- **CustomScreenerPanel 整合（Trust Cliff C-2）**: count と list は同一 `itemPasses`。B-3.5 gate は pass 算入なので count も list も同時に変えること。
- **発光系禁止**: screener は plain bordered + token shadow。新規 crow/gate も border + tinted-bg のみ。
- **投資色ルール**: 上昇=緑/下落=赤/緊急=amber/cyan=ブランド色（上昇に使わない）。
- **screener_v2 scope に閉じる**（legacy=screenerV2 false 完全不変）。
- **paged SELECT 罠**: screener_fundamentals に新カラム（cfps_3y_rising / latest_beat）追加時は別 fetch 分離 or migration 先行（`feedback_paged_select_missing_column_trap.md`）。
- **環境制約（v259 から継続・本セッションで回避策確立）**: この remote 実行環境は組織 egress ポリシーで `beatscanner-production.up.railway.app` への CONNECT を 403 拒否（迂回禁止）。→ 視覚/curl 検証は **CI dogfood（GitHub runner・egress 開放）経由**で行う（本セッションで確立、MCP で起動→verdict 取得）。sticky 検索バー / VITE_ ARG/ENV 同期 不触。
- **重い操作前に compact / 機能着地は新セッション**（本 handover はその実践）。

## 🧹 掃除
- handover **v259 は削除可**（本 v260 最新）。v255 も残っていれば削除可。
- PR #6（SPEC + 本 handover、doc-only）を merge すれば main で `ls handover_*.md` 発見可。Sprint 実装は同 branch に積んで PR 拡張、または新規 PR。
- snap 系 untracked / `/tmp` 診断は ephemeral。
- PR #6 監視 subscription は本セッション終了で消滅。新セッションで扱うなら再購読。
