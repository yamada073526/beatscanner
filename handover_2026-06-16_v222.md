# handover v222 (2026-06-16 overnight autopilot: Task#4 retest A先行 **本番 deploy** + tool-call崩壊 再発防止 三重anchor + breakout SPEC **v2是正**)

> 前提: v221 supersede(v219/v220/v221 は本 v222 に集約・削除可)。本セッション後半 = user 就寝中 autopilot(朝4:30 復帰)。**安全境界遵守**: 本番への**コード deploy はゼロ**(doc は local commit のみ・push は朝レビュー後)。重大判断(decision⑫ 等)は user 温存。
> SSOT: memory `project_resistance_retest.md`(retest) / `project_breakout_signal.md`(breakout) / `feedback_toolcall_plaintext_corruption.md`(崩壊防止) / SPEC `docs/specs/SPEC_2026-06-16_breakout-signal_draft.md`(**v2**)。

## ✅ 本セッション完了
- **Task#4 retest A先行公開 LIVE**(commit `36d9ac8`・**push 済**・本番検証 PASS): `/api/scanner/retest` 新設(DB SELECT・§38 teaser・default filter `vs_SPY>0 ∩ dBHi≤10% ∩ rsSelf≥40`)=**本番22件**(HSHP 54.6→GNTX 0.3、NVDA shallow 含む) + ScreenerPane「リテスト接近」chip/HeroSection 06 + StateCompass label「旧抵抗→支持転換」 + **開発フラグ廃止(isRetestEnabled/isRetestDogfood)→ `plan==='premium'` mount gate 一本化**。masking=**Option2(表示gateのみ)**、/api/technical 物理除去+fetchTechnical認証は **Phase B DEFER**。nightly retest 保存も稼働確認(signal_as_of 2026-06-16)=502 は今夜分回復。
- **tool-call プレーンテキスト化 再発防止 三重anchor**(commit `c6ec05a`・**push 済**): コンテキスト過重で tool 呼び出しが `<invoke>` 生XML化する再発障害。memory `feedback_toolcall_plaintext_corruption.md` + MEMORY.md index + **CLAUDE.md「必ず守るルール」**に明文化。核心=「特定 tool のバグでなくコンテキスト重量の問題/再試行は悪化/初発で stop+`/compact`/大ファイル・Workflow生出力は sub-agent 委譲・git diff レビュー」。
- **breakout SPEC v2 是正**(overnight Workflow 11体・~978k tokens、**local commit 予定・未 push**): §12.2 BLOCKING **9件全て本文(§1-§6)反映** + **bo_* 改名(bo_confirmed/pending/soft/extended)を全 reference 伝播** + retest item#6「✅解消済(36d9ac8)」マーク + StateCompass 色矛盾(§2.3.1↔§2.4)を**2層分離で解消**(StateCompass=warn固定 / chip=§2.4 tone表)。735→1054行。検証=`all_reflected:true / missing:[] / bo_rename一貫:true`。**コードは未変更・要 user レビュー**。

## 🔴 朝イチ user 決裁待ち(これが breakout 着手の唯一の残ゲート)
- **decision⑫(pending × tier)**[§12.4]: decision①(pending を出す)と⑦(分類ラベル=Premium物理除去)が無料層で両立しない(6体中5体 blocking/major)。**合議推奨**=pending を「無料 viz の中立注記(色なし・例『日中上抜け/終値未確認』)」、Premium は確度判別(confirmed/soft/extended)+screener+nightly push に分離。→ 集客価値と Trust Cliff を左右、locked 化要。
- **user_forks 8件**(breakout SPEC §8 に集約・全て未 lock): ① **state namespace 最終形 `bo_*` か `newhigh_*` か(伝播のため先 lock 必要)** ② extended 閾値 A(>25%)/B(>10%暫定)/C(>15%)+SMA50閾値30/50% ③ stage filter 形式(SMA50 AND vs SMA200単独)+検出激減時の訴求降格是非 ④ screener RS 閾値(universe_percentile 70/80/rs_vs_spy>0) ⑤ 3signal 同時の priceCell 優先順位(retest>breakout>cup 暫定、cup confirmed 逆転検証) ⑥ bo_soft chip の ×{VOL_RATIO} 動的 inject 可否 ⑦ 相対出来高 chip tone(中立 muted 既定) ⑧ StateCompass confirmed を緑にするか warn 固定か(暫定 warn)。
- **未 push doc commit のレビュー&push 判断**: SPEC v2 + handover v222 を local commit 止め(無人 prod deploy 回避)。`git show` で確認 → 問題なければ `git push origin main`。

## 🟡 breakout 着手の3ゲート 現況(§12.1)
1. SPEC 本文 blocking 是正 → **✅ overnight 反映済(要レビュー)**
2. decision⑫ 決裁 → **🔴 user 決裁待ち(唯一の残)**
3. retest A先行 infra → **✅ 解消(36d9ac8)**
→ **2 を決裁 + state namespace lock すれば Generator 着手 or 再6体合議へ**。

## 📊 残バックログ(DEFER)
- **Phase B masking 強化**(認証境界・max effort): /api/technical retest payload 非Premium物理除去 + fetchTechnical 認証付与。現状 plan gate で P0 leak は止まっており優先度中。
- **Task#5 retest B本番公開**(6体合議 賛成1/6=DEFER): 配信flow gate + mailer retest テンプレ + LP/UpgradeModal + 公開前6体合議。
- v219繰越(D2構造改修 / B7図解3層 / B7 §38 false negative)未着手。

## ⚠️ 触ると危険 + 運用(継承)
- **tool-call崩壊防止**(新 CLAUDE.md ルール): 大ファイル(main.py ~19k行)/Workflow生出力をメインに取り込まない、git diff レビュー、初発で stop+compact。
- bo_* 改名は実装時に `_STATE_PRIORITY`(L18817)/`_detect_signal_transitions`(L16548)等の旧 breakout_* 直参照へ伝播必須(§12.3 MAJOR)。
- **masking 実態の乖離**(§12.3): get_technical に plan gate 無し=cup_handle も全ユーザーに返している。backend 物理除去は新規 plan 判定が必要(Phase B の前提)。
- nightly cup-scan 3000銘柄同期 scan=502 risk(continue-on-error で隠れる)。発光系/sticky検索バー/aggregator LLM import禁止/blocklist⇄prompt_negatives mirror/plan gating=getPlan経由。
- deploy=git push origin main→/health commit一致→bundle grep。**git add は明示path**。

## 📌 次セッション 推奨着手
1. /fetch-handover(本ファイル)。
2. **decision⑫ + user_forks(特に①state namespace)を順に決裁** → SPEC final lock。
3. 未push doc commit(SPEC v2 + v222)をレビュー → push。
4. breakout 再6体合議 or 直 Generator(gate 全充足後)。
