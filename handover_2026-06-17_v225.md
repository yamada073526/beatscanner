# handover v225 (2026-06-17 就寝autopilot: breakout Sprint 2-6 **全完了**・本番反映済。残=screener flag→default ON promote の funnel-cro判断 + DEFER群)

> 前提: v224 supersede。本セッション = 就寝中 autopilot (user 〜翌4:30)。**並行 CAN-SLIM セッションは main.py + CustomScreenerPanel.jsx 担当**(本セッションは両ファイル不触、明示path commit徹底)。
> SSOT: memory `project_breakout_signal.md`(全反映済) / `nightly_502_gateway_persist.md` / SPEC `docs/specs/SPEC_2026-06-16_breakout-signal_draft.md`(v3、§6=screener)。

## ✅ breakout Sprint 2-6 全完了 (本番反映・検証済)
- **Sprint 2** nightly配線 `d9eb305` / **Sprint 3** 出来高viz `31ea49e` + 視覚fix(v2 dead-zone `22f60c0` → **その後別セッションが v3 `b1a01a3` / v4 `5917c21`「軸×10+barGap0/categoryGap6%」で iterate=現状はv4**) / **Sprint 4** Premium分類+api.js `e021466` / **Sprint 6** §38 blocklist `b269a2f`。
- **Sprint 5 (本セッション)**: 
  - backend `/api/scanner/breakout` `57080f6` — scanner_retest mirror + rs_ratings JOIN(universe_percentile) + default filter(confirmed/vmult≥1.5/exclude_extended/≥70) + ⚠️`_fetch_premium_status_from_auth` で**非Premium は items 物理空配列化 + count_locked のみ**(retestと違い frontend blur非依存=backend leak防止、§6.2)。
  - nightly freshness gate `8350579` — `/api/scanner/breakout` as_of を **warning-only** で監視(0件日の誤発火回避、chkでhard-failさせない)。
  - frontend screener section `e1fb4f4` — **feature flag `?breakout_screener=1`/localStorage、default OFF=完全no-op**(fetch/render/chip 全て出ない=既存screener影響ゼロ)。BREAKOUT_STATE_LABEL_JP(cup と物理分離)+fetchBreakout(supabase.auth.getSession→Bearer)+locked(ProTeaser)/empty/items 3分岐。
  - **headless 検証済**(`snap-breakout-screener.mjs` / `snap-volume-bars.mjs`、.env DOGFOOD creds で認証注入): Free=items:[]+count_locked(leak無)、Premium authed=本日**12件 confirmed**(LION vmult4.29/RS95/52週高値 等)、§38 leak無(pivot/price 出さず)、flag OFF=DOM出力ゼロ。

## 🔴 user 判断待ち (最優先・promote 前必須)
- **screener section の flag → default ON promote = funnel-cro/Trust Cliff 判断**。現状 flag OFF で誰にも出ていない(安全)。user が `?breakout_screener=1` で dogfood → OK なら default ON に昇格。
  - ⚠️ **本日 confirmed 12件**は原則2(認知過負荷)でやや多めかも。`min_percentile` を 70→80 に上げる or default 維持を user 判断(toggle 実装済)。SPEC §6.4 想定は S&P500 3-5件 / russell3000 15-30件。
- **訴求**=「新高値ブレイク」維持(v224 user決定)。

## 🟡 残 (breakout、後続)
- **BUY_ZONE_DESC_JP narration render導線**(BreakoutZoneCard 等)= 要追加(Sprint4で辞書だけ先行=dead but safe、BuyZoneCardは現状cup_handleのみ読む)。
- **③再計測 (F②③)**: breakout 配線後に S&P500 で confirmed/extended 件数を再計測。激減なら訴求降格(funnel-cro gate)。※本セッションで本番 confirmed 12件(default filter)を実測=激減でない。
- email配信(§4.2-A(4) DEFER) / masking Phase B 物理除去(/api/technical の bo_* payload、retest Option2 と同スプリント DEFER)。

## 🔵 残バックログ (retest 系・別系統・DEFER・carry-forward)
- **Phase B masking 強化**: retest/breakout の /api/technical payload を非Premium 物理除去 + Authorization配線 + cache key plan-tier分離(blast大)。現状 plan==='premium' 表示gate(P0 leak止め済)。優先度中。
- **Task#5 retest B本番公開**: retest 配信(6体合議 賛成1/6=DEFER)。配信flow gate + mailer専用テンプレ + 公開前6体合議。breakout email配信も同後続Phase。
- v219繰越(D2構造改修 / B7図解3層 / B7 §38 false negative)未着手 DEFER。

## ⚠️ 触ると危険 + 運用
- **main.py / CustomScreenerPanel.jsx は CAN-SLIM セッション領域** — breakout作業では不触。明示path `git add`(`-A`禁止、`feedback_parallel_session_commit_entanglement`)。
- bo_* state は additive(cup の breakout_*/_CUP_TRANSITION_MAP/_STATE_PRIORITY 不変)。pending は extended 昇格しない(§4.1)。
- 発光系/sticky検索バー/aggregator LLM import禁止/blocklist⇄prompt_negatives mirror。
- deploy=git push origin main→/health commit→bundle grep。nightly 502=gateway timeout で persist(502≠stale、DB freshnessで判定)。
- **screener Premium gate は backend 物理除去**(frontend blur非依存)。retestをコピーして frontend blur だけにすると bo_confirmed leak。
- authed chart/screener は **.env DOGFOOD creds + snap-*.mjs で headless 目視検証可**(盲目チューニング禁止)。

## 次セッション 推奨着手
1. /fetch-handover(本ファイル) + memory `project_breakout_signal`。
2. 朝: scheduled-task「breakout-nightly-verify」(09:30 JST 発火)の nightly breakout 件数報告を確認。
3. user が screener flag を dogfood → promote 判断(min_percentile 調整含む)。
4. CAN-SLIM の main.py 作業完了確認後でないと main.py 触る作業は衝突注意。
