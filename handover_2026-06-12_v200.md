# 引き継ぎ書 v200 (2026-06-12、 決算ハイライト改善 + ガイダンス履歴基盤 Sprint1-4 完遂 → 次=Phase2 or backfill運用)

長セッション (PriceLadder round12 → 決算ハイライト新設+default ON → ガイダンス履歴基盤 4 sprint) を完走。 最新本番 = `81f9696`。 **このセッションは context 50%+ 消費したため新セッションへ移行済 (この handover で文脈復元)**。

## 🎯 完了 (全て本番 live + 自動検証済)

| 領域 | 内容 | commit |
|---|---|---|
| PriceLadder round12 | 当日 sparkline をラベル横一体化 + 縮尺 sqrt+cap72 | `738d9be` |
| 決算ハイライト | EarningsFlashSummary 新設 → **default ON** (?flash=0=kill)。EPS/売上(億ドル小数1桁)/YoY/来期 | `b4ab7b1`→`1ff24bb` |
| 決算ハイライト改善 | 売上小数1桁 + 来期ガイダンス並置行 (時系列順「ガイダンス(発表時)→現コンセンサス」) | `a545466`→`16752a6` |
| ▲▼→↑↓ 統一 | 会計表記 ▲=マイナス衝突回避。アプリ全体 (前方視界/格付け/portfolio α 等) | `bb71f5d`/`0742f31`/`e448c67` |
| S5b funda バッジ | funda 一覧 PASS+near-miss に C/A/N/S (user OK 確定) | `6b06588` |
| **ガイダンス履歴基盤 Sprint1-4** | guidance_snapshots + 前回比 raised/lowered + 発表時比 + バッジ (?guidance_pit=1 opt-in) | `fc99cb0`〜`1a9119b` |

ガイダンス基盤の全貌・§38境界・将来AIチャット retrieval 設計は memory **[[project_guidance_history_foundation]]** に記録済。

## 🔴 次セッション最優先 (推奨順)

### 1. ガイダンス履歴基盤の運用開始確認 (軽、最初に)
- **nightly cron 稼働**: `nightly_guidance.yml` が毎朝 09:10 JST 蓄積。 GitHub Actions で発火確認 (CRON_SECRET は既存共用)。
- **backfill 実行済 (2026-06-12)**: WL∪保有 9 銘柄 → 21 行 upsert。 SNOW(4)/ASO(8) は revision pair あり=前回比バッジが出る。 AMZN/NVDA は次Qポイントガイダンスのみ=同一期更新せず available:false が正 (バグでない)。 GOOGL/MSFT は call ガイダンスで 8-K 数値なし=row 0 が正 (coverage limit)。 7203.T/NAT は非米国で SEC filing なし。 ⚠️ **endpoint は ~120s/9銘柄で Railway HTTP timeout 際どい → 大量銘柄は `{"tickers":[...]}` で 3-5 銘柄ずつ batch 推奨** (初回 1 回 timeout した)。 新規 WL 追加銘柄は個別追走可。
- **発表時比バッジの開花待ち**: consensus_snapshots は 6/6 蓄積開始。 直近決算が 6/6 以降の銘柄から「発表時比」 バッジが出始める (それ以前は guidance_revision=前回比のみ表示が正常)。

### 2. `?guidance_pit=1` dogfood → default ON 昇格 (user 判断)
- 決算ハイライトと同じ昇格経路。 user が `?guidance_pit=1` で SNOW/CRM/MU 等 (8-K ガイダンス保有銘柄) を目視 → OK なら `isGuidanceHistoryEnabled()` (EarningsFlashSummary.jsx) を default true 化 + snap の default URL 検証。
- 確認: 通期=前回比修正バッジ / 来期=発表時比バッジ / 「↗ ガイダンス詳細へ」 導線が forward-outlook へ scroll。

### 3. ★Phase 2: セグメント別売上 + 四半期グロスマージン (次の大物、planner→6体合議→実装)
- 決算ハイライト/速報 note 模範の**残り 2 群**。 backend 拡張が必要なため v199-200 で DEFER していた本丸。
- 模範 (じっちゃま note): 「iPhone 売上 570億 vs 予想567/前年468、 サービス 308億 vs 予想300/前年267、 グロスマージン 49.3%」。
- データ: セグメントは `build_segment_summary` (main.py、visualize endpoint のみ) → guidance/basic への軽量配線が要る。 四半期グロスマージンは quarterly-history への `grossProfitRatio` 追加。 セグメント予想比は FMP consensus API 未接続 → 実績+前年比のみ (予想比は欠損 graceful)。
- SPEC: `SPEC_2026-06-10_earnings-flash-summary.md` §5 Sprint 3 に骨子あり。 planner で詳細化 → 6体合議 (3軸全active) → 実装。

## ☀️ 確認済 (再確認不要)
- 決算ハイライト default ON / S5b funda バッジ = user dogfood OK 済。
- ガイダンス履歴: SNOW FY2027 売上 $5.66B→$5.84B を raised 判定、 snap PASS、 §38 BAN なし。 per-filing 履歴 key で前回値保持確認済。

## 🟡 残バックログ (継承)
- 上記 🔴3 (Phase 2)。 Stripe Premium 配線 (launch blocker、pricing=autopilot対象外)。 scan async化 (3000 universe 502)。 v5→v4 昇格 (dogfood 十分後)。
- ▲▼ 監査タスク (spawn chip 発行済) は `0742f31`/`e448c67` で主要 file 着地済、 残りは chip 起動で。

## ⚠️ 危険箇所 / 運用知見 (新規)
- **guidance_snapshots は per-filing 履歴** (unique に source_accession 含む)。 「期ごと最新1行」 に戻すと raised/lowered が壊れる ([[project_guidance_history_foundation]])。
- **§38 境界**: 「上方修正/下方修正」 は前回**会社**ガイダンス比のみ事実 OK、 現コンセンサス比は NO-GO。 pre-commit Check 7 は `@company-guidance-revision` 行注釈ホワイトリストで限定許可 (全面解除禁止)。 表示語は `GUIDANCE_REVISION_JP` (修正語) と `GUIDANCE_PIT_CONSENSUS_JP` (発表時比、修正語なし) を**別 dict**で分離。
- **方向記号は ↑↓ / 中立は —** (▲▼ 全廃、会計 ▲=マイナス衝突)。 新規実装でも踏襲。
- backfill/snapshot は CRON_SECRET 認証 (`railway variables --json` で取得)。 EDGAR は 10req/s 厳守。
- aggregator/ への LLM import 禁止 (Check 3) / 発光系 .panel-card / sticky 検索バー / VITE_ ARG/ENV 不触。
