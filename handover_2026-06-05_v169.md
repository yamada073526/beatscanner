# BeatScanner Handover v169 — スクリーナー richness 完了 + Pane3 void 充填 + 原則4 北極星 + ガイダンスサプライズ SPEC (2026-06-05 autopilot)

> 前半: user dogfood でスクリーナー richness ④③-a③-b を 3 巡（v1→v3）。後半: user 出勤中 autopilot で
> Pane3 void 充填 / 原則4 北極星 / ガイダンスサプライズ 3体合議→SPEC。
> **全 commit push 済 (origin/main 同期)**。 frontend deploy は **`railway up` のみ**（CLAUDE.md override）。

## 🟢 本セッション完了（全 deploy + 検証済）

### スクリーナー richness（user dogfood 3 巡、deploy 済 CSS `index-B6BISNr2.css` / 以後 RS ランキングで JS `index-Bv4a2NPs.js`）
- **④ 行 hover 左アクセントバー**（`f9173d6`）: `.screener-hero-row::before`（`.news-list-card` idiom、scaleY 0→hover 1）。user「これでいい」確定。
- **③-a ambient 明滅**（`952120e` v3）: user「注意して見ても分からない」→ 真因 = radial が hero card 背面に隠れて不可視。**中心を下方 42% の空きへ移動** + base 0.16→0.22 + mask 26% + 周期 8s→**5s** + scale 1.13。
- **③-b card hover 発光**（`952120e` v3）: user「もっと強く、Pane3 の発光流用」→ inset-only を **Pane3 `.panel-card:hover` 級の外側 halo（`0 0 28px` rgba accent、whitelist 値）**に置換。lift なし（内部 row と compound 回避）、screener scope 維持。harness で `after_boxShadow=0 0 28px` 実証。
- **ForwardOutlookSection 発光統一**（`952120e`）: v146 で素 div だった「前方視界」を **`.panel-card` 化**で Pane3 標準の scroll arrival + hover 発光に統一（useArrivalSpotlight 自動 observe）。本番 JS で `forward-outlook className=panel-card` 確認済。
- 検証 harness 新規: `frontend/scripts/snap-screener-hero.mjs`（hover 前後の computed transform/boxShadow 差分で発光を機械実証、4条件遵守）。

### Pane3 下部 void 充填（`8c06e1d`、JS `index-Bv4a2NPs.js`、目視確認済）
- user「Pane2/3 とも空きが寂しい」→ 目視で**真の void = Pane3(ScreenerPane) 下部 60%**（3 section は curated 交差で本日 0/0/5 と data-sparse）。
- **3 section grid の下に full-width「相対強度ランキング(RS≥80)」section** を追加。`rsLeader.items`(section1 交差の fetch 元、top5 以外破棄してた full list)を流用 = **追加 fetch ゼロ**。HeroSection に `columns` prop 追加で多列 grid 化。§38 中立(RS percentile + 免責)。snap で「04 相対強度ランキング」3列描画 + visibleTicker 5→20 確認。
- ⚠️ サブエージェント案「Explorer idle に RS」は CustomScreenerPanel が **mount 時 auto-run**（main.py 風、ScreenerPane でなく CustomScreenerPanel line 853）するため前提不成立だった → 実コード/目視検証で軌道修正。

### 原則4 北極星「人力の代替」（`42ecb43`、CLAUDE.md + memory feedback_design_principles）
- user 明示: 原則4「1クリックを減らせ」の北極星 = **「投資家が毎日人力でやっているチェック（決算速報・ガイダンス照合・スクリーニング・チャート見回り・保有監視）を BeatScanner に丸投げできる」**。新機能採否（特にスクリーナー）は「これは人力手間を代替するか？」で判断。最上位プラン(email/LINE 配信)がこの原則の最終形 =「月5万でも安い」価値。既存5原則の文言・番号は不変、解釈を北極星として追記。

## 🟡 判断待ち（user 帰宅後）

### ガイダンスサプライズ（前方視界強化）= DEFER-SPEC → `docs/specs/SPEC_2026-06-05_guidance-surprise.md`
3 体合議（データ可用性/金融§38/表示）converged。**売上ガイダンス vs consensus は実装可・EPS(CRWD 速報の主役)は 8-K schema に EPS field 無で不可**。§38 設計は確定（色なし・▲—▼・「上回る/おおむね同水準/下回る」・「上方修正/上振れ/強気」NO-GO）。**user が案 A(売上のみ)/案 B(EPS まで=8-K schema 拡張)を選択 → /planner で詳細化 → /generator**。§38 設計済なので scope 判断さえ頂ければ着手可。

### 🔴 既存 Trust Cliff バグ（独立・要修正）
データ agent 発見: **META/TSLA で `forward.next_q.period_end_date = 2028`（約2年後）** = `_compute_forward_outlook`(main.py:6478) の「来期」選択(`d > floor_d`)が一部銘柄で誤期を拾う。現行 ForwardOutlookSection の consensus YoY 自体に影響。ガイダンスサプライズ前に単独修正推奨。※本番 `guidance/basic?ticker=X` 直 curl は cold で「データ見つかりません」（analyze flow/認証で populate）→ 再現には analyze 後 or BYPASS_TOKEN。

### Pane2(CustomScreenerPanel) の void
user は「Pane2/3 とも空き」と指摘したが、本セッションは Pane3 のみ充填。Pane2(ファンダ5条件 Explorer)は auto-run 後の fundamental 結果が疎な時に下部空き。北極星(人力代替)に沿う content で埋める案を要検討（DEFER）。

### richness 微調整（dogfood 待ち）
③-a を 5s/下方配置/強度↑↑ でもまだ見えなければ「カードが覆う」構造要因確定 → 別アプローチ（可視領域限定レイヤー等）。③-b は Pane3 と同じ発光感のはず。B4「概況 stat 帯」は RS ランキングが breadth 内包で冗長と判断、要否は dogfood 後。

## ⚠️ 触ると危険（継続）
- 発光系（.panel-card/.bs-panel/.surface-card/tier-m-glow）・新 glow host・新 box-shadow（whitelist 値 or token のみ）。
- **Workspace isScreener 分岐 = master-detail 心臓部**。ScreenerPane の HERO_CACHE は ScreenerPane 内に閉じる。
- `_compute_forward_outlook` / sec_guidance.py（§38 + 8-K LLM 抽出層）。ForwardOutlookSection は backend flag 読むだけ（再計算禁止）。
- sticky 検索 div / VITE_ ARG/ENV / DailyDigest summary flex / aggregator LLM import 禁止。

## release status
pre-release。launch blocker = Stripe Premium 配線。
