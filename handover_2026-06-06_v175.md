# BeatScanner Handover v175 — 案B コンセンサス修正トレンド Sprint 1+2 着地 + FTD バナー (2026-06-06)

前セッション (v174) から移行。**B-Top2「市場局面 FTD バナー」** と **案B (コンセンサス時系列)**(現称「コンセンサス修正トレンド」)の Sprint 1+2 を完遂。全 commit push 済。次セッションは **案B Sprint 3 (nightly cron) を 6 体合議 + `/effort max` gate で続行** する (user が「新セッションで続行」を選択、context テキスト化 2 回の予防のため session を切った)。

## 🔴 次セッション最優先: 案B Sprint 3 (nightly snapshot cron) — 重い gate

**SPEC が SSOT**: [`docs/specs/SPEC_2026-06-06_consensus-revision-trend.md`](docs/specs/SPEC_2026-06-06_consensus-revision-trend.md) §5 Sprint 3 + §7 + §8。

着手前に **3 つの gate を必ず通す** (SPEC §7 + CLAUDE.md effort 3層):

1. **`/effort max` を proactive 通知**してから着手 (新 backend cron endpoint + `X-Cron-Secret` 認証境界 = 「真の難所」条件)。完了後 `/effort high` に戻す。
2. **6 体合議必須** (`multi-review` skill)。SPEC §7: 軸②Trust Cliff + 軸③新endpoint/RLS/cron が full active。構成 = 金融§38 + Anthropic engineer(RLS/cache) + マーケター(Trust Cliff) [Opus] + ui-designer + frontend-architect + qa-dogfooder [Sonnet, mixed model]。
3. **universe 決定 (user judgment 要)**: snapshot 対象を「保有 + WL + RS上位 + Cup-Handle 検出の和集合」(既定案) にするか、既存 `nightly_scan.yml` (23:07 UTC, russell3000上位/SP500) に相乗りするか。**Supabase 容量試算**と絡む (現状 RS で 166MB/500MB=33%、retention 90日 cleanup も Sprint 3 で確定)。

**Sprint 3 触るファイル**: `backend/app/main.py` (`/api/cron/consensus-snapshot` endpoint、既存 `verify_cron_secret` helper L14713 再利用)、`.github/workflows/nightly_consensus.yml` (新規、`nightly_scan.yml` 雛形)。⚠️ **Railway native cron は発火停止済 → 必ず GitHub Actions + CRON_SECRET** ([[feedback_railway_native_cron]])。
**足場は実装済**: `backend/app/aggregator/consensus_history.py` の `fetch_and_build_snapshot(client, ticker, snapshot_date, period_type, limit)` が FMP fetch → row dict 整形まで完了。cron は「これを呼んで `sb.table("consensus_snapshots").upsert(rows, on_conflict=SNAPSHOT_CONFLICT_KEYS)`」を組むだけ。
**完了判定**: workflow_dispatch 手動実行で snapshot 行が増える + freshness verify step が today 確認 → commit。

> ⚠️ **データ蓄積の性質**: drift は同一会計期の snapshot が **2 点以上**揃って初めて出る。cron が 2 晩以上回るまで全銘柄 `insufficient` (=「蓄積中」表示が正)。Sprint 5 の見た目確認は数日後。

## ✅ 本セッション完了 (全 commit push 済)

- **FTD バナー (B-Top2)** (`7b61c63`、deploy+本番検証済): `useFtdMap` を `Pane1MacroSection` から共有 refactor → `ScreenerPane` 最上部に「今スキャンしていい相場か」1 行。文言は静的 dict (§38)。本番 chunk `ScreenerPane-C6KYufwg.js` に `ftd-regime-banner` 確認済。
- **案B Sprint 1 — DBモデル + snapshot 足場** (`79499a9`): `consensus_snapshots` テーブル (migration + grants)、`aggregator/consensus_history.py` (純粋数値、LLM import ゼロ)、test 33。**Supabase SQL Editor で migration 2 ファイル実行済 + service_role × 4 権限確認済** (anon/authenticated は SELECT なし = Premium 保護 OK)。
  - ⚠️ SPEC §5 の `fiscal_period text (2026-Q4)` → **`fiscal_date date` に変更**して実装 (FMP `date` から四半期導出は AAPL 等 非カレンダー会計年度で誤ラベルになるため。period 識別子は生の期末日 + `period_type`)。
- **案B Sprint 2 — drift 算出** (`fe3e62c`): `visualizer/calc.py` に `classify_consensus_drift(snapshots, window_days=30, threshold_pct=0.5)`。直近会計期 (窓内 min fiscal_date) の eps/revenue avg を snapshot_date 昇順で隣接比較、±0.5%超で up/down カウント、2点未満は insufficient。test_calc.py 新規 20、合算 53 passed、既存回帰なし。

## 📋 案B 残 Sprint (SPEC §5 が詳細 SSOT)

- **Sprint 4 — drift API** (`/api/analyst/consensus-drift?ticker=`): `consensus_history.py` で Supabase fetch → `classify_consensus_drift` 呼出。6h cache (key=`ticker::window_days`)、sources schema + signal_quality 降格、analyst prefetch 相乗り。**6 体合議 gate** (Sprint 3 と同枠で実施可)。
- **Sprint 5 — frontend 表示**: `AnalystPanel.jsx` L439 `anp-grid` に 4 つ目の `anp-cell`「コンセンサス修正」。「過去30日: ▲上方修正N回 / ▼下方修正M回」(緑/赤、**シアン不使用**)、`▲`/`▼` 格調シンボル、直近スナップショット日併記、insufficient は「蓄積中」muted。深掘り timeline は Pro gate。`data-testid="consensus-drift-cell"` を全 render path に。3 体合議で可。
- **Sprint 6 (任意)**: ¥10k tier「上方修正された保有銘柄を朝 push」の素材接続 (設計メモのみ)。

## 📋 残バックログ (v174 から carry-forward、silent drop 防止)

- **count-up「入場時リセット (0→上昇)」解消** (soft「できれば」、未着手): view 前の表示を FINAL→0 に。対象 = `ForwardOutlookSection.jsx` MetricBlock / `GuidanceCard.jsx` (数字+ArcProgress+surprise) / `AnalystPanel.jsx` (TargetPriceRangeBar legend + RecommendationStackedBar legend)。詳細は v174 §🔴。
- **案C (RPO/ARR)** — SaaS 将来売上先行指標、sec_guidance に rpo/arr + SaaS 限定 gate。2.5人日。
- **通期 EPS YoY の正確化** — 前年 non-GAAP actual を quarterly surprises 4Q集計 or annual estimates 過去取得 (現状 basis mismatch で抑止)。
- 旧 v171 8件 (継続): ①データ脈動(scan時刻field) ②Hero常時微光(発光tradeoff) ③Pane2 hover統一 ④frontend graceful 422 ⑤chart wipe: hard reset 時のみ wipe が出ない (据置) ⑥segment i18n Supabase永続化(低) ⑦B4「本日の概況 stat 帯」(見送り) ⑧Pane3 詳細 (JudgmentDetail) の content 拡充提案 (13F/ETF逆引き/競合/FTD chip、要SPEC化)。
- **Stripe Premium 配線** (launch blocker、継続)。
- **nightly pre-warm cron (保有銘柄)** (#2-B、任意): cold 初回短縮、GitHub Actions。

## ⚠️ 触ると危険 (継続)

- `_compute_forward_outlook` §38 ガード / 発光系 (.panel-card 等) / sticky 検索 / **aggregator LLM import 禁止** (pre-commit Check 3) / VITE_ ARG/ENV 同期。
- **SummaryBrief の fetch gate** (`guidanceSecLoading` 依存で確定後1回生成、dep 戻すと #3 二度生成再発)。
- **inline 関数コンポーネント禁止** (transition 用、ForecastBarRow 教訓 = render毎 remount で transition 不発、module-level に hoist)。

## 🧹 context 衛生の教訓 (今セッションで再確認)

本セッションで **テキスト化 (ツール呼び出しが地の文化) が 2 回**発生。真因 = planner subagent 起動 + SPEC 全文 read で 1 セッション中に context が二度膨張。**重い subagent (planner/generator/multi-review 等) を 1 回挟むごとに `/compact`** を入れる運用が本質的予防。Sprint 3 の 6 体合議は subagent 6 体起動で特に高リスク → 新セッション開始が正解だった。SSOT: [`docs/references/tool_reliability_prevention.md`](docs/references/tool_reliability_prevention.md) / [[feedback_tool_text_ification]]。

## release status

pre-release。launch blocker = Stripe Premium 配線。
