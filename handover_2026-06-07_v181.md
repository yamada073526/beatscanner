# 引き継ぎ書 v181 (2026-06-07)

CAN-SLIM Phase 3 を S4b → S5a → S5b-backend と進めたセッション(autopilot 起点 → user 復帰後に S5 着手)。**S5 の backend データ層が完成**。残は frontend(バッジ列 + Pro ロック)。

## 🎯 完了済み (全て merge+deploy+本番検証済)

### S4b — read endpoint A/N/S 公開 (`ab001b0`)
col_map に A/N/S 5条件追加(scaled列) + count="exact"(BLOCK④) + excluded を uncomputable/unavailable 分割。本番 Rule5 全 PASS(near_high72/buyback200/roe193/eps_cagr79/volume_surge52)。詳細は memory。

### S5a — null_reason per-cause backend (`0e2a70a`)
- migration `null_reasons jsonb` 追加(MCP適用済)。
- populate(`_compute_one`)が各 null の原因を分岐 track → JSONB 保存(roe: sector_guard/negative_equity/data_missing、cagr: loss_base/insufficient_history、eps_yoy: turnaround/no_prior_year、N/S: data_missing)。**tuple arity 9→10**(全 return 統一)。
- read endpoint に `null_reason_counts` 集計(NULL 行の null_reasons を Python 集計、合計==excluded_count を `uncounted` で保証)。helper arity 6→7。
- pytest 296 / C・A・N・S 後方互換維持 / aggregator 不触(HG安全)。
- **⚠️本番 null_reasons 実値は nightly 08:07 JST で populate**(今日は全 "unknown"、None-preserve で同日行は NULL のまま)。コードは `isinstance(dict)` で graceful。

### S5b-backend — rows endpoint (`55afb5f`)
- **新設 `GET /api/scanner/canslim/rows?tickers=A,B,C`** = 結果セット ticker 群の C/A/N/S 全値 + null_reasons を1回で返す(per-ticker (c) 理由併記の素地。単一条件 read は達成銘柄のみで per-ticker null 理由を引けない問題を解消)。
- DB SELECT only、ticker IN(最大200)、additive、既存 endpoint 不変。
- 本番検証: AAPL roe146.7/JPM roe=None(業種guard)/MCD roe=-434(stale, nightly で negative_equity 化)/graceful(空・未知→rows:{})。

## 🔴 次セッション最優先: S5b-frontend + S5c (frontend)
SPEC = `docs/specs/SPEC_2026-06-07_canslim-phase3-sprint5.md`。**設計は全確定済**(下記)、frontend 実装のみ。**`designing-workspace-ui` + `funnel-cro` 経由必須**、visual harness(snap-*.mjs)で目視検証推奨。
- **S5b-frontend**: `CustomScreenerPanel.jsx` の結果行に rows endpoint(`fetchCanslimRows` を api.js に追加)で **C/A/N/S バッジ列(各柱max4、chip増殖しない)** + **null 理由ラベル(neutral gray `--text-muted`、§4 dict で reason_code→UIラベル)** + **as_of timeAgo**。既存 [Unknown]=gray+? idiom 流用、新 design token 増やさない。全 render path に data-testid。
- **S5c**: near_high=Pro(¥980) ロック。非blur ProTeaser 流用、lucide Lock(🔒禁止)。確定コピー: eyebrow「Pro 限定」/見出し「新高値圏フィルター」/説明1文目太字「EPS成長・自社株買い・出来高の条件は無料のまま。」+2文目「これに『52週高値への接近度』を掛け合わせた絞り込みが Pro で開きます。」/CTA「Pro で解放する → ¥980/月」/副リンク「無料の3条件で今すぐ絞り込む」(near_high 外して即再スキャン)。funnel-cro Trust Cliff 7項目必須。
- 着地後 **3体合議**(ui-designer + frontend-architect + qa-dogfooder、SPEC §7)。

### reason_code → UI ラベル dict(S5b frontend 静的 dict、§38/§5 clean)
| code | UI ラベル |
|---|---|
| sector_guard | 業種特性のため対象外 |
| negative_equity | 自己資本マイナスのため算出対象外 |
| data_missing | データ取得中 |
| loss_base | 赤字決算を含むため対象外 |
| insufficient_history | 上場3年未満 |
| turnaround | 黒字転換のため前年比なし |
| no_prior_year | 前年同期データなし |

## 🟡 重要知見
- **null_reasons は nightly で populate**: 今日の DB は null_reasons=NULL(migration 直後)。read の `null_reason_counts`/rows の null_reasons は今日 "unknown"/null、**08:07 JST nightly 後に実 per-cause 値**。明朝 `curl /api/scanner/canslim?condition=roe&min_pct=17` で `null_reason_counts` が sector_guard/negative_equity に分かれるか確認(JPM等 financials=sector_guard、MCD=negative_equity 期待)。
- **rows endpoint の MCD roe=-434**: 今日 stale(None-preserve)。nightly で guard が roe=null 化 + null_reasons negative_equity。frontend は -434 等の異常値を表示しない or 理由ラベルに振る設計を S5b で(AAPL型 ROE §5補完=金融 option b と同じ思想)。
- 黒字転換 halo は今回見送り(sp500 turnaround=0件、中小型 universe 後)。
- SSOT: memory `project_canslim_screener_expansion.md`(S5 全記録 + 確定設計 + reason dict)。

## 📊 残バックログ (Phase4 / 繰り越し、v180 から継続)
- **@dataclass ScanResult**: tuple arity が S5a で **9→10** に増えた(`_compute_one` の返り値に null_reasons 追加)。Phase4 で `@dataclass ScanResult` 化して arity 罠を解消する必要性が増した。+ generator worktree assert を SKILL 明文化 / helper を aggregator 移設(HG Check3 保護)/ scan async・chunk 化(3000 universe 502)/ A/N/S batch endpoint / None-preserve に sentinel(computed-None vs not-provided 区別)。**nightly populate path の blast radius あり、無監視 ship 非推奨**。
- **Phase4 (PMF/Stripe後)**: I条件(13F機関保有) / Part B 買値登録→損切利確自動push。
- **Stripe Premium 配線**(launch blocker、pre-release 最重要、N=Pro gate 実体化にも必要)。
- count-up「入場時リセット」解消 / 案C(RPO/ARR) / 通期 EPS YoY 正確化 / nightly pre-warm cron(保有銘柄)。
- **RS scan 健全性**(継続 monitoring): v181 時点 rs_ratings=2359行 健全。次セッションも DB 直接 SELECT で確認。

## ⚠️ 触ると危険 (CLAUDE.md + SPEC §6)
sticky検索バー / 発光系(.panel-card/.bs-panel/.surface-card) / design_system.md 色 token(S5は新token増やさない=既存 --text-muted 流用) / VITE_ ARG/ENV / aggregator LLM import / 既存 migration・既存 endpoint(canslim-scan/scanner/canslim 本体) / railway.toml cron。

## メモリ更新済
`project_canslim_screener_expansion.md` に S5a/S5b-backend 着地 + 確定設計 + reason dict + 残 frontend。MEMORY.md index も S5b-backend まで更新。
