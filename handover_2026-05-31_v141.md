# BeatScanner Handover v141 — backlog A2/2b/D2/D4 着手 (2026-05-31)

> v140 の残タスク (A2/2b/D2/D4/B3) を優先順で着手。 A2/2b/D2/D4-Sprint1+3 を本番デプロイ+検証済。
> D4-Sprint2 (screener) と B3 が残。 v140 handover は本 v141 で置換 (削除可)。

## ⚠️ git 状態 (最重要・次セッション必読)
- **working tree に 9 file の未コミット変更があり、 それが既に本番 LIVE** (railway up = ローカルファイル直送のため)。
- git HEAD = `f5c5d86` は本セッションの変更を含まない。 **本番 = working tree (未コミット)**。
- 変更 file: LandingPage.jsx / StockPriceChart.jsx / UpgradeModal.jsx / BuyZoneCard.jsx / CustomScreenerPanel.jsx / ScreenerPane.jsx / lib/buyZoneLabels.js / index.css / docs/references/elevation_scale.md
- → **次セッション冒頭で commit 推奨** ([[feedback_commit_proactive]])。 user が commit 依頼してこなかったため未実施。
- 本番 bundle: JS `index-DKz_wSXc.js` / CSS `index-R2O0bAY1.css` (検証済)。

## 🎯 本セッション完了済 (本番デプロイ+chunk直接grep検証済、 deploy-verify-discipline 準拠)
| タスク | 内容 | 検証 |
|---|---|---|
| **A2** | LP サンプルカード絵文字 lucide/方向ドット化 (📄→Eye / 🟢🔴→色ドット緑赤(色ルール) / ⚡削除 / 📌→Clock) | LP chunk: 旧絵文字全0 ✅ |
| **2b** | チャート◯マーカーの意味を **EarningsTooltip 拡張**で表示 (SVG dot 直hover の二重tooltip+真っ白事故を回避)。breakout緑◯=「ブレイクアウト確定点/Pivot上抜け」、handle_low=「取っ手の底」。date キー照合、cupRequiresPro 時 非発火で Pro leak なし | main JS ✅ |
| **D2** | Premium 訴求「抵抗線」→「ピボット価格」(UpgradeModal:38 / LandingPage)。overhead_resistance は過去 [FILTER] verdict で意図削除済→訴求を実装に整合 (Trust Cliff 解消)。Pivot は Premium 表示済で一致 | main+LP chunk ✅ 旧訴求0 |
| **D4 Sprint1** | breakout チップ強調: `breakout_confirmed` に green slow glow pulse (2.8s、色ルール緑) 追加 + screener CupResultCard/OneillResultCard chip に `data-cup-state` 付与 (既存 amber pending pulse もscreenerで発火)。全 prefers-reduced-motion guard 付。green shadow を elevation_scale.md ALLOWED-SHADOW 登録 | CSS keyframe+shadow ✅ / data-cup-state ✅ |
| **D4 Sprint3** | 呼称: 全 visible "breakout" → 「ブレイクアウト」(chip label / BuyZoneCard hero・meta・title / buyZoneLabels narration / ScreenerPane Hero desc)。chip「breakout 確定」vs screener「ブレイクアウト確定」混在 Trust Cliff も解消 | main+JudgmentDetail chunk ✅ 旧表記0 |

## 🔴 残タスク (次セッションはここから)
### D4 Sprint 2 — screener breakout フィルタ (3体合議 verdict、 QA「#1 Trust Cliff リスク」)
- SCANNER_FILTERS (`CustomScreenerPanel.jsx:14-19`) に `{ key:'breakout', label:'ブレイクアウト' }` を **'cup' の直後**に追加 (state machine 流れ)。
- 実装は **frontend intersection** (backend 増設ゼロ、 'oneill' フィルタ前例)。`runCupFilter` に `if(filterKey==='breakout')` branch → `fetchCupHandleScanner('cup')` → state filter。
- **QA 絶対条件 (これ無しは Trust Cliff 最重要バグ)**:
  1. `breakout_extended` (ATH付近・追いかけ買い) を **物理除外**。
  2. **pivot 乖離率 +8% 超も除外** (O'Neil buy zone = pivot~+5%、 +3% 余裕)。`payload.pivot.price` と current/today_close で計算 → **要データ形状検証 (scanner item に today_close があるか)**。
  3. `state==null` guard + `Number.isFinite(pivot)` guard。
  4. 含める state = `breakout_confirmed` (+`breakout_pending` は UI 区別の上で条件付)。
- **masking 整合**: cup scanner の masked item は state/payload 剥奪 → free user で state filter すると空表示。 → **breakout フィルタは premium:true 推奨** (both/oneill と同様、 空表示=「動かないアプリ」回避)。CupScannerResults に filterKey==='breakout' の filterLabel 分岐追加。
- 工数 ~0.3 人日。 reviewer: frontend-arch + QA 両者 条件付賛成。

### B3 — LP 未ログイン空 detail pane の cyan 枠
- `PaneDetailView.jsx` 空状態の cyan 枠が [[feedback_no_baseline_cyan]] 違反か、 選択先アフォーダンス (意図的) か **本番スクショで視覚判定**が必要。 コードだけでは判定不可。

### (低優先・v140 から継続)
- tier 中間価格 + 機能配分レビュー / Pane3 読込速度改善。

## 🟡 本セッションの知見
- **2b**: Recharts ReferenceDot への hover tooltip は **既存 Tooltip 拡張が安全** (SVG dot 直hover は main Tooltip と二重発火 + Chart Overlay Safety リスク)。date キー照合 ([[feedback_price_date_overlay_time_key]])。
- **D4 呼称**: 「新波動入り」は `blocklist.js:61` で BAN (金商法 §38 断定的将来予測)。 確立済用語「ブレイクアウト」採用。
- **index.css 技術的負債 (未修正)**: `.ds-chip[data-cup-state="breakout_pending"]` が **6655 と 11528 で重複** (後者 cup-chip-pulse 2.4s + border!important が cascade で勝ち、 6655 ds-chip-cup-pulse 2s は dead)。今回は visual 変更リスク回避で未統合、 Sprint2 着手時に grep 確認して統合推奨。
- D4 3体合議は ui-designer + frontend-arch + qa-dogfooder の3体 (Sonnet)、 全 verdict は本 handover の Sprint2 spec に集約済。

## ⚠️ 触ると危険 (継続)
- 発光系 `.panel-card/.bs-panel/.surface-card` (design_recipes §C) / sticky 検索バー / VITE_ ARG/ENV / aggregator への LLM import 禁止 / **「じっちゃま」 UI 禁止**。

## release status
pre-release。 Premium は LP「近日公開」 (Stripe 未配線=購入不可)。 Pro ¥980 のみ実購入可。
