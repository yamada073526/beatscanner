# 引き継ぎ書 v259 (2026-06-23) — screener mockup v8 踏襲 B-3 (merge済) + Phase C (PR #3 draft) 着地

> v255 を置換。本セッションで **① B-3 (mseg + Premium lock crow) を main に merge / ② Phase C (旬のセクター master-detail) を draft PR #3 化** を実施。
> ⚠️ **視覚 dogfood は B-3・Phase C とも未検証**（この実行環境は egress ポリシーで本番ドメイン拒否 → snap 不可）。次セッションまたは user が本番 `?screener_v2=1` で目視確認必須。
> branch `claude/gallant-wozniak-fxpbrz`。main HEAD `7c2cefe`（B-3 反映済）。local HEAD `b8e833b`（Phase C・PR #3）。v255 は削除可。

---

## 🧭 30 秒サマリー

| 項目 | 状態 |
|---|---|
| **branch** | `claude/gallant-wozniak-fxpbrz`。local HEAD `b8e833b`（Phase C 1 commit、origin/main の上に rebase 済）|
| **main / 本番** | origin/main `7c2cefe` = B-3 反映済。Railway auto-deploy は実行されるはず（**この環境からは検証不可**）|
| **PR #2 (B-3)** | **merged**（rebase-merge `7c2cefe`）。mseg + Premium lock crow |
| **PR #3 (Phase C)** | **draft open**。旬のセクター master-detail。merge は user 判断待ち |
| **screener_v2 flag** | default **OFF**（一般ユーザー無変更）。dogfood は `?screener_v2=1` |
| **🔴 最重要の未検証** | **B-3・Phase C の視覚 dogfood**（egress block で本セッション不可）→ 本番目視で確認 |
| **保留（要設計）** | **B-3.5 gate 南京錠**（pass 挙動変更＋B-4 と絡む）|
| **mockup SSOT** | `docs/specs/mockups/screener-strategy-presets-v8.html`（承認済 visual SSOT）|

---

## ✅ 本セッション完了

### ① B-3 — mseg ＋ Premium lock crow（PR #2 merged → main `7c2cefe`）
B-2 で統一した `.crow` に mockup v8 の lock/mseg 層を追加（`screener_v2` scope）。`CustomScreenerPanel.jsx` + `index.css`。
- **mseg（個別緩急 精度セグメント）**: adv ON 時、grade crow 内に 緩/標/厳/最厳 を full-width 表示。ロジックは既存 `renderGradeRow` と同一（`setOverrides`）。Free は §4.2 ちら見せ（`disabled` でなく `onClick→setAdvLockNudge` nudge）、Pro は per-facet override 可。v2 で失われていた個別緩急を mockup の視覚で復元。
- **Premium lock crow**: locked facet を「非表示(`return null`)」→ **南京錠 crow で表示**「Premium で解錠」（Trust Cliff #2・件数0誤解を解消）。locked 判定を freshness check より**前**へ移動。**表示専用で `itemPasses`/件数に不参加 → count==list (C-2) 不変**。
- **二重表示防止**: v2 で inline lock crow に出す key（`pivot_distance`/`breakout`/`ad_volume` = `CROW_INLINE_LOCKED_KEYS`）を (2f) 別 section から除外。legacy 挙動は完全不変。
- CSS: `.screener-crow__{lockicon,lock-cta,mseg,mseg-btn,mseg-v}`（gold は `color-mix(var(--color-gold))`、token のみ）。build/design-check/eslint PASS。

### ② Phase C — 旬のセクター master-detail（PR #3 draft `b8e833b`）
`hot_sector` preset を Phase A 暫定の銘柄リスト → mockup v8 の master-detail に置換。
- **master**: `filteredItems` を sector で grouping、`sector_rs_median`(sr) 降順。縦バー（最上位=主戦場 amber / 残り=上位 緑）＋セクター名＋合致銘柄数＋sr 値。クリックで detail 切替。
- **detail**: 選択セクターの合致銘柄を `rs_percentile` 降順 Top3（ticker＋企業名＋「5条件達成」）。行クリックで `onSelect`（判定遷移）。
- **整合**: master/detail とも `filteredItems` の view で件数整合を自然担保。pass 不変・表示専用。`activePreset` state を新設し `applyStrategyImpl` で追跡（`hot_sector` のとき `isSectorView`、他 preset/解除で従来リストに戻る）。
- CSS: `.screener-sec{md,master,row,detail,legend}`（gain/warning/gold token、cyan 不使用）。build 48.14 kB / design-check / eslint PASS。

---

## 🔴 次セッション最優先

### 1. B-3・Phase C の視覚 dogfood（本セッション環境で不可だった）
- **この remote 実行環境は組織 egress ポリシーで `beatscanner-production.up.railway.app` への CONNECT を 403 拒否**（README: 回避禁止・報告のみ）。`/health` 検証も headless snap も実施できなかった。
- B-3 は main 反映済（Railway auto-deploy 済のはず）。**本番 `?screener_v2=1` で**: ① grade crow の mseg（緩/標/厳/最厳、Free 淡色＋Pro 解錠 nudge）/ ② Premium lock crow（南京錠＋「Premium で解錠」が conds 内に inline 表示・(2f) 重複なし）/ ③ Phase C は PR #3 merge 後に hot_sector で master-detail（セクター一覧＋Top3）。
- ローカル `file://dist` は backend `locked_facets`/universe が無く lock crow/sector 集計を再現できない → 本番 dogfood 必須。

### 2. PR #3（Phase C）の merge 判断
- draft。user が ① 本番 dogfood 後に merge か、② 先に B-3 を本番確認してから、の判断待ち。merge 経路は PR #2 と同様（draft 解除 → rebase-merge）。

---

## ⏸️ 保留（要設計・単独で切れない）

### B-3.5 — gate 南京錠
mockup の gate（必須条件を南京錠・トグル不可）化。**単なる表示変更でなく pass 挙動変更を伴う**ため本セッションでは着手せず（user 合意）:
- 現状 `ocf_margin_pct` / `ocf_gt_netincome` は**トグル可能な任意条件**（default OFF）でありながら別 section で「必須・変更不可」と矛盾表示。gate 化 = これらを**常時適用** → **デフォルト件数が変わる**（count==list の Trust Cliff 領域）。
- 「どの条件を gate にするか」は本来 **preset 毎に異なる**（B-4 = preset→conds）ため、gate は B-4 と絡む。先に B-4 を設計するか、gate の意味論を user と確定してから。
- 関連コードコメント: `CustomScreenerPanel.jsx` の「.crow 南京錠化は B-3」「mseg/gate南京錠は B-3」「tier・available(準備中 disabled=B-3)」。tier/available「準備中」disabled は現状トリガー条件がデータ未存在の no-op フックなので低優先。

### Phase B-4（preset→conds 配列）
preset 毎に表示 conds を変える（決算合格=成長性/収益の質/モメンタム、新高値ブレイク=型/需給 等。mockup `PRESETS[].conds`）。現状は universe 全体共通の facet。gate と一体で設計するのが筋。
- ※ v255 が「**想定より大きい再アーキテクチャ**」と評した Phase B（preset 駆動条件モデル）は **B-1（PRESET_CONDS レジストリ）/ B-2（.crow 統一）/ B-3（mseg + lock crow）で着地済**。残るは B-3.5(gate) と本 B-4。

### cfps_3y_rising（連続性条件・未実装）— carry-forward from v255
- **`cfps_3y_rising`（CFPS 3年連続増加）は未実装**。v255 で defer 済（年次 cash-flow 未 fetch = +1 FMP call で「0-call fields」前提に抵触するため）。
- 「決算合格」preset の連続性条件（EPS/売上/CFPS の monotonic）のうち `eps_3y_rising`/`rev_3y_rising` は #4 で deploy 済（nightly populate）だが **CFPS だけ欠落**。mockup p1 conds の `cfps3`（CFPS の連続性）に対応する実データが無い。
- 実装するなら backend に年次 cash-flow fetch 追加（FMP call 増を許容するか要判断）か、条件から CFPS 連続性を除外するかの設計判断が必要。

---

## 🟢 v255 から解決済み（carry-forward 不要）
- **🔴 funda_pass backend bug（v255 最優先・別track）→ 修正済**（commit `b5370b4`）。root cause = cond2/3/4 が四半期 QoQ 単調増加を要求し季節性で優良株全滅（True=0）。原典どおり**年次(Annual)3年連続増加**に是正 + nightly cron 登録（手動 refresh のみで停止していた）。→ 「決算合格」中核が常時0件の問題は解消（**本番で True>0 を要再確認**）。
- **タイル件数3問題 #2 Premium マスク** → B-3 lock crow で改善（Free に南京錠表示）。#1 funda_pass は上記で解消。**#3 core grades 過剰適用**（preset に標準精度 eps/roe/rs 上乗せで sector_leader 実6→タイル1）は**未対応**（preset 件数を「定義条件のみ」にするか要判断、B-4 で扱う）。

---

## 🟡 user 確定事項 / 重要知見
- **mockup v8 = 承認済 visual SSOT**。実装はこれに忠実化中（発光/card 安全 recipe + token + Trust Cliff は維持）。
- **lock crow は mockup 通り表示**（Free でも locked 条件を南京錠で見せ Pro 訴求）= user 明示（本セッション）。
- B-3 を main に merge = user 明示承認（本セッション）。deploy 経路は「PR を main に rebase-merge」。
- **環境制約**: この remote 環境は本番ドメインへ egress 不可（視覚検証・curl 検証は user 側 or 許可された環境で）。

## ⚠️ 触ると危険 / 規律
- **発光系**（`.panel-card`/`.bs-panel`/`.surface-card`）→ screener は plain bordered + token shadow。新規 crow/mseg/sector も border + tinted-bg のみ（生 shadow なし）。
- **CustomScreenerPanel 整合（Trust Cliff C-2）**: count と list は同一 `itemPasses`。lock crow / sector master-detail は**表示専用で pass に不参加**（itemPasses 不変を死守）。新 facet/flag 追加時は全 extra 構築箇所（filteredItems/presetCounts/facetLevelCounts/emptySuggest/sectorOptions/mcapOptions）+ 依存配列に配線。
- **投資色ルール**: 上昇=緑(gain)/下落=赤(loss)/緊急=amber(warning)/cyan=ブランド色（上昇に使わない）。sector master は 主戦場=amber・上位=緑、cyan 不使用で準拠済。
- **paged SELECT 罠**: screener_fundamentals に新カラム追加時は別 fetch 分離 or migration 先行（`feedback_paged_select_missing_column_trap.md`）。
- screener_v2 flag default OFF。deploy = PR を main に merge（Railway auto）。sticky 検索バー / VITE_ ARG/ENV 同期 不触。

## 🧹 掃除
- handover **v255 は削除可**（本 v259 最新）。※ v256-v258 は既に存在せず（前セッションで削除済の模様）、本セッションは commit から状態復元した。
- PR #2 branch は merged。PR #3 が同 branch（`claude/gallant-wozniak-fxpbrz`）の現行 head。
- session-only cron `452d6845`（PR #3 自己チェックイン・hourly :17）は本セッション終了で消滅。新セッションで PR #3 を扱うなら再購読/再設定。
- snap 系 untracked は残置可。`/tmp` 診断ファイルは ephemeral。
