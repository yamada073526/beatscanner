# 引き継ぎ書 v205 (2026-06-13 = 朝feedback + autopilot + content-audit、Phase2 着手用クリーン引継ぎ)

最新本番 = `4ae664b`。/health commit + バンドル grep + dogfood で全件検証済。**context 66% 到達でフレッシュ移行**。
**次の最優先 = Phase 2 forward-outlook (EBITDA/FCF マージン guidance)** — 下記 §次セッション参照。

---

## 🟢 本セッション完了 (全て本番 live・user OK 確定)

### ④来期コンセンサス拡充 (Phase 1a+1b + total_expenses) — 完遂
- `497614d`/`7e159c8` Phase1b: guidance_extras (OpEx/capex) 8-K/transcript 抽出。field enum + period_type + basis +
  source_quote、label_jp=静的 FIELD_LABEL_JP、few-shot+BAD-8 独立 bp3、max_tokens 2048、per-item §38 verify。
- `104b594` 金の左バー継承 / `4393d93` 会社見通し行の lazy fetch スケルトン (突然感解消)。
- `a10523d` 億ドル中途改行解消 (ForecastBarRow minWidth+nowrap) + 決算詳細リンク hover時cyan化 (.ds-flash-detail)。
- `8618e6f` **total_expenses(総費用) enum 追加** (user承認): META「total expenses $162-169B」を旧「営業費用」誤ラベル
  → 正「総費用」表示。opex(operating expenses のみ)/total_expenses(total costs and expenses) を厳密区別 + BAD-8-5/6 + GOOD-8-3。

### autopilot (就寝中) — guidance_extras systemic 誤ラベル発見・修正
- `89625f8` **allowlist guard**: field enum=[opex,capex] のみで LLM が total expenses/EBITDA/SBC/D&A を opex に誤ラベル
  (META/PINS/DASH)。source_quote に "operating expense"/"capital expenditure"/"total expense" を逐語で含む item のみ keep。
- `9b1f94a` ETF exposure allowlist に GICS 残り6セクター SPDR (XLC/XLI/XLP/XLU/XLB/XLRE) 補完。

### content-audit — AXP 系貸金業の売上偽Beat 解消 (Trust Cliff、user承認)
- `bc9c1c9` **当期売上**: AXP/COF/SYF/DFS 等が利息gross計上で +12% 偽Beat → `_is_interest_heavy_revenue`
  (interestIncome/revenue≥20%) で判定し threshold=0 抑止。V/MA/PYPL(利息≒0)は本物Beat 保持。3経路に income_row 配線。
- `2894473` **forward来期売上YoY** も同精緻化: `_fwd_rev_suppress`=銀行 or 貸金業のみ抑止 → V=+11.8%/MA=+11.5%/PYPL=+2.1% 露出。
- `1d5f33a`/`4ae664b` regression assert [1f]+[1d] 更新 (本番 全 PASS)。memory [[feedback_revenue_basis_mismatch]] 更新。

### ⑤ETF組入銘柄 / ②planV2 = 検証のみで解決済 (handover note が stale だった、前 v204 参照)

---

## 🔴 次セッション最優先: Phase 2 forward-outlook (EBITDA / FCF マージン guidance)

**何を**: guidance_extras を EBITDA / FCF マージン (+ FCF 絶対額) へ拡張。autopilot dogfood で **PINS=Adjusted EBITDA
$256-276M / DASH=SBC・D&A** 等が現状 enum 外で drop 中。これらを正ラベルで表示する。
**段取り (gate 必須)**:
1. 元 SPEC `docs/specs/SPEC_2026-06-12_forward-outlook-expansion.md` の Phase 2 scope を確認 (営業/EBITDA/FCF マージンは「非GAAP注記コスト+精度低」で Phase 2 指定)。
2. **3 体合議 gate** (LLM品質軸 active = 新抽出拡張): ui-designer + frontend-architect + qa-dogfooder or 金融+§38。
3. 実装は total_expenses (8618e6f) を**参照実装**として踏襲: schema field enum 追加 (ebitda 等) + FIELD_LABEL_JP + bp3 独立 few-shot/BAD + main.py allowlist guard (source_quote に "ebitda"/"free cash flow" 逐語) + frontend mirror。
4. **§38**: EBITDA は profit 指標 = 全中立色維持、捏造禁止、basis(non_gaap)注記必須。
- ⚠️ **operating margin は既に q_margin (type=operating) で対応済** → Phase 2 の新規は EBITDA/FCF のみ。
- hallucination-guard skill 経由必須 (4層)。

---

## 🟡 DEFER (user 判断・別途)
- **GOOGL/AMZN capex coverage**: 両社 capex は call 開示だが 8-K 明示数値なく extras=空 (coverage 欠落、バグでない)。transcript whitelist 拡張は §38-sensitive。
- **モーダル文章推敲** (GuidanceInfoModal/ForwardOutlookInfoModal): user「今はまだ着手しなくていい」明示 → 保留。
- **上級者 review backlog / motion 提案**: 要相談。
- content-audit を AI図解(visualize trends beatMargin)へ拡大 (Pitfall 3、`CONTENT_AUDIT_FULL=1`)。

## ✅ v204 から closed (v205 backlog から意図的に除外、ドロップでない)
- **guidance_extras enum 拡張 (LLM schema 変更)**: `8618e6f` total_expenses(総費用) で完遂・user承認・META検証済 → closed。
- **個別株 (NVDA/MSFT) の来期コンセンサス** dogfood: user「OKです」確定 (営業費用/設備投資/総費用・金バー・スケルトン) → closed。
- **ETF (SPY/QQQ/VTI) チャート** dogfood: 個別株 overlay 抑止を user 確認済 → closed。
- **PINS/DASH/META の来期** dogfood: 誤ラベル除去 (META=総費用のみ/PINS/DASH=空) を user 確認済 → closed。

## 🔵 実機 dogfood
- 本セッション分は逐次 dogfood 済 (改行・hoverリンク・AXP当期売上抑止・V/MA来期売上YoY 全て user OK)。残なし。

## ⚠️ 危険箇所 (不変)
- 発光系 `.panel-card/.bs-panel/.surface-card` / sticky 検索バー / VITE_ ARG/ENV / aggregator LLM import 禁止。
- guidance_extras の **allowlist guard** (`main.py _compute_forward_outlook`): source_quote 逐語照合が前提。EBITDA enum 追加時はこの allowlist にも "ebitda" を追加。
- 売上基準ミスマッチ guard (`_is_interest_heavy_revenue` + `_guard_revenue_basis_mismatch`): interestIncome/revenue≥20% で貸金業判定。閾値変更時は content-audit-check.sh [1d][1f] で V/MA 巻き込み回帰を確認。
- deploy = `git push origin main` (Railway auto ~30-60s)。`railway up` 使わない。
- 実装 SSOT: [docs/specs/SPEC_2026-06-12_forward-outlook-expansion.md](docs/specs/SPEC_2026-06-12_forward-outlook-expansion.md) §7。

## 🧰 検証 harness
- guidance dogfood: `curl "本番/api/guidance/{t}/basic?with_guidance=1"` (company_guidance_extras) / `curl "本番/api/guidance/{t}"` (revenue.verdict)。
- 回帰: `bash scripts/content-audit-check.sh` (known-pitfall 全 assert、本番対象)。
- `frontend/scripts/snap-forward-extras.mjs` (未追跡、DOGFOOD creds で来期 extras headless 検証)。
