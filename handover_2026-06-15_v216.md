# handover v216 (2026-06-14 夜 autopilot → 2026-06-15 朝 実行)

> 前提: v214/v215 を継ぎ superseded(削除済)。本番 = `11405f1`(全 deploy・検証済)。
> 経緯: 2026-06-14 夜 autopilot で content 監査(BABA EPS Trust Cliff 等を検出・DEFER) → user 起床 → 全承認 → 朝に全実行+検証。

## ✅ 本セッション完了 (全 deploy・検証済、commit 順)
- **BABA EPS -91% 偽 miss → 海外決算 単位混在ガード** `8f26301`: 中国 ADR (BABA=CNY) で FMP 実績EPS(GAAP/ordinary) vs アナリスト予想(non-GAAP/ADS,USD) が単位を跨ぎ -91.2% 偽 miss + 来期 EPS YoY +489% artifact。`_guard_eps_currency_mismatch`(reportedCurrency!=USD かつ |surprise|>=70% で verdict=unknown + actual/estimated null化 + confidence降格)。較正: 外貨10銘柄で BABA のみ isolate、TSM/ASML/SAP/JD/BIDU/NVO の正常 verdict は保持。guidance_basic + _guidance_impl 両路 mirror。
- **外貨建て企業の金額を USD 換算** `e6e0b73` (user 承認: USD換算): BABA/TSM/ASML 等で revenue が native 通貨(¥243B 等)のまま「億ドル」表示の systemic Trust Cliff。`_usd_per_unit`(FMP forex USD{CUR} 逆数, 6h cache) で revenue(current+forward, 集計値なので FX で正確) を USD 化。**EPS(per-share+ADS比で換算不能)+会社ガイダンス系(出し手単位不確実)は suppress** (`_apply_foreign_usd_to_forward`)。検証: BABA $36B/TSM $35.9B/ASML $10.1B 等 sane、NVDA/AAPL 無影響。
- **ETF ラベル二重表示 解消 + 図解「詳しく見る」default ON** `bdc9a8b`: EtfOverviewPanel の MetricChip label+hint 二重を単一日本語ラベルに。図解下層L3の累進開示 `?diagram_essence` を **default ON 昇格**(?diagram_essence=0 が kill、機能自体は既存で flag OFF だった)。snap 検証済(Premium auth)。
- **subscription ちらつき → localStorage optimistic 復元** `1bad10e`: useSubscription の async fetch 解決まで getPlan='free' で Premium に一瞬 free UI。同 user の last-known sub を `bs_sub_cache_<userId>` に keyed cache、mount で optimistic 復元→fetch reconcile(<500ms)。**gating ロジック不変・復元値は必ず fetch 上書き・backend gate 不変で security 不変**。
- **BABA insights が SpaceX 記事 → KB relevance 修正** `11405f1`: `_analyze_text_to_insights` の両 prompt に「ticker が分析の主題である言及のみ対象、比較対象/例示の言及だけなら found:false」追加。BABA insights は実内容(Pupu/Meituan競争)に是正済。
- **Supabase security hardening** (migration `security_hardening_2026_06_15` 直接適用、repo 外): `rls_auto_enable()` の anon/authenticated EXECUTE を REVOKE(event trigger は owner 経由で不変)、`set_updated_at()` に `SET search_path=''`。get_advisors で warning 解消確認。
- **メモリ棚卸し 本質再編** (~/.claude/memory、repo 外): 過剰統合の解消。canslim 47KB→3分割 / Railway 7→1統合 / FMP 5→1 / 日本語出力 4→1 / 章サマリー・cup-handle・backtest を reference(設計)+project(着地記録)に分離 等。148→134 files、index 1:1 整合・orphan/dangling/NEW broken link すべて 0 を**独立検証済**。backup=`memory_backup_2026-06-15`(安全網、git外)。

## ⚪ Supabase 認証 hardening (2026-06-15 着地)
- ✅ 無料 hardening 適用済: Minimum password length 6→8 / Password requirements=Letters and digits (Email provider 設定)。
- ⏸️ Leaked password protection (HaveIBeenPwned) は **Supabase Pro 限定機能 → 保留**。Free では有効化不可 (dashboard でエラー)。本番で Pro 契約時に自動付帯。get_advisors の `auth_leaked_password_protection` WARN は「Free では不可・許容」として**今後フラグ不要**。

## 🟡 follow-up 候補 (任意・次セッション)
- **content-audit-check.sh に assert 追加** (再発防止 3点sync の②): 「BABA eps verdict 抑止」「BABA revenue が USD レンジ($20-60B)」「外貨 reporter の forward eps=None」。runtime guard(①)は実装済、registry(③)は本 handover。
- **新 pitfall を memory 化**: 「ADR/外貨 reporter の EPS 単位混在 + revenue 通貨表示」を content-audit 知見として memory 追加 (MEMORY.md は再編で余裕あり)。
- **D: SPY/QQQ** は JudgmentDetail:591 で ETF→EtfOverviewPanel routing 済 = 決算速報は元々非表示(コード変更不要・確認済)。

## 📊 未着手 backlog (SSOT = `BACKLOG.md`、本セッションでは未着手)
v214/v215 から継続。計画 SSOT は `BACKLOG.md` (2026-06-14 user walkthrough 判定)。主要な「残(やる)」:
- **記事自動配信 (KB連携)** A1/A2/A3/B15 — 北極星「管=配信」本丸。
- **高額プラン差別化** A9 ¥9,800 tier / B1 CAN-SLIM / B3 transcript LLM / B13 Stripe。
- **テクニカル/Pane3** 🔴 B7 図解 redesign / B10 縮尺タブ / D2 Pane3冒頭 verdict 再設計 — **user が Fable5 デザイン対話を希望**(SPEC 先行禁止)。
- **配信(管)** A8 プラン変更UI+メール配信登録/解除 / D1 ガイダンス修正3面表示(planner SPEC化決定済)。
- **個人化** B9 backtest+レコメンド。 **release** B11 特商法表記+funnel計測。 **D3 プットコールレシオ**。
- 小: 旧 Chip 版ウォッチ★(非 compass)gold のまま揃えるか / §38免責の最短化残置 — user 判断待ち。

## ⚠️ 触ると危険 (継承)
- 発光・カード系 / sticky 検索バー / aggregator LLM import 禁止。
- **plan gating SSOT** (getPlan 経由、手組み三項 drift 禁止)。subscription cache は gating ロジック不変で追加した。
- **EPS verdict / 外貨換算**: `_guard_eps_currency_mismatch`(70%閾値) / `_apply_foreign_usd_to_forward`(EPS suppress + revenue FX) / `_usd_per_unit`(FMP forex)。閾値変更や per-share換算追加は多 ADR(BABA/TSM/ASML/SAP/JD/SONY) で over-suppression 検証必須。
- deploy = `git push origin main` → `/health` commit 一致 → 本番 chunk grep。`git add` は明示 path。lazy chunk(EtfOverviewPanel等)は entry grep で拾えない→authed snap で DOM 確認。backend deploy ~90s。

## deploy 済 commit (本セッション)
`8f26301`(BABA EPS) → `e6e0b73`(USD換算) → `bdc9a8b`(ETF+図解) → `1bad10e`(subscription) → `11405f1`(insights、現本番)。
