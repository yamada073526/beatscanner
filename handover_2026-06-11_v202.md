# 引き継ぎ書 v202 (2026-06-11 夜 autopilot = dogfood feedback 6 件 ship + v5 Beat/Miss hero 再設計)

就寝中 autopilot (20:49→)。最新本番 = `4c35983`。全機能 deploy + 検証済 (bundle grep / headless snap)。

## 🎯 完了 (本番 live、検証済)
| # | 内容 | commit | flag | 検証 |
|---|---|---|---|---|
| B3 | 「今期 決算結果」 の？モーダルを過去実績(Beat/Miss)説明へ全書換 (旧=将来ガイダンス説明で誤+§38抵触語「株価上昇を決定づける」も除去)。閾値 ±3% は backend `_verdict` と mirror | `040a8a7` | default | bundle grep ✅ |
| B2 | KpiStrip の「EPS Beat」chip 撤去 (今期決算結果+決算ハイライトと三重表示)。可逆 | `040a8a7` | default | bundle grep ✅ (残「EPS Beat」=8Q履歴サマリー、別件) |
| B5 | ForwardOutlook「会社の次期見通し」→「会社の見通し（原文）」改名 + 「展開で全文」text削除 + SEC8-K出典を展開部内へ + hover強化(1px枠+cyan) | `2256e6f` | default | bundle grep ✅ |
| B4 | 5条件カード: 展開アニメ (AnimatePresence+m.div height spring) + 各タイトル横「？」chip (展開不要でモーダル) + 旧展開内ⓘ撤去。入れ子button回避=絶対配置トグル+pointer-events | `659e579` | default | snap-conditions ✅ (chip5/展開/モーダル/行非展開/pageerror0) |
| **A** | **決算ハイライト v5.3 = Beat/Miss hero 再設計**。予想比(サプライズ%)を 20px/700+緑赤 hero、結果15px、予想13px muted、前年比14px色。列 予想→結果→予想比→前年比。**3体 design review 反映** | `3bc6a74`→`b0e6acd` | `?flash_v5=1` opt-in | snap GM_V5 ✅ |
| B1 | **ETF組入 panel** (新規 prototype)。backend `/api/etf-exposure` (FMP asset-exposure→主要US ETF allowlist curation, 24h cache) + `EtfExposurePanel` | `db4234f`→`4c35983` | `?etf_exposure=1` opt-in | curl 4ticker + snap ✅ |

## 🔴 次セッション最優先 (user 判断待ち = 朝サマリー A/B)
1. **v5.3 Beat/Miss hero の dogfood OK?** → OK なら `isFlashV5Enabled()` default true 昇格 + `?flash_v5=0` kill + CLS minHeight 再計測。`?flash_v5=1&pane3_v5=1` で確認。
2. **ETF panel: 配置/default ON/allowlist の確定**。現在バリュエーション直後 hairline + `?etf_exposure=1`。配置(プロフィール? バリュエーション隣?)・主要 ETF の範囲(現12本)・default ON 可否が user 判断。`?etf_exposure=1&pane3_v5=1` で確認。
3. **B6 アイコン規則 (DEFER、提案を朝サマリーに記載)**: L1章扉=なし統一 / L2カード見出し=一部に BarChart3/Building2/FileBarChart2 / AccordionS-トグル=chevronのみ / DiagramCard=独自 VizSectionLabel / ProfileCard=gold bar。**推奨=Option A (装飾アイコン全廃、icon は機能用途のみ=chevron/外部リンク/？。階層はタイポ+gold barで)**。横断適用は blast radius 大のため user 承認後。
4. **B5 来期コンセンサスの内容拡充 (DEFER §38)**: 現状 EPS/売上のみ。margin/segment guidance 等の追加は §38(将来予測)重 + 8-K抽出拡張要。SPEC 化して 6体 gate 推奨。

## ⚠️ 重要知見 (新規メモリ化済)
- **[[feedback_judgmentdetail_dual_mount_paths]]**: JudgmentDetail は `!isV5` と `isV5(pane3_v5)` で section を**別 mount**。新規 section は両パスに置く、検証は `?pane3_v5=1` で。B1 で 1 サイクル消費 (anyPanel=0、ttm は別 mount で出てた)。
- **[[fmp_etf_asset_exposure]]**: 銘柄→組入ETF は `/stable/etf/asset-exposure` (Ultimate)。leveraged ETF (HEMI 29055%等) が weight 上位汚染 → 主要US ETF allowlist 必須。セクター適応 (JPM→XLF/VTV, XOM→XLE)。
- **色 §38 (v201 から継続)**: 過去確定実績(予実差/前年比)=緑/赤OK、来期/水準=中立。v5.3 の hero 緑赤はこの射程内。
- **3体 vs 6体 review**: v5.3 は frontend 局所 + §38 既定のため **3体 (ui+frontend+qa)** で十分だった ([[feedback_multi_review_3_panel_workflow]])。

## ⚠️ 触ると危険 (不変)
- 発光系 (.panel-card/.bs-panel/.surface-card/tier-m-glow) — B4 は内部のみ触り wrapper 不触で回避。sticky 検索バー / VITE_ ARG/ENV / aggregator LLM import 禁止。
- deploy = `git push origin main` (Railway auto ~30s、/health commit で確認)。
- flag 一覧: `?flash=0/flash_gm=0/flash_seg=0/guidance_pit=0/flash_v2=0/flash_v3=0/flash_v4=0` (default ON kill) / `?flash_v5=1`(opt-in) / `?etf_exposure=1`(opt-in、新規) / `?pane3_v5=1`(現 dogfood レイアウト)。
- harness: `snap-flash-gm.mjs`(GM_V5=1 で v5) / `snap-conditions.mjs`(5条件) / `snap-etf.mjs`(ETF panel)。全て `node --env-file=.env`、localStorage opt-in + 認証注入。
