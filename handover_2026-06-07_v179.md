# 引き継ぎ書 v179 (2026-06-07)

CAN-SLIM Phase 3 で **Sprint 3 (S条件) + Sprint 4a (backend hardening)** を着地させたセッション。次は **Sprint 4b (read endpoint A/N/S 公開)** から (ただし明朝 fresh nightly 後の方が検証クリーン)。

## 🎯 完了済み (全て merge+deploy+本番Rule5検証済)

### Sprint 3 — S条件 populate (main `85ba19f`)
- `buyback_yield` (自社株買い額TTM/時価総額) + `volume_surge_pct` ((当日vol/averageVol-1)*100、+40%独立カラム)。
- helper `_calc_buyback_yield` を per-ticker `valuation-extras` と共有=**二重実装解消** (main review で検出・hotfix、finite-float-first で旧`_pick`と1:1、本番 per-ticker 値完全一致=回帰なし)。
- **data-source 発見**: averageVolume は batch-quote/quote に**無く** `/stable/profile` のみ → sector guard で温まる profile 24h cache 相乗りで**追加call ゼロ**。
- migration `volume_surge_pct` (MCP適用済)。tuple 7→9。**6体合議 gate = 6/6 条件付賛成・反対ゼロ**。

### Sprint 4a — backend hardening (main `9f271d7`、6体合議 BLOCK①②③解消)
- **①単位統一(方式B)**: `near_high_pct_scaled`/`buyback_yield_pct` の pct新カラム (migration `unit_unify.sql` MCP適用済) に post-gather で×100書込。helper は ratio維持(per-ticker は DB read せず波及ゼロ)、旧 ratio カラムは None で vestigial化。
- **②ROE individual guard (main hotfix)**: ★初版は存在しない `debtToEquityTTM` を読み**非機能**だった(MCD roe=-434/PM=-105 が live)→ `_roe_equity_guard` + `ratios-ttm.shareholdersEquityPerShareTTM<0` 判定(疑わしい roe>50%/<0 のみ追加fetch=コスト最小)。roe_computed 405→**377**(28件NULL化)で発火確認。AAPL(146.7)/NVDA(111.7)は正equity保持。
- **③YoY near-zero NULL**: `|eps_prev|<0.05`→None(9999%アーティファクト排除)。MU+682%等 genuine保持。
- 即修正可: turnaround call-site `turnaround,` / GHA分母 processed_count / _MIN_VALID 200。pytest 280。Evaluator L4 PASS(方式B=L4のみ)。

## 🔴 次セッション最優先: Sprint 4b (read endpoint A/N/S 公開)
- SPEC = `docs/specs/SPEC_2026-06-07_canslim-phase3-sprint4.md` §5 Sprint4b。
- `_fetch_screener_fundamentals_by_condition` col_map に `eps_cagr`/`roe`/`near_high`(→**near_high_pct_scaled**)/`buyback`(→**buyback_yield_pct**)/`volume_surge` 追加 + `count="exact"` 分離(Supabase 1000行上限、BLOCK④) + excluded を uncomputable/unavailable 分割 + C(eps_yoy)後方互換維持。
- **本番 curl で `condition=near_high&min_pct=95` / `buyback&min_pct=2` が非空**を確認(S4a単位統一が効いて全除外されない=BLOCK①解消の最終門番)。
- ⚠️ **Trust Cliff (公開境界)**: A/S=free / N=将来Premium (read は free、gate は frontend、S5でロック面言語化)。固有名詞(O'Neil/CAN-SLIM)を出さない。
- 起動: `Agent subagent_type=generator`、SPRINT_NUMBER=4b。着地後 main で本番 Rule5。

## 🟡 重要知見 (永続化済、特に運用)
- **⚠️⚠️ None-preserve 検証規律**: upsert は「None=既存値保持」。**同一 calc_date の re-populate では「値→None」遷移(guard/vestigial化)を DB反映できない**(MCD roe=-434 が今日残存)。guard発火は **roe_computed の count減少**で確認、同日 DB値で「NULL化済」と早合点しない。明朝 fresh nightly(08:07 JST) で MCD roe=NULL/旧ratioカラム=NULL のクリーンデータ化。今日の messy データは read が roe≥17 filter で stale負値を除外するため benign。
- per-ticker `valuation-extras` は helper を都度計算し DB read しない → screener カラムの scale変更は per-ticker 表示に無影響。
- generator は worktree 必須(S3 で main 直接実装事故。S4a brief に worktree assert 明記で再発防止成功)。migration は autonomy hook BLOCK のため **main 作成 + MCP/SQL Editor 適用**(human-in-the-loop)。
- 詳細 SSOT: memory `project_canslim_screener_expansion.md` (S3/S4a 全記録 + 6体合議 verdict + None-preserve)。

## 📊 残バックログ (council 由来、SPEC 申し送り済)
- **S5 frontend**: chip増殖せず結果行内バッジ列(各柱max4) / `null_reason`(sector_guard/calc_impossible/data_missing) / N=Premium ロック面の価値言語化 / 黒字転換は halo_sweep+中小型universe後に前面訴求(sp500で0件) / as_of「X日前」。
- **AAPL型ROE §5補完**: ROE単独を「優良」ラベルにしない(金融option b、S5 display)。AAPL(正小資本→146.7)は guard捕捉外。
- **Phase4**: tuple 9→`@dataclass ScanResult` / generator worktree assert を SKILL明文化 / helper を aggregator移設(HG Check3保護、main.py は死角) / scan async・chunk化(3000 universe 502) / A/N/S batch endpoint / None-preserve に sentinel(computed-None vs not-provided区別)。
- **Phase4 (PMF/Stripe後)**: I条件(13F機関保有) / Part B 買値登録→損切利確自動push。

## 📋 繰り越しバックログ (CAN-SLIM 外・未着手、v178 から継続)
- **Stripe Premium 配線** (launch blocker、pre-release 最重要、N=将来Premium の gate 実体化にも必要)。
- **RS scan 健全性確認**: rs_ratings は v176→v178 で 2359行に健全回復済 (2026-06-06 は当日 partial 3行)。継続 monitoring。
- count-up「入場時リセット」解消 / 案C(RPO/ARR) / 通期 EPS YoY の正確化 (basis mismatch 抑止中) / nightly pre-warm cron (保有銘柄)。

## ⚠️ 触ると危険 (CLAUDE.md)
sticky検索バー / 発光系(.panel-card/.bs-panel/.surface-card) / VITE_ ARG/ENV同期 / aggregator への LLM import(pre-commit Check3) / 既存 migration・既存 endpoint(canslim-scan/scanner/canslim/cup-scan/quarterly-history) / railway.toml cron。

## メモリ更新済
`project_canslim_screener_expansion.md` に S4a着地 + ROE guard hotfix + None-preserve検証規律 + S4b次手。MEMORY.md index は「Phase3進行中」のままで可。
