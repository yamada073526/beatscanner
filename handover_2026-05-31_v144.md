# BeatScanner Handover v144 — Pane 3 読込速度改善 + AI要約高速化 + 記事リンク強化 (2026-05-31 夜)

> v143 残タスク「Pane 3 読込速度改善」 を計測→真因→施策で着地。 続けて user dogfood feedback 2 件
> (AI要約 5s / 記事リンク) を着地。 全て本番デプロイ + 検証済。 v143 handover は本 v144 で置換 (削除済)。
> 詳細 SSOT: memory [[project_pane3_load_perf]] / [[feedback_deploy_verify_discipline]] (/health.commit marker)。

## ✅ git 状態 (クリーン・同期済)
- working tree **clean**、 最終 commit `eda3ab5`、 **origin/main へ push 済 = git=本番一致**。
- 本番 entry chunk: `index-DvskHniN.js`。 backend commit は **`curl -s <prod>/health | jq -r .commit`** で
  確認可 (v144 で追加した marker、 現在 `eda3ab53bf87`)。
- deploy 機構: GitHub 連携で main push → Railway 自動 build (~3-4分、 queue 済なら <30s)。 push 前 `git pull --rebase` 推奨。

## ✅ 本セッション完了 (全て本番デプロイ + 実測検証済)
| commit | 内容 | 検証 |
|---|---|---|
| `12625a8` | **Tier1 frontend GET coalescing** (api.js `dedupGet`): 重複 fetch 撲滅 (analyst **4→1** / insights 2→1 / guidance/basic 2→1 / price-history・news・ir-links 各2→1)。 prefetch 結果を panel mount が 0ms 取得。 + guidance/basic 6h cache | endpoint smoke 200 / guidance cold1.55→warm0.54 |
| `1d4b338` | **Tier2a insights KB prompt cache** (cache_kb flag、 不変 KB を cached system block へ) | VIST 27.6→SCCO 18.9 (cache read) |
| `9575ee9` | **Tier2b insights KB→Supabase 永続化** (本命): cold 24s が ticker/24h **1回** (全 user 共有) に | SQM 24.2s knowledge_base → clear → **0.86s supabase** |
| `be772be` | `/health.commit` に Railway git SHA (backend deploy 検証の恒久手段) | 反映 poll で確認 |
| `1f9ea02` | **Task A 冒頭AI要約 高速化**: 結果cache + true line streaming + system_cache。 旧 buffer→一括yield の「5s blank」 を解消 | 再訪 5.79→**0.82s** / cold **TTFB 0.89s** (first line 0.9s で progressive) |
| `05b2d81` | **Task B 解説記事リンク強化**: title + 相対日付「4日前」 + **新タブ(_blank)** + ↗。 撤去でなく強化 (1行 revert 可) | snap: target=_blank/title/「解説記事 · 4日前」 確認 |
| `eda3ab5` | AI詳細レポート(detail) に結果cache (再オープン即時) | 17.5s → **1.1s** |
| `c1286c6` | **Tier3 charts chunk 分離** (recharts / lightweight-charts): Pane 3 から未使用の lightweight-charts を外す (config のみ・chart 描画完全同一) | snap: recharts読込/lightweight非読込/chart正常描画。 Pane3 cold load **-197KB (gzip 63KB)** |

## ⚠️ user dogfood で確認してほしい点 (本番 demo rate limit で snap 完全検証できず)
- **AI要約**: 銘柄クリック後、 要約が ~0.9s で出始め progressive 表示か (再訪で即時か)。 ← curl 検証済だが実 UI 体感を確認
- **記事リンク**: Pane 3 末尾の記事カードが「タイトル + 4日前 + ↗」 で、 click で新タブか。 ← AAPL で snap 確認済
- **insights**: 訪問済銘柄が 0.8s か、 新銘柄の初回のみ ~15-25s か (= 仕様、 24h で全 user 共有 warm)

## 🔴 残タスク (user 判断待ち、 順不同)
- **Tier 3 phase 2**: recharts (575KB) を全 Pane 3 chart component で lazy-load。 中リスク (chart 真っ白 history + 多 component Suspense)、 warm cache の user 体感には出ない (新規/cold 向け) → **要判断**
- **Tier 2c**: insights 生成 (15-25s, output生成律速) を Haiku 化で短縮。 §38/景表法 guard 再検証必須 → **user 判断**
- (v143 から継続) メール: ポートフォリオ健康度通知 (Signature ¥9,800 看板、 Stripe+PMF が前提) / 法務 (特商法・privacy、 本格集客直前) / 指標バー右 scroll fade (低優先)

## 🟡 本セッションの知見 (memory 化済)
- **[[project_pane3_load_perf]]**: 全施策 SSOT。 非自明な発見 = **insights/要約の latency は input でなく output 生成律速** (KB cache しても floor 13-18s 残る)。 dedup pattern (prefetch なのに component 再fetch) は横展開可。
- **[[feedback_deploy_verify_discipline]] 更新**: backend deploy は `/health.commit` で一意検証。 in-memory warm-read は `/api/insights/cache/clear?ticker=X` で in-memory 消して Supabase/再生成 path を直接叩ける。
- **Task A true streaming pattern**: stream endpoint が「全chunk buffer→一括yield」 だと見かけ streaming で blank。 行完成ごとに per-line 後処理して yield (whole-text と byte 一致) が解。

## ⚠️ 触ると危険 (継続)
- 発光系 `.panel-card/.bs-panel/.surface-card` / sticky 検索バー / VITE_ ARG/ENV / aggregator への LLM import 禁止 / 「じっちゃま」 UI 禁止。
- DailyDigest summary は flex 維持 (「・」 再発防止)。
- **新規**: api.js `dedupGet` は GET 専用・成功時のみ cache・返却時 clone (参照共有 mutation 事故防止)。 insights refresh は `invalidateInsightsCache` で stale 破棄。

## release status
pre-release。 Premium/Signature は LP「近日公開」 (Stripe 未配線)。 Pro ¥980 のみ実購入可。
