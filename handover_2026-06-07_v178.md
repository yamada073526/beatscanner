# 引き継ぎ書 v178 (2026-06-07)

CAN-SLIM Phase 3 Sprint 2 (N条件 near_high_pct populate) を着地させ、本番 populate の運用問題を発見・解消したセッション。次セッションは **Phase 3 Sprint 3 (S条件 + MINOR3件)** から。

## 🎯 完了済み (全て merge+deploy+本番 Rule5 検証済)

### Phase 3 Sprint 2 — N条件 populate (main HEAD `d240bef`)
- `_calc_near_high_pct(price, year_high)` helper (純Python、`price/yearHigh`、None/0/負/型不正→NULL、§38/§5 欠損ガード)。
- canslim-scan に **yearHigh+price を `/stable/batch-quote` 一括 pre-fetch** (100銘柄chunk、追加 per-ticker FMP call ゼロ。`batch_quotes()` の field=`symbol`/`price`/`yearHigh`)。
- `_compute_one` tuple **6→7要素** (全 return 6箇所 + post-gather unpack + upsert 呼び出し)。
- `_upsert_screener_fundamental` に `near_high_pct` 引数 (None保持で C/A 回帰なし) + graceful fallback を turnaround/near_high_pct 両対応に一般化。
- response に `near_high_computed_count`。単体テスト16件 (高値圏/at-high/低位株/ATH超/精度/yearHigh欠損0負/price欠損0負/型ガード/import)。
- 副次: docstring stale comment「ratios-ttm」→「key-metrics-ttm」修正 (実コードは v177 で hotfix 済)。
- **Evaluator 4層 PASS (pytest 230)**。SPEC `docs/specs/SPEC_2026-06-07_canslim-phase3-conditions.md` を main で tracking 化 (`d240bef`)。

### 本番 populate + Rule5 検証 (このセッションの主成果)
- **着手前チェックで screener_fundamentals が空と判明** → 原因2件を特定:
  1. **commit タイミング**: canslim-scan の nightly step は commit `e987631`(09:55 JST) が当日 nightly(09:14 JST 開始)の **41分後 landed** → 初回 nightly に含まれず、本番で一度も populate されていなかった。「本番検証済」を謳う前に **DB の実行数を直接 SELECT** する規律 (read endpoint の graceful empty は空でも正常応答で気付けない)。
  2. **502→bg完了**: cup/rs/canslim scan は Railway gateway の **5分(300s)タイムアウトで HTTP 502** だが**バックグラウンドで完走しDB書込** (rs 今日2359行)。GHA各stepは continue-on-error で**全体 success に見える** = GHA success≠populate成功、DB直接確認必須。
- 手動 populate (sp500 top500、user 承認) を **2回** 実行 (Sprint2 deploy 前後)。最終 (deploy後): processed=500, eps_computed=485, a_cagr=430, roe=405, **near_high_computed=499**, HTTP200 282s。
- **near_high_pct 実値検証**: min0.218/max0.999/avg0.820、>1.0=0/≤0=0、≥95%=72銘柄。AAPL0.970/KO0.962/GOOGL0.902(高値圏)↔押し目MSFT0.750等↔低位0.218。
- **A条件 sector guard 検証**: JPM/BAC/WFC/C/O/AMT が全て ROE NULL ✅、PLTR/UBER は赤字履歴で CAGR NULL ✅、AAPL ROE146.7/NVDA CAGR201.4 は実値。
- **read C filter が「集計前」→実データ(187銘柄≥18%)に切替確認**。

## 🔴 次セッション最優先: Phase 3 Sprint 3 (S条件 + MINOR3件)
- SPEC §S3。6 sprint: S1✅→S2✅→**【次=S3】→着地で backend 6体合議 gate (user 指示、SPEC §7の「S2着地で」より1つ後ろ)**→S4 read拡張+excluded分割→S5 frontend→S6 本番populate+3体/L4。
- **S3 内容**: ①S条件 buyback_yield (既存 per-ticker 計算 `main.py:1060-1091` を共有 helper 化、二重実装禁止) + 出来高急増 (gate1=+40%独立カラム、ただし schema に volume カラム要確認→無ければ migration 追加 or cup従属、gate1=独立カラム)。②MINOR: 巨大YoY clip (MU+682%/MCHP+418% が C filter に **live**、実値だが §5、上限clip)。③MINOR: cleanup 最新calc_date保護。④MINOR: GHA upsert失敗率warning (`nightly_scan.yml`)。
- **gate1 確定 (Generator に渡す)** (v177 から不変): A=ROE17%単一+広め除外✅ / N=単独可+extended警告(near_high95%)✅ / turnaround採用✅ / **S=出来高+40%独立カラム** / A・S=free・N将来Premium / batch endpoint=Phase4送り。
- 起動: Agent subagent_type=generator、SPRINT_NUMBER=3。**着地後に main 側で本番 dry_run/populate で Rule5 実データ検証** (generator/evaluator は real-data 検証が甘い)。

## 🟢 Supabase MCP 有効化済 (v177 保留 → 本セッションで稼働確認)
- `mcp__supabase__execute_sql` で本番 DB を直接 read/SQL 可能 (project `qwaepmdyybfsnsyitqrm`)。migration 適用は `apply_migration` 可だが **human-in-the-loop 厳守** (SQL提示→user承認→適用)。
- 本番 write (canslim-scan 発火等) は auto-mode classifier が agent判断の prod write を **拒否する** → user 承認 (AskUserQuestion 等) を取ってから実行。CRON_SECRET は `railway variables --json` で取得可。

## 📊 運用メモ
- 明朝 08:07 JST nightly (3000 universe) が canslim を **near_high_pct 込みで初の自動 full populate** (今日の sp500-500 手動分を calc_date 2026-06-08 で上書き)。小型株含むため turnaround 銘柄が出る可能性 → 黒字転換バッジ(S5)の視覚確認素材。
- turnaround は sp500 top500 で 0件 (大型株に稀、妥当)。ロジックは pytest 済。
- A/N 条件は DB populate 済だが **read endpoint 未公開** (col_map 拡張は S4) → 早期露出 Trust Cliff なし。

## 📋 繰り越しバックログ (CAN-SLIM 外・未着手、v177 から継続)
- **Stripe Premium 配線** (launch blocker、pre-release 最重要)。
- **RS scan 健全性確認**: rs_ratings 今日 2359行で健全回復 (v176 の partial 3行問題は解消)。2026-06-06 は3行のまま (その日の partial)。
- count-up「入場時リセット」解消 / 案C(RPO/ARR) / 通期 EPS YoY の正確化 (basis mismatch 抑止中) / nightly pre-warm cron (保有銘柄)。
- **scan 502 の恒久対策**: 3000 universe canslim の async task化/chunk endpoint化 (背景完了が確実でないリスク)。S3 の GHA warning と関連。

## ⚠️ 触ると危険 (CLAUDE.md)
sticky 検索バー / 発光系 (.panel-card/.bs-panel/.surface-card) / VITE_ ARG/ENV 同期 / aggregator への LLM import (pre-commit Check 3) / 既存 migration・既存 endpoint (canslim-scan/scanner/canslim/cup-scan/quarterly-history) / railway.toml cron。

## メモリ更新済
`project_canslim_screener_expansion.md` に Phase3 S1/S2 完了 + 運用知見2件 (502/commit タイミング)。MEMORY.md index 更新済。
