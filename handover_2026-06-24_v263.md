# 引き継ぎ書 v263 (2026-06-24/25) — beat/cfps 全量 populate 完了 / cup Sprint 1 push 済(f99fddf) / 次=beat/cfps Phase 2 (planner SPEC)

> 起床後セッションで前版を更新。成果: **① beat/cfps 全量 populate 完了(6/24, beat 2424/cfps 2374) / ② cup gate Sprint 1 を実装・push(f99fddf) / ③ 次セッション最優先 = beat/cfps Phase 2(backend freshness + frontend、planner SPEC 推奨)**。
> 🔴 **次セッション最優先 = beat/cfps Phase 2 着手。前提(全量 populate)は満たされた**。

## 🧭 30 秒サマリー

| 項目 | 状態 |
|---|---|
| **main / 本番** | `f99fddf`(cup Sprint 1 反映)。deploy 検証は §🔬検証 参照 |
| **populate** | ✅ 6/24 **全量**(2444行 / beat 2424 / cfps 2374)。手動 full scan が遅延完走(下記 ⚠️訂正) |
| **cup Sprint 1** | ✅ 実装 + push 済(`f99fddf`)。視覚 dogfood 未(user 後回し) |
| **beat/cfps Phase 2** | 🔴 **次セッション最優先・着手可能**。backend freshness + frontend(下記) |
| **cup Sprint 2/3** | DEFER(premium applied gate、後) |

---

## ⚠️ 重要な訂正(前版からの更新)
- 前版で「手動 populate 失敗→nightly 待ち」と書いたが、**手動 full scan(11:43 UTC trigger, run 28095956319)の canslim backend は遅延完走しており、実際は 6/24 全量 populate 成功**していた。poll を 48分で打ち切り「失敗」と誤判断したのが原因。
- **教訓: canslim 3000 の完走は >1時間かかる**。次回 populate 監視は poll を長め(90分+)に、または Supabase DB を直接確認して判定する(`/api/scanner/canslim` の as_of proxy でなく `screener_fundamentals` の calc_date を ground truth に)。

---

## ✅ 完了(本セッション = autopilot + 起床後)
1. **migration 2本適用**(`latest_beat`/`cfps_3y_rising` カラム + service_role GRANT + index、Supabase MCP `apply_migration`、ground truth 確認)
2. **cup Premium gate SPEC 起票**(採用確定): `docs/specs/SPEC_2026-06-24_screener-cup-premium-gate.md`(3 sprint, multi-review 3体)
3. **beat/cfps 全量 populate**(6/24, 2444行 / beat true73%・cfps true28% 相当・null 率 <3%、配線正常を ground truth 確認)
4. **cup Sprint 1 実装 + push**(`f99fddf`): `CustomScreenerPanel.jsx` に cup lock crow を new_high_break preset へ追加(件数不変・C-2 安全・build PASS)

---

## 🔴 次セッション最優先: beat/cfps Phase 2
**前提 ✅ 満たされた(全量 populate 済)。planner で SPEC 起票推奨**(backend+frontend にまたがるため)。

### 隠れ前提(handover A の補正・最重要)
- **backend 追加が必要**: `_build_universe_payload`(main.py:20160)の `freshness` map に `latest_beat`/`cfps_3y_rising` を追加(screener_fundamentals の calc_date)。
- ⚠️ **現状 freshness map に beat/cfps の key が無い**(funda/ocf_margin/ocf_gt_netincome/rs/cup/pivot_distance/ad_volume/breakout/funda_pass のみ)。frontend で CROW_BINARY_META に足しても **L1028 `if(!universe.freshness[meta.freshness]) return null` で crow 非表示**になる。これが Phase 2 の隠れた blocker。

### frontend(cup Sprint 1 = commit f99fddf と同パターン)
- `PRESET_CONDS` に `latest_beat`/`cfps_3y_rising` 述語(`kind:'flag'`, `pass: item.X === true`)
- `CROW_BINARY_META` に追加(label/th:null/freshness:'latest_beat'|'cfps_3y_rising'/**locked なし** = tier 非依存)
- `CROW_LAYOUT`(品質 group が妥当) + `PRESET_DISPLAY_CONDS` に追加
- ⚠️ beat/cfps は cup と違い **locked でない**(全ユーザーに値が来る)→ lock crow 経路でなく freshness 経路。freshness が無いと出ない(上記 backend 前提)。

### 設計判断(planner で詰める)
- **gate vs 任意トグル**: gate(常時ON・件数絞る)にすると earnings_pass 等の件数が変わる。推奨=**まず任意トグル条件(件数デフォルト不変・SAFE)**、gate 昇格は user 判断。
- **どの preset**: beat/cfps は決算・収益の質 → `earnings_pass` が意味的整合(要設計)。
- 検証: ローカル build + CI dogfood(`screener_v2_dogfood.yml`)。

---

## ⏸️ その他残タスク
- **B. cup Sprint 1 視覚 dogfood**(user 後回し): push 済。`?screener_v2=1` → 「絞り込み」→ `new_high_break` preset → 「カップ・ウィズ・ハンドル」lock crow が1度だけ出て件数不変を確認。snap は B-2 用で timeout したため未検証(本番手動 dogfood)。
- **C. cup Sprint 2/3**(premium applied gate、後): SPEC §5。Sprint 2 で `countPreset` を tier-aware 化(C-2 死守)。premium 検証は DOGFOOD_TEST secrets or user 手動 dogfood。

---

## ⚠️ 触ると危険 / 規律(継続)
- **CustomScreenerPanel C-2**: count(countPreset/PRESET_PREDICATES.extra/applyStrategyImpl)と list は同一述語・同時変更。
- **嘘の南京錠回避**: freshness 無し / 未 populate のフィールドを applied gate にしない。
- **発光系禁止**(`.panel-card/.bs-panel/.surface-card`)/ sticky 検索バー / VITE_ ARG/ENV 同期。
- **投資色ルール**(上昇=緑/下落=赤/緊急=amber/cyan=ブランド色)。gate「必須」pill は gold。
- **screener_v2 scope に閉じる**(legacy 不変・default OFF・`?screener_v2=1` opt-in)。
- **実装は main が直接**(委託は調査・多視点レビューのみ)。`railway up` 使わない(deploy = main push)。
- **環境制約(環境依存)**: 本セッションはローカル(darwin)で egress OK(本番 curl/Supabase MCP/apply_migration 可)。次セッションが remote 環境なら本番 URL/GitHub artifact が CONNECT 403 → 検証は CI dogfood 経由必須。

---

## 🔬 検証 (cup Sprint 1 deploy) — ✅ 完了
- push: `e5bde02 → f99fddf` 完了。
- deploy: 本番 commit `f99fddfd787b` 反映確認(Railway auto-deploy ~3分)。
- bundle: 本番 `CustomScreenerPanel-CVe16e-B.js` に cup tooltip「ベース完成からのブレイク初動の型」を grep 確認 = **cup Sprint 1 は本番反映済**。
- ⚠️ 視覚 dogfood は未(user 後回し): `?screener_v2=1` → 「絞り込み」→ `new_high_break` → cup lock crow の見た目確認は次回。

## 🧹 掃除
- handover v262 は削除済(commit f99fddf)。本 v263 が最新。
- 未 commit(tracked): なし(cup Sprint 1 push 済、本 handover は別途 commit)。
- untracked: docs/specs/*.md(SPEC reference)、frontend/scripts/snap-*.mjs(visual harness、使い捨て)。
