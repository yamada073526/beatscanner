# 引き継ぎ書 v180 (2026-06-07)

autopilot 自律セッション。handover v179 最優先の **Sprint 4b (read endpoint A/N/S 公開)** を main 直接実装 → 本番 Rule5 全 PASS で着地。次は **Sprint 5 frontend** だが design/Trust Cliff 判断が必要なため **DEFER-SPEC** (user 承認待ち)。

## 🎯 完了済み (merge+deploy+本番Rule5検証済)

### Sprint 4b — read endpoint A/N/S 公開 (main `ab001b0`、autopilot 自律)
- **実装** (`_fetch_screener_fundamentals_by_condition`、main.py): col_map に **eps_cagr→eps_cagr_3y / roe→roe / near_high→near_high_pct_scaled / buyback→buyback_yield_pct / volume_surge→volume_surge_pct** を追加。⚠️ SPEC §5 本文の col 名 (`near_high_pct`/`buyback_yield`) は S4a 方式B で **stale**、新 scaled カラムが正 (handover v179 の corrected mapping 採用)。
- **BLOCK④ count integrity**: 達成件数を `count="exact"` で正本化 (`len(items)` の Supabase 1000 行上限を回避、failed_count 水増し防止)。
- **excluded 分割**: NULL∩turnaround=true→`uncomputable_count` / 残り→`unavailable_count`、旧 `excluded_count` は後方互換維持 (== uncomputable+unavailable の不変条件)。
- helper arity 3→6、単一 call site 更新。C(eps_yoy) response 後方互換 (frontend が読む total/failed/excluded/items/as_of 不変、新 key は additive)。frontend api.js JSDoc も更新。
- **実装判断**: SPEC §5 の stale col 名を generator に渡す誤読リスク + backend 局所完全把握済のため、autopilot で **main 直接実装 + 厳格検証** を選択 (worktree 不使用 = main session 自身の serial 実装、S3 事故=generator の main 実装とは別カテゴリ)。
- **検証**: import OK / pytest **289** (新規 S4b 9件 = fake supabase で stale col 回帰検出 + 分割不変条件 + count integrity) / frontend build OK / grep 構造一致。

### 本番 Rule5 検証 (全 PASS)
| 条件 | total_count | 備考 |
|---|---|---|
| near_high≥95 | **72** | GL 99.9 / ELV / PFG (scaled pct 正) |
| buyback≥2 | **200** | CHTR 28.7% / CRM / IT (pct 正) |
| roe≥17 | **193** | CLX / STX / CL (messy MCD -434 は filter 除外) |
| eps_cagr≥25 | **79** | GE / PODD / CRM |
| volume_surge≥40 | **52** | LULU / COO / CMG |
- **count integrity**: near_high&min_pct=0 → total499/failed0/excluded0 = universe499 (頭打ちなし)。
- **C後方互換**: 必須key欠落なし / excluded(14)==uncomputable(0)+unavailable(14)。
- **messy データ benign**: 今日 MCD roe=-434 が DB live (None-preserve) だが roe≥17 filter で除外、read に混ざらず (v179 予測どおり)。明朝 fresh nightly で clean 化。

## 🔴 次セッション: Sprint 5 frontend (A/N/S 表示) — ⚠️DEFER-SPEC (user 判断必須)
read endpoint は素地 (uncomputable_count/unavailable_count/as_of) を S4b で提供済。S5 は以下の **design/Trust Cliff 判断が絡むため autopilot では ship せず申し送り**:
1. **結果行内バッジ列** (各柱 max4、chip 増殖しない) — バッジ layout は design 判断。
2. **`null_reason` 色分け** (amber=uncomputable=計算不可 / gray=unavailable=欠損) — ★**新 design token (色 taxonomy) の確定が必要** (CLAUDE.md「触ると危険」)。
3. **N=Premium ロック面の価値言語化** — Trust Cliff + pricing 判断。
4. **AAPL型ROE §5補完**: ROE単独を「優良」ラベルにしない (金融 option b)。AAPL(146.7、正小資本) は S4a guard 捕捉外のため S5 display で抑止。
5. 黒字転換 halo (中小型 universe 拡大後、sp500 は0件) / as_of「X日前」timeAgo。
- 固有名詞 (O'Neil/CAN-SLIM) を UI に出さない (Phase1 hotfix 踏襲)。

## 🟡 重要知見 (永続化済)
- **None-preserve 検証規律** (v179 から継続): 同一 calc_date re-populate で「値→None」遷移が DB 反映されない。guard 発火は count 減少で確認、同日 DB 値で早合点しない。明朝 fresh nightly (08:07 JST) で MCD roe=NULL/旧 ratio カラム=NULL に clean 化。
- **RS scan 健全性確認 (継続 monitoring、今回 autopilot 確認済)**: rs_ratings=2359行 / screener_fundamentals=499行 (両方 2026-06-07、健全)。6/6 の partial 3行 degradation は解消継続。**次セッションも DB 直接 SELECT で継続確認** (GHA success≠populate成功、502→bg完了の運用知見)。
- SSOT: memory `project_canslim_screener_expansion.md` に S4b 全記録追記済。

## 📊 残バックログ (Phase4、council 由来)
- **@dataclass ScanResult** (tuple 9 要素を dataclass 化) / generator worktree assert を SKILL 明文化 / helper を aggregator 移設 (HG Check3 保護) / scan async・chunk 化 (3000 universe 502) / A/N/S batch endpoint / None-preserve に sentinel (computed-None vs not-provided 区別)。**いずれも nightly populate path の blast radius があり、scan を回さず検証不可 → 無監視 ship 非推奨**。
- **Phase4 (PMF/Stripe後)**: I条件(13F機関保有) / Part B 買値登録→損切利確自動push。

## 📋 繰り越しバックログ (CAN-SLIM 外)
- **Stripe Premium 配線** (launch blocker、pre-release 最重要、N=将来Premium gate 実体化にも必要)。
- count-up「入場時リセット」解消 / 案C(RPO/ARR) / 通期 EPS YoY 正確化 / nightly pre-warm cron (保有銘柄)。

## ⚠️ 触ると危険 (CLAUDE.md)
sticky検索バー / 発光系(.panel-card/.bs-panel/.surface-card) / VITE_ ARG/ENV同期 / aggregator への LLM import(pre-commit Check3) / 既存 migration・既存 endpoint(canslim-scan/scanner/canslim/cup-scan/quarterly-history) / railway.toml cron / **新 design token (色 taxonomy) の確定** (S5 の null_reason 色分けで該当)。

## メモリ更新済
`project_canslim_screener_expansion.md` に S4b 着地記録追加。MEMORY.md index も S4b まで更新。
