# handover 2026-06-26 v274 — 決算期ガード migration 本番適用完了 + screener P2 方針確定 + scan stale 発見

> 前 session (v273) の最優先 = PR #25 の決算期ガード migration を本番適用 → **完了**。
> 加えて screener 改修 (P2 方針確定) を 1 件着地し、その過程で **本番 scan の重大な既存問題 (issue #27)** を発見。

## ✅ このセッションで完了

| 項目 | 内容 | 検証 |
|---|---|---|
| migration 本番適用 | `screener_fundamentals.last_report_date` (text) + index + service_role GRANT | Supabase へ実クエリで column/index/GRANT 確認 (curl 経由・MCP routing caveat あり↓) |
| PR #26 merge | `.mcp.json` + `docs/references/mcp_supabase_setup.md` (SSOT) | main 反映済 |
| PR #25 merge | seasonchip + 決算期混同ガード (conflict は統合版で解決) | CI green / 本番 deploy 済 (Railway ACTIVE = 17cfb40) |
| last_report_date populate 実証 | nightly_scan を canslim_only=true で手動 trigger → **上位 80 銘柄 80/80 populate** (calc_date≠last_report_date を実データ確認) | Supabase 実クエリ |
| PR #29 (本 handover と同梱で merge) | screener `sector_leader` の隠れフィルタ解消 (適用済 grade eps_yoy/eps_cagr/rs を DISPLAY_CONDS に追加・表示専用・件数不変) | build pass / sub-agent review PASS / AUDIT 追記 |

## 🔴 最優先 — 次セッション着手候補

### A. screener 改修の続き (件数実測不要・issue#27 非依存・すぐ着手可)
- **hot_sector の隠れフィルタ疑い** (`docs/specs/AUDIT_2026-06-25_screener-mockup-implementation-divergence.md` の「2026-06-26 追記」§残 参照): `PRESET_PREDICATES.hot_sector.grades` は eps_yoy/eps_cagr/roe/rs の grade 閾値を適用するが `PRESET_DISPLAY_CONDS.hot_sector` は `funda_pass` のみ。funda_pass は binary flag で grade 閾値を保証しない疑い。**funda_pass の backend 定義 (5条件が grade 閾値と一致するか) を確認**し、一致なら現状維持・不一致なら sector_leader と同じ表示専用 fix。
- sector_leader `inst_holders_qoq_pct` の dead 表示整理 (binBindings 未登録で renderCrow null)。

### B. issue #27 (重大・別タスク・並行可) — 本番 scan が gateway 502 で stale
- full universe (russell3000/3000) の nightly_scan が Railway gateway 5分 502 で完走せず persist 0 → screener 全体 (RS/canslim/cup/last_report_date) が 2 日 stale。**screener 改修と並行で別セッション進行を user 希望**。
- 詳細・修正案・受入基準は **GitHub issue #27** に self-contained で記載。小 universe (80) は回避可と実証済。
- 暫定: nightly universe を gateway-safe な小値に縮小する選択肢あり (issue #27 修正案3)。

## 🟡 残 (user 承認 / 件数実測 gated)
- P1-b の grade **除去** (eps_cagr/eps_yoy/inst gate化/mcap cap) は件数を変える → issue #27 の scan 復旧後に本番件数実測 → user 承認。
- PR #25 Sprint 4: seasonchip「主に」除去は本番 verify 後 (screenerV2 default OFF のため未到達)。

## ⚠️ 重要な運用知見 (このセッションで判明)
- **Supabase MCP routing caveat**: egress をセッション開始**後**に開けた場合、その既存セッションでは MCP tool が `Failed to execute SQL query` で失敗するが **curl (同一 Management API) は 201 で通る** (Node undici が HTTPS_PROXY 非使用)。fresh session (egress 最初から開) で MCP が動くか要確認。curl fallback は `docs/references/mcp_supabase_setup.md §9` に検証済コマンド。
- **委託は必ず main が裏取り**: 本セッションで調査 sub-agent が screener P2-d を「Trust Cliff」と誤報 → main の直接 grep で「実際は honest」と訂正。報告≠事実を実証。

## ⚠️ 触ると危険 (継続)
- screener `extra` オブジェクト 5 箇所重複構築 (新次元は 5 箇所 + deps 全同期)。表示専用変更は extra に folding しない。
- `PRESET_DISPLAY_CONDS` は表示専用 (件数 count==list 不変)。`PRESET_PREDICATES`/`itemPasses`/`buildActiveGrades` が件数 SSOT。
- `_compute_one` tuple arity = 19 (last_report_date 追加済)。発光系 / sticky 検索バー / VITE_ ARG/ENV 同期 / aggregator の LLM import 禁止。

## ⚙️ 環境メモ
- deploy: `git push origin main` → Railway auto (本番 ACTIVE commit は /health の commit で確認)。session から本番 URL は egress 403 (api.supabase.com のみ許可済・Railway URL 未許可)。
- canslim-scan は GitHub Actions `nightly_scan.yml` (UTC 23:07 定時 / workflow_dispatch で universe_size・canslim_only 入力可)。Railway cron ではない。
