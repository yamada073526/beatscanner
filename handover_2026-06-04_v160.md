# BeatScanner Handover v160 — スクリーナ Phase 2 (sector/mcap filter) + 朝 dogfood 4 修正を本番 LIVE (2026-06-03→04)

> autopilot (就寝中) で screener 絞り込み Phase 2 を ship → user 起床・dogfood → 改善 4 件 (D1/D3/D4/B) を
> 実装・査読・deploy・検証まで完遂。 v159 を本 v160 で置換。
> **現本番 bundle: `index-BAEGWfAV.js`**。 chunks: CustomScreenerPanel-JpnBt9q5 / DiagramCard-BSEY49tC / ScreenerPane-CNihAe2P / JudgmentDetail-DJI7_g8I。

## 🟢 本番 LIVE (全て検証済 = build+grep+本番curl)

### スクリーナ絞り込み Phase 2 (commit d056df6、 3体合議 SHIP)
- backend `GET /api/screener/universe-meta` (純データ・LLM 非経由・24h cache): universe 全銘柄の `{sector, mcapBand}`。 本番 count=2449 / mega897・mid949・small603。
- frontend: RS スクリーナに折りたたみ filter panel (sector top6 data-driven+その他 / mcap帯 大型$10B+/中型$2-10B/小型<$2B、 各 count + 基準日 tooltip)。 複数選択 (sector AND mcap、 各次元内 OR)、 0件明示。
- memory: [[feedback_facet_filter_count_integrity]] (facet count = filter predicate 同一集計、 Trust Cliff)。

### 朝 dogfood 改善 4 件 (commit 6433a94 / 517b78a)
- **B (Cup 絞り込み拡張)**: RS の絞り込みを `useMetaFilter` hook + `MetaFilterPanel` component に抽出し RS/Cup 共有。 Cup タブ (216件) に同 filter 配線。 masked 銘柄は ticker 不可視で filter 中自動除外、 Premium teaser 維持。 ⚠️ scope = RS+Cup のみ (O'Neil/breakout/both は低件数 or mask で見送り、 user 合意)。
- **D4 (Trust Cliff)**: 図解「次Qガイダンス」 でソースに数値ガイダンス記載なし (NKE 型) のとき、 旧 amber「抽出精度が不足」=アプリ不具合 の誤読。 `hasAnyStructured=false` を 3 分岐目で中立 info 化 (「企業が次期の数値見通しを非開示とする場合があります」)、 精度chip も数値ゼロ時非表示。 §38 サブエージェント査読で「(当アプリの抽出漏れではありません)」=§5 不実証リスク指摘 → 企業行動の事実記述に修正済。
- **D1 (Hero chip 整列)**: ScreenerPane の Hero 3 列で description 行数差により銘柄チップ開始 y がズレ。 description を 2 行 clamp + min-height で揃え + Cup の冗長 description 短縮 (§38 disclaimer 維持、 全文 title hover)。
- **D3 (図解 skeleton 巻き戻し)**: loading→Suspense fallback の別 instance remount で narration が stage0「決算データを集めています」 に巻き戻る。 開始時刻を親 (StickyDiagramAccordion) に lift し両 skeleton instance で共有 → 単調進行。

## 🟢 russell3000 nightly scan = LIVE 化 (今朝 手動 dispatch で確定)
- GitHub Actions nightly (cron 7 23 UTC) は scheduled 遅延で未発火だったため手動 `gh workflow run nightly_scan.yml` で起動・完走。
- RS universe **989 → 1699** に拡大 (russell3000 効果確定)。 ⚠️ universe-meta は 2449、 RS rated は 1699 = 差 ~750 は **6ヶ月 RS history を持たない新規/低流動 ticker が skip** (期待挙動)。 full 2449 coverage 化は別 backend task (RS history 要件緩和、 優先度低)。

## 🔴 user 判断待ち — D2 (planner SPEC 完成、 gate 待ち)
**SPEC: `docs/specs/SPEC_2026-06-04_screener-pane2-relocation.md`** (commit 879d8f9)。 user 要望:
1. スクリーナーをペイン2へ移設 (結果クリック→ペイン3分析で検索結果が消える問題の解消)
2. ペイン3分析ページにウォッチリスト追加ボタン (現状ペイン2 watchlist からのみ)
- **planner 推奨 = 案B (master-detail)**: `ScreenerPane.handleSelect` の `setActiveTab('home')` を**外して** tab 離脱させない → 結果 state (filter/sort/scroll) 維持。 案A (丸ごとペイン2移設) は Pane 責務大改修で 6体。
- 工数 ~2.0-2.5 人日、 3体合議。 backend/LLM/DB 不変で frontend 局所 (revert 容易)。
- **要 user 確認 6 点** (SPEC §要確認): 採用案 A/B / Hero 3 セクションの置き場所 / WL ボタンの形 (単一 ticker 即追加 Chip 推奨) / 設置位置 / WorkspaceScreenerModal の扱い / 3体で進めるか。

## ⚠️ 触ると危険 (継続)
- 発光系 (`.panel-card/.bs-panel/.surface-card`) / sticky 検索 / VITE_ ARG/ENV / aggregator LLM import 禁止 / 「じっちゃま」UI / DailyDigest summary flex。
- DiagramCard: outer wrapper overflow:hidden / 新規 `<style>` / 新規 glow host 禁止。 図解末尾クリップ max-height 11000px (新 section 追加時実測再確認)。
- **GuidanceSection の 3 分岐** (narrative_only / !hasAnyStructured / low): 文言は §38/§5 査読済、 安易に変えない。
- **MetaFilterPanel / useMetaFilter** は RS/Cup 共有。 片方の挙動を変えると両方に波及。 filter reset は条件付き render (unmount) 前提。

## 🟡 残バックログ
- D2 実装 (上記 gate 後)。 RS full universe coverage (1699→2449、 history 要件)。
- ⑩ 全銘柄 rollout (§38 gate) / LP ガイダンス unlock / release 準備 (**Stripe Premium 配線=Trust Cliff blocker** / 法務 / QA e2e)。
- Supabase 実容量 SQL 計測 (russell3000 で 3x 行) / global coverage / ETF保有 / vision 70+ 構造案。
- 古い handover (v30〜v158 大量残存) 掃除 (user 承認後 一括削除可)。

## release status
pre-release。 Premium/Signature は LP「近日公開」。 Pro¥980 のみ実購入可。 AI図解 (案A+FMP①②③④⑤) + russell3000 screener + RS/Cup sort/filter が Premium 訴求の核。
