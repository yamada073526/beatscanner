# AUDIT — 銘柄分析ペイン3 mockup 忠実度監査 (2026-07-01)

> mockup-fidelity skill Phase B。正本 = `pane3-full-v5.html`（全体）+ `pane3-technical-buyzone-v6.html`（③テクニカル章のみの改善案）。
> 分類: **F**=実装を mockup へ戻す（事故 drift）/ **M**=mockup 側が誤りで実装維持 / **N**=新規実装 / **P**=課金 gate 設計 / **I**=意図的保全。
> 各 drift は 4体並列サブエージェント監査 → main が ground-truth で独立裏取り（報告≠事実）。

## 正本 mockup
| 範囲 | mockup |
|---|---|
| ペイン3 全体 | `docs/specs/mockups/pane3-full-v5.html` |
| ③テクニカル章のみ | `docs/specs/mockups/pane3-technical-buyzone-v6.html` |

統合 mockup（v5 のテクニカル章を v6 で差し替えた単一正本）作成は **監査後**に判断（user 合意）。

---

## ③テクニカル・買い場 — gap 分析（main 裏取り済 ✅）

⚠️ サブエージェントは StockPriceChart を「構造のみ」確認で **gap を過大評価**していた。以下は main が grep/curl で訂正した ground-truth。

| 要素 | 当初 agent 主張 | ground-truth（main 裏取り） | 結論 |
|---|---|---|---|
| pivot ライン | 新規・高優先 | `StockPriceChart.jsx:172-211,552-586` に ReferenceLine + tooltip 実装済 | **既存** |
| buy zone 帯 | なし・高優先 | 同ファイル L586 で `ReferenceArea`/`ReferenceDot` 派生 props 抽出済 | **ほぼ既存**（要描画確認） |
| backend `cup_handle.pivot` | 要確認 | endpoint が `pivot{price,date}` + `pivot_distance_pct` + `box_support` + `ad_volume_ratio` を返却 | **データ源あり** |
| PriceLadder 配置 | 独立・再構成中規模 | `JudgmentDetail.jsx:818,833` で チャート+PriceLadder の 1ユニットに統合済 | **統合済** |
| buy zone 色（§38）| 緑なら是正要 | v6 mockup は `#94a3b8`(neutral グレー)・緑не使用 | **§38 既に安全** |

### v6 の「本当の新規 gap」（mockup L274-278 由来）= 買いゾーン状態の累進開示
- **未検出銘柄**は「— 未検出（対象外）」表示（MSFT/GOOG/NVDA 非表示・cup scanner=0 と整合・正しい挙動）
- **「ブレイク待ち→ブレイク確認」遷移**で初めて「↓ 買値 −8% 損切り」+ ブレイク確認ゾーン（Pivot〜+5%）強調が出現
- **ブレイク前に損切りラインを見せない**（「下落余地=買い場」の押し目買い誤読をじっちゃまプロトコルが否定）
- 未確認: 「押し目・リスク指標 4 tiles」（52週高値距離 / pivot 距離 / ATR / 出来高トレンド）の UI 有無 — データは backend にあり（pivot_distance_pct / ad_volume_ratio 等）

→ **③テクニカルは想定より軽い**。新規実装の核は「buy-zone 状態の累進開示」+「4 risk tiles（要確認）」。pivot/帯/データ/配置は流用可。

---

## ①決算 — 確定済み ground-truth（裏取り済 ✅）

| # | 箇所 | 事実 | 分類 |
|---|---|---|---|
| 1 | 5条件 充足数バッジ | 全 pane3 mockup（v1/v2/v4/v5/ws）で「2/5 合致」はタイトル右 inline（左寄せ）。実装も忠実 → **drift でない** | （対象外） |
| 2 | 来期コンセンサス 色 | mockup は来期を緑（§38 違反）/ 実装は色なし（正） | **M**（mockup を直す・実装維持） |
| 3 | 来期コンセンサス 表示方法 | レイアウト差（agent 結果待ち） | F?（結果待ち） |
| 4 | 良い決算の連続回数 | mockup にあり・実装に UI なし。backend は **PR #117 = OPEN draft（未 merge）** に存在 | **N**（backend merge + frontend 配線） |

---

## L0/判定 — 受領 + main 裏取り済 ✅

実コンポーネント = `sections/L0IdentityBand.jsx`（border=`var(--border)` neutral / bg=`var(--surface-1)` flat）。

| # | 箇所 | mockup | 実装 | 分類 | 備考 |
|---|---|---|---|---|---|
| 1 | L0 ゴールド縁取り | gold border + shadow | neutral border | **要 user gate** | ⚠️ agent 根拠 commit `a1f9c3e` は**捏造**（git に存在せず）。ただし memory `feedback_gold_accent_continuity`（gold 全 panel 一貫でないと noise）が実在 → I 寄りだが事故と決め打ちしない |
| 2 | L0 glass 質感 | glass グラデ | flat | 要確認 | `design_recipes §C-2` 入れ子 surface 禁止と非衝突か |
| 3 | 1w/1m チップ枠 | `.chip` 枠 | プレーンテキスト | **F** | `Chip.jsx` primitive 適用漏れ |
| 4 | WL 追加ボタン配置 | 右上 | 価格行右端 inline | **F** | 配置のみ |
| 5 | 判定カード ゴールド縁取り | gold | neutral | **要 user gate** | #1 と同根拠 |
| 6 | 相場地合いサマリー | 3 セル grid | 1 行 + バッジ | **F** | §38 文言は実装維持 |
| 7 | RS 数値 | 数字 + ゲージバー | ラベルのみ | **F** | RS 閾値ロジック不変 |
| 8 | 最終更新「X 分前」 | あり | L0 に見当たらず | **F?** | CLAUDE.md「動的データに最終更新併記」と整合・要確認 |

**注**: gold(#1,#5) は agent の捏造 commit を破棄。git 証拠なし + memory 原則あり → **Phase C で user gate**。

## ⑤その他 — 結果待ち 🔄
（サブエージェント completed・drift 表を通知受領後にここへ統合。市場の声 Free/Pro gate = P・Trust Cliff 観点 funnel-cro 経由）

---

## 次フェーズ（Phase C）
- 全 drift 確定後 → SPEC 化 + Sprint 分割
- `multi-review` 合議対象: 市場の声 Pro gate（funnel-cro + §38）/ 発光復活（postmortem）/ ③テクニカル累進開示の §38
- 実装は新セッション（大ファイル StockPriceChart 1907行・JudgmentDetail 1000+行）
