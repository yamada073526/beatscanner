# AUDIT — 銘柄分析ペイン3 mockup 忠実度監査 (2026-07-01)

> mockup-fidelity skill Phase B。正本 = `pane3-full-v5.html`（全体）+ `pane3-technical-buyzone-v6.html`（③テクニカル章のみの改善案）。
> 分類: **F**=実装を mockup へ戻す（事故 drift）/ **M**=mockup 側が誤りで実装維持 / **N**=新規実装 / **P**=課金 gate 設計 / **I**=意図的保全。
> 各 drift は 4体並列サブエージェント監査 → main が ground-truth で独立裏取り（報告≠事実）。

## 正本 mockup
| 範囲 | mockup |
|---|---|
| ペイン3 全体 | `docs/specs/mockups/pane3-full-v5.html` |
| ③テクニカル章のみ | `docs/specs/mockups/pane3-technical-buyzone-v6.html` |

統合 mockup（v5 のテクニカル章を v6 で差し替えた単一正本）作成は **監査後**に判断（user 合意）。

---

## ③テクニカル・買い場 — gap 分析（main 裏取り済 ✅）

⚠️ サブエージェントは StockPriceChart を「構造のみ」確認で **gap を過大評価**していた。以下は main が grep/curl で訂正した ground-truth。

| 要素 | 当初 agent 主張 | ground-truth（main 裏取り） | 結論 |
|---|---|---|---|
| pivot ライン | 新規・高優先 | `StockPriceChart.jsx:172-211,552-586` に ReferenceLine + tooltip 実装済 | **既存** |
| buy zone 帯 | なし・高優先 | 同ファイル L586 で `ReferenceArea`/`ReferenceDot` 派生 props 抽出済 | **ほぼ既存**（要描画確認） |
| backend `cup_handle.pivot` | 要確認 | endpoint が `pivot{price,date}` + `pivot_distance_pct` + `box_support` + `ad_volume_ratio` を返却 | **データ源あり** |
| PriceLadder 配置 | 独立・再構成中規模 | `JudgmentDetail.jsx:818,833` で チャート+PriceLadder の 1ユニットに統合済 | **統合済** |
| buy zone 色（§38）| 緑なら是正要 | v6 mockup は `#94a3b8`(neutral グレー)・緑не使用 | **§38 既に安全** |

### v6 の「本当の新規 gap」（mockup L274-278 由来）= 買いゾーン状態の累進開示
- **未検出銘柄**は「— 未検出（対象外）」表示（MSFT/GOOG/NVDA 非表示・cup scanner=0 と整合・正しい挙動）
- **「ブレイク待ち→ブレイク確認」遷移**で初めて「↓ 買値 −8% 損切り」+ ブレイク確認ゾーン（Pivot〜+5%）強調が出現
- **ブレイク前に損切りラインを見せない**（「下落余地=買い場」の押し目買い誤読をじっちゃまプロトコルが否定）
- 未確認: 「押し目・リスク指標 4 tiles」（52週高値距離 / pivot 距離 / ATR / 出来高トレンド）の UI 有無 — データは backend にあり（pivot_distance_pct / ad_volume_ratio 等）

→ **③テクニカルは想定より軽い**。新規実装の核は「buy-zone 状態の累進開示」+「4 risk tiles（要確認）」。pivot/帯/データ/配置は流用可。

---

## ①決算 — 確定済み ground-truth（裏取り済 ✅）

| # | 箇所 | 事実 | 分類 |
|---|---|---|---|
| 1 | 5条件 充足数バッジ | 全 pane3 mockup（v1/v2/v4/v5/ws）で「2/5 合致」はタイトル右 inline（左寄せ）。実装も忠実 → **drift でない** | （対象外） |
| 2 | 来期コンセンサス 色 | mockup は来期を緑（§38 違反）/ 実装は色なし（正） | **M**（mockup を直す・実装維持） |
| 3 | 来期コンセンサス 表示方法 | レイアウト差（agent 結果待ち） | F?（結果待ち） |
| 4 | 良い決算の連続回数 | mockup にあり・実装に UI なし。backend は **PR #117 = OPEN draft（未 merge）** に存在 | **N**（backend merge + frontend 配線） |

---

## L0/判定 — 受領 + main 裏取り済 ✅

> ⚠️ **2026-07-01 訂正 (C10 事件)**: 下表の #3-8 は当初、実在しない phantom component `sections/L0IdentityBand.jsx` を根拠に「mockup の状態」まで hallucinate して分類していた。実コンポーネントは `Hero.jsx`（border=`var(--border)` neutral / bg=`var(--surface-1)` flat）。正本 mockup v5 を ground-truth で再照合した訂正版は [`AUDIT_pane3-L0-fidelity_2026-07-01.md` §1](AUDIT_pane3-L0-fidelity_2026-07-01.md#1-監査台帳-c10-l0-3-8-の-ground-truth-訂正) 参照。#3-8 は訂正後の内容に置換済み（本表）。

| # | 箇所 | mockup | 実装 | 分類 | 備考 |
|---|---|---|---|---|---|
| 1 | L0 ゴールド縁取り | gold border + shadow | neutral border | **要 user gate** | ⚠️ agent 根拠 commit `a1f9c3e` は**捏造**（git に存在せず）。ただし memory `feedback_gold_accent_continuity`（gold 全 panel 一貫でないと noise）が実在 → I 寄りだが事故と決め打ちしない |
| 2 | L0 glass 質感 | glass グラデ | flat | 要確認 | `design_recipes §C-2` 入れ子 surface 禁止と非衝突か |
| 3 | 1w/1m チップ枠 | **plain text**（`.chip` 枠ではない） | プレーンテキスト | **no-op（訂正）** | 台帳が誤り。mockup 側も plain text（v5 L289 `<span class="mini">`）→ drift なし |
| 4 | WL 追加ボタン配置 | **row2 = id行の下**（右上ではない） | row2（本 sprint 修正済） | **F（着地済）** | 訂正: mockup は右上でなく row2（v5 L291）。実装は「右上/id-meta → row2」へ修正済 |
| 5 | 判定カード ゴールド縁取り | gold | neutral | **要 user gate** | #1 と同根拠 |
| 6 | 相場地合いサマリー | **L0 に無し**（地合いは §③ `.plchip` pill のみ） | 1 行 + バッジ | **やらない（訂正）** | mockup に該当 UI 自体が存在しない新規機能要求だった → user 決定「やらない」 |
| 7 | RS 数値 | **数字 badge のみ**（ゲージバーは無し） | ラベルのみ | **やらない（訂正）** | mockup にゲージバーは無い（v5 L305 `.rs-badge`）→ user 決定「やらない」 |
| 8 | 最終更新「X 分前」 | **mockup 全体に 0 件**（mockup 根拠なし） | 追加済（本 sprint） | **F（着地済・根拠は CLAUDE.md ルール）** | mockup 忠実化でなく CLAUDE.md「動的データに最終更新併記」ルール根拠で追加 |

**注**: gold(#1,#5) は agent の捏造 commit を破棄。git 証拠なし + memory 原則あり → **Phase C で user gate**。

## ⑤その他 — 監査完了 ✅（サブエージェント監査 → main が grep 独立裏取り済）

⑤「その他」章 = `JudgmentDetail.jsx` が束ねる 5 fold。各 fold の実装と v5 mockup（L566-636）の drift を下表に確定。全 file:line は main が grep 裏取り済（報告≠事実）。

### 5-1. アナリスト視点（`AnalystPanel.jsx` / fold 親 = `MarketEvalSection.jsx`）
| # | 観点 | mockup | 実装（裏取り済 file:line） | 分類 | 備考 |
|---|---|---|---|---|---|
| a1 | 初期展開 | `<details open>`（唯一の初期 open） | `defaultOpen={false}`（`MarketEvalSection.jsx:68`） | **F?（要 user 判断）** | analyst は @no-llm prefetch cache（`MarketEvalSection.jsx:19`）ゆえ open 可能。但し「2秒でわかる」原則① vs 全 fold 一律 collapsed の統一感 → Phase C 判断 |
| a2 | 目標株価レンジバー/レーティング積み上げ/モメンタム/格付け変更 | 各 UI あり | `TargetPriceRangeBar`/`RecommendationStackedBar`/`MomentumKpi`/`RatingChangesTimeline` 実装済（`AnalystPanel.jsx:440-458`） | **I** | 構造存在を確認。内部 visual（◇マーカー形状・色トークン）は **未確認**（Phase D 着手時に diff） |
| a3 | 課金 gate 文言「🔒 Free は機関名を伏せ字。Premium で全表示・鮮度 N日前」（**gold 常時可視**） | gold 色テキスト常時表示 | 伏せ字ロジックは実装済（`isPro = canUse('analyst_estimates', plan)` → firm 頭文字+`*`、`AnalystPanel.jsx:216-223`）。但し gate 説明は **`title` 属性 tooltip のみ**（`title={isPro ? '' : 'Pro でフル firm 名を表示'}` `:233`／鮮度も title 内 `:300`） | **P＋F** | gate 機能(P)は動作。**常時可視の説明文が無く hover 限定**＝touch/mobile で「なぜ伏せ字か」「Premium で何が得られるか」が不可視 = **Trust Cliff**。F=常時可視文言の追加 |

### 5-2. 市場の声（fold 親 = `MarketEvalSection.jsx` / 本体 = `InsightsPanel.jsx`）
| # | 観点 | mockup | 実装（裏取り済 file:line） | 分類 | 備考 |
|---|---|---|---|---|---|
| b1 | summary 文言 | 動的「強気・直近ニュースの論点」 | 固定「直近ニュースの論点要約」（`MarketEvalSection.jsx:92`） | **F（但し技術制約あり）** | コメント明記「市場の声は LLM source のため静的説明文に留める」（`:19-20` `feedback_accordion_collapsed_unmount`）。センチメント方向(強気/弱気)は LLM 由来 → collapsed summary に出すには **非 LLM の軽量 sentiment signal が別途必要**。単純復元は不可。Phase C 設計判断 |
| b2 | 総合バッジ/統合見解/2カラム強気弱気 | あり | `SentimentBadge`(`InsightsPanel.jsx:204`)・`FullView` の summary+bull/bear 2カラム（`:225-318`） | **I** | 構造一致。件数は API 依存（mockup 3点固定より柔軟）。バッジは展開後表示（折りたたみ summary には出ない → b1 と同根） |
| b3 | 課金 gate「🔒 Free は冒頭1文のみ。Pro で全文+根拠」 | gold 文言 | `TeaserView`(blur+CTA)＋`FullView`＋`LockedSection`/`InsightsGhost`（`InsightsPanel.jsx:464-479`・`352`・`5`）／未ログインは `NonLoggedTeaserView`(`:352`) | **P（実装済・I 寄り）** | mockup の単純 gold 文言より高度な teaser UI に発展済。gold 明示文言は無いが gate は明確に機能。**Phase C multi-review 対象（funnel-cro + §38）はこの実装の妥当性確認** |

### 5-3. 過去 8Q 決算反応（fold 親 = `JudgmentDetail.jsx` / 本体 = `EarningsReactionPanel.jsx`）
| # | 観点 | mockup | 実装（裏取り済 file:line） | 分類 | 備考 |
|---|---|---|---|---|---|
| c1 | summary 文言＋Pro tag | 動的「Beat 6回 平均+5.1%…」＋`<span class="pro-tag">Pro</span>` | 固定「発表翌日の株価変化」（`JudgmentDetail.jsx:967`）／`AccordionSection` に **pro-tag サポート 0件**（`primitives/AccordionSection.jsx` grep 0） | **F** | **§⑤ 最大の機会損失**。8Q の実績数値（Beat 回数/平均%）は **非 LLM 算出データ**ゆえ collapsed summary に出すのは技術的に可能。＋ Pro tag indicator の primitive 拡張が要る |
| c2 | 反応カード3枚/四半期テーブル | あり | カード3枚 grid（`EarningsReactionPanel.jsx:103-129`）・四半期 ul/grid（`:143-187`）。並び Beat→In-line→Miss 一致 | **I** | table→ul/grid だが情報同等。disclaimer「将来の値動きを保証しない」(`:189`)は mockup 超の §38 配慮 = 良い拡張 |
| c3 | In-line の色 | 灰（`--text-muted` 中立） | **amber**（`VERDICT_COLOR['in-line']='var(--color-warning)'` `:18`、card で使用 `:106`） | **M（要デザイン判断）** | 色ルール「amber=緊急・警告」。In-line(サプライズなし=中立事象)に警告色は意味論的に逸脱。mockup の無彩色が「方向性なき情報は無彩色」の精神に近い → mockup 側が正・実装 amber が drift の可能性。断定せず Phase C デザイン判断 |
| c4 | 課金 gate（fold 全体 Pro） | summary に Pro tag | `PremiumLock feature="earnings_8q"` で panel 全体 wrap（`JudgmentDetail.jsx:969-976`） | **P（実装済）** | 全体ロックは機能。但し折りたたみ時点で Pro 要否が分からない（c1 の Pro tag 欠落と同根） |

### 5-4. Insider 取引（fold 親 = `JudgmentDetail.jsx` / 本体 = `InsiderPanel.jsx`）
| # | 観点 | mockup | 実装（裏取り済 file:line） | 分類 | 備考 |
|---|---|---|---|---|---|
| d1 | summary 文言 | 動的「直近90日 買付 2件 $1.2M」 | 固定「直近 90 日の売買」（`JudgmentDetail.jsx:987`） | **F** | summary 動的化 drift（c1/b1 と同パターン。Form 4 件数/金額は非 LLM ゆえ復元可能） |
| d2 | Form 4 経営者売買リスト（CEO 買付/CFO 売却 10b5-1） | あり | `InsiderPanel.jsx:132-237`、役職/取引種別を日本語訳+tooltip（`:28,69,149`） | **I** | 構造一致。「10b5-1」表記が実 UI に出るかは **未確認**（`translateTxType` 戻り値未読） |
| d3 | 13F lockbox「FMP Ultimate で開放予定（restricted）」 | restricted 文言＋SEC EDGAR 13F リンク | restricted 分岐で **「FMP Ultimate で開放予定」文言を意図的削除**、SEC EDGAR 13F 検索導線のみ残す（`InsiderPanel.jsx:251-263`、コメント `:253-254`「v115 round 3 user feedback: 実装都合の文言削除、Trust Cliff + 景表法 §5 防止維持」） | **M（mockup 誤り・実装維持）** | **重要**: mockup は実装都合(現プラン制限)を露出する Trust Cliff/景表法 §5 リスク文言を含む。実装は user 判断で既に是正済。**統合 mockup 作成時に mockup 側をこの実装に合わせる** |

### 5-5. ニュース・IR・10-K（fold 親 = `ContextSection.jsx` / NewsPanel + IRLinksPanel + TenKLinksPanel）
| # | 観点 | mockup | 実装（裏取り済 file:line） | 分類 | 備考 |
|---|---|---|---|---|---|
| e1 | summary 文言 | 動的「最新12件・3時間前更新」 | 固定「一次ソースへのリンク」（`ContextSection.jsx:44`） | **F** | summary 動的化 drift（同パターン。ニュース件数/鮮度は非 LLM ゆえ復元可能） |
| e2 | ニュース項目リスト（鮮度付き） | `dt`「3h」「1d」 | `NewsPanel` `timeAgo()`「N時間前」（`NewsPanel.jsx:16-25`） | **I** | 形式差のみ。日本語正式表記の方が UX 良 |
| e3 | リンクピル（IR/直近決算Call/SEC EDGAR/10-K の4種） | 1 fold にフラット4ピル | 3 section 分割（NewsPanel/IRLinksPanel/TenKLinksPanel）に集約（v294、`ContextSection.jsx:10-16,55-66`） | **I** | 構造化は意図的。**IRLinks/TenKLinks 内部のリンク種別が mockup 4種を網羅するかは未確認**（Phase D で確認） |
| e4 | 課金 gate | なし（無料想定） | gate なし | （対象外） | 双方 gate なしで整合 |

### §⑤ 総括
- **横断パターン = summary 静的化（F・4 fold: b1/c1/d1/e1）**: mockup は collapsed summary に動的実績数値を出して「パッと見2秒」（原則①）を満たす設計。実装は固定文字列に後退。
  - **8Q/Insider/ニュースは非 LLM 算出データ → 動的化は技術的に可能（クリーンな F）**。8Q の「Beat 回数+平均%」復元が最大の訴求回復。
  - **市場の声(b1)のみ LLM source 制約**（collapsed unmount）で単純復元不可 → 非 LLM sentiment signal の設計が要る。
- **課金 gate（P）現状**: アナリスト=伏せ字機能あり/常時可視文言なし(F)・市場の声=teaser UI 実装済・8Q=PremiumLock 全体ロック・Insider 13F=SEC EDGAR 導線(意図的是正済)・ニュース=gate なし。**「Pro gate 未実装」の fold は無い**（handover の「市場の声 Pro gate = P」は既実装の妥当性確認が論点）。
- **§38/色ルール**: 明確な §38 違反なし。**c3 In-line の amber は色ルール運用上の論点**（M 寄り・デザイン判断）。
- **mockup を直す(M)**: d3（13F 実装都合文言）・c3（In-line amber は要判断）。統合 mockup 作成時に反映。
- **未確認（正直申告・Phase D で確認）**: ① a2 アナリスト各 component の内部 visual/色トークン ② d2 「10b5-1」表記の実 UI 出力 ③ e3 IRLinks/TenKLinks の内部リンク種別 ④ Insider 13F restricted の真因（FMP Ultimate 契約済でも別 endpoint 制限か）。

---

## Phase C 決定事項（2026-06-30 user 承認済・3体レビュー synthesis 反映）✅

**Phase B（監査）完了**: ③テクニカル / ①決算 / L0判定 / ⑤その他 の全 4 ブロック裏取り済。全 drift を分類確定。
以下は user が「すべて承認」＋ 2点（アナリスト/期間別リターン）を3体レビュー（金融Opus + funnel-cro + frontend/§38）にかけた上で確定した**最終方針**。次セッションは `planner` で SPEC化 + Sprint分割 → gate1 承認 → Phase D 実装。

### 確定した実装方針
| # | 項目 | 確定方針 | 出所 |
|---|---|---|---|
| C1 | **gold 復活**（L0 #1/#5 縁取り） | **復活する**（user 承認）。⚠️ Phase D 実装時に glow postmortem `feedback_gold_accent_continuity`（gold は全 panel 一貫でないと noise）必読・danger zone | user 承認 |
| C2 | **summary 動的復元** | 8Q「Beat回数+平均%」/ Insider「買付件数+金額」/ ニュース「件数+鮮度」を collapsed summary に動的復元（**非LLMデータゆえ可**）。8Q は **Pro tag primitive 拡張**も必要（AccordionSection に pro-tag サポート追加） | user 承認 |
| C3 | **市場の声 summary** | LLM source 制約（collapsed unmount）で単純復元不可 → **非LLM sentiment signal の設計が要る**（別途・Phase D で判断 or defer） | §⑤ b1 |
| C4 | **市場の声 Pro gate** | **既存 teaser UI（blur+CTA）を維持**（gold 明示文言は不要）。3体レビューで妥当と確認 | user 承認 |
| C5 | **In-line の色** | amber → **neutral（灰）に変更**（mockup 準拠・M）。色ルール「警告=amber」を中立事象に使わない | user 承認 |
| C6 | **アナリスト視点** | **折りたたみ維持**（mockup v5 の `<details open>` は古い意図 → **mockup を実装に合わせる=M**）。f-sum が既に目標$/n を常時表示し原則①充足。**gate 常時文言は追加しない**（決定E・3体中2体が不要） | 3体レビュー + user 決定E |
| C7 | **期間別リターン** | **L0(Hero) mini を 1W/1M → 1W/1M/3M に拡張**（決定A）。データ既取得で perf無影響・§38 safe・blast最小（Hero.jsx:272-277 局所）。**§③ の8期間フルグリッドは折りたたみ維持**（2026-06-30 de-noise user gate を覆さない・10Y outlier を前面化しない） | 3体レビュー + user 決定A |
| C8 | **①決算 来期コンセンサス** | 色 = mockup緑は§38違反 → **実装の色なしを維持（M）**。表示方法レイアウトは Phase D で再確認（F?） | §①-2/3 |
| C9 | **良い決算の連続回数** | **新規実装（N）**: PR #117（OPEN draft）を merge + frontend 配線 | §①-4 |
| C10 | **L0 F項目**（2026-07-01 訂正済・詳細 [`AUDIT_pane3-L0-fidelity_2026-07-01.md`](AUDIT_pane3-L0-fidelity_2026-07-01.md)） | WL追加ボタン配置（右上→row2）/ 最終更新「X分前」（CLAUDE.mdルール由来）を実装。chip枠(#3)は drift なし(no-op)、相場地合い3セルgrid(#6)・RSゲージバー(#7)は mockup に該当 UI 無し → **やらない**。§38文言・RS閾値ロジックは不変 | L0 #4,8（着地済）/ #3(no-op)/ #6,7(やらない) |
| C11 | **③テクニカル累進開示** | **新規実装（N）**: buy-zone状態の累進開示（未検出=「対象外」/ ブレイク確認後に初めて損切りライン表示）+ 押し目・リスク4 tiles（データは backend にあり）。**§38核心**: ブレイク前に損切りライン非表示。pivot/帯/データ/配置は流用 | ③ gap分析 |

### 統合 mockup（Phase C 後半）
v5 のテクニカル章を v6 で差替 + 確定した M 項目を mockup 側に反映: C5（In-line 灰）/ C6（アナリスト open→closed）/ d3（13F「FMP Ultimate」文言削除）/ C8（来期コンセンサス色なし）。

### Sprint 分割の指針（次セッション planner で確定）
- 低リスク frontend 局所（L0 F項目 C10 + 期間別リターン C7）を1 sprint
- gold 復活 C1 は **danger zone 単独 sprint**（glow postmortem 必読）
- ⑤ summary 動的復元 C2 + In-line色 C5 + Pro tag primitive を1 sprint
- ①決算 C8/C9（#117 merge 含む）を1 sprint
- ③テクニカル累進開示 C11 は最大・**§38 multi-review 対象**・新セッション重い
- **同一 file 複数 sprint は sprint間 commit 必須**（JudgmentDetail.jsx は C6/C7/C11 で触る）。selector は data-testid 優先。
- 実装は新セッション・専用 branch（大ファイル StockPriceChart 1907行・JudgmentDetail 1000+行）
