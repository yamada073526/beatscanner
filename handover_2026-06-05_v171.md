# BeatScanner Handover v171 — dogfood polish 完了 + 案B/screener content を fresh session へ (2026-06-05)

> 長セッション (richness 3巡 + autopilot 2round + dogfood 反復)。 context 68% で user 指示により fresh session へ移行。
> **全 commit push 済 (origin/main 同期)**。 deploy = **`railway up` のみ** (CLAUDE.md override)。 **現本番 CSS: `index-zPKv6bZu.css`**。

## 🟢 本セッション完了 (deploy + 検証済)

### スクリーナー richness (dogfood 多巡)
- **④ 行 hover 左アクセントバー** (`.screener-hero-row::before`、 scaleY 0→hover 1) — user 確定。
- **③-a ambient 明滅 v5**: base 0.38 + 周期 3.5s + opacity 0.25-1.0 + scale 1.20 + 中心 top:42%。 ⚠️**user「まだもう少し強くてもいい」**継続 — ただし下方に RS ランキングが乗り一部隠れる構造要因あり。 さらに強化 or 可視領域限定 layer を検討。
- **③-b card hover**: Pane3 `.panel-card:hover` parity = ①0.4s ゆっくり ②translateY(-3px) ふわっと ③bg cyan-tint lighten + 外側 halo glow (whitelist 値)。 user OK。
- **① RS≥80 相対強度ランキング** (3 section の下、 full-width): rsLeader.items 流用 (追加 fetch 0)、 **縦読み (column-flow 5行 = rank 1-5 縦の列 3 つ)**。 user OK。
- **⑦ Pane2「あと1条件でPASS(4/5)」可視 section** (CustomScreenerPanel): FAIL collapsible の 4/5 を引出し void 充填。 zero fetch。 user OK。

### ⑥ 🔴 前方視界 period=2028 バグ修正 (最優先・user OK)
- 真因: FMP `/stable/analyst-estimates` は date **降順**返却 → `_fetch_eps_data` の `limit=12` が META/TSLA 等 long-horizon 銘柄で near-term truncate → `_compute_forward_outlook` が 2028 を「来期」 と誤選択。
- 修正 (`main.py`): **`limit 12→40`** + `_compute_forward_outlook` に「来期が today+370日超なら return None」 guard。 FMP-direct で limit=40→2026-06-30 確証。
- ⚠️ guidance/basic 直 curl は analyze flow 前提で **prod 挙動未検証** → dogfood (META 分析→前方視界が 2026 か)。

### ガイダンス「次期見通し」折りたたみアニメ (user OK 要望)
- native `<details>` (瞬時 toggle カクつき) → controlled button + **grid-rows 0fr→1fr smooth animation** + ChevronRight 90°回転 (`GuidanceCard.jsx`)。 detail view のため dogfood 検証。

### 原則4 北極星「人力の代替」 (CLAUDE.md + memory feedback_design_principles)
- 「投資家が毎日人力でやるチェックを BeatScanner に丸投げ」 = 新機能採否の最重要判断軸。

## 🔴 fresh session 最優先タスク

### A. 案B ガイダンスサプライズ (EPS まで) — user 確定 → `docs/specs/SPEC_2026-06-05_guidance-surprise.md` に **3 step 実行計画完成**
- user「じっちゃま速報レベルが目標、 全て挑戦したい」。 前提 period バグ修正済。 3 体合議 + §38 設計確定 (色なし・▲—▼・「上回る/おおむね同水準/下回る」、「上方修正/上振れ/強気」NO-GO)。
- **supervised 推奨** (§38 表示を実 ticker CRWD で user 目視 review 必須 + 8-K q_eps LLM 抽出精度確認 + latency architecture 判断)。
- Step1 `sec_guidance.py` schema に q_eps/fy_eps 追加 (q_revenue mirror + few-shot に CRWD EPS 例) → Step2 `_compute_forward_outlook` で会社 guidance vs consensus wiring → Step3 `ForwardOutlookSection.jsx` に GuidanceSurpriseRow。 各検証方法は SPEC 参照。

### B. スクリーナー scroll content 拡充 (user 要望「scroll が寂しい」、 北極星レビュー済)
- **Top1: 今週の決算予定 × RS上位** (0.5人日) — RS≥80 leaders (既取得) × `earning_calendar` 交差で「RS強い × 今週決算」 表示。 ⚠️ `fmp_client.earning_calendar(from,to)` + main.py の `_EARNINGS_RANGE_CACHE` は実装済だが **frontend 露出 endpoint の wiring 確認が要** (holdings-meta 系の next_earnings_date 機構が近い)。 北極星合致 最高。
- **Top2: 市場局面 FTD バナー** (0.3人日) — 既存 `useFtdMap()` hook 流用、「今スキャンしていい相場か」 1行。
- 配置: RS ランキング (section 04) の下。 §38 中立化 (「推奨でない」 併記)。

## 🟡 dogfood してほしい (headless 検証不可)
- ⑥ **META/TSLA 分析→前方視界が 2026 (来期) か** (2028 でないこと)。 ★最優先確認。
- ガイダンス「次期見通し」 折りたたみが smooth アニメになったか。
- ③-a がまだ弱ければ報告 (構造要因なら別 approach)。

## ⚠️ 触ると危険 (継続)
- 発光系 (.panel-card/.bs-panel/.surface-card/tier-m-glow)・box-shadow は whitelist 値 or color-mix token のみ。
- `_compute_forward_outlook`/`_fetch_eps_data` の estimates (limit=40 維持、 降順 truncate 再発防止)。 `sec_guidance.py`=§38+8-K LLM 抽出層 (案B で触る、 hallucination-guard 必須)。 ForwardOutlookSection は backend flag 読むだけ。
- Workspace isScreener 分岐=master-detail 心臓部。 sticky 検索 div / VITE_ ARG/ENV / aggregator LLM import 禁止。
- guidance/basic 直 curl は analyze flow 前提 (`_get_fmp_key`=X-FMP-Api-Key header / env fallback)。

## release status
pre-release。 launch blocker = Stripe Premium 配線。
