# Handover v304 — §③ 価格 v6 正本確定 + PriceLadder Sprint 1 着地 / 次は verdict bar

> 作成 2026-06-30。branch `claude/technical-buy-zone-l4-vdxl5d`（PR #140 draft）。前任 v303 の続き。

## このセッションでやったこと

### 1. 原版 vs 本日版 の 5体合議 比較レビュー → v6 正本確定
- **比較対象**: A=`docs/specs/mockups/pane3-full-v5.html` の §③（案A spine ladder）vs B=`pane3-technical-buyzone-v5.html`（verdict bar + 出来高ゲージ + de-noise）。
- **5体（認知設計/ブランド世界観/§38・色規律[Opus]/じっちゃまO'Neil[Opus]/実装可能性）全軸 B 勝者**。A は §38 重大違反（rail 緑赤グラデ・zone 緑赤塗り・Pivot 距離平文・SVG 凡例で `Pivot $215.62`/`−8% $177` 漏洩）。
- **収束指摘**: §38 体とプロトコル体が独立に「`.room` の −8% 常時表示」を問題視（逆算漏洩 + 押し目買い誘発）。
- **正本 = `docs/specs/mockups/pane3-technical-buyzone-v6.html`**（B骨格 × A価格地図統合 × 必須修正）。commit `7283665`。snap PNG = `frontend/.visual/buyzone-mockup-v6.png`。

### 2. user 承認済みの設計判断（gate 通過）
- **−8% = 累進開示で抑制**: ブレイク待ち中は非表示、`ブレイク確認` 遷移後に「買値−8%損切り」表示。
- **RS = 現状維持**: 既存6体合議の RS tone band（40-79=muted）が「強くない」を表現済み・amber は over-signaling で**不採用**（mockup の amber はイラスト扱い → v6 mockup も neutral に戻し済）。
- **rail 緑赤グラデは不採用**（ブランド体推奨 vs 法務体 → §38 優先で中立 rail 維持）。

### 3. PriceLadder Sprint 1 実装・検証・コミット（`cec3743`）
`frontend/src/components/PriceLadder.jsx`（+93/−5）:
- **損切り −8% 累進開示**: `stop` レベルは `cup.state ∈ {breakout_support, breakout_extended}` でのみ提示。ラベル「(現在−8%)」→「(買値−8%)」。**旧 Q2「常時無料」を更新**。
- **余地 callout**（現在価格直下・`data-testid=price-ladder-room`）: 「ブレイク確認まで [+X% ｜ 🔒 Premium]」（距離は pivot 由来=Premium gate、**矢印不使用=本 component の §38 規律**）+「損切り目安 買値−8%」（確認時のみ）。
- **ブレイク確認 出来高ゲージ**（`data-testid=price-ladder-volgauge`）: `volRatio` ×1.40 閾値+針。達成のみ gain 色（過去確定事実）、AND 条件明記、`pivot && volRatio` 有限時のみ。新規 fetch 0。
- 検証: `vite build` ✓ / `vitest` 130 passed / design grep 0件 / token 実在13個確認。⚠️ **実描画の目視は未実施**（authed 必要・user gate）。

## 🔴 次セッションの本命タスク（user 承認済・新セッション推奨理由＝大ファイル context 過重回避）
**Sprint 2 = verdict bar を主軸に v6 の残り要素を実装。**
1. **verdict bar**（⏳ ブレイク待ち 2秒 anchor）: §③ section 親。chips/RS/cup/GC は **`StockPriceChart.jsx`（1907行）** に集約、§③ 構成は **`JudgmentDetail.jsx`（大）**。`cup.state` → 3状態（watch/confirm/caution）の**静的辞書**で（LLM 不使用）。state enum は `features/judgment/constants/stateCompassText.js` の `COMPASS_PRICE_LABEL`（cup_pivot/breakout_support/breakout_extended/cup_completing/box_support/pullback_to_support、breakout namespace bo_*）。
2. **期間別リターン折りたたみ**: `features/judgment/primitives/ReturnGrid.jsx` は**既存**（Hero/JudgmentDetail で使用中）。§③ への fold 配置。長期 3Y+ は別 fetch（初期は 1Y 内先行）。
3. **チャート 52週高値ライン**（低優先・ラダーに既出）: `StockPriceChart.jsx`。価格は載せず §38-safe。`distLines` は ↑↓ 使用（chart は別 §38 慣習 = `feedback_chart_hover_direction_symbol`、PriceLadder は矢印 BAN と区別）。

## 残バックログ（v303 から継続）
- #4 §④ 図解(L5 Pro gate) / #2 未ログイン完全 workspace 化(案B) / PR #139(hr削除) merge は別途 user gate。

## 厳守事項
- 件数/方針 SSOT は user 承認 gate。検証=ground-truth（`cd frontend && npx vite build` + `npx vitest run` + design grep 0件）。
- deploy=PR squash-merge→Railway auto-deploy、**merge は user gate**。本番視覚も user gate（authed 不可・preview 禁止）。
- danger zone: 発光系(.panel-card/.bs-panel/.surface-card)・sticky 検索バー・`index.css`（screener 並行編集中）は触らない。
- **大ファイル（StockPriceChart 1907行 / JudgmentDetail）は offset/limit の targeted 読み or sub-agent 委譲**（全読みしない＝context 過重回避）。
- PriceLadder の §38 規律: **矢印 BAN**・緑赤の方向色 BAN・Premium 距離は逆算漏洩させない。
- mockup の視覚確認は `node frontend/scripts/snap-buyzone-mockup.mjs v6`（cross-env 済・macOS/Linux 両対応）→ `.visual/`。
- 和文応答（tool description も和文）。
