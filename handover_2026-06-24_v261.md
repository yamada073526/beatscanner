# 引き継ぎ書 v261 (2026-06-24) — B-3.5 gate 南京錠 本番反映完了（PR #8 merged・件数不変・本番 dogfood PASS）

> v260 を置換。本セッションの成果: **① B-4/B-3.5 Sprint 2-5 を実装し gate 南京錠を本番反映（PR #8 squash-merged → main `c4dee6e`）/ ② ocf 二重表示の Trust Cliff を inline 南京錠一本化で解消（件数不変）/ ③ cup の gate 化を ground truth 確認の上 user 決定で defer / ④ dogfood に gate アイコン検証ステージ追加し本番で overall PASS（アイコン洗練 verdict）**。
> 🟢 **次セッションの最優先候補 = なし（B-4/B-3.5 は着地）**。残るのは「データ整備後の gate 拡張（cfpsgt/beat）」と「cup の Premium 限定 gate（やるなら）」の 2 つ（いずれも defer 中・着手は任意）。

## 🧭 30 秒サマリー

| 項目 | 状態 |
|---|---|
| **main / 本番** | `c4dee6e`（B-3.5 gate 南京錠 反映済）。Railway auto-deploy 完了・本番 dogfood で検証 PASS |
| **PR #8** | ✅ **squash-merged**（gate 南京錠 + dogfood gate stage）。subscription はマージで自動解除済 |
| **gate の挙動** | `earnings_pass`=ocf_margin_pct + ocf_gt_netincome / `sector_leader`=ocf_margin_pct が**南京錠（常時ON・トグル不可・金色「必須」pill）**|
| **件数** | **不変**（count パス未変更。gate にした ocf 系は元から両 count パスで ON）|
| **cup** | 🟡 **defer**（free/anon は `cup_state=null` マスク → applied gate にすると free が全滅＝嘘の南京錠）|
| **検証手段** | CI dogfood（GitHub runner・egress 開放）を MCP で起動→`get_job_logs` で verdict 取得。本環境は本番/Azure とも egress 403（迂回禁止）|
| **screener_v2** | default **OFF**（`?screener_v2=1` で opt-in）。一般ユーザーは未露出 = blast radius 最小 |

---

## ✅ 本セッション完了（4 件）

### ① B-4/B-3.5 Sprint 2-5 実装 → gate 南京錠 本番反映（PR #8 merged）
- `frontend/src/components/CustomScreenerPanel.jsx`:
  - `PRESET_GATE_CONDS`: `earnings_pass=[ocf_margin_pct, ocf_gt_netincome]` / `sector_leader=[ocf_margin_pct]`。
  - `renderCrow` に `isGate` 分岐（lockicon + ラベル + 閾値 + 「必須」pill・**トグルなし**）。
  - `earnings_pass` の display conds に `ocf_gt_netincome` を追加（南京錠で可視化）。
  - 旧 `screener-gate-list`（別 section の必須リスト）を **JSX + CSS とも廃止** → conds 内 inline 南京錠に一本化。
  - `emptySuggest` は gate 条件を「外す提案」候補から除外。
- `frontend/src/index.css`: `.screener-crow.is-gate` / `__gate-pill` を **gold token + color-mix のみ**（発光禁止・raw hex/shadow/`!important` なし、design-system-check PASS）。

### ② ocf 二重表示の Trust Cliff を解消（件数不変）
- 旧実装は `ocf_margin_pct`/`ocf_gt_netincome` を **①トグル可能な任意条件（CROW_LAYOUT）** と **②別 section の「必須・変更不可」（screener-gate-list）** で**二重表示** → 「必須と言いながら外せる」矛盾。
- inline 南京錠一本化で解消。本番 dogfood の DOM audit で **旧 `screener-gate-*` 消滅 + `gate-crow[data-gate]:2`** を確認（run #12 では `screener-gate-*:2` が残っていた）。
- **件数不変の担保**: count 関数本体（`filteredItems`/`countPreset`/`PRESET_PREDICATES`/`applyStrategyImpl`）は**一切未変更**。gate にした ocf 系は元から両 count パスで ON のため件数は構造的に不変。

### ③ cup の gate 化を ground truth 確認の上 defer（gate-1 の「cup=即可」を覆す重要訂正）
- v260 gate-1 では「cup は即 gate 化可」とされていたが、本セッションで **`main.py:20456-20484` を確認 → free/anon は `cup_state=null`（Premium マスク）** と判明。
- これを applied gate にすると **free ユーザーで earnings_pass/sector_leader が全滅（嘘の南京錠）** → 重大 Trust Cliff。
- user に ground truth を提示し選択を仰いだ結果 **「今回は defer」** に決定。実装しかけた cup commit（`0305ddd`）は reset で除去済。**cup は引き続き Premium lock crow として表示**（南京錠化しない）。

### ④ dogfood に gate アイコン検証ステージ追加・本番 PASS
- `frontend/scripts/snap-screener-v2-dogfood.mjs` に **B-3.5 gate stage** を追加: earnings_pass を選択し ocf gate 行を zoom 撮影 → Haiku vision で「トグルなしの必須条件か」+「アイコンが洗練されてダサくないか」を判定。DOM audit に `gate-crow[data-gate]` count も追加。
- 本番 dispatch run #17（`c4dee6e`）で **overall PASS**。gate stage verdict:
  - 「南京錠 + 黄色『必須』pill 表示、ON/OFF トグルなし、固定描画」= pass(high)
  - 「金色系南京錠 + 黄色 pill が暗色背景と調和、発光/はみ出しなし」= **pass(high)＝ダサくない判定**
  - `gateCount:2`（ocf_margin + ocf_gt_netincome）。mseg/lock crow/Phase C/B-4 も全 PASS。
- ※ アイコンの**最終主観 gate は user**（CLAUDE.md「視覚は user gate に回す」）。user には実機 URL + artifact link を提示済（画像は egress 403 で main から送付不可）。

---

## ⏸️ 残タスク（いずれも defer・着手は任意）

### A. cup の Premium 限定 gate（やるなら medium）
- 設計案: `plan==='premium'` 等 tier-aware で **cup を premium にだけ applied gate**、free には従来の lock crow を出す（tier 分岐 count==list）。
- 要 ground truth: `cup_state` のマスク条件（`main.py:20456-20484`）と plan 判定の frontend 受け渡し。
- 着手判断: 原則4「人力の代替」に資するか？ cup gate は「飾り」寄りなので優先度低。user と要相談。

### B. cfpsgt / beat のデータ整備 → 本物の gate 昇格（各 medium・FMP+1/銘柄/夜）
- v260 と同じ（未着手）。`cfpsgt`（CFPS 3年連続増加）/`beat`（決算 Beat）は universe 未 populate。
- 手順: migration（`screener_fundamentals` に `cfps_3y_rising`/`latest_beat` 追加）→ `_compute_one`(`main.py:22236付近`) + `_upsert_screener_fundamental`(`main.py:21743`) + `_build_universe_payload` SELECT 追加 → gate 化。
- **paged SELECT 罠**（`feedback_paged_select_missing_column_trap.md`）: 新カラムは別 fetch 分離 or migration 先行。
- **嘘の南京錠回避**: データ整備前に gate 化しないこと。

---

## ⚠️ 触ると危険 / 規律（継続）
- **CustomScreenerPanel 整合（Trust Cliff C-2）**: count と list は同一述語。gate 拡張時は count パス（applyStrategyImpl + PRESET_PREDICATES.extra）と list を同時に変える。今回の ocf gate は両方元から ON なので不変だった。
- **発光系禁止**: screener は plain bordered + token shadow。gate/crow も border + tinted-bg + gold token のみ。
- **投資色ルール**: 上昇=緑/下落=赤/緊急=amber/cyan=ブランド色（上昇に使わない）。gate の「必須」pill は gold（amber 系）。
- **screener_v2 scope に閉じる**（legacy 完全不変・default OFF）。
- **環境制約**: この remote 環境は組織 egress で `beatscanner-production…` も GitHub artifact（Azure blob）も **CONNECT 403**（迂回禁止）。→ 視覚/curl 検証は **CI dogfood（GitHub runner）経由**で MCP 起動→`get_job_logs` で verdict 取得。スクショ画像を main から user に直接送付するのは不可（artifact link or 本番実機を案内）。
- **dogfood の使い方**: `actions_run_trigger`(run_workflow, `screener_v2_dogfood.yml`, ref=main) → run 完了待ち → `list_workflow_jobs` で job_id → `get_job_logs`(return_content, tail_lines≈340) で stages verdict 読取。deploy 完了前に走ると旧本番を見るので **merge 後 ~200s 待ってから dispatch**。
- **実装は main が直接**（委託は調査・多視点レビューのみ）。重い操作前 compact / 機能着地は新セッション。

## 🧹 掃除
- handover **v260 は削除可**（本 v261 最新）。
- PR #8 は merged・subscription 解除済。再購読不要。
- branch `claude/zealous-shannon-dedvdx` は merged。local main は `c4dee6e` に同期済。
- SPEC `docs/specs/SPEC_2026-06-24_screener-v2-b4-preset-conds-b35-gate.md` は実装完了（Sprint 2-5 着地）。歴史参照として残置可。
