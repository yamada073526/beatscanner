# BeatScanner Handover v146 — 真っ白修正 + 前方視界 + 指数ニュース/P1 + 夜間scan復旧 + LP訴求 (2026-06-01)

> v145 起点。 user 4 タスク (C/A/D/B) + 追加 (夜間scan E / 指数P1 / LP宿題) を自律実装。 全て本番 LIVE + 実測検証済。
> v145 handover は本 v146 で置換 (削除)。 **⚠️次セッション最優先 = 指数の RS 表示を非株式で非表示 (§残タスク)**。

## ✅ git 状態 (クリーン・同期済)
- working tree clean、 最終 commit **`9aacad0`**、 origin/main へ push 済。
- 本番 backend commit: `curl -s .../health | jq -r .commit`。 frontend bundle は push 毎に hash 変化。
- deploy: GitHub main push → Railway 自動 build (~3-4分)。 frontend 変更は `index-*.js` hash 変化で反映判定、
  backend-only は `/health.commit` で判定。 **LP は lazy chunk (LandingPage-*.js) なので main bundle grep では検出不可**。

## ✅ 本セッション完了 (全て本番デプロイ + 実測検証済)

### C. スクリーナー真っ白バグ修正 — [[feedback_pane_error_boundary]] (commit cd6cc5c)
- **真因**: lazy `ScreenerPane` の **stale chunk** (旧 tab × 新 deploy で chunk hash 404) → Suspense throw →
  **ErrorBoundary 不在で React tree が root から unmount → 全画面真っ白**。 login 非依存・stale-tab 依存
  (fresh tab=demo では非再現)。 Sentry が "Loading chunk failed" を明示 drop = 頻発の傍証。
- **修正**: `PaneErrorBoundary` 新設 (chunk→cooldown内1回自動リロード救済 / generic→contained fallback) +
  Workspace pane2/3 を `key={activeTab}` で包む + main.jsx で `<App/>` を最終ネット + sentry.js
  `captureException` 正式化 (window.Sentry dead path 是正)。 本番 bundle に `pane-error-chunk` grep 確認。

### A. 前方視界 (来期コンセンサス YoY) — [[project_forward_visibility]] (commit 0fb6185→6df4b9c)
じっちゃまプロトコル条件4「来期コンセンサスが前年同期比を超えているか」 の穴埋め。 6 体合議 全員条件付賛成。
- **backend** `guidance/basic` に `forward` field (`_compute_forward_outlook`、 純 Python・LLM ゼロ):
  来期 = analyst-estimates(quarter) で最終報告日後の最近接、 前年同期 = date 照合 (非暦年決算対応、 index offset 不可)。
- **色なし (§38)**: 来期=将来に緑/赤を塗る=我々の評価=断定的判断とみなされうる → ▲▼+中立トーン予測棒のみ。
  verdict ラベル禁止。 §5 免責常時表示 + 出典 + アナリスト数。 **frontend は backend flag を読むだけ (再計算禁止)**。
- **売上基準ミスマッチ横展開 (難所)**: 金融セクター (threshold<40) の来期"売上"YoY は**一律抑止** (EPS は保持)。
  per-ticker 乖離検出を試みたが V/MA(real +11%)/AXP(artifact -1.6%) の recent 乖離が 8-13% で**閾値分離不能**
  → 「金融は来期売上比較を出さない、 EPS は基準問題なしで出す」 の安全不変条件に。 非金融は cap なし (NVDA +96%)。
- **EPS ガード**: 前年赤字→来期黒字=「黒字転換」 (% なし)、 |前年EPS|<0.05=None。
- **analyst 数 field 修正 (既存バグ)**: FMP /stable は `numAnalystsEps/Revenue` (旧 `numberAnalystEstimated*` は
  全銘柄 null だった)。 forward + 既存 signal_quality 両方に fallback 適用 (NVDA=25/27社 表示に)。
- **検証**: WMT +5.4%/EPS+9.2%(じっちゃま例一致) / NVDA +95.9% / 金融(JPM/COF/AXP/V/MA) 売上抑止+EPS表示 /
  SNAP赤字→EPS None。 snap 視覚: 色なし・免責・予測棒・analyst数 全 OK。 V graceful (売上注記+EPS表示) 確認。
- **回帰ネット**: content-audit-check.sh [1c]銀行・与信抑止 + [1d]金融売上抑止/EPS保持。 **PASS**。

### D. 指数タブ改善 (ニュース空白 + P1 hero/bars/context) — (commit cc00376 / c0bd313 / 7a09e04)
指数レビュー(sub-agent)で「未完成感の 80% = ニュース完全空白」 + placeholder/数値羅列を指摘。
- **ニュース空白 (2 段の bug)**: ① `^GSPC` 等はニュース API が空 → `NewsPanel` に `newsTicker` prop で fetch だけ
  proxy ETF に振替 (^GSPC→SPY 等、 `INDEX_NEWS_PROXY`)。 ② ★真因: 指数ビューが **MotionProvider(LazyMotion)
  scope の外**で NewsPanel の `SectionFadeSubtle`(m.div) が animation feature 無し→ `initial=hidden`(opacity:0)
  固着で恒久不可視 (DOM に記事10件あるが見えない)。 R1-R3 (whileInView/animate いじり) 全不発、 R4 で
  `IndicesDetailView` を MotionProvider で wrap して解決。 + SectionFadeSubtle を `initial=hidden→animate=visible`
  (IO 非依存) 化。 検証: opacity 0→1、 本番スクショで「最新ニュース」 grid 表示確認。
- **P1 hero/bars/context** (designing-workspace-ui + design-system-check PASS): 「指数詳細」 placeholder →
  価格ヒーロー (現在値+当日変化(gain/loss色)+52週レンジ位置) / 期間テーブルに方向バー (|pct|/maxAbs) /
  TIER1.desc を意味の1行表示。 data=market-indices(price/change_pct/type)+sparkline 1y。 残 P2-A(セクター色dot=要色token決定)/P2-B(選択行border)。

### A宿題. LP「来期見通し」訴求 — funnel-cro (commit 9aacad0)
Hero subcopy に「来期見通し」 追加 (アプリの「前方視界—来期見通し」 label と完全一致=Trust Cliff #1 OK、 Free公開)。
⚠️「ガイダンス」(8-K低精度) とは別訴求なので「ガイダンス」 表記は不使用 (コメント更新済)。

### E. 夜間スクリーニング(RS/Cup-Handle)停止の診断・修正 — [[feedback_railway_native_cron]] (commit 43ca266)
user 報告「5/26 以降 夜間スクリーニング稼働せず」 = **事実**。
- **真因**: Railway cron (railway.toml [[crons]]) が **5/26 以降発火していない** (scheduler 側、 CLI から修復困難)。
  endpoint は正常 (russell3000/1000 を **rs=62s / cup=97s** で完走、 0-11 failed)。 = scale/timeout でも code でもない。
- **本日分は手動トリガーで復旧済**: rs calc_date=2026-06-01/989名、 cup 179件 (本番 read endpoint で verified)。
- **恒久対策**: `.github/workflows/nightly_scan.yml` 新設 (cup→rs→notify 直列、 23:07 UTC、 GH Actions=確実)。
  workflow は active 登録済。 Railway scan cron は idempotent なので残置 (復活しても無害)。
- **✅ CRON_SECRET 追加済 (user 実施) + workflow_dispatch で completed success 確認**。 23:07 UTC 毎日自動実行。
  (cup-scan 97s + rs-scan は GH runner で遅め=数分だが --max-time 1200 内で完走。 連続実行で FMP 競合の可能性あるが idempotent)。
- ⚠️ 他 Railway cron (insights/og-image/profile-summary/article-notify) も同様に不発の可能性 (今回未調査、 多くは on-access refresh で実害小)。 全 cron の GH Actions 移行は要検討。

### user 質問への回答 (backlog 確認)
- **transcript ガイダンス**: v146 前方視界は **consensus** で前方を提供済 (AAPL も +15.3%売上/+20%EPS 表示確認)。
  AAPL/MSFT 等が 8-K に載せない **会社自身のガイダンス数値**を出すには transcript LLM が必要 = Phase 2/3 backlog
  ([[sec_guidance_8k_coverage_limit]])。 コア前方視界は consensus で着地済なので transcript は enhancement。
- **Co-Pilot 機能** (ポートフォリオ健康度配信 / Cup-Handle メール・LINE / AI chat): [[project_signature_tier_10k_strategy]]
  の Signature ¥10k 看板そのもの。 順序制約 (Stripe→PMF→DAU100→招待制) で保留中。 今回 hardening した nightly scan
  基盤が Co-Pilot push の土台になる。

### B. Signature ¥10k — 既存決定通り保留 (実装せず)
[[project_signature_tier_10k_strategy]] の順序制約 (Stripe Premium 配線→PMF→DAU100→招待制) を厳守。
PM agent も「A 優先、 Signature は pricing/Stripe 後」 で一致。

## 🔴 残タスク (pre-release: コンテンツ→release準備→集客)
- **⚠️最優先 (user 指摘・content バグ): 指数で RS(レラティブストレングス) を非表示に**。 StockPriceChart 見出しの
  「RS -0.7% 中位」 が指数(^GSPC 等)・非株式(VIX/DXY/^TNX/CL=F/JPY=X)でも出ている。 RS は個別株が SPY 比どれだけ
  強いかの指標で、 **S&P500 自身の RS vs SPY ≈ 0%=無意味**、 非株式は完全に無意味。 StockPriceChart に `hideRS` prop
  (or index ticker 判定) を足し、 IndicesDetailView 経由では非表示にする。 finance リテラシー高 user の Trust Cliff 候補。
- **指数 P2** (残): P2-A Pane2 セクター色 dot (要 category 色 token 決定=design SSOT escalation) / P2-B 選択行 border 強調 (trivial)。
- **②transcript ガイダンス** (会社自身の数値): §38 重・複数日。 設計SPEC→sub-agent review→実装 の順 (前方視界 A と同パターン)。 user: 急がない・おまかせ。
- **③Co-Pilot (Signature)**: Stripe 配線+DAU100 が前提 (順序制約)。 今回 hardening した nightly scan が push 看板の土台。
- **A Phase 2 候補**: 会社開示ガイダンスレンジ(8-K) vs consensus 併記 (coverage gate)。
- **release 準備**: Stripe Premium/Signature 配線 (pricing 決定要) / 法務 / QA(e2e)。

## ⚠️ 触ると危険 (継続)
- 発光系 `.panel-card/.bs-panel/.surface-card` / sticky 検索バー / VITE_ ARG/ENV / aggregator LLM import 禁止 /
  「じっちゃま」UI 禁止 / DailyDigest summary は flex 維持。
- **forward 売上ガードは金融一律抑止**: per-ticker 乖離検出は V/MA/AXP 分離不能で断念済。 緩めると AXP artifact 再発。
- **PaneErrorBoundary**: lazy component 追加 pane は必ず boundary 配下。 chunk 自動リロード救済を外さない。
- **forward は backend flag のみ frontend で読む**: GuidanceCard RevenueRow のような frontend 再計算は guard すり抜け。
- **★`m.*` (framer-motion LazyMotion) は MotionProvider scope 内でのみ animate する**: scope 外だと feature 未ロードで
  `initial` (hidden=opacity:0) に固着し**恒久不可視**になる (v146 指数ニュースで R1-R3 を空回りさせた真因)。 SectionFade(Subtle)/
  m.div を使う新ビューは必ず `<MotionProvider>` で wrap するか、 既存 scope (JudgmentDetail / DiagramCard が wrap 済) 配下に置く。
  whileInView は scroll container でも発火しないので、 確実表示が要るなら `initial→animate` (IO 非依存) を使う。

## release status
pre-release。 Premium/Signature は LP「近日公開」 (Stripe 未配線)。 Pro ¥980 のみ実購入可。
