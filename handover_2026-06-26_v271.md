# handover 2026-06-26 v271 — D-8 screener sort 着地 (PGE + multi-review + 本番 merge) + Q4 残

> 前提 (不変): 正本 mockup `docs/specs/mockups/screener-strategy-presets-v8.html` を完璧再現。
> 本セッションで **D-8 sort を SPEC→実装→multi-review→main merge→本番 CI 緑**まで完走。
> 現本番 = `e1156e40` (main、Railway auto-deploy)。残は **Q4 (cup 状態トグル / seasonchip) のみ**。

## ✅ このセッションで ship + 検証済 (蒸し返し禁止)

**D-8 スクリーナー結果のソート機能** を PGE (planner) → 直接実装 → multi-review 3 体 → PR #21 → main squash merge で着地。

| 項目 | 内容 | 検証 |
|---|---|---|
| **Sprint 1 (backend)** | universe item に生 mcap / volume を additive 追加 (`main.py` の `_fetch_screener_base_universe` base dict + `_build_universe_payload` item)。追加 FMP call ゼロ (company-screener が marketCap/volume を既に返す、帯化後に捨てていた生値を通すだけ)。`mcap_band` はフィルタ用に残す (役割分離) | `py_compile` 通過・grep 裏取り |
| **Sprint 2 (frontend)** | `PRESET_METRIC_KEY` / `sortRows` 純関数 (None 末尾・ticker tiebreak) + `sortKey` state + `displayItems` useMemo。`CustomScreenerPanel.jsx` | `npm run build` 通過 |
| **Sprint 3 (frontend)** | 結果見出しに sort select (合致度順/時価総額/出来高/主要指標/セクター = 5 option) + caret SVG。`isSectorView` で非表示 (mockup line 342)。CSS は Tailwind + var() token のみ (発光系非接触) | grep 裏取り |
| **multi-review 修正** | ① 「主要指標」を未マップ preset で `disabled` 化 (silent fallback の Trust Cliff 回避) ② preset 切替 (`applyStrategyImpl`) + 全クリアで sortKey を 'relevance' に reset | build 通過 |
| **本番 CI** | main `e1156e40` で 3 件 success: Screener v2 Dogfood / Vision Eval / Playwright Smoke | GitHub Actions (ランナーから本番検証) |

**SPEC**: `docs/specs/SPEC_2026-06-25_screener-sort-master-detail.md` (gate 1 user 承認済)。
**multi-review verdict**: 3 体 (ui-designer/frontend-architect/qa-dogfooder) 全員 CONCERN・**BLOCK ゼロ**。実装の核 (sortRows / displayItems / Trust Cliff C-2 件数整合 / None-safe / backend additive / §38 配慮) は正しいと確認済。

## ⚠️ 検証の留保 (正直な記録・次セッションが知るべき)

- **session から本番 URL に直接到達不可**: egress proxy が `beatscanner-production.up.railway.app` を **org network policy で 403 拒否** (README: 403/407 はリトライ禁止・報告のみ)。そのため本 session では `/health` commit 一致確認・universe endpoint の mcap/volume field curl・authed snap を**実行できなかった**。代替検証は GitHub Actions の本番 CI 緑 (上記)。
- **未実施の最終確認 (user 目視推奨)**: 新 sort dropdown (結果見出し右) が本番で**目視で出ているか**。CI が Railway deploy 完了を待ってから叩いたかは workflow 定義未確認のため、deploy タイミングの留保あり。次 session で本番到達可能なら `curl .../api/scanner/universe?universe_size=3000` で items[0] に `mcap`/`volume` field を確認。

## 🔴 残タスク — Q4 (defer・着手時は別途 SPEC 化が必要)

- **cup 状態トグル** (`.th.state`): mockup p2 (新高値ブレイク) の「型」条件をクリックで状態循環 (ブレイク確定→取っ手形成中→カップ形成中→すべて) し再フィルタ。backend に `cup_state` 既存 (item に格納済)。
- **seasonchip**: 各 preset の対象範囲を gold pill 表示 (例「対象: 直近の決算シーズン（過去90日 / 2026 Q1）」)。mockup `.seasonchip` (line 127) / `p.season`。小規模・cosmetic 寄り (原則5 認知コスト低減)。
- D-8 で pane は idle 62% に広がっており追加 UI の余地あり。

## 🟢 意図的変更 = 維持 (mockup に戻さない)

- **D-8 新規 preserve**:
  - **default sort = 'relevance' (合致度順)**: mockup line 270 は `'mcap'` default だが、BeatScanner はスクリーナーの本質 (戦略合致順 = 原則4) と整合させ合致度順を default に維持 + 「合致度順」option を先頭追加 (user gate 1 確定)。`PRESET_METRIC_KEY` 未マップ preset では「主要指標」option を disabled。
  - **preset 別 metric**: `earnings_pass`=eps_yoy_pct / `new_high_break`=pivot_distance_pct / `sector_leader`=ocf_margin_pct。`sortRows` の None 末尾固定。
- **v270 継続**: strategy tile 縦レイアウト / Pane2 idle 62% / I-1〜I-6 / X-8 mseg Free locked cursor:pointer / X-11 strategy-bar bg-subtle。

## ⚠️ 触ると危険 (継続)

- 発光系 (`.panel-card / .bs-panel / .surface-card`) — design_recipes §C-1〜C-4。
- `.screener-control-bar` は nowrap 1行固定。sticky 検索バー / VITE_ ARG/ENV 同期 / aggregator の LLM import 禁止。
- pre-commit no-unused-vars BLOCK (dead code 削除漏れ注意)。
- **sortKey state は activePreset 付近 (L701) で宣言** — applyStrategyImpl (L726) の reset 都合。displayItems useMemo より前に置くこと。

## ⚙️ ツール / 環境メモ

- deploy: `git push origin main` → Railway auto (~90-120s)。本 session は **本番到達不可** (上記 policy 403) のため deploy 反映は GitHub Actions CI で代替確認。
- 検証 snap (前 session 由来・untracked・回帰用): `frontend/scripts/snap-screener-{adv-switch,refine-header,pane-width,precision-label}.mjs`。D-8 用 snap は本 session では本番到達不可のため未作成。
- PGE: `/planner <要望>` → SPEC → user gate 1 → 実装。**実装は main が直接手を動かす** (CLAUDE.md 正直さ: 委託は調査/多視点のみ)。
- branch 運用: 本 session は feature branch `claude/confident-ptolemy-52h4tz` で実装 → PR #21 → main squash merge → branch は merge 済。
