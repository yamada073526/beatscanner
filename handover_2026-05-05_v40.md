# beatscanner 引き継ぎ書 v40

**作成日**: 2026-05-05
**前バージョン**: v39 (handover_2026-05-04_v39.md)
**本番URL**: https://beatscanner-production.up.railway.app/
**Railway**: https://railway.com/project/30bcb8f8-98ca-4b25-bb99-54c6d3b36a62
**GitHub**: https://github.com/yamada073526/beatscanner

> 📝 v39 完了後、本セッションでは **ロック演出統一・機能ゲーティング整合・Stripe Portal・決算ハイライト改修・Webhook完全対応・Checkout後ポーリング** を実施。さらに本番環境での Stripe 決済〜解約フルフローを実機検証して完了。v30〜v39 の本文は引き続き有効。本書は v39 → v40 の差分のみ記載。

---

## 0. v40 の概要

| カテゴリ | 件数 | 主な施策 |
|---|---|---|
| 🔒 ロック演出統一 | 1 | Ghost + Aurora Veil + Inline CTA の共通 LockedSection コンポーネント新設 |
| 🎯 機能ゲーティング整合 | 3 | isSubscribed 統一 / ウォッチリスト3件制限 / スクリーナー Pro 化 |
| 💳 Stripe Portal | 1 | 特商法対応の自己解約フロー追加 |
| 📊 決算ハイライト改修 | 2 | ConferenceAnalysis プロンプト改修 + 4列乖離テーブル |
| ⏳ Checkout ポーリング | 1 | 決済完了後のサブスク状態確認（2秒×最大30秒） |
| 🐛 Webhook 修正 | 3 | Stripe SDK v10 対応 / metadata._obj_get / current_period_end item fallback |
| ✅ 本番検証 | 1 | Stripe 決済〜解約フルフローを実機確認 |

---

## 1. LockedSection — ロック演出の共通コンポーネント (新設)

**ファイル**: `frontend/src/components/LockedSection.jsx`

### 設計
```
┌─────────────────────────────────┐
│  hint (オプション: 1行説明)      │
│  ┌──────────────────────────┐   │
│  │  children (ghost skeleton) │  │
│  │  ← 実コンテンツの形状を模倣  │  │
│  └──────────────────────────┘   │
│  ████ locked-aurora (背景流れる)│
│  ▓▓▓▓ locked-veil (下に向かうフェード)│
│       [ 🔒 続きを見る · Pro ]   │  ← locked-cta chip
└─────────────────────────────────┘
```

### Props
| Prop | 型 | 説明 |
|---|---|---|
| `children` | ReactNode | ghost skeleton（実コンテンツの形状を模倣） |
| `ctaLabel` | string | chipのテキスト（例: "続きを読む"） |
| `onUpgrade` | () => void | chip クリック時のコールバック |
| `minHeight` | number | 最低高さ（default: 320） |
| `hint` | string? | 上部1行説明（例: "AI が決算を図解で解説"） |

### 適用箇所
- 市場の声 (InsightsPanel)
- AI詳細レポート (DetailReport)
- カンファレンス要点 (ConferenceAnalysis)
- アナリスト視点 (ConferenceAnalysis内)

### CSS クラス（index.css に追加）
- `.locked-section` — position:relative, overflow:hidden
- `.locked-content` — ghost を包むラッパー（ポインタイベント無効）
- `.locked-aurora` — シアン aurora アニメーション背景
- `.locked-veil` — 上→下 フェードアウト
- `.locked-cta` — 中央浮遊 chip ボタン
- `.ghost-bar` — グレーアニメーションの骨格バー

---

## 2. 機能ゲーティング整合

### 2.1 isSubscribed への統一

**変更前**: `isProUser = isPro()` (FMP API キー有無で判定する旧ロジックが混在)

**変更後**:
```js
// App.jsx
const isProUser = isPro() || isSubscribed;
```

- `isPro()`: FMP API キー保有者（BYOK 上級者向け）
- `isSubscribed`: Stripe サブスク契約者（active / trialing）
- 両方を OR で Pro 扱いにして機能解放

### 2.2 ウォッチリスト3件制限（Free）
```js
// App.jsx L568
if (!isProUser && watchlist.length >= 3) {
  upgrade.open('ウォッチリスト');
  return;
}
```
Free ユーザーは3銘柄まで。4件目追加でUpgradeModal表示。

### 2.3 スクリーナー Pro 化
ドロワーのスクリーナーボタンクリック時に `!isProUser` チェックを追加 → Upgrade Modal 表示。

### 2.4 UpgradeModal 2列化
**変更前**: Free / Pro / Premium の3列（旧 BYOK 構造が残存）

**変更後**: Free / Pro の2列
- Premium 列削除
- APIキー誘導文言削除
- Pro の機能リストを整理

---

## 3. Stripe Customer Portal（特商法対応）

### バックエンド
**エンドポイント**: `POST /api/stripe/portal`

```python
# main.py
session = stripe.billing_portal.Session.create(
    customer=customer_id,
    return_url=f"{app_url}/",
)
return {"url": session.url}
```

- JWT 認証必須
- `subscriptions` テーブルから `stripe_customer_id` を取得
- サブスクなしユーザーは 404

### フロントエンド
- `useSubscription.js` に `openPortal()` 関数追加
- App.jsx ドロワーメニュー内に「💳 サブスクリプションを管理」ボタン追加
- `user && isSubscribed` の場合のみ表示（Proユーザー専用）
- クリックで Stripe Customer Portal にリダイレクト

---

## 4. 決算ハイライト分析改修（旧カンファレンス要点）

### ConferenceAnalysis.jsx の変更
**ファイル**: `frontend/src/components/ConferenceAnalysis.jsx`

**A. プロンプト改修（アナリスト視点）**
- 旧: カンファレンスコール要点の抽出中心
- 新: 四半期業績・コンセンサス乖離・マージン軌道をアナリスト視点で要約

**B. 4列乖離テーブル（新設）**
```
四半期 | アナリスト予想EPS | 実績EPS | サプライズ%
FY2024 Q3 | $X.XX | $X.XX | +X.X%
```
`fetchAnalystData` から取得した過去データを表形式で表示。

**C. 会計四半期ラベル変換**
決算発表日 (YYYY-MM-DD) → `FY2024 Q3` 形式に変換するヘルパー追加（米国企業の慣習に基づく簡易ロジック）。

**D. LockedSection 適用**
- ConferenceAnalysis 全体: Free ユーザーには locked 演出
- アナリスト視点セクション: 個別に locked 演出

---

## 5. ニュース改善

**ファイル**: `frontend/src/components/NewsPanel.jsx`

- **cap**: 20件 → 50件
- **段階表示**: 初期10件表示 → 「もっと見る (+X件)」ボタンで追加読み込み
- 一覧を一度に展開せず UX を向上

---

## 6. Checkout 完了後ポーリング（Webhook タイムラグ吸収）

**背景**: Stripe Checkout 完了 → `/?checkout=success` リダイレクト後、Webhook が Supabase を更新するまで数秒のタイムラグがある。この間 `isSubscribed=false` のまま Pro 機能が見えず、ユーザーが混乱する。

**実装**（`App.jsx` + `useSubscription.js`）:

```js
// ?checkout=success 検知時
// 1. URL から ?checkout=success を除去 (replaceState)
// 2. 「🔄 決済を確認中...」トースト表示
// 3. 2秒間隔・最大15回(30秒)で refetchSub() をポーリング
// 4. isSubscribed=true になったら「🎉 Pro 会員になりました！」トースト
// 5. 30秒超過で「⚠️ ページを再読み込みしてください」トースト
```

`useSubscription.js` に `refetch()` 関数を追加してポーリングから呼び出せるようにした。

---

## 7. Webhook 修正（3件）

### 7.1 Stripe SDK v10 対応（`8552c02`）
**問題**: SDK v10 で `event["data"]["object"]` がエラーになる場合がある。

**修正**: 属性アクセス優先に変更
```python
sub = getattr(getattr(event, "data", None), "object", None) or event["data"]["object"]
```

**その他**:
- `sub.items.data` を try/except で囲み、失敗時は `_obj_get` にフォールバック
- Webhook ハンドラ全体を `try/except` で包み、例外時も **常に 200 を返す**（500 での無限リトライ防止）
- 各ステップに `print` ログ追加

### 7.2 metadata の `_obj_get` 対応（`928a8b0`）
**問題**: Stripe SDK v10 で `session.metadata` が `StripeObject` 化 → `.get()` で AttributeError。

**修正**:
```python
def _obj_get(obj, key, default=None):
    # StripeObject（属性アクセス）と dict（キーアクセス）の両方に対応
    try:
        return getattr(obj, key, None) or (obj.get(key) if hasattr(obj, 'get') else default)
    except Exception:
        return default
```

### 7.3 `current_period_end` の item レベル fallback（`d2ebe58`）
**問題**: Stripe API バージョン 2025-03-31 以降、`current_period_end` がサブスクリプションレベルから **アイテムレベル** に移動。

**修正**:
```python
cpe = _obj_get(sub, "current_period_end")
if cpe is None and first_item is not None:
    cpe = _obj_get(first_item, "current_period_end")
```

---

## 8. LP バグ修正（v40 大型コミットに含む）

### 8.1 Hero 重複バグ
- 未ログイン時に Hero が2重表示されるケースを修正
- 条件: `!result && !user`（`showLP` 変数ではなく `user` オブジェクトで判定）

### 8.2 7日間無料 CTA 動線修正
- LP の「7日間無料で試す →」クリック → Google ログイン → Stripe Checkout への動線が一部で途切れていたバグを修正

---

## 9. 本番環境 Stripe フルフロー検証（本セッション）

### 検証結果

| ステップ | 結果 |
|---|---|
| LP「7日間無料で試す」→ Google ログイン → Stripe Checkout | ✅ |
| Checkout 完了 → `?checkout=success` リダイレクト | ✅ |
| `checkout.session.completed` Webhook → Supabase `status=trialing` | ✅ |
| Supabase: `trial_end=2026-05-12`, `current_period_end=2026-05-12` | ✅ |
| Stripe Customer Portal 経由解約（期間終了時）| ✅ `cancel_at_period_end=true` |
| Stripe ダッシュボード即時キャンセル → `customer.subscription.deleted` | ✅ |
| Webhook エンドポイント応答: **200 OK** | ✅ |
| Supabase `status=canceled` への更新 | ✅（Stripe Webhook ログで確認） |

### 開発者用 Pro アクセス復元
テスト後、Supabase `subscriptions` テーブルを直接編集してPro復元:
- `status` = `active`
- `current_period_end` = `2099-12-31 00:00:00+00`

**現在の状態**: `status=active` / `current_period_end=2099-12-31`（実質無期限 Pro）

---

## 10. 動作確認チェックリスト（v40 追加分）

### A. ロック演出
- [ ] 未Pro時: 市場の声にロック画面表示（Ghost skeleton + Aurora + 「続きを見る · Pro」chip）
- [ ] 未Pro時: AI詳細レポートタブクリック → UpgradeModal 表示
- [ ] 未Pro時: ウォッチリスト4銘柄目追加 → UpgradeModal 表示
- [ ] 未Pro時: スクリーナーボタンクリック → UpgradeModal 表示
- [ ] Pro時: 全ロック解除・全機能アクセス可能

### B. Stripe Customer Portal
- [ ] Pro ユーザーのドロワーに「💳 サブスクリプションを管理」表示
- [ ] Free ユーザーのドロワーには表示されない
- [ ] クリックで Stripe Customer Portal にリダイレクト
- [ ] Portal から解約操作が可能

### C. Checkout ポーリング
- [ ] 決済完了後 `/?checkout=success` で「🔄 決済を確認中...」トースト表示
- [ ] 数秒後「🎉 Pro 会員になりました！全機能が解放されました」トースト表示
- [ ] URL から `?checkout=success` が除去される

### D. 決算ハイライト
- [ ] Pro 時: ConferenceAnalysis にアナリスト視点テキストが表示
- [ ] Pro 時: 4列乖離テーブル（四半期/予想EPS/実績EPS/サプライズ%）が表示
- [ ] Free 時: LockedSection でロック演出

### E. ニュース
- [ ] 初期表示10件 → 「もっと見る」で追加表示
- [ ] 最大50件まで表示

---

## 11. ファイル変更サマリー（v40 全コミット）

| コミット | ファイル | 内容 |
|---|---|---|
| `3a3814f` | LockedSection.jsx (新設) | Ghost + Aurora Veil ロック演出コンポーネント |
| `3a3814f` | index.css | locked-section / ghost-bar CSS追加 |
| `3a3814f` | App.jsx | isSubscribed 統一・ウォッチリスト制限・スクリーナーPro化・LP バグ修正 |
| `3a3814f` | InsightsPanel.jsx | LockedSection 適用 |
| `3a3814f` | DetailReport.jsx | LockedSection 適用 |
| `3a3814f` | ConferenceAnalysis.jsx | プロンプト改修 + 4列乖離テーブル + LockedSection |
| `3a3814f` | NewsPanel.jsx | cap 20→50 + 段階表示 |
| `3a3814f` | UpgradeModal.jsx | 2列化（Premium削除） |
| `3a3814f` | ConditionCard.jsx | 軽微調整 |
| `3a3814f` | useSubscription.js | openPortal() 追加 |
| `3a3814f` | main.py | /api/stripe/portal エンドポイント追加 |
| `3a3814f` | CLAUDE.md | v39 確立ルール10件昇格 |
| `48b3049` | App.jsx | checkout=success ポーリング実装 |
| `48b3049` | useSubscription.js | refetch() 関数追加 |
| `8552c02` | main.py | SDK v10 対応・try/except・ログ追加 |
| `928a8b0` | main.py | metadata._obj_get 対応 |
| `d2ebe58` | main.py | current_period_end item レベル fallback |

---

## 12. 残タスク（v41 以降）

### 🔴 最優先
| タスク | 詳細 |
|---|---|
| **v40 引き継ぎ書作成** | ✅ 本書が該当 |
| **本番ブラウザ実機テスト** | チェックリスト A〜E を実機確認 |

### 🟡 中優先（機能追加）
| タスク | 詳細 |
|---|---|
| **指標バー再設計 (v42 統合)** | 「ダサい」フィードバック対応 — 左ラベル + 3-4行構成で再設計、上級者拡充と統合実装。詳細は §16 |
| **企業ロゴ表示** | ティッカー / 企業名と並べて公式ロゴを表示 (clearbit/logo.dev 等)。設計思想「図解で認知コスト ↓」に直結 |
| **指標バー Phase 2c** | ピン機能 / Closed バッジ / 動的 TTL。詳細は §14-5 |
| **時事ニュース セクション新設** | ホームタブに「Today's Brief」(マクロ/地政学ニュース)。詳細は §14 |
| **指標バー v42 (上級者拡充 + 期間切替)** | 14→30+ 指標拡充 / 1D/1W/1M/6M/1Y 切替 / Markets ▾ トグル / Zustand + JSON SSOT 化。詳細は §15 |
| **4半期単位の決算詳報** | 現在の3年通期 PASS/FAIL に加え、四半期トレンド分析（Pro ¥980 同梱） |
| **AI 自動生成長文記事** | 銘柄毎の決算/会社紹介記事を Claude で自動生成（Pro ¥980 同梱） |
| **決算前メール/LINE 通知** | ウォッチリスト銘柄の決算前 N 日に自動通知（Premium ¥1,980 候補） |
| **Premium ¥1,980 ローンチ** | 通知 + 高頻度更新 + 過去データ無制限 |

### 🟢 低優先（技術改善）
| タスク | 詳細 |
|---|---|
| **Sentry 導入** | 本番エラー監視（推測値: 30分実装） |
| **Stripe Webhook Idempotency** | 重複処理対策（Stripe イベントIDで dedup） |
| **memo / useMemo 適用** | レンダリング最適化 |
| **slowapi レート制限** | demo 以外のエンドポイントにも適用 |
| **「現在 X 名利用中」カウンター** | 信頼シグナル（β人数 20+ で表示開始） |

---

## 14. 指標バー拡充 + 時事ニュース セクション 設計案（v41 候補）

> 本セッションで 3 つの専門家サブエージェント（金融アナリスト / Webアプリ設計 / UI/UX）にレビュー依頼し、以下の設計方針を確定。実装着手前にユーザー最終承認を得ること。

### 14-1. 指標バーに追加すべき指標（金融アナリスト視点）

**最低限追加 5 件（Tier 1）**:
| ティッカー | カテゴリ | 役割 | データ |
|---|---|---|---|
| `DX-Y.NYB` (DXY) | 為替 | ドル全体強弱（USD/JPY より上位概念） | yfinance |
| US2Y / イールドスプレッド (US10Y - US2Y) | 金利 | 逆イールド = リセッション先行指標 | FRED API 推奨 |
| HYG | クレジット | ハイイールド債、リスクオフ初動（VIX より先行） | FMP / yfinance |
| TLT | 債券 | 長期米国債、グロース株との相関逆転シグナル | FMP / yfinance |
| `CL=F` (WTI) | コモディティ | インフレ・地政学・エネ直撃、中東情勢の代理 | yfinance |

**追加候補 7 件（Tier 2）**:
- セクター ETF: XLK / XLF / XLE / SMH（半導体）
- 暗号資産: BTC-USD（流動性相場のセンチメント）
- コモディティ: HG=F（Dr. Copper、世界景気）
- 海外指数: ^N225（日経225、米市場クローズ後の補完）

**除外推奨**:
- 既存 `VTI` は SPY と重複 → 削除推奨
- 銀(SLV) / EUR/USD（DXY で代替）/ 上海総合 / DAX / Put-Call比率 / AAII（API 取得困難 or 規約リスク）

**データ精度の懸念と推奨修正**:
- VIX/^TNX は FMP `batch_quotes` で取得失敗ケース多 → yfinance フォールバック必須
- USD/JPY: FMP は `USDJPY`、yfinance は `JPY=X` → ティッカー二系統管理層が必要
- 60秒キャッシュ → **動的 TTL**（通常 60s / 米市場 8:30-10:00 ET と 14:00-16:00 ET は 15s）
- 米市場クローズ後は **「Closed」バッジ**を出し、動いている群（FX/原油/BTC）と停止群を視覚分離
- `change_pct` の基準（前日比 / 24h / settlement）が市場ごとに違う → ツールチップで明示

### 14-2. 表示方法（Webアプリ設計エキスパート視点）

**推奨: タブ切替 + 2行グリッド**（Notion DB view 流）
```
┌─────────────────────────────────────────────────┐
│ [主要] [為替・債券] [セクター ETF] [海外]       │ ← チップタブ
│ ┌────┬────┬────┬────┐                           │
│ │SPX │NDX │DJI │QQQ │  ← 2行グリッド固定        │
│ ├────┼────┼────┼────┤                           │
│ │VIX │10Y │JPY │GLD │  最終更新 2 分前         │
│ └────┴────┴────┴────┘                           │
└─────────────────────────────────────────────────┘
```
- カテゴリ内のみ横スクロール許容、チップ active 時のみシアン 1px border
- マルキー（自動スクロール）は **絶対不採用**（ニールセン NN/g で長年 NG、ブランドトーン衝突）

**代替: 主要 4 + Disclosure ドロワー**
- First-fold は ^GSPC / ^IXIC / ^DJI / USDJPY のみ、右端「すべての指標 ▼」で展開
- Apple Settings / Stripe "Show more" 流。ヘビーユーザーだけが展開

### 14-3. 即適用したい UX 改善（UI/UX デザイナー視点・指標バー）

タブ切替実装と並行 or 前段で適用すべき 5 件:
1. **3 + 8 の階層化**: S&P500 / NASDAQ / DOW を「主指標」として高さ 60px・価格 18px、他は 44px・14px
2. **変動率に色帯背景** (rgba(52,239,129,0.08) / rgba(248,113,113,0.08))。スキャン速度 3 倍
3. **🔵🔴 emoji 撤去**（Apple/Linear トーンと矛盾）→ 代わりに ±2% で文字 bold、±5% で左に 2px 色バー
4. **「最終更新」を行頭・左揃え** + シアンドット点滅（1.5s, opacity 0.4↔1.0）で「LIVE 感」
5. **右端フェードに `>` シェブロン**を重ねる（横スクロール可能性のアフォーダンス）

**カテゴリラベル左端固定**（タブ切替を採用しない場合のフォールバック案）:
```
[指数]   S&P  NASDAQ  DOW  RUSSELL  VIX
[ETF]    QQQ  SPY  DIA  IWM  GLD  TLT
[マクロ] US10Y  USD/JPY  WTI  BTC
```
- ピン機能（☆ アイコン + localStorage）でユーザー上位 5 件カスタマイズ → モバイル 4 件固定の不満解消

**アクセシビリティ必須項目**:
- ▲▼ 三角は維持（色だけでなく形でも上下表現、色覚配慮）
- aria-label で「S&P 500、5800.12、前日比プラス0.45パーセント」を読み上げ
- 価格は **右揃え + tabular-nums**（桁ズレで認知負荷増）
- 更新フラッシュは 300ms 以内・1回のみ、`prefers-reduced-motion` 尊重

### 14-4. 時事ニュース セクション設計

**配置**: 検索バー直下、ウォッチリストの**上**
- 理由: マクロニュース = 市況の文脈、個別銘柄判断（ウォッチリスト/Movers）の前に来るべき
- Bloomberg Terminal の "市況サマリー → 個別" 思想

**形式**: 「Today's Brief」型 — 縦リスト 3 件即読み + 「すべて表示 →」で展開
```
┌─────────────────────────────────────────┐
│ Today's Brief             すべて表示 →  │
├─────────────────────────────────────────┤
│ [HIGH] マクロ・地政学    2 時間前 翻訳済│
│ S&P500 年末目標、GS が 6,500→6,800 に  │
│ 上方修正。利下げ織込みと AI 設備投資   │
│ Goldman Sachs · 関連: SPY NVDA →       │
└─────────────────────────────────────────┘
```
- **1 件**: 見出し 16px bold + 要約 60〜80字 (13px, line-height 1.6) + ソース + 経過時間 + 関連ティッカーチップ
- **重要度バッジ**: `HIGH`(amber) / `市場全体`(シアン) / `地政学`(パープル) — 文字+色で色覚配慮
- **左端 2px 縦バー**で impact 視覚化（amber=高 / cyan=中 / gray=低）
- 24h 超 opacity 0.5、48h 超は「過去のニュース」アコーディオン格納
- クリック = **インライン展開**（モーダルは離脱誘発で却下）、外部リンクは矢印明示
- カルーセルやサムネ画像は **入れない**（30秒スキャンを破壊）

**データソース選定**（推奨スタック）:
| ソース | コスト | 用途 |
|---|---|---|
| **FMP General News** (`/v3/general_news`) | 既契約に含む | メイン一次ソース |
| **AlphaVantage NEWS_SENTIMENT** | 無料 25req/日 | sentiment スコア補強 |
| **Claude Haiku** | ~$0.10/日 | L3 LLM 分類 + 日本語翻訳要約 |

**マクロ判定 3 層フィルタ**:
1. **L1 キーワード辞書** (~80語): Fed/FOMC/CPI/PPI/NFP/GDP/Treasury yield/oil/OPEC/Iran/China tariff/S&P 500/recession/IPO/Powell/ECB/BOJ 等
2. **L2 ティッカー数**: 個別銘柄 1社のみ言及 → 除外、3社以上 or インデックス ETF 言及 → 通過
3. **L3 LLM 分類**（Claude Haiku、1日 ~100件、$0.10/日）: `{macro: bool, importance: 1-5, category: 'monetary'|'fiscal'|'geopolitics'|'earnings_outlook'|'commodities'|'tech_sector'}`

**重要度 Tier**:
- **Tier S (5)**: FOMC声明 / Powell発言 / CPI・雇用統計の前後24h
- **Tier A (4)**: 主要IBの目標株価改定 / GDPナウキャスト / 地政学衝撃
- **Tier B (3)**: セクター規制 / 大型IPO観測（SpaceX 等）/ 大手CEO発言
- **Tier C (2)**: 業界トレンド / 調査レポート

UI: Tier S/A のみデフォルト、B/C は「もっと見る」展開

**更新頻度**:
- 通常: 15分バッチ
- FOMC日 / CPI発表日: **5分バッチ**に自動切替（経済カレンダー API トリガ）
- フロント: 60秒ポーリング、hash 比較で差分のみ反映

**4 実例の取得可能性検証**:
| ユーザー例 | 取得 | 経路 |
|---|---|---|
| S&P500 年末目標 IB 上方修正 | ○ | FMP General News + L1 "S&P 500"+"target" |
| SpaceX IPO 観測 | △ | FMP は頻度低、Benzinga 補強で○ |
| OpenAI CFO/CEO 発言乖離 | △→○ | "OpenAI" を L1 追加要、L3 LLM で乖離検知 |
| イラン⇔米国情勢 | ○ | "Iran"+"oil" / Reuters geopolitics tag |

→ **L1 キーワード辞書のメンテナンスが運用品質の 8 割**。月1回見直しを運用ルール化。

### 14-5. 実装ロードマップ案（提案）

**Phase 1: 指標バー UX 即適用（半日〜1日）** — ✅ v41 で完了 (`c3debd9` / `ee1b03a`)
- 🔵🔴 emoji 撤去 / 変動率色帯 / 階層化 / シェブロン / LIVE ドット / items-center

**Phase 2a: 指標拡充 (Tier 1)** — ✅ v41 で完了 (`d683b44`)
- VTI 削除、DXY / TLT / HYG / WTI 追加 (11 → 14)
- per-symbol fallback (FMP 部分応答時に欠落分のみ yfinance 補完)
- frontend RISK_TYPES に bond / credit / commodity を拡張

**Phase 2b: タブ切替 UI** — ✅ v41 で完了 (`78a739b`)
- 2 タブ構成 (主要 / マクロ)、ARIA tablist + キーボード ←/→
- localStorage で選択タブ永続化 (bs_marketTab)
- minHeight 76px で CLS 防止

**Phase 2c: 仕上げ（v41-v42 任意）**
- ピン機能 (☆ + localStorage)
- **並び順カスタマイズ** (ユーザー要望 v41): ドラッグ&ドロップで指標を並べ替え、localStorage 永続化
  - 推奨実装: `@dnd-kit/core` + `@dnd-kit/sortable` (軽量、a11y 対応、HTML5 DnD より柔軟)
  - 範囲: タブ内で並び替え可、タブ間移動は v42 で
  - モバイル: 長押しドラッグ、または編集モード切替
- Closed バッジ (米市場クローズ時に「動いている群」と「停止群」を視覚分離)
- 動的 TTL (FOMC / CPI 日は 15s に短縮、平時 60s)
- US2Y / 2s10s スプレッド (FRED API 連携、5Y BEI 等のインフレ期待)

**Phase 3: 時事ニュース MVP (3〜5日)**
- バックエンド: `/api/news/macro` エンドポイント (FMP General News + L1 キーワード)
- 段階展開: L1 → L2 → L3（Claude Haiku は段階的に追加）
- フロント: Today's Brief コンポーネント、ホームタブ検索バー直下に配置
- 翻訳: 既存 news-translation スキル流用

**Phase 4: 高度化 (任意・後日)**
- 経済カレンダー連動の動的更新頻度
- L3 LLM 分類精度向上
- 重要度 Tier S/A バッジ運用

---

## 15. v42 候補: 上級者向け指標拡充 + 期間切替 設計案

> 本セッションで 3 専門家サブエージェントにレビュー依頼。実装着手前にユーザー最終承認を得ること。

### 15-1. 上級者向け指標 トップ 10（金融アナリスト視点）

| # | 指標 | 取得 | 価値 |
|---|---|---|---|
| 1 | **2s10s スプレッド** (FRED `T10Y2Y`) | FRED 必須 | リセッション先行指標、US10Y 単独より遥かに高価値 |
| 2 | **5Y/10Y BEI** (FRED `T5YIE` / `T10YIE`) | FRED 必須 | インフレ期待の市場織込み、FOMC 後の真の解釈に必須 |
| 3 | **KRE** (地方銀行 ETF) | yfinance | SVB ショック以降、信用不安の即応指標。XLF より感応度高 |
| 4 | **HG=F** (銅 / Dr. Copper) | yfinance | 世界景気の体温計、中国景気と直結 |
| 5 | **^VVIX** (VIX of VIX) | yfinance | テールリスク、VIX 低水準時に価値 |
| 6 | **^MOVE** (債券ボラ) | yfinance 不安定 / FRED | VIX 株、MOVE 債券のセット運用 |
| 7 | **HYG/LQD レシオ** (計算) | yfinance | クレジット相対選好の純粋指標 |
| 8 | **^SSEC / ^HSI** (中国・香港) | yfinance | 米中対立・中国景気の織込み |
| 9 | **NG=F / HO=F** (天然ガス・暖房油) | yfinance | WTI 単独より季節性・地政学解像度向上 |
| 10 | **USDCNH=X / EURUSD=X** | yfinance | DXY (EUR 比率 57%) の偏りを補完 |

### 15-2. 「日本人特化」指標 推奨 5 件

| 指標 | 取得 | 注意 |
|---|---|---|
| **オマーン原油 (DME Oman)** | ❌ 商用契約必須 | **代替: Brent (BZ=F) を強く推奨** — 日本輸入原油 90% 中東依存で Dubai/Brent が実質連動、yfinance 安定取得可 |
| ^N225 (日経225) + 1306.T (TOPIX ETF) | yfinance ○ | ホームバイアス補正に必須 |
| USDJPY=X + EURJPY=X (クロス円) | yfinance ○ | 米株日本人投資家の P/L は円建て |
| JGB 10Y | yfinance 不安定 / FRED `IRLTLT01JPM156N` | 日銀 YCC 修正時に必須 |
| NKD=F (CME 日経先物) | yfinance ○ | 米国時間の日経織込み先取り |

→ ユーザー要望「オマーン原油」は **DME Oman は商用 API 専用で取得不可、Brent (BZ=F) で 90% 代替可**。

### 15-3. 期間切替 推奨実装方針

**データ層（Webアプリ設計エキスパート視点）**:
- **推奨: ヒストリカル一括取得 + クライアント派生**
- `/api/market-history?period=1Y` で 1Y 日足を全指標分一括取得 → 1D/1W/1M/6M はクライアント側スライス計算
- 期間切替が **完全 0ms**（Bloomberg / TradingView 体感）
- バックエンドキャッシュ: 当日値 60s / 履歴 1h / 1Y 終値 24h
- 容量: 30 銘柄 × 252 営業日 × 8byte ≈ 60KB → Python dict + TTL で十分、Redis 不要
- クライアント: **localStorage + SWR (stale-while-revalidate)** で段階表示

**計算ロジック（金融アナリスト視点）**:
- 取引日基準で統一: 1W=5 / 1M=21 / 6M=126 / 1Y=252 営業日前
- 配当: 価格リターン（adjusted close、スプリットのみ調整）が個人投資家に直感的
- VIX / 金利 / BEI 系は **絶対値変化（pt 表示）**、`display_mode: 'pt'|'pct'` メタフラグで出し分け
- データソース: **yfinance 単一ソースに統一**（FMP との混在で 6M/1Y で数 % ズレるリスク）

**UI（UI/UX デザイナー視点）**:
- **チップタブを右上配置**: pill 型 `[1D] 1W 1M 6M 1Y`、選択 chip のみシアン地・白文字
- chip 高 24px + タップ領域 32px、横 padding 10px、font 11px / 500
- 切替アニメ: **opacity 0→1 (120ms) クロスフェードのみ**（数値カウントアップは禁止 — 「データが動いてる」と誤認）
- 段階フェード: `transition-delay: index * 8ms` で 1 行ずつ更新（最大 240ms 以内）
- `prefers-reduced-motion` で全アニメ無効化

**期間別の色帯閾値（重要）**:
| 期間 | ±閾値 | 理由 |
|---|---|---|
| 1D | 1.0% | 通常レンジ |
| 1W | 3.0% | 週次ボラ |
| 1M | 5.0% | 現状維持 |
| 6M | 15% | セクター ETF 想定 |
| 1Y | 25% | 年次レンジ |

色帯透明度を 1Y 時に 60% → 30% へ自動減衰（全画面緑/赤の防止）。

### 15-4. 上級者モード切替の体験設計

- **「上級者向け」表記は使わない**（卑下を誘発、Apple/Notion 流に反する）
- 折り畳みトグル「**Markets ▾ / Markets ▴**」（無印で段階開示）
- 常時 6〜8 指標表示、▾ 展開で残り 22+ 指標を 3 行グリッド
- localStorage で記憶、初回 = 折り畳みデフォルト
- ログイン済 + 過去展開履歴 → デフォルト展開
- 「簡易/詳細」モーダル切替は不要

### 15-5. データ精度の落とし穴 トップ 5

1. **米市場クローズ後の "1D" のズレ**: 日本朝に見ると "前日終値 vs 前々日"。`as_of` タイムスタンプ併記必須
2. **yfinance vs FMP の adjusted close 混在**: 配当調整有無で 6M/1Y で数 % ズレる → 1 ソース統一
3. **VIX の period 計算は意味薄**: 平均回帰指標。「現値 + 1Y レンジ内パーセンタイル」表示の方が情報量大
4. **休場日の市場カレンダー違い**: 米株休場日に日本株・為替は動く → 各指標に `market_calendar` メタ
5. **新規上場・ティッカー変更**: `period='1y'` で None / NaN → fallback `'max'` で最古日付からの変化率に切替

### 15-6. 拡張性・保守性（v42 で先行整備推奨）

Webアプリ設計エキスパートの提言: **Phase 2b 実装時に「v42 の器」を先に作れば 2 度作りを避けられる**

- `frontend/src/config/marketSymbols.json` に **シンボル定義を外出し**
  - フィールド: ticker / label_ja / label_en / region / category / display_mode / sparkline_color
  - バックエンドも同 JSON を読む（FastAPI 起動時 load）= SSOT
- 状態管理: **Zustand 採用**（Context API は 30 指標 × 期間切替で再レンダー地獄、selector が必要）
  - `marketStore`（指標データ）/ `uiStore`（period・mode）の 2 分割
- パフォーマンス: `React.memo` + 個別 selector / SVG 1-path Sparkline / CSS containment / Intl.NumberFormat シングルトン化
- アクセシビリティ: 数値変化は `aria-live="polite"` + throttle（連呼防止）

### 15-7. v42 ロードマップ

**Step 1 (Phase 2b) — ✅ 完了**: タブ切替 UI、シンボル定義は MARKET_SYMBOLS 配列のまま

**Step 2 (v42-α)**: 器整備
- シンボル定義を JSON 外出し（marketSymbols.json）
- Zustand store 導入
- `/api/market-history` 新設 + localStorage SWR
- 期間ピル UI（1D のみライブ、1W/1M/6M/1Y はモック）
- 指標数 14 据え置きで UX を固める

**Step 3 (v42-β)**: 拡張
- 30+ 指標へ拡張（Tier 2: KRE / HG=F / ^VVIX / BZ=F / ^N225 / NG=F / USDCNH=X 等）
- Markets ▾ トグルで段階開示
- FRED API 連携（2s10s / BEI など）
- A/B 観測: 「デフォルト=簡易」で開始、Mixpanel 等で切替率測定

### 15-8. 避けるべきパターン

1. **インラインセグメント切替（各 Item 内に期間チップ）**: 14 個並べると視覚ノイズで全壊
2. **モード切替モーダル**: クリック数増・離脱誘発、トグル 1 つで十分
3. **色帯透明度を期間問わず固定**: 1Y で全画面緑/赤になり「色で異常を捉える」UI が無効化
4. **数値カウントアップアニメ**: 金融 UI で「データが動いている」と誤認、クロスフェード 150ms が正解
5. **Bloomberg Terminal 寄せの超高密度 UI**: 個人投資家の「朝の 30 秒チェック」を破壊。Apple 寄り 7 割 / Bloomberg 寄り 3 割が目安

---

---

## 13. デプロイ運用（変更なし）

```bash
cd frontend && npm run build
git add <files>
git commit -m "feat|fix|perf|refactor: ..."
git push origin main   # → Railway 自動ビルド開始
```

### Railway Cron（v40 完了時点）
```toml
# 1. 朝 4:00 JST: 全ウォッチリスト market_insights 更新
schedule = "0 19 * * *"
command = "curl -X POST -H \"X-Cron-Secret: $CRON_SECRET\" .../api/insights/refresh/batch"

# 2. 15分毎: /api/movers キャッシュ warmup
schedule = "*/15 * * * *"
command = "curl -s .../api/movers > /dev/null 2>&1"

# 3. 10分毎: /health keepalive
schedule = "*/10 * * * *"
command = "curl -s .../health > /dev/null 2>&1"
```

### v40 完了時点の最新コミット
- `d2ebe58` — fix(webhook): current_period_end item レベル対応

---

## 16. 指標バー再設計 + 上級者拡充 統合方針 (v42 候補)

ユーザー報告「現状の指標バーがダサい」(2026-05-05) を契機に、UI/UX + Web 設計エキスパートに統合レビュー依頼。両者ともに **意味的グルーピング不在** が「ダサさ」の本質と特定し、レイアウトと上級者拡充を同時に再設計する方針で合意。

### 16-1. 「ダサい」根本分析 (UI/UX)

1. ラベルが下段だけにある **左右非対称**
2. 上段=指数+ETF / 下段=VIX+金利+為替+債券+コモディティの **意味カテゴリ混在**
3. 主指標 16px と ETF 14px の階層差で **視覚ウェイト偏り**
4. 区切り線が等間隔でなくリズム崩れ
5. 14 個の指標が 2 段に「とりあえず」並んでいる感

### 16-2. 推奨レイアウト (両者収束: 案 A + D ハイブリッド)

**左固定カテゴリラベル + 3-4 行セマンティック分割:**

```
┌─────────────────────────────────────────────────────────────┐
│  [1D] 1W 1M 6M 1Y          Markets ▾   Updated 2m ago        │
├──────┬──────────────────────────────────────────────────────│
│ 指数 │ S&P  NDX  DOW  RUT                          →  ›       │
│ ETF  │ SPY  QQQ  IWM  GLD  TLT  HYG               →  ›       │
│ マクロ│ VIX  US10Y  DXY  USDJPY                    →  ›       │
│ コモ │ WTI  BRENT  HG                              →  ›       │
└──────┴──────────────────────────────────────────────────────│
```

- 左 64-72px 固定ラベル (10-11px uppercase tracking-wider グレー、border-r 1px)
- 行内は CSS Grid または横スクロール (拡張時の破綻防止)
- 主指標 (S&P/NASDAQ/DOW) の階層差はフォントサイズ廃止 → 左 2px シアンバーで表現
- LIVE + 期間 Segmented Control + Markets ▾ は最上段独立行

### 16-3. 期間切替 (1D/1W/1M/6M/1Y) 実装方針

- `/api/market-history?period=1Y` で 30 指標 × 252 日 を **1 リクエスト一括** (≈15KB gz後)
- TTL 1h、JST 6:30 で寄り付き前 invalidate
- フロント: `history[ticker]` 保持 → `useDerivedSeries(ticker, period)` で slice + memo
- 1D は 5 分足が必要なため別 endpoint (`?intraday=1`) に分離
- Segmented Control: cyan-500/15 + 1px border、active のみ cyan-400 text、150ms クロスフェード
- 数値カウントアップ禁止 (チラつきで認知負荷増)

### 16-4. データ層 SSOT 推奨スキーマ

`frontend/src/config/marketSymbols.json`:
```json
{
  "ticker": "^GSPC",
  "label": { "ja": "S&P 500", "en": "S&P 500" },
  "short": "SPX",
  "category": "index",
  "region": "us",
  "tier": 1,
  "default_visible": true,
  "format": { "type": "price", "decimals": 2 },
  "sort": 100
}
```

- バックエンドも同 JSON を読む (FastAPI 起動時 load) = SSOT
- カテゴリ: index / etf / fx / bond / credit / commodity / crypto / sector / regional
- ティア: 1 (常時) / 2 (展開時) / 3 (上級者) — Markets ▾ で段階開示

### 16-5. 状態管理 — Zustand 採用

- 30 指標 × 5 期間 × ピン/並び順 は組合せ爆発 → Context 不適
- `marketStore`: quotes / history / lastUpdated / fetch actions
- `uiStore`: activePeriod / expandedCategories / pinnedTickers / order / view
- `persist` middleware で uiStore のみ localStorage 化
- 既存 `bs_*` キーは migrate 関数で吸収

### 16-6. ピン機能 + 並び順 (DnD)

- `@dnd-kit/core` + `@dnd-kit/sortable` 採用 (react-dnd は重い)
- ピン: ☆ icon、カテゴリ内先頭に sticky
- 並び順: `uiStore.order: Record<category, ticker[]>`
- カテゴリ越境 DnD は v43 へ繰延 (UX 検証後)
- アクセシビリティ: dnd-kit の keyboard sensor を必ず有効化

### 16-7. 統合実装ロードマップ

| Phase | 内容 | 工数 | 独立デプロイ | 価値 |
|---|---|---|---|---|
| **P1 即時改善** | 案 A の左ラベル + 3-4 行化 + 既存 14 指標で再構成 | 4-6h | 可 | ダサさ即解消 |
| **P2 SSOT 化** | `marketSymbols.json` 新設、FE/BE 双方が読む。30+ 指標準備 (tier1 のみ表示) | 6-8h | 可 | 拡張準備 |
| **P3 Zustand + 期間切替 + Markets ▾** | history endpoint + store 化 + Segmented Control + 段階開示 | 16-20h | 可 (P2 必須) | 上級者拡充の核 |
| **P4 ピン + DnD** | dnd-kit 統合 + persist + ☆ UI | 8-10h | 可 (P3 必須) | カスタマイズ |

合計 34-44h。**P1 を先行リリース**すると「ダサい」体感を数日で解消可能。

### 16-8. 設計思想との整合 (5 原則)

| 原則 | 本案での対応 |
|---|---|
| 1. 読み手に負担をかけない | 左ラベル統一でグルーピング即把握、2 秒理解 |
| 2. 毎日開きたくなる | LIVE + 「Updated 2m ago」で動いてる感 |
| 3. シンプルかつリッチ | カテゴリラベル + 微弱な border 装飾 (Bloomberg/Apple のミックス) |
| 4. 1 クリックを減らせ | Markets ▾ で段階開示 (常時タブ切替を回避) |
| 5. 図解で認知コスト ↓ | 主指標シアンバー / 期間ピル / カテゴリ色分け で文字を減らす |

### 16-9. 避けるべきパターン

- ティッカーテープ式自動スクロール (NN/g 長年 NG)
- カードグリッド (密度低下、決算アプリのフォーカス毀損)
- タブ切替復活 (1 クリック増、ユーザー却下済み)
- Bloomberg 風スプレッドシート (威圧感)
- 主指標だけ大文字化 (現状の不安定の元凶)
- 自動カウントアップ演出 (チラつき)

---

**v40 完了。Stripe 決済〜解約フルフロー本番検証済み。次セッションは本番ブラウザ実機テスト + 4半期決算詳報（Pro同梱機能）実装から着手すること。**

> 📝 本書は v39 引き継ぎ書を継承。v30〜v39 の全機能は引き続き有効。
> v40 では「ロック演出統一・機能ゲーティング・Stripe Portal・Webhook修正・決済フルフロー検証」に絞って記載。
> ファイル名: `handover_2026-05-05_v40.md`
