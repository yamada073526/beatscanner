# 引き継ぎ書 v191 (2026-06-08 夜→翌朝、ファンダ章 hairline + 見出し階層 redesign 完了、autopilot)

v5 ペイン3 ファンダ章を「枠だらけ → 枠なし hairline + 余白 + 見出し階層(L1-L4)」に再設計。grill→3体合議×3セット→dogfood 2巡の PDCA を回し、user 就寝中に autopilot で v191 まで実装・deploy・snap 検証済み。本 handover が SSOT。設計の永続 SSOT は **`docs/references/design_recipes.md §C-11`**(4原則 + 見出し4階層)。

## 🎯 完了 (本番 live、全 deploy + snap pageerror 検証済、全て v5 限定・v4/v2/legacy 完全不変)

### v189 `3e059e9` — hairline 化 (枠なし連続面 pilot)
- ファンダ章を「面の引き算」: コンテナ背景なし、**5条件カードのみ発光カードで主役維持**、従属3セクション(決算/バリュエーション/会社概要)を border-top hairline + space-8 余白で連続面化。
- TtmValuationPanel の inline 枠撤去 (frameless のまま、出典 footer 保持)。会社概要 defaultOpen=false。章サマリーに bg-subtle(後に v190 で非表示化)。

### v190 `657e853` — Phase A: 「決算」L2 冠新設 + 階層統一
- **「決算」L2 セクション冠を新設**(今期/来期コンセンサスを傘下に)。決算/バリュエーション/会社概要の3冠を L2 token (13px/700/0.08em/uppercase/`--text-primary`) で統一。
- 会社概要 title を L2 統一 (AccordionSection `titleStyle` prop、module.css 不触)。バリュエーション sub 注釈を差別化。**章サマリー「N条件中Mクリア」を v5 非表示**(5条件カードと重複・冗長、user①)。

### v191 `b7333af` — chevron 整列 + 親子区別 + 今期/来期 L3 + 4原則ルール化
- **C**: `design_recipes.md §C-11` に **4原則(近接/整列/反復/コントラスト)+ 見出し4階層(L1章扉>L2冠>L3サブ>L4値)** を明文化(user が「ノンデザイナーズ4原則をルールに加えたい」と明示)。
- **A1**: AccordionSection に `chevronPosition='right'` prop。会社概要の chevron を右端 ghost 化(opacity 0.4→hover 1.0)し L2 冠と左端整列。`.chevronRight` 追加。
- **A2/A3**: TtmValuationPanel の親子距離拡大(8→16px)+ metric grid を軽インデント + **gold left accent**(引用ブロック idiom、密閉感回避)。
- **B**: GuidanceCard/ForwardOutlookSection に `headingVariant='l3'` prop。「今期決算結果」「来期コンセンサス」を L3 サブ見出しに降格(lucide除去 + small/muted/uppercase)、今期/来期を同格化。**⚠️ panel-card枠・tier-m-glow・？モーダル・ScorecardCell(Beat値=L4)・§38免責は全て不触**(3体一致: 枠を外すと useArrivalSpotlight observe 消失 + skeleton 崩壊 = glow_postmortem §v62 違反)。

## 🔴 user 起床後 dogfood 必須 (authed/premium は headless 描画不可、snap dark のみ検証)
`?pane3_v5=1` で NVDA/AAPL 等を **light/dark 両方**で目視:
1. **3つの L2 冠(決算/バリュエーション/会社概要)が左端揃って見えるか**、L1「ファンダメンタル」と自然に区別できるか。
2. **今期/来期コンセンサスが L3 同格**に見えるか(今期は ScorecardCell の Beat 値で十分目立つか)。
3. **バリュエーションの gold left accent + インデント**で親子が分かるか(やりすぎ=密閉感がないか。ui が懸念、強すぎたら border opacity を下げる)。
4. **会社概要 chevron が右端 ghost**(opacity 0.4)で「畳める」と気づけるか(密やかすぎないか)。

## 🟡 残タスク / 次の方向
- **横展開**: 4原則 + hairline 言語を **テクニカル章・その他章**へ展開(ファンダ pilot 着地後の予定)。横展開2章目確定時に **SectionLabel / HairlineSection primitive 化**(frontend verdict: 今は inline、2章目で抽出)。
- **章サマリー速報スタイル再設計**: memory `project_chapter_summary_jitchama_style.md`(じっちゃま速報= EPS/売上/セグメント/ガイダンス要点を §38セーフ事実文。6体 gate)。現状 v5 非表示。
- **GuidanceCard/ForwardOutlook の panel-card 枠**: 今回は残した(発光リスク)。もし dogfood で「今期カードがまだ浮く」なら、発光系を慎重に扱う別タスク。
- **v5 → v4 昇格**: dogfood OK 後に `?pane3_v4` 同経路で全ユーザーへ(価格 ladder と合わせて)。

## ⚠️ 運用知見 / 危険箇所
- **共有 component の v5 のみ変更は prop で**(default で v4/v2/legacy 不変): AccordionSection(`titleStyle`/`chevronPosition`) / GuidanceCard・ForwardOutlookSection(`headingVariant`) / FundamentalsAccordion(`renderSection`/`sectionHeadingStyle`) / TtmValuationPanel(inline)。
- **GuidanceCard は panel-card + tier-m-glow + ？モーダル + §38免責 + SignalQualityChip が密集**。見出し(h3/h4)以外は触らない。枠は外さない。
- **build green ≠ ランタイム安全**(Vite は未定義変数を catch せず)。検出 = `snap-v5-dogfood.mjs` の console/pageerror listener(前回 isUnified 事故の検出器)。
- 発光系 `.panel-card/.surface-card` / sticky検索バー / VITE_ ARG/ENV / aggregator LLM import。
- **Railway 連続 push で queue 詰まり** → feedback はまとめて1 deploy(今回 v189/v190/v191 は別 push だが各々間隔あり問題なし)。

## ⏳ pending (継承)
- 明朝 09:00 JST scheduled task `canslim-null-reasons-verify` / S5b バッジ列 pixel 未確認。
- スクリーナー メンタルモデル再編 grill-me(発表会FB §C、未着手)。Pane3 が落ち着いたら次。

## 📋 deferred backlog (継承)
- upgrade モーダル design polish / screener tier 方針 / Pane2 検索結果レイアウト。
- Stripe Premium 配線(launch blocker) / @dataclass ScanResult 化・scan async/chunk化(3000 universe 502)。
