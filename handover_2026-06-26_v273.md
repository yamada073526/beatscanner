# handover 2026-06-26 v273 — seasonchip + 決算期混同ガード着地・残=migration 適用 (Supabase MCP 経由)

> 前提: PR **#25** (`claude/q4-seasonchip-planner-4rz0s7`) に Q4 締めの 2 機能 = **seasonchip** (cosmetic) と
> **決算期混同ガード** (backend Sprint 1-2 + frontend Sprint 3) を実装済。6 体合議 BLOCK 解消済。
> **次セッションの最優先 = この PR の migration を Supabase MCP 経由で本番適用すること**。

## 🔴 最優先 — 次セッション着手必須: migration 適用

1. **新セッションは `.mcp.json` を含むブランチで起動済の前提** (`claude/q4-seasonchip-planner-4rz0s7`、または PR #25 を main merge 後)。起動時 MCP trust 承認が必要。
2. **まず接続確認** (正直さ: 報告≠事実):
   - `ToolSearch "+supabase"` で Supabase MCP tool が出るか
   - env 確認 (値は伏せる): `[ -n "$SUPABASE_ACCESS_TOKEN" ]` / `$SUPABASE_PROJECT_REF`
   - 両方 OK でなければ適用に進まず user に状況報告 (env は claude.ai Environment Variables・新セッションでのみ注入)
3. **適用**: `docs/migrations/2026-06-26_screener_fundamentals_last_report_date.sql` を Supabase MCP `apply_migration` で本番適用 (ADD COLUMN IF NOT EXISTS `last_report_date text` + index + GRANT。additive・冪等)。
4. **直後に canslim-scan を 1 回 trigger** して `last_report_date` を populate → 全 NULL 空白 (全行「決算日不明」) を回避 (migration ファイル冒頭の deploy 順序 note 参照)。
5. **検証**: migration ファイル末尾の確認クエリ (column 追加 / GRANT / scan 後の data) を `execute_sql` で実行。本番反映後、screener の決算 preset で「決算 YYYY-MM-DD」が出るか目視 (※ session から本番 URL 到達は egress 403 で不可な場合あり)。
6. **Supabase MCP は read-only でない** (DDL 用)。PAT は account 全体権限なので扱い注意 (`docs/references/mcp_supabase_setup.md` 参照)。

## ✅ このセッションで ship + 検証済 (PR #25・蒸し返し禁止)

| 機能 | 内容 | 検証 |
|---|---|---|
| **seasonchip** | 各 preset の対象範囲を gold pill 表示 (不変文言・mockup の動的値 過去90日/Q1 は除去)。`CustomScreenerPanel.jsx` + `.seasonchip` class | multi-review 3 体 BLOCK ゼロ・build 緑 |
| **ガード Sprint 1** | `screener_fundamentals` に `last_report_date` migration + `_compute_one` tuple arity **18→19** (entry_date_str を全 return 6 + unpack + comment 同期) + upsert (None-preserve + optional_cols graceful fallback) | py_compile・arity 全7箇所19要素を実数確認・backend-architect PASS |
| **ガード Sprint 2** | `_build_universe_payload` で別 fetch graceful merge (latest_beat と同隔離 SELECT) + item dict 露出 + freshness key | py_compile・Trust Cliff「1カラム欠落で全 fundamentals 消失」隔離踏襲 |
| **ガード Sprint 3** | `ScreenerRow` に「決算 YYYY-MM-DD」honest 併記・NULL「決算日不明」(amber italic)。決算関連 preset 限定 | build 緑・token 違反ゼロ・legacy/cupState/extra/itemPasses 不触 |
| **6体合議** | 金融 PASS / backend PASS / 法務 条件付きBLOCK→3点解消 / frontend PASS / ui PASS / qa WARN(rollout全NULL) | SPEC §9 に verdict 記録 |
| **Supabase MCP** | `.mcp.json` + `docs/references/mcp_supabase_setup.md` | valid JSON・push 済 |

- SPEC: `docs/specs/SPEC_2026-06-26_seasonchip.md` / `docs/specs/SPEC_2026-06-26_earnings-period-guard.md`

## ⚠️ 正直な到達範囲 (over-claim 厳禁・法務 reviewer 最重要指摘)

「決算期混同を機械的に防止し**徹底した**」と言えるのは **backend データ基盤まで**。表示到達は限定:
1. **screenerV2 path (opt-in・`isScreenerV2()` 移行期 default OFF) の earnings_pass / new_high_break のみ**。大半ユーザー (legacy 行) には**現時点未到達** (SPEC §6 物理隔離)。V2 が C-16 ゲートで default ON 昇格時に全ユーザー到達。
2. **honest 表示であって述語フィルタでない** (stale 除外の窓 gate 未実装 = 分岐B 設計)。
3. **(B) YoY ペア誤マッチ (±60日窓) は未解消** (§8 自認・将来 audit 素地のみ)。
- backend は全 universe cover だが UI surface は上記限定。これを誇大報告しないことが正直さの核心。

## 🟡 残タスク (Sprint 4・依存 gate・未着手)

- **seasonchip「主に」除去**: `earnings_pass` の「対象: 主に直近の決算シーズン」→「対象: 直近の決算シーズン」(断定に戻す)。**Sprint 1-3 が本番 verify された後のみ**着手 (機械保証なき断定は Trust Cliff 再発)。`SEASON_LABEL` dict の 1 行。
- 法務追加 gate: 断定文言 (seasonchip = 全ユーザー到達) と ガード表示 (screenerV2 限定) の**到達層不一致**を要判断 — V2 昇格を待つ or seasonchip を screenerV2 同期にする。

## ⚠️ 触ると危険 (継続)

- **extra オブジェクトは 5 箇所で重複構築** (filteredItems/presetCounts/facetLevelCounts/emptySuggest/適用中サマリ)。新フィルタ次元は 5 箇所 + deps 全同期。seasonchip / 決算日併記は**表示専用で extra に folding しない**。
- **`_compute_one` tuple arity = 19** (last_report_date を cfps_3y_rising と err の間)。新指標追加時は全 return + unpack を同数に (feedback_pge_loop_pitfalls ルール1)。
- **legacy 行 (screenerV2=false) は触禁止** (SPEC §6 物理隔離)。`ScreenerRow` は `if(screenerV2)` 内のみ。
- 発光系 (`.panel-card / .bs-panel / .surface-card`)・sticky 検索バー・`.screener-control-bar` nowrap・VITE_ ARG/ENV 同期・aggregator の LLM import 禁止。
- universe payload の新カラムは**別 fetch graceful merge** (同一 SELECT に混ぜると 1 カラム欠落で全 fundamentals 消失・main.py:20234-20237 教訓)。

## ⚙️ ツール / 環境メモ

- deploy: `git push origin main` → Railway auto。本 session は本番 URL 到達不可 (egress proxy 403)、検証は GitHub Actions CI で代替。
- **PR #25 の CI 監視**: 本 session の self check-in cron は session-only で消滅。必要なら新セッションで再確認 (`mcp__github__pull_request_read get_check_runs`)。CI 3 件 (cup E2E / smoke / dogfood) は本番に対し実行 = 未 merge の本 PR 変更は反映されない。
- **Supabase MCP**: `.mcp.json` (npx local + PAT)。env `SUPABASE_ACCESS_TOKEN` / `SUPABASE_PROJECT_REF` は claude.ai Environment Variables。新セッション起動 + trust 承認で有効。詳細 `docs/references/mcp_supabase_setup.md`。
- 実装は main が直接手を動かす (CLAUDE.md 正直さ: 委託は調査/多視点のみ)。
